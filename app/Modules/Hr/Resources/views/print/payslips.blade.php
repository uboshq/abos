{{--
    এক বা একাধিক বেতনশিট — প্রতিটা নিজের পাতায়।

    ── কেন এই মোড়কটা, সরাসরি print.document নয় ──────────────────────
    কোরের টেমপ্লেট একটা ডকুমেন্ট চেনে, একগুচ্ছ নয়। পুরো রানের বিশটা শিট
    এক ফাইলে দরকার, নাহলে বিশবার ক্লিক করতে হত আর একটা বাদ পড়লে কেউ
    ধরত না। তাই এখানে শুধু পাতা ভাগ করা হয় — ভেতরের কাগজটা কোরেরই।

    কোরে গুচ্ছের ধারণা যোগ করা হয়নি ইচ্ছাকৃতভাবে: এখন পর্যন্ত একমাত্র
    বেতনশিটেরই একসাথে অনেকগুলো লাগে, আর একজনের দাবিতে শেয়ার্ড কিছু
    বানানো নিষেধ (সেকশন ১৯.৮)।
--}}
@extends('print.layout')

@section('body')
    @foreach ($documents as $index => $doc)
        @if ($index > 0)
            <div style="page-break-before: always;"></div>
        @endif

        @include('print.document-body', ['doc' => $doc])
    @endforeach
@endsection
