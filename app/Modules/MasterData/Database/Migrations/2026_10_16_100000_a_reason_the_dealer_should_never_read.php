<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * কারণটা ডিলার পড়তে পারবেন কি না।
 *
 * ── কেন `context` যথেষ্ট নয় ──────────────────────────────────────────
 * সারিতে আগে থেকেই `context` আছে (`sales_return` · `cancellation` …),
 * আর দেখে মনে হয় ওটাই কাজটা করে। **করে না** — `context` বলে কারণটা
 * **কোন কাজে ব্যবহার হবে**, আর এখানে প্রশ্নটা **কে সেটা পড়তে পারবেন**।
 *
 * একই `context = 'cancellation'`-এর ভিতরে দুই জাতের কারণ থাকে:
 *
 *   "গ্রাহক চাননি"   → ডিলারকে বলা যায়
 *   "মজুদ নেই"       → বলা যায় না
 *
 * ⚠️ ── কেন দ্বিতীয়টা বলা যায় না ─────────────────────────────────────
 * মালিকের সিদ্ধান্ত (৩ সেপ্টেম্বর ২০২৬): **ডিলার মজুদ দেখবেন না।**
 * অর্ডার নেওয়ার পর্দায় সংখ্যাটা লুকানো সহজ, কিন্তু **প্রত্যাখ্যানের
 * কারণ দিয়েও সেটা ফাঁস হয়** — "মজুদ নেই" লেখা মানে এক লাইনে সবটা বলে
 * দেওয়া।
 *
 * ⓘ ভুলবার্তা দিয়ে ফাঁসটা সূক্ষ্ম: *"এত পাওয়া যাবে না"* লিখলেও
 * বাইনারি খোঁজ চালিয়ে সংখ্যাটা বের করা যায় — ৫০ চাও হ্যাঁ, ৭৫ চাও না,
 * ৬২…। তাই কারণের তালিকাটাই এমন হতে হবে যাতে মজুদের কথা বলা না যায়।
 *
 * ── কেন ডিফল্ট `false` ───────────────────────────────────────────────
 * `true` দিলে **আজকের প্রতিটা কারণ সারি হঠাৎ ডিলারের পর্দায়** চলে আসত,
 * আর তার মধ্যে মজুদ-সংক্রান্ত কিছু থাকলে সেটাই ফাঁস — মাইগ্রেশন চালানোর
 * মুহূর্তে, কারো কিছু না করেই।
 *
 * **লুকানোই নিরাপদ ডিফল্ট।** কোনটা দেখা যাবে সেটা কোম্পানি নিজে
 * সেটিংস থেকে ঠিক করবে, একটা একটা করে।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('mdm_reason_codes', function (Blueprint $table): void {
            $table->boolean('visible_to_dealer')->default(false)->after('context');
        });
    }

    public function down(): void
    {
        Schema::table('mdm_reason_codes', function (Blueprint $table): void {
            $table->dropColumn('visible_to_dealer');
        });
    }
};
