<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * পেমেন্ট মেথডের খাতটা ঐচ্ছিক হলো।
 *
 * ── কী ধরা পড়ল, ৪ সেপ্টেম্বর ২০২৬ ───────────────────────────────────
 * সরাসরি বিক্রয়ের জমার ঘরে "Method" আর "Reference" **চিরকাল ফাঁকা**
 * থাকত, আর কারণটা এক ধাপ পিছনে: নতুন কোম্পানি একটাও পেমেন্ট মেথড পায়
 * না। ⚠️ ফলে চেকের নম্বর বা bKash-এর TrxID লেখার ঘরটা **খুলতই না**, আর
 * ওটা ছাড়া মাস শেষে ব্যাংকের কাগজের সাথে টাকা মেলানো যায় না।
 *
 * ── কেন সারিগুলো দেওয়া যেত না ────────────────────────────────────────
 * `MasterListService`-এর মন্তব্যে যুক্তিটা লেখাই ছিল: *"পেমেন্ট মেথড কোন
 * হিসাবের খাতে বসবে তা কোম্পানি ছাড়া কেউ জানে না"*। কথাটা ঠিক — নতুন
 * কোম্পানিতে ব্যাংক (১১০২) আর মোবাইল মানি (১১০৫) কেবল **মাথা**, তাদের
 * নিচে কোনো খাত নেই, আর মাথায় টাকা বসে না। তাই "bKash" মেথডটা কোন খাত
 * দেখাবে, সেটা সত্যিই অজানা।
 *
 * ⭐ **কিন্তু উপসংহারটা ভুল ছিল, আর ভুলটা সূক্ষ্ম:** উপায় আর খাত দুইটা
 * আলাদা প্রশ্ন। সারিটা বলে **টাকা কীভাবে এল**, খাত বলে **কোথায় গেল**।
 * প্রথমটার উত্তর সবার এক — নগদ, চেক, ব্যাংক, মোবাইল ব্যাংকিং; দ্বিতীয়টা
 * প্রতিটা কোম্পানির নিজের।
 *
 * ── কেন এটা কোডকে দুর্বল করে না ─────────────────────────────────────
 * ⓘ কোড **আগে থেকেই** খালি খাত সামলায় — `PosService:342`:
 *
 *     'account_id' => $method?->account_id ?? ($data['account_id'] ?? null)
 *
 * অর্থাৎ ডাটাবেসের কড়াকড়িটা কোডের দরকারের চেয়ে বেশি ছিল। কাউন্টারের
 * পর্দা প্রতিবার খাতটা নিজেই জিজ্ঞেস করে, আর `CollectionService` গ্রুপ
 * খাত বা টাকা-নয় এমন খাত এমনিতেই ফিরিয়ে দেয় — **পাহারাটা ওখানেই, আর
 * সেটাই ঠিক জায়গা**।
 *
 * ⚠️ যে কোম্পানি খাতটা বসিয়ে রাখে, তার আচরণ এক চুলও বদলায় না: খাত
 * থাকলে সেটাই আগে, আর পর্দায় আপনা থেকে বসে।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('mdm_payment_methods', function (Blueprint $table) {
            /*
             * ⚠️ `->change()` foreign key ফেলে দেয় না, কিন্তু MySQL-এ
             * কলামের সংজ্ঞা নতুন করে লেখে — তাই `constrained()`-এর
             * নিয়মটা (`restrictOnDelete`) আলাদা করে ধরে রাখতে হয় না,
             * ওটা আলাদা constraint হিসেবে টেবিলেই থাকে।
             */
            $table->foreignId('account_id')->nullable()->change();
        });
    }

    public function down(): void
    {
        /*
         * ⚠️ ফেরত যাওয়ার আগে খালি সারিগুলোর কী হবে সেটা এই মাইগ্রেশন
         * জানে না — একটা খাত অনুমান করে বসিয়ে দেওয়া মানে **ভুল খাতে
         * টাকা**, আর সেটা নীরব। তাই ফেরত যাওয়া হয় কেবল কলামের নিয়মে;
         * খালি সারি থাকলে ডাটাবেস নিজেই আটকাবে, আর সেটাই বলবে কী করতে
         * হবে।
         */
        Schema::table('mdm_payment_methods', function (Blueprint $table) {
            $table->foreignId('account_id')->nullable(false)->change();
        });
    }
};
