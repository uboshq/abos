<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

/**
 * ভাড়ার গাড়ি, দালাল আর ডিলার — মালিকের ছাঁটাই আপগ্রেডের পথেও পৌঁছানো।
 *
 * ── কী হয়েছিল ────────────────────────────────────────────────────────
 * মালিক ৪ সেপ্টেম্বর ২০২৬-এ পক্ষের ধরনের চূড়ান্ত তালিকা দিলেন, আর তাতে
 * **ভাড়ার গাড়ি (RENTAL) ও দালাল (BROKER) নিজে কেটে দিলেন**; পুরনো ডিফল্ট
 * **ডিলার (DEALER)-ও বাদ**, তার জায়গায় পরিবেশক (DISTRIB)। `installDefaults`
 * এখন এই তিনটা আর বসায় না — কিন্তু সেটা কেবল **নতুন** কোম্পানির কথা।
 *
 * ── কেন এই মাইগ্রেশন লাগল ─────────────────────────────────────────────
 * একটা পুরনো মাইগ্রেশন (`..._the_transporter_was_never_a_party`) চলমান
 * প্রতিটা কোম্পানিতে RENTAL ও BROKER বসিয়ে দিয়েছিল, আর DEALER তো আগের
 * seed থেকেই সব কোম্পানিতে ছিল। ⛔ ফলে আজ যে ক্রেতা আপগ্রেড করবেন তাঁর
 * ব্যবস্থায় মালিকের কেটে দেওয়া তিনটা ধরন **ফিরে বসে থাকত** — নতুন ইনস্টল
 * পরিষ্কার, অথচ আপগ্রেডের পথ নোংরা। এই মাইগ্রেশন সেই পথটা পরিষ্কার করে।
 *
 * ── কেন শুধু অব্যবহৃতগুলো ─────────────────────────────────────────────
 * কোনো সরবরাহকারী বা গ্রাহক যদি ইতিমধ্যে ওই ধরনে বসে থাকেন, ধরনটা মুছলে
 * তাঁর `party_type_id` এমন কিছুর দিকে দেখাত যা নেই (FK `nullOnDelete`, তাই
 * শ্রেণিটা **নীরবে খালি** হয়ে যেত) — আর সেটা তথ্য হারানো। তাই ব্যবহৃত সারি
 * রেখে দেওয়া হয়; কেবল কেউ-ব্যবহার-করেননি এমন সারি মোছা হয়। DEALER সম্ভবত
 * ব্যবহৃত, তখন সেটা রয়ে যাবে — আর সেটাই ঠিক।
 */
return new class extends Migration
{
    /** মালিকের কেটে দেওয়া তিনটা কোড। */
    private const CUT = ['RENTAL', 'BROKER', 'DEALER'];

    public function up(): void
    {
        foreach (self::CUT as $code) {
            $rows = DB::table('mdm_party_types')->where('code', $code)->get(['id']);

            foreach ($rows as $row) {
                // ব্যবহৃত কিনা — দুই তালিকাই দেখা হয়, কারণ RENTAL/BROKER
                // সরবরাহকারীর দিকে আর DEALER গ্রাহকের দিকে ছিল। soft-deleted
                // সারিও গোনা হয় (raw কোয়েরিতে scope লাগে না): মুছে-ফেলা
                // গ্রাহকও ওই id-র দিকে দেখান, তাই ধরনটা তখনো "ব্যবহৃত"।
                $used = DB::table('suppliers')->where('party_type_id', $row->id)->exists()
                    || DB::table('customers')->where('party_type_id', $row->id)->exists();

                if (! $used) {
                    DB::table('mdm_party_types')->where('id', $row->id)->delete();
                }
            }
        }
    }

    /**
     * ফেরানোর চেষ্টা নেই — ইচ্ছাকৃত।
     *
     * ⛔ মুছে ফেলা সারি ফিরিয়ে আনা যায় না (কোন কোম্পানিতে কোনটা ছিল, সেই
     * তথ্যই আর নেই)। এখানে সারিগুলো আবার বসালে সেটা হত একটা মিথ্যা rollback —
     * পরে কেউ সেটা দেখে বিশ্বাস করে rollback চালাতেন, আর ভুল ধরন ফিরে আসত।
     * পরিষ্কার no-op-ই সৎ: এই মাইগ্রেশন সামনের দিকে চলে, পেছনে নয়।
     */
    public function down(): void
    {
        // ইচ্ছাকৃতভাবে খালি — উপরের কমেন্ট দেখুন।
    }
};
