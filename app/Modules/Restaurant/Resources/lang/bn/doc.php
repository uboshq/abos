<?php

declare(strict_types=1);

/*
 * কাগজের ধরনের নাম — নম্বর সিরিজের পর্দায় দেখা যায়।
 *
 * ⛔ এই ফাইলটাই ছিল না, ২১ সেপ্টেম্বর ২০২৬ পর্যন্ত। ⓘ `module.php`-এ
 * `'CKG' => 'restaurant::doc.production'` ঘোষণা করা ছিল, আর নম্বর সিরিজের
 * পর্দা ওটা `__()` দিয়ে ডাকত — কিন্তু কোনো ভাষাতেই শব্দটা না থাকায়
 * ব্যবহারকারী পর্দায় **চাবিটাই** দেখতেন: `restaurant::doc.production`।
 */
return ['production' => 'রান্নার কাগজ'];
