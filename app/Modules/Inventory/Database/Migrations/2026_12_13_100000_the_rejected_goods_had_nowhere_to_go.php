<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * বাতিল মালের যাওয়ার জায়গা ছিল না।
 *
 * ── ⛔ যে অবস্থাটা ছিল ───────────────────────────────────────────────
 * পরিদর্শনে বাতিল হলে মালটা আটকে যেত, আর **চিরকাল আটকেই থাকত**।
 * ⓘ গুদামে জায়গা নিত, মজুদের মূল্যে গোনা হত, অথচ বিক্রি করা যেত না।
 *
 * ── ⚠️ কেন একটা ঘর লাগল ─────────────────────────────────────────────
 * বিনাশ একবারে পুরোটা নাও হতে পারে: পঞ্চাশটার মধ্যে বিশটা আজ, বাকিটা
 * সরবরাহকারী দেখতে আসার পরে। ⓘ তাই *"এই কাগজটা আর কতটা আটকে রেখেছে"*
 * প্রশ্নের উত্তর দিতে হলে **কতটা ইতিমধ্যে গেছে** সেটা মনে রাখতে হয়।
 *
 * ⛔ গুদামের মোট আটকানো সংখ্যা দিয়ে হিসাব করা যেত না: দুইটা পরিদর্শনের
 * বাতিল মাল একই তাকে পাশাপাশি থাকাটাই স্বাভাবিক, আর তখন একটা কাগজ
 * দিয়ে অন্য কাগজের মাল বিনাশ করা যেত।
 *
 * ── ⓘ কেন `2026_12_07`-এ যোগ করা হলো না ────────────────────────────
 * ঐ মাইগ্রেশনটা ইতিমধ্যে লাইভে চলে গেছে। ⚠️ চলে যাওয়া মাইগ্রেশন
 * বদলালে নতুন ইনস্টলে ঘরটা আসত আর পুরনোতে আসত না — আর পার্থক্যটা
 * ধরা পড়ত অনেক পরে, একটা ৫০০-তে।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('inv_quality_inspections', function (Blueprint $table) {
            /*
             * ⓘ শূন্য ডিফল্ট, কারণ আজ পর্যন্ত কোনো কাগজ থেকেই কিছু
             * বিনাশ হয়নি — ⚠️ `nullable` করলে প্রতিটা হিসাবে
             * `?? '0'` লিখতে হত, আর একদিন কেউ ভুলত।
             */
            $table->decimal('disposed_qty', 18, 4)->default(0)->after('rejected_qty');
        });
    }

    public function down(): void
    {
        Schema::table('inv_quality_inspections', function (Blueprint $table) {
            $table->dropColumn('disposed_qty');
        });
    }
};
