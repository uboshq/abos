<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * দোকান দামটা মনে রাখত, নীতিটা নয়।
 *
 * ── ⛔ কী ভাঙা ছিল, ৬ সেপ্টেম্বর ২০২৬ ────────────────────────────────
 * মালিক শর্তটা মনে করিয়ে দিলেন: *"পণ্য এন্ট্রির সময় একবার ক্রয়মূল্য আর
 * markup / margin / বিক্রয়দর — এই তিনটার একটা বসাবে, বাকিগুলো অটো হবে।
 * পরের বার আর বসাতে হবে না। কিন্তু ক্রয়মূল্য কমলে বা বাড়লে সতর্কবার্তা
 * আসবে ও বিক্রয়মূল্য বদলাবে।"*
 *
 * মেপে দেখা গেল শেষ দুইটা কাজই করত না:
 *
 *     পণ্য বাছলে বসত   ক্রয়দর ৮ · বিক্রয়দর ১০ · markup খালি · নোঙর খালি
 *     ক্রয়দর ৮ → ৯     বিক্রয়দর ১০-ই থাকত        ⛔
 *
 * ⓘ `reprice()` নোঙর ছাড়া কিছুই বসায় না — আর সেটা **ঠিকই করে**, নাহলে
 * কেউ শুধু ক্রয়দর লিখলেই একটা বিক্রয়মূল্য ভেসে উঠত যেটা কেউ বাছেনি।
 *
 * ⛔ আসল ফাঁকটা এখানে: **`inv_products`-এ নীতিটা রাখার কোনো জায়গা ছিল না।**
 * `purchase_price` আর `sale_price` জমা থাকত — দুইটা **সংখ্যা**। কিন্তু
 * *"এই পণ্যটা আমি ৪০% মার্জিনে বেচি"* — এই **নিয়মটা** কোথাও লেখা হত না।
 *
 * ⚠️ আর দুইটা এক জিনিস নয়: সংখ্যা বলে *কত*, নীতি বলে *কেন*। ⓘ ক্রয়দর
 * বদলালে সংখ্যাটা অচল হয়ে যায়, নীতিটা হয় না — নীতিই নতুন সংখ্যা বানায়।
 *
 * ── কেন দুইটা কলাম, একটা নয় ─────────────────────────────────────────
 * শতাংশটা একাই যথেষ্ট নয়: ৫০% markup আর ৫০% margin **দুইটা আলাদা দাম**
 * (১৫০ বনাম ২০০)। ⚠️ কোনটা বোঝানো হয়েছে তা না জানলে শতাংশটা অর্থহীন।
 *
 * ⓘ `price` নোঙরটাও আসল একটা বিকল্প — প্যাকেটে ছাপা দাম বা ডিলারের সাথে
 * ঠিক করা দাম। তখন ক্রয়দর বদলালেও **দামটা টেকে**, বদলায় markup ও margin।
 *
 * ── কেন `null`, আর ডিফল্ট কোনো নীতি নয় ──────────────────────────────
 * পুরনো প্রতিটা পণ্যের নোঙর `null` — অর্থাৎ *"এই পণ্যের কোনো নীতি এখনো
 * বলা হয়নি"*। ⚠️ ডিফল্টে কোনো একটা বসিয়ে দিলে ব্যবস্থা এমন একটা নিয়ম
 * ধরে নিত যা কেউ বলেনি, আর সেই ধরে-নেওয়া নিয়মেই দাম বদলাত।
 *
 * ⓘ প্রথম যেদিন কেউ তিনটার একটা লিখবেন, সেদিনই নীতিটা লেখা হবে।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('inv_products', function (Blueprint $table) {
            /*
             * কোন ঘরটা মানুষ নিজে লিখেছিলেন — সেটাই নীতি।
             *
             * ⓘ `string`, `enum` নয়: নতুন একটা নোঙর যোগ করতে (ধরা যাক
             * "সরবরাহকারীর তালিকা অনুযায়ী") তখন একটা মাইগ্রেশন লাগত।
             */
            $table->string('pricing_anchor', 16)->nullable()->after('sale_price');

            /*
             * নোঙরের সাথের শতাংশ — কেবল markup/margin-এ অর্থবহ।
             *
             * ⚠️ চার দশমিক, দুই নয়। ⓘ ৬ সেপ্টেম্বর মাপা হয়েছে: উঁচু
             * margin-এ দুই দশমিক ৯০,০০০ টাকার লাইনে **৩০০ টাকা** সরিয়ে
             * দেয়, কারণ সূত্রটা `দাম = খরচ ÷ (1 − margin/100)` আর হর
             * শূন্যের কাছে গেলে ছোট গোলমাল বিশাল হয়। কলামটাও তাই
             * পর্দার নির্ভুলতাই রাখে — নাহলে সেভ করার সময় আবার হারাত।
             */
            $table->decimal('pricing_pct', 12, 4)->nullable()->after('pricing_anchor');
        });
    }

    public function down(): void
    {
        Schema::table('inv_products', function (Blueprint $table) {
            $table->dropColumn(['pricing_anchor', 'pricing_pct']);
        });
    }
};
