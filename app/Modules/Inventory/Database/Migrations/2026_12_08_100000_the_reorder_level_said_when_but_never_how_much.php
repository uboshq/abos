<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * পুনঃক্রয়ের স্তর বলত **কখন**, কোনোদিন বলত না **কতটা**।
 *
 * ── ⭐ মালিকের স্পেক, ২৩ সেপ্টেম্বর ২০২৬ ──────────────────────────────
 * *"Minimum Stock · Maximum Stock · Reorder Level · Safety Stock ·
 * Reorder Quantity · Lead Time · Stock Coverage"*।
 *
 * ── ⓘ আজ পর্যন্ত যতটুকু ছিল ─────────────────────────────────────────
 * `products.reorder_level` — একটাই সংখ্যা, আর ড্যাশবোর্ডে *"এর নিচে
 * নেমেছে"* তালিকা। ⚠️ ওটা প্রশ্নের প্রথম অর্ধেকের উত্তর দেয়: **কখন**
 * কিনতে হবে।
 *
 * ⛔ দ্বিতীয় অর্ধেকটা — **কতটা** কিনব — কোথাও লেখা ছিল না। ⓘ ফল:
 * তালিকাটা দেখে মানুষ আন্দাজে অর্ডার দিতেন, আর আন্দাজ দুই দিকেই ভুল
 * হয়: কম কিনলে দুই সপ্তাহ পরে আবার একই তালিকায়, বেশি কিনলে টাকা
 * গুদামে পড়ে থাকে।
 *
 * ── ⚠️ তিনটা ঘর, আর তিনটাই ঐচ্ছিক ───────────────────────────────────
 * ⛔ বাধ্যতামূলক করলে হাজার পণ্যের মাস্টারে তিন হাজার সংখ্যা বসাতে হত,
 * আর কেউ সেটা করত না — ⓘ তখন ঘরগুলো শূন্য থাকত, আর শূন্য মানে
 * *"সর্বোচ্চ মজুদ শূন্য"*, যা প্রতিটা পণ্যকে চিরকাল অতিরিক্ত দেখাত।
 *
 * ⭐ তাই `null` মানে *"এই পণ্যের জন্য বলা নেই"*, আর তখন পরিকল্পনার
 * রিপোর্ট ঐ সারিতে কোনো দাবি করে না।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('inv_products', function (Blueprint $table) {
            /*
             * সর্বোচ্চ মজুদ — এর উপরে গেলে টাকা তাকে পড়ে আছে।
             *
             * ⓘ `reorder_level`-এর উল্টো দিক। ⚠️ দুইটা একসাথে না থাকলে
             * *"অতিরিক্ত মজুদ"* বলে কিছু মাপা যায় না, আর স্পেকের
             * `Overstock Detection` কেবল একটা নাম হয়ে থাকত।
             */
            $table->decimal('max_level', 18, 4)->nullable()->after('reorder_level');

            /*
             * একবারে কতটা কেনা হয় — অর্থনৈতিক অর্ডারের পরিমাণ।
             *
             * ⓘ প্রায়ই এটা প্যাকের গুণিতক (এক কার্টন, এক বস্তা), আর
             * সেজন্যই এটা হিসাব করে বের করা যায় না — ⛔ সরবরাহকারী
             * আধা কার্টন বেচেন না।
             */
            $table->decimal('reorder_qty', 18, 4)->nullable()->after('max_level');

            /*
             * অর্ডার দিলে মাল আসতে কত দিন।
             *
             * ⚠️ এই একটা সংখ্যাই বলে দেয় পুনঃক্রয়ের স্তরটা যথেষ্ট কি
             * না: ⓘ দিনে দশ পিস বিক্রি হয় আর মাল আসতে সাত দিন লাগে
             * মানে স্তরটা সত্তরের নিচে হলে মাঝপথে ফুরিয়ে যাবে।
             *
             * ⛔ পণ্যে রাখা হলো, সরবরাহকারীতে নয় — একই পণ্য দুইজনের
             * কাছ থেকে আসতে পারে, কিন্তু পরিকল্পনার প্রশ্নটা পণ্যের।
             */
            $table->unsignedSmallInteger('lead_days')->nullable()->after('reorder_qty');
        });
    }

    public function down(): void
    {
        Schema::table('inv_products', function (Blueprint $table) {
            $table->dropColumn(['max_level', 'reorder_qty', 'lead_days']);
        });
    }
};
