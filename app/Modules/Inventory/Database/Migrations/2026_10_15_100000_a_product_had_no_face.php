<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * পণ্যের একটা মুখ — তালিকায় ও কাউন্টারে যেটা দেখা যায়।
 *
 * ── কেন কলামটা "ছবিটা কোনটা" নয়, "প্রধানটা কোনটা" ────────────────────
 * একটা পণ্যের একাধিক ছবি থাকা স্বাভাবিক — সামনে, পেছনের লেবেল, কার্টন।
 * ছবির **পথ** এখানে বসালে আজ একটা ছবি রাখা যেত, আর কাল গ্যালারি চাইলে
 * এই মাইগ্রেশনটাই ভাঙতে হত — যেটা চলে যাওয়া মাইগ্রেশনে নিষেধ।
 *
 * তাই কলামটা কেবল একটা **নির্দেশক**: ছবিগুলোর মধ্যে কোনটা মুখ। ছবিগুলো
 * থাকে [[AttachmentEngine]]-এ, যে পণ্যের যত খুশি কাগজ রাখতে পারে
 * (`source_module`/`source_entity`/`source_entity_id`)। গ্যালারি যোগ
 * করতে ভবিষ্যতে **এখানে হাত দিতে হবে না** — কেবল একটা পর্দা লাগবে।
 *
 * ── কেন আলাদা কোনো ফাইল-ব্যবস্থা নয় ──────────────────────────────────
 * ইঞ্জিনটা আগে থেকেই আছে আর যা যা লাগে সব দেয়: UUID নাম (মূল নামটা কেবল
 * দেখানোর জন্য, তাই `../../.env` লিখে বাইরে লেখা যায় না), নিষিদ্ধ
 * এক্সটেনশনের তালিকা, আকারের সীমা, sha256, সংস্করণ, নরম-মোছা।
 *
 * ⚠️ আর সবচেয়ে জরুরি — **ফাইলগুলো `storage/app/private`-এ**, আর
 * পরিবেশিত হয় [[AttachmentController::download()]] দিয়ে, যে আগে
 * `authorize('view', $document)` চালায়।
 *
 *
 * ── ⚠️ দুইটা জিনিস এখানে ভুল হয়েছিল, দুইটাই নীরব ─────────────────────
 * **এক · টেবিলের নাম `products` নয়, `inv_products`** — মডেলটা
 * `$table = 'inv_products'` বলে, তাই কোড পড়ে ধরা পড়ে না; ধরা পড়ে
 * মাইগ্রেশন চালালে।
 *
 * **দুই · ফাইলটা প্রথমে `database/migrations/`-এ ছিল।** মডিউলের
 * মাইগ্রেশন মডিউলের নিজের ফোল্ডারে বসে
 * (`ModuleServiceProvider::registerMigrations()` → `loadMigrationsFrom`),
 * আর `inv_products` Inventory-র টেবিল। বাইরে রাখলে ক্রমের কোনো
 * নিশ্চয়তা থাকে না — টেবিলটা তৈরি হওয়ার আগেই এই ফাইলটা চলতে পারে।
 *
 * ⓘ ভাগ করা `abos_test`-এ ভুলটা **লটারি**: নিজের টেস্ট একা চালালে হয়তো
 * সবুজ, আর অন্য কারো টেস্ট আগে চললে ১৭টা ত্রুটি। ৩ সেপ্টেম্বর ২০২৬-এ
 * ঠিক তাই হয়েছিল — A1-এর `DirectSaleTest`-এর ১৭টার একটাও চলেনি।
 *
 * `storage/app/public`-এ রাখলে **প্রতিটা পণ্যের ছবি URL অনুমান করেই যে
 * কেউ দেখতে পারত — লগইন ছাড়া, অন্য কোম্পানির লোকও**। এটা বহু-টেন্যান্ট
 * পণ্য আর ক্রেতারা একে অপরের প্রতিযোগী; পণ্যের ছবি মানে কে কী বেচে আর
 * কোন ব্র্যান্ড ধরে। টেন্যান্ট বিচ্ছিন্নতা এখানে সুবিধা নয়, বাধ্যবাধকতা।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('inv_products', function (Blueprint $table): void {
            /*
             * ⚠️ `nullOnDelete`, `cascadeOnDelete` নয় — আর পার্থক্যটা বড়।
             *
             * cascade দিলে **একটা ছবি মুছলে পণ্যটাই মুছে যেত**, তার
             * প্রতিটা মুভমেন্ট ও ক্রয়মূল্যের ইতিহাসসহ। একটা ভুল ক্লিকের
             * দাম গোটা পণ্য।
             *
             * null দিলে যা হয়: ছবিটা গেল, পণ্যটা রইল, শুধু মুখটা খালি।
             * ওটাই সত্যি, আর ওটাই ফেরানো যায়।
             */
            $table->foreignId('primary_image_id')
                ->nullable()
                ->after('barcode')
                ->constrained('attachments')
                ->nullOnDelete();
        });
    }

    public function down(): void
    {
        Schema::table('inv_products', function (Blueprint $table): void {
            $table->dropConstrainedForeignId('primary_image_id');
        });
    }
};
