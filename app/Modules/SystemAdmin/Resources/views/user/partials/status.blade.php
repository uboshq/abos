{{--
    সক্রিয় না নিষ্ক্রিয়।

    ── ⚠️ কেন নামের পাশ থেকে সরিয়ে আলাদা কলামে ────────────────────────
    আগের চালে চিহ্নটা নামের পাশেই বসত। ⓘ মালিকের নমুনায় এটা নিজের একটা
    কলাম, আর কারণটা ব্যবহারিক: কলাম হলে **চোখ এক লাইন নিচে নেমেই**
    পরেরটা পড়ে, নাম-লেখার দৈর্ঘ্য ধরে ধরে খুঁজতে হয় না।

    ⛔ মুছে ফেলার বোতাম নেই বলেই এই ঘরটা এখানে সবচেয়ে জরুরি — নিষ্ক্রিয়
    করাই ABOS-এ "বিদায়", আর সেটা কার বেলায় হয়েছে তা তালিকা থেকেই
    দেখা যাওয়া দরকার।
--}}
@if ($user->is_active)
    <x-ui.badge tone="success">{{ __('core.state.active') }}</x-ui.badge>
@else
    <x-ui.badge tone="draft">{{ __('core.state.inactive') }}</x-ui.badge>
@endif
