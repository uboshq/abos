{{--
    দুই ধাপের যাচাই — চালু না বন্ধ।

    ── ⭐ মালিকের নমুনা, ২১ সেপ্টেম্বর ২০২৬ ─────────────────────────────
    ⓘ কলামটা ছোট, কিন্তু প্রশ্নটা বড়: কার অ্যাকাউন্টে কেবল একটা
    পাসওয়ার্ডই সব। ⚠️ তালিকায় না দেখালে কেউ কোনোদিন খুঁজে দেখত না।

    ── ⛔ `mfa_secret` থাকা যথেষ্ট নয় ──────────────────────────────────
    মানুষ শুরু করে মাঝপথে ছেড়ে দিতে পারেন — তখন চাবিটা সারণিতে বসে থাকে
    কিন্তু লগইনে কিছুই চাওয়া হয় না। ⓘ `mfa_confirmed_at` বসে কেবল তখনই
    যখন তিনি একটা কোড মিলিয়ে দেখিয়েছেন, অর্থাৎ জিনিসটা সত্যিই চলছে।
--}}
@if ($user->mfa_confirmed_at)
    <x-ui.badge tone="success">{{ __('system_admin::field.two_step_on') }}</x-ui.badge>
@else
    <span class="text-2xs text-(--color-ink-muted)">{{ __('system_admin::field.two_step_off') }}</span>
@endif
