<?php

declare(strict_types=1);

/*
 * How a role's name is written on screen.
 *
 * Only the names the code itself creates and that read badly raw. A role
 * someone makes up is not listed here and does not need to be — without a
 * translation the name is shown as it is (see App\Core\Support\RoleLabel).
 */
return [
    'super_admin' => 'Super Admin',
];
