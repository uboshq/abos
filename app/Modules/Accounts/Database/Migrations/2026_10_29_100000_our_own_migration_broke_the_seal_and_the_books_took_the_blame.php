<?php

declare(strict_types=1);

use App\Core\Security\LedgerChain;
use App\Models\Company;
use Illuminate\Database\Migrations\Migration;

/**
 * আমাদের নিজের মাইগ্রেশন সিল ভেঙেছিল, দোষ পড়েছিল খাতার উপর।
 *
 * ── কী ঘটেছিল, ৫ সেপ্টেম্বর ২০২৬ ─────────────────────────────────────
 * ডিপ্লয় দুইবার ব্যর্থ হয়ে নিজে থেকে পিছিয়ে গেছে। প্রথমে মনে হয়েছিল
 * মাইগ্রেশন ভেঙেছে — ভাঙেনি। সব মাইগ্রেশন চলেছে, অনুমতি বসেছে, ক্যাশ
 * তৈরি হয়েছে। থেমেছে **শেষ ধাপে**, `abos:books-check`-এ:
 *
 *     খতিয়ানের চেইন ভেঙেছে — FMART · সারি #৫৭৯-এ
 *     খতিয়ানের চেইন ভেঙেছে — PROV · সারি #৪৬-এ
 *     খতিয়ানের চেইন ভেঙেছে — QAFULL · সারি #৪৯৭-এ
 *     খতিয়ানের চেইন ভেঙেছে — TDEPOT · সারি #১৫৬-এ
 *
 * বার্তাটা বলে *"কেউ অ্যাপের বাইরে দিয়ে খাতা বদলেছে"*। ⛔ কেউ বদলায়নি।
 *
 * ── কীভাবে প্রমাণ হলো ───────────────────────────────────────────────
 * পিছিয়ে যাওয়া (পুরনো) কোডে একই ডাটাবেসে `abos:books-check` চালানো
 * হয়েছে — **পাঁচটা কোম্পানিতেই মিলেছে, EXIT=0**। অর্থাৎ ডেটা অক্ষত;
 * ভাঙাটা এসেছে **নতুন কোডের সাথে**।
 *
 * ── আসল কারণ ────────────────────────────────────────────────────────
 * [[2026_10_24_100000_one_payable_head_held_three_different_debts]]
 * দলে বসে থাকা টাকা নিচের খাতে সরায়:
 *
 *     UPDATE ledger_entries SET account_id = <leaf> WHERE account_id = <group>
 *
 * ⭐ কাজটা **ঠিক** — দলে বসা টাকা কোনো যোগফলে আসে না, আর একই দিনে
 * কোডেও ২৩টা জায়গায় একই জিনিস সারানো হয়েছে।
 *
 * ⛔ ভুলটা হলো: `account_id` [[LedgerChain::SIGNED]]-এর ভিতরে। প্রতিটা
 * সারির ছাপ আগের সারির ছাপ ধরে বাঁধা, তাই একটা সারি বদলালে **তার পরের
 * সবগুলো** ছাপ ভুল হয়ে যায়। সিল ভেঙে সরানো হয়েছে, নতুন সিল বসেনি।
 *
 * ── কেন এটা ছোট ব্যাপার নয় ──────────────────────────────────────────
 * ⚠️ এটা বিক্রির পণ্য। গ্রাহকের পর্দাতেও ওই একই বাক্য উঠত — *"কেউ
 * আপনার খাতা বাইরে থেকে বদলেছে"* — একটা মিথ্যা অভিযোগ, তাঁর নিজের
 * বইয়ের উপর। হিসাবের সফটওয়্যার এর চেয়ে খারাপ কথা বলতে পারে না।
 *
 * ── কেন নতুন মাইগ্রেশন, পুরনোটা সংশোধন নয় ──────────────────────────
 * ⚠️ চলে যাওয়া মাইগ্রেশন এক অক্ষরও বদলানো হয় না — `deploy.sh` বদলে
 * যাওয়া মাইগ্রেশনে ইচ্ছে করেই থামে, আর অন্যদের ডাটাবেসে ওটা ইতিমধ্যেই
 * চলেছে। তাই সংশোধনটা এখানে, ওটার **পরে**।
 *
 * ── ⛔ এটা "পাহারা নিভিয়ে দেওয়া" নয় ────────────────────────────────
 * চেইনের কাজ **অ্যাপের বাইরের** বদল ধরা। এই বদলটা বাইরের নয় — আমাদের
 * নিজের, ঘোষিত, একটা মাইগ্রেশনের ভিতরে। ⓘ [[LedgerChain::canonical()]]
 * -এর মন্তব্যে এই মুহূর্তটার কথা আগেই লেখা ছিল: *"সব সারি নতুন করে ছাপ
 * দিতে হবে, একটা মাইগ্রেশনে, ঘোষণা করে।"*
 *
 * ⚠️ সিলটা যা ঢাকছে না: ছাপ বসার **আগে** যদি সত্যিই কেউ কিছু বদলাত,
 * সেটা এখন আর ধরা যাবে না। তাই এই ফাইলটা কী কী সরানো হয়েছিল তা উপরে
 * লিখে রাখে, আর চালানোর সময় কয়টা সারিতে নতুন সিল বসল তা ছাপে।
 */
return new class extends Migration
{
    public function up(): void
    {
        foreach (Company::query()->withoutGlobalScopes()->orderBy('id')->get() as $company) {
            $before = LedgerChain::verify((int) $company->id);

            if ($before['ok']) {
                continue;
            }

            $sealed = LedgerChain::reseal((int) $company->id);

            $after = LedgerChain::verify((int) $company->id);

            /*
             * ⛔ সিল বসিয়েও না মিললে থেমে যাওয়াই ঠিক।
             *
             * ⚠️ তখন ভাঙাটা `account_id` সরানোর নয় — অন্য কিছু, আর
             * সেটা চুপচাপ পার করে দেওয়া মানে একটা সত্যিকারের কারচুপিকে
             * সিল দিয়ে ঢেকে দেওয়া। ⓘ `deploy.sh` তখন গোটা ডিপ্লয়
             * ফিরিয়ে দেবে, আর সেটাই কাম্য।
             */
            if (! $after['ok']) {
                throw new RuntimeException(sprintf(
                    'সিল বসানোর পরেও %s-এর খাতা মেলেনি (সারি #%s) — এটা আমাদের মাইগ্রেশনের কাজ নয়, হাতে দেখুন।',
                    (string) $company->code,
                    (string) ($after['broken_at'] ?? '?'),
                ));
            }

            echo sprintf(
                "  %s — %d টা সারিতে নতুন সিল বসল (ভেঙেছিল #%s থেকে)।\n",
                (string) $company->code,
                $sealed,
                (string) ($before['broken_at'] ?? '?'),
            );
        }
    }

    /**
     * ফেরার পথ নেই, আর সেটাই সৎ।
     *
     * ⓘ পুরনো (ভুল) ছাপগুলো কোথাও রাখা হয়নি — রাখলে সেটা হত ভাঙা
     * অবস্থাটা ফিরিয়ে আনার একটা যন্ত্র, যেটার কোনো কাজ নেই। ⚠️ চেইন
     * ভুল অবস্থায় ফেরানোর একটাই মানে হত: ডিপ্লয় আবার আটকে যাওয়া।
     */
    public function down(): void
    {
        // ইচ্ছাকৃতভাবে খালি — উপরের ব্যাখ্যা দেখুন।
    }
};
