{{-- খাতায় না-ওঠা কাগজের নম্বর — নিজের পাতায় নিয়ে যায়।

     ⓘ কাগজটা খুলে দেখেই বোঝা যায় কেন সেটা খাতায় ওঠেনি (বাতিল, খসড়ায়
     ফেরত, নাকি পোস্টিং ভেঙেছিল), তাই লিংকটাই এই তালিকার আসল কাজ। --}}
<x-ui.drill :source="$row['source']" :id="$row['id']">{{ $row['document_no'] ?: '—' }}</x-ui.drill>
