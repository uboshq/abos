<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * আমাদের লেখা চেক পাশ হওয়ার আগেই ব্যাংক থেকে টাকা কমিয়ে দিত।
 *
 * ── কী ভাঙা ছিল ─────────────────────────────────────────────────────
 * সরবরাহকারীকে চেক দিলে [[PaymentService::confirm]] দাখিলা বসাত
 * **Dr প্রদেয় / Cr যে খাত বাছা হয়েছে** — অর্থাৎ ব্যাংক **তখনই** কমত।
 *
 * ⛔ কিন্তু চেক লেখা আর টাকা যাওয়া এক জিনিস নয়। কাগজটা তিন দিন পকেটে
 * থাকতে পারে, ব্যাংকে গিয়ে ফেরতও আসতে পারে। ততক্ষণ ব্যাংকের খাতা
 * **বাস্তবের চেয়ে কম** দেখাত, আর ব্যাংকের কাগজের সাথে মেলাতে গেলে
 * প্রতিটা অমিলের কারণ হাতে খুঁজতে হত।
 *
 * ── এই ভুলটা এই রিপোতে আগে একবার ধরা পড়েছে ───────────────────────────
 * উল্টো দিকে, বিক্রয়ে: *"হাতে চেক ইতিমধ্যেই ব্যাংকের টাকা ছিল"* — গৃহীত
 * চেক সরাসরি ব্যাংকে বসত। সারাই হয়েছিল ১১০৪ (হাতে আসা চেক) দিয়ে।
 *
 * ⚠️ **ক্রয়ের দিকে ঠিক একই ভুল রয়ে গিয়েছিল**, আর কেউ জানত না — কারণ
 * ক্রয়ের পর্দায় "কীভাবে দেওয়া হলো" ঘরটাই ছিল না, তাই চেক বাছার সহজ
 * পথও ছিল না। ⓘ ৪ সেপ্টেম্বর ২০২৬-এ জমার প্যানেল বসানোর পর পথটা তৈরি
 * হয়, আর তখনই সুপ্ত বাগটা নাগালে চলে আসে।
 *
 * ── এই মাইগ্রেশন কী করে ──────────────────────────────────────────────
 * ইঞ্জিনটা আগে থেকেই সম্পূর্ণ — [[ChequeService]] ইস্যু করা চেকের তিনটা
 * ধাপই জানে (দেওয়া · ভাঙানো · ফেরত)। যা ছিল না তা হলো **জোড়া**:
 * কোন পরিশোধের বিপরীতে কোন চেক।
 *
 * ⚠️ জোড়াটা `pur_payments`-এ বসে, `acc_cheques`-এ নয় — আর সেটা
 * ইচ্ছাকৃত। **Accounts কারও উপর দাঁড়ায় না** (`depends_on` খালি), সবাই
 * তার উপর দাঁড়ায়। চেকের টেবিলে `payment_id` বসালে Accounts-কে
 * Purchase-এর নাম জানতে হত, আর নির্ভরতার ক্রমটা উল্টে যেত।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('pur_payments', function (Blueprint $table) {
            /*
             * ⓘ `nullable` — বেশিরভাগ পরিশোধ নগদ বা ব্যাংক ট্রান্সফার,
             * তাদের কোনো চেক নেই। আর পুরনো সব সারিরও নেই।
             *
             * ⚠️ `nullOnDelete` নয়, `restrictOnDelete`: চেকটা মুছে গেলে
             * পরিশোধটা এমন এক অবস্থায় পড়ত যেখানে সে বলছে "চেকে দেওয়া
             * হয়েছে" অথচ চেকটা নেই — আর তখন খতিয়ানের দাখিলাটা কার,
             * সেই প্রশ্নের উত্তর থাকত না। ⓘ চেক এমনিতেও মুছে ফেলা যায়
             * না, বাতিল করা যায় (নিয়ম ৫)।
             */
            $table->foreignId('cheque_id')
                ->nullable()
                ->after('instrument_date')
                ->constrained('acc_cheques')
                ->restrictOnDelete();
        });
    }

    public function down(): void
    {
        Schema::table('pur_payments', function (Blueprint $table) {
            // ⚠️ বিদেশি চাবি আগে, কলাম পরে — উল্টো করলে MySQL ইনডেক্স
            // আঁকড়ে থাকে আর `down()` ব্যর্থ হয়
            $table->dropForeign(['cheque_id']);
            $table->dropColumn('cheque_id');
        });
    }
};
