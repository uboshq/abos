<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * কন্টেইনার এল, আর ঋণপত্রের নম্বরটা কোথাও লেখা রইল না।
 *
 * ── কেন এই ঘরগুলো ───────────────────────────────────────────────────
 * NEXUS/ABOS এগারোটা শিল্পের জন্য, আর তার একটা **আমদানি-রপ্তানি**।
 * ওই ব্যবসায় একটা ক্রয় বিল কখনোই একা দাঁড়ায় না — তার পিছনে একটা
 * ঋণপত্র, একটা বিল অব এন্ট্রি, একটা জাহাজ আর একটা বন্দর থাকে। ⛔ আজ
 * পর্যন্ত `pur_bills`-এ ওদের একটারও ঘর ছিল না, তাই নম্বরগুলো হয়
 * `narration`-এ গদ্য হয়ে বসত, নয়তো কোথাও বসতই না।
 *
 * ⚠️ **আর যেদিন কাস্টমস বা ব্যাংক জিজ্ঞেস করে "কোন LC-র মাল"**, গদ্য
 * থেকে সেটা খুঁজে বের করা যায় না — খোঁজা যায় না, রিপোর্টও হয় না।
 *
 * ── ⛔ কেন এগুলো ধ্রুবক নয়, আর কোনো তালিকাও নয় ──────────────────────
 * বন্দর, জাহাজ আর দেশের নাম **লেখার ঘর**, ড্রপডাউন নয়। মালিকের স্থায়ী
 * নিয়ম: যে তালিকা গ্রাহকভেদে বদলায় সেটা সেটিংসের সারি, কোডের ধ্রুবক
 * নয়। ⓘ কিন্তু সারি বানানোর আগেই ঘরগুলো দরকার, আর ফাঁকা লেখার ঘর
 * ভুল ড্রপডাউনের চেয়ে সৎ। ⚠️ যেদিন কোম্পানি নিজের বন্দর-তালিকা রাখতে
 * চাইবে, সেদিন এই ঘরগুলো ওই তালিকার দিকে দেখাবে — কলাম বদলাতে হবে না।
 *
 * ── ⚠️ যা এই মাইগ্রেশন **করে না** ───────────────────────────────────
 * ⛔ শুল্ক, বন্দর খরচ বা ভাড়া পণ্যের ক্রয়মূল্যে যোগ করে না। ⓘ ঘরগুলো
 * কেবল **রাখে** — ঠিক যেমন `transport_cost` আজ রাখে আর ক্রয়মূল্যে
 * ঢোকে না, আর পর্দাতেও সেটা লেখা আছে। ⚠️ landed cost আলাদা কাজ; এখানে
 * নীরবে অর্ধেক করলে প্রতিটা পণ্যের লাভ ভুল দেখাত।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('pur_bills', function (Blueprint $table): void {
            // ঋণপত্র — ব্যাংক এই নম্বরেই কাগজ খোঁজে
            $table->string('lc_no', 64)->nullable()->after('supplier_bill_no');

            /*
             * বিল অব এন্ট্রি — কাস্টমসে খালাসের কাগজ।
             *
             * ⓘ তারিখটা আলাদা, কারণ *"কবে খালাস হলো"* আর *"বিলের তারিখ"*
             * এক দিন নয়, আর শুল্ক-বছরের হিসাব খালাসের তারিখ ধরে চলে।
             */
            $table->string('be_no', 64)->nullable()->after('lc_no');
            $table->date('be_date')->nullable()->after('be_no');

            // জাহাজ বা ফ্লাইট, আর যে বন্দরে নামল
            $table->string('vessel', 120)->nullable()->after('be_date');
            $table->string('port_of_entry', 120)->nullable()->after('vessel');

            /*
             * ⚠️ ঋণপত্রের নম্বর ধরে খোঁজা হবেই — ব্যাংক ওই নম্বরটাই বলে।
             * ⓘ কোম্পানি ধরে, কারণ দুইটা কোম্পানির LC নম্বর এক হতে পারে।
             */
            $table->index(['company_id', 'lc_no']);
        });
    }

    public function down(): void
    {
        Schema::table('pur_bills', function (Blueprint $table): void {
            $table->dropIndex(['company_id', 'lc_no']);
            $table->dropColumn(['lc_no', 'be_no', 'be_date', 'vessel', 'port_of_entry']);
        });
    }
};
