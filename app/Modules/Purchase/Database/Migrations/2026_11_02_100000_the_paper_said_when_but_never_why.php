<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * কাগজটা বলত **কবে**, কোনোদিন বলত না **কেন**।
 *
 * ── মালিকের সিদ্ধান্ত (৫ সেপ্টেম্বর ২০২৬) ────────────────────────────
 * কাগজের মাথার তৃতীয় ঘরটার নাম **`Payment Terms`**, আর তালিকা সাতটা:
 *
 *     Cash · COD · ৩/৭/১৫/৩০ দিনের বাকি ·
 *     Cr. Upto Closing date · a fixed date
 *
 * ⛔ `Date Range` বাদ — একটা পাওনার **একটাই দেয় তারিখ**; রেঞ্জ হলে
 * বকেয়ার বয়সের রিপোর্ট বিলটাকে কোনো ঘরে ফেলতে পারত না।
 *
 * ── ⛔ কেবল `due_on` রাখলে যা হারাত ──────────────────────────────────
 * সাতটা বিকল্পের সবগুলোই শেষে একটা **তারিখ** দেয়, আর ওটা `due_on`-এ
 * বসে। ⚠️ কিন্তু তারিখটা ধরনটা বলে না: *"৫ সেপ্টেম্বর"* মানে নগদে
 * কেনা, নাকি মাল পৌঁছে দিয়ে টাকা (COD) — দুইটার তারিখ **একই দিন হতে
 * পারে**, ব্যবসায়িক অর্থ আলাদা।
 *
 * ⛔ **আর সেটাই নীরব ক্ষতি:** *"এই মাসে COD-তে কত কিনলাম"* প্রশ্নের
 * উত্তর কোথাও থাকত না, যদিও ব্যবহারকারী প্রতিটা বিলে ঘরটা ভরেছেন।
 * ⓘ ঘরটা ভরা হয় অথচ কোথাও পৌঁছায় না — এই রিপোতে ওটাই "মৃত ঘর", আর
 * মৃত ঘর মৃত বোতামের চেয়েও খারাপ: বোতাম কিছু করে না, ঘরটা **একটা
 * উত্তর হারায়**।
 *
 * ── ⚠️ কেন ধরনটা, আর দিনসংখ্যাটা নয় ─────────────────────────────────
 * দিনসংখ্যা একটা **নীতি** (কোম্পানির শর্ত, যা বদলাতে পারে); তারিখটা
 * একটা **ঘটনা** (এই কাগজে যা ঠিক হয়েছিল)। ⛔ দিনসংখ্যা রেখে দিলে কেউ
 * শর্ত বদলালে গত বছরের বন্ধ বিলগুলোর পরিশোধের তারিখও নীরবে সরে যেত।
 * ⭐ তাই খাতায় থাকে **ধরন + তারিখ**, শর্তের আইডি নয়।
 *
 * ── ⓘ মানগুলো ছোট আর স্থির ──────────────────────────────────────────
 *
 *     cash        হাতে হাতে
 *     cod         মাল পৌঁছে দিয়ে টাকা
 *     credit      কত দিনের বাকি — কত দিন তা `due_on`-এ
 *     month_end   বিলের মাসের শেষ দিন পর্যন্ত
 *     fixed       নিজের বাছা তারিখ
 *
 * ⚠️ **এগুলো আচরণ, তালিকা নয়** — তাই কোডেই থাকে। ⭐ যেটা গ্রাহকভেদে
 * বদলায় সেটা **দিনসংখ্যাগুলো** (এক গ্রাহকের ৫ দিন, আরেকজনের ৪৫), আর
 * ওগুলো আগে থেকেই সারি (`mdm_payment_terms`), কোডের ধ্রুবক নয়।
 *
 * ⓘ nullable, কারণ পুরনো প্রতিটা বিলের কোনো ধরন লেখা নেই — আর সেটাই
 * সত্যি। ⛔ পিছিয়ে গিয়ে একটা ধরন বসিয়ে দেওয়া মানে এমন একটা কথা লেখা
 * যা কেউ কোনোদিন বলেনি।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('pur_bills', function (Blueprint $table): void {
            $table->string('payment_term', 16)->nullable()->after('due_on');

            /*
             * ⚠️ *"এই মাসে COD-তে কত কিনলাম"* — প্রশ্নটা মাসে একবার
             * আসে, আর তখন কোম্পানির সব বিল ছেঁকে দেখতে হয়। ⓘ তারিখটা
             * সাথে, কারণ প্রশ্নটা কখনোই ধরন ছাড়া একা আসে না।
             */
            $table->index(['company_id', 'payment_term', 'trx_date']);
        });
    }

    public function down(): void
    {
        Schema::table('pur_bills', function (Blueprint $table): void {
            $table->dropIndex(['company_id', 'payment_term', 'trx_date']);
            $table->dropColumn('payment_term');
        });
    }
};
