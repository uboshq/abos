<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * যে গাড়িটা মাল নিয়ে এল, বিলে তার কোনো চিহ্ন ছিল না।
 *
 * ── কী ছিল না ────────────────────────────────────────────────────────
 * বিক্রয়ের চালানে বাহকের চারটা ঘর আছে — কে আনল, কত ভাড়া, কোন গাড়ি,
 * কে চালাল। ⛔ ক্রয়ে একটাও ছিল না। মেপে দেখা গেছে `app/Modules/Purchase`
 * জুড়ে `transport_cost` শব্দটা **শূন্যবার**।
 *
 * ── কেন এটা কেবল একটা অনুপস্থিত ঘর নয় ────────────────────────────────
 * মালিকের কথা: *"পরিবহনকারী মানে মাল আনার খরচ — নেওয়ার খরচও আছে এতে।"*
 * অর্থাৎ ভাড়াটা **ক্রয়মূল্যের অংশ**: ১০০ টাকার মাল + ৫ টাকা ভাড়া মানে
 * পণ্যটা ১০৫-এ পড়েছে, ১০০-এ নয়।
 *
 * ⚠️ ঘরটা না থাকায় ভাড়াটা হয় কোথাও লেখাই হত না, নয়তো আলাদা খরচ হয়ে
 * বসত — আর দুইটাতেই **প্রতিটা পণ্যের লাভ বেশি দেখাত**, ঠিক ভাড়ার
 * পরিমাণে। যে ব্যবসায় মার্জিন তিন শতাংশ, সেখানে এটা লাভ আর লোকসানের
 * তফাত।
 *
 * ── এই মাইগ্রেশন কেবল ঘর বানায় ───────────────────────────────────────
 * ⓘ ভাড়া কীভাবে সারিগুলোয় ভাগ হবে আর ক্রয়মূল্যে ঢুকবে, সেটা আলাদা
 * কাজ (নকশা: *"আনার খরচ ও ক্রয়মূল্য"*, ৪ সেপ্টেম্বর ২০২৬)। **ঘর আগে,
 * হিসাব পরে** — নাহলে দুইটা অসম্পূর্ণ জিনিস একসাথে ট্রিতে থাকত।
 *
 * ── কেন নাম আর সংযোগ দুইটাই ──────────────────────────────────────────
 * বিক্রয়ের দিকে ঠিক এই শিক্ষাটা হয়েছে: শুধু নাম রাখলে একই মানুষ
 * "করিম ট্রান্সপোর্ট" আর "Karim Transport" হয়ে দুইজন হয়ে যান, আর
 * *"এই পরিবহনকারীকে এই মাসে কত দিলাম"* প্রশ্নের উত্তর থাকে না।
 *
 * ⓘ তবু নামের ঘরটাও থাকছে: যে গাড়ি একবারের জন্য ভাড়া করা হয়েছে তাকে
 * পক্ষ বানানোর দরকার নেই, আর তখন নামটাই একমাত্র তথ্য।
 *
 * ⚠️ `nullOnDelete` — কেউ পরিবহনকারীকে মুছলে **বিলটা টিকে থাকবে**,
 * কেবল জোড়াটা খুলবে, আর নামটা ঘরে থাকায় কাগজটা তবু পড়া যাবে। বিল মুছে
 * গেলে তার সাথে মজুদের চলাচল আর খতিয়ানের সারিও যেত।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('pur_bills', function (Blueprint $table) {
            $table->string('carrier_name', 120)->nullable()->after('supplier_bill_no');

            $table->foreignId('carrier_id')
                ->nullable()
                ->after('carrier_name')
                ->constrained('suppliers')
                ->nullOnDelete();

            /*
             * ⓘ টাকার ঘরগুলোর সাথে একই মাপ (১৮,৪) — নাহলে যোগ-বিয়োগে
             * এক পয়সা করে হারাত, আর সেটা ধরা পড়ত মাস শেষে।
             */
            $table->decimal('transport_cost', 18, 4)->default(0)->after('carrier_id');

            $table->string('vehicle_no', 40)->nullable()->after('transport_cost');
            $table->string('driver_name', 120)->nullable()->after('vehicle_no');
        });
    }

    public function down(): void
    {
        Schema::table('pur_bills', function (Blueprint $table) {
            /*
             * ⚠️ বিদেশি চাবিটা আগে, তারপর কলামটা — উল্টো করলে MySQL
             * ইনডেক্স আঁকড়ে থাকে আর `down()` ব্যর্থ হয়।
             */
            $table->dropForeign(['carrier_id']);

            $table->dropColumn([
                'carrier_name', 'carrier_id', 'transport_cost', 'vehicle_no', 'driver_name',
            ]);
        });
    }
};
