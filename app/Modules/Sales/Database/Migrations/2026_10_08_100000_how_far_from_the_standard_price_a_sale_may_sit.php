<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * মান দাম থেকে কতটা সরা যাবে, আর সরলে কী হবে।
 *
 * ── আজকের নিয়মটা ভোঁতা ───────────────────────────────────────────────
 * আজ ABOS-এ **যেকোনো** ছাড়েই অনুমোদন লাগে — দশ টাকার ছাড়েও, দশ হাজারের
 * ছাড়েও ([[SalesInvoiceService::assertDiscountApproved()]])।
 *
 * ফল দুইদিকেই খারাপ। কাউন্টারে পাঁচ টাকার ছাড় দিতে গিয়ে বিল আটকে
 * থাকে, তাই লোকে ছাড় দেওয়াই বন্ধ করে দেয় — বা আরও খারাপ, দর কমিয়ে
 * লেখে যাতে ছাড়ের ঘরটা ছুঁতে না হয়। তখন খাতায় ছাড়টা আর দেখাই যায় না,
 * আর "আমরা কত ছাড় দিলাম" প্রশ্নের উত্তর মিথ্যা হয়ে যায়।
 *
 * ── যা এই ঘরটা যোগ করে ───────────────────────────────────────────────
 * একটা **সহনসীমা**: মান দামের কত শতাংশ এদিক-ওদিক স্বাভাবিক। তার ভেতরে
 * কিছুই হয় না। সীমা ছাড়ালে তিনটার একটা — মানা, সতর্কতা, নয়তো আটকানো।
 *
 * ── কেন নিচে আর উপরে আলাদা সুইচ ───────────────────────────────────────
 * মান দামের **নিচে** বেচলে টাকা যায়; **উপরে** বেচলে গ্রাহক যায়। কিছু
 * ডিপো কেবল প্রথমটা পাহারা দেয় — দ্বিতীয়টা তাদের কাছে বিক্রয়কর্মীর
 * কৃতিত্ব। একটা সুইচ রাখলে যারা কেবল নিচেরটা আটকাতে চান তাঁদের
 * উপরেরটাও সইতে হত, আর তখন তাঁরা পুরো নিয়মটাই বন্ধ করে দিতেন।
 *
 * ── কেন সারিটা না থাকাও একটা উত্তর ────────────────────────────────────
 * যে কোম্পানি কোনোদিন সীমা বসায়নি, সে **কাউকে থামাতে বলেনি**। তাই সারি
 * না থাকা মানে "সব চলবে" — অনুপস্থিতিকে সবচেয়ে কড়া নিয়ম ধরে নিলে
 * আপগ্রেডের দিন সকালে প্রতিটা কাউন্টার থেমে যেত।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('sal_pricing_rules', function (Blueprint $table) {
            $table->id();
            $table->publicId();

            /*
             * কোম্পানি-প্রতি একটাই।
             *
             * পণ্য ধরে ধরে আলাদা সীমা রাখা যেত, কিন্তু সেটা শুরুতেই
             * এমন একটা ছক চায় যা কেউ ভরবে না — দুই হাজার পণ্যের
             * প্রতিটায় সীমা বসানোর কাজটা কেউ করে না, আর অর্ধেক ভরা
             * ছক মানে অর্ধেক পাহারা।
             *
             * একটা সীমা দিয়ে শুরু। পণ্য ধরে ব্যতিক্রম লাগলে সেটা
             * পরে যোগ হবে, আর তখন কোনটা সাধারণ নিয়ম সেটা জানা থাকবে।
             */
            $table->foreignId('company_id')->unique()->constrained('companies')->cascadeOnDelete();

            /* ৫.০০ মানে মান দামের ৯৫% থেকে ১০৫% পর্যন্ত স্বাভাবিক */
            $table->decimal('tolerance_percent', 7, 4)->default(0);

            /*
             * সীমা ছাড়ালে কী।
             *
             * `allow` — কিছুই না, শুধু লেখা থাকে
             * `warn`  — পর্দায় সতর্কতা, কিন্তু বিল আটকায় না
             * `block` — সারিটাই নেওয়া হয় না
             *
             * অনুমোদনের পথটা আলাদা রাখা হয়নি: ABOS-এ ছাড়ের অনুমোদন
             * আগে থেকেই আছে, আর দুইটা ব্যবস্থা একই প্রশ্নের দুই উত্তর
             * দিলে কোনটা মানা হবে তা বলা যেত না।
             */
            $table->string('policy', 12)->default('allow');

            $table->boolean('applies_below')->default(true);
            $table->boolean('applies_above')->default(true);

            $table->foreignId('updated_by')->nullable()->constrained('users')->nullOnDelete();
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('sal_pricing_rules');
    }
};
