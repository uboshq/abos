<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * অর্ডারটা কোন দরজা দিয়ে এল।
 *
 * ── কেন এটা লাগে, আর কেবল পরিসংখ্যানের জন্য নয় ───────────────────────
 * ডিলার এখন নিজে পোর্টাল থেকে অর্ডার দিতে পারবেন (মালিকের সিদ্ধান্ত,
 * ৩ সেপ্টেম্বর ২০২৬)। ততদিন প্রতিটা অর্ডার বসাতেন কোম্পানির কেউ, তাই
 * "কোথা থেকে এল" প্রশ্নটার উত্তর ছিল "ভিতর থেকে" — একটাই উত্তর, তাই
 * কলামেরও দরকার ছিল না।
 *
 * ⚠️ ── আর এটাই `created_by` খালি থাকার একমাত্র ব্যাখ্যা ──────────────
 * `sal_orders.created_by` foreign key `users`-এ যায়, আর **ডিলার
 * `users`-এ নেই** — সে `customers`-এ, আলাদা গার্ডে। তাই পোর্টাল থেকে
 * আসা অর্ডারে ওই ঘরটা খালি থাকবে ([[…created_by_customer_id]] আলাদা
 * মাইগ্রেশনে বসছে)।
 *
 * এই কলামটা না থাকলে ছয় মাস পরে কেউ একটা খালি `created_by` দেখে ধরে
 * নিতেন সারিটা **সিডার বা কমান্ড লাইন থেকে** এসেছে। খালি ঘরের একটা
 * ব্যাখ্যা থাকা দরকার, আর সেটা ডেটাতেই থাকা দরকার — মন্তব্যে নয়।
 *
 * ── ⭐ আর একটা কারণ, যেটা ছয় মাস পরে দামি হবে ────────────────────────
 * ডিলার **মজুদ না দেখে** অর্ডার দেবেন (মালিকের সিদ্ধান্ত)। মানুষ তখন
 * স্বাভাবিকভাবেই **বেশি চাইবেন** — "যদি কমিয়ে দেয়"। অর্থাৎ পোর্টালের
 * অর্ডারের সংখ্যা আর আসল চাহিদা এক নয়।
 *
 * কেউ পরে ওই সারিগুলো ধরে চাহিদার পূর্বাভাস বানালে **ভুল হবে**, আর
 * কারণটা কোডে খুঁজে পাওয়া যাবে না। এই কলামটা থাকলে অন্তত সারিগুলো
 * **আলাদা করে চেনা যায়**।
 *
 * ── ⚠️ কেন মানগুলো স্থির, অথচ রিপোর নিয়ম "তালিকা মানে সারি" ──────────
 * মালিকের দাঁড়ানো নিয়ম: যে তালিকা **গ্রাহকভেদে বদলায়** সেটা সেটিংসের
 * সারি, কোডের ধ্রুবক নয়।
 *
 * এখানে নিয়মটা ভাঙা হয়নি — **এই তিনটা ব্যবসার তালিকা নয়, ব্যবস্থার
 * নিজের**। `portal` মানে "আমাদের পোর্টাল সফটওয়্যারটা", `sr` মানে
 * "মাঠকর্মীর অ্যাপ", `counter` মানে "দোকানের পর্দা"। কোনো কোম্পানি
 * চতুর্থ একটা দরজা **যোগ করতে পারে না** — ওটা যোগ হয় কোড লিখলে।
 *
 * ⓘ পার্থক্যটা এই প্রশ্নে: *"গ্রাহক কি সেটিংস থেকে নতুন একটা বসাতে
 * পারবেন?"* পারলে সারি, না পারলে স্থির। এখানে পারেন না।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('sal_orders', function (Blueprint $table): void {
            /*
             * ডিফল্ট `counter` — আজকের প্রতিটা সারি ওখান থেকেই এসেছে।
             *
             * ⚠️ nullable রাখা হয়নি ইচ্ছাকৃতভাবে: null মানে "জানি না",
             * আর তখন পুরনো সারিগুলো নিয়ে প্রশ্নটা চিরকাল খোলা থাকত।
             * আজ পর্যন্ত পোর্টাল বা SR থেকে একটাও অর্ডার আসেনি, তাই
             * `counter` **অনুমান নয়, সত্য**।
             */
            $table->string('source', 16)->default('counter')->after('customer_id');

            // "পোর্টাল থেকে কী কী এল" — রিপোর্টের সবচেয়ে সাধারণ প্রশ্ন
            $table->index(['company_id', 'source']);
        });
    }

    public function down(): void
    {
        Schema::table('sal_orders', function (Blueprint $table): void {
            $table->dropIndex(['company_id', 'source']);
            $table->dropColumn('source');
        });
    }
};
