{{--
    ⚠️ এই ফাইলের কোনো মন্তব্যে **কম্পোনেন্টের ট্যাগ কোণ-বন্ধনীসহ লিখবেন না**
    (কোণ-বন্ধনী ছাড়া, যেমন `x-ui.date`) — Blade মন্তব্য ফেলে দেওয়ার **আগে**
    কম্পোনেন্টের ট্যাগ খোঁজে, তাই মন্তব্যের ভিতরের ট্যাগটাও সে **আসল খোলা
    ট্যাগ** ধরে নেয়। বন্ধ না পেলে সে বাকি পুরো ফাইলটা গিলে ফেলে, আর ভুলটা
    দেখা যায় একদম শেষে: *"unexpected end of file, expecting endif"* — যে
    লাইনটার সাথে আসল ভুলের কোনো সম্পর্ক নেই।

    ⓘ একই কথা JavaScript-এর `/* */` মন্তব্যেও খাটে — ওগুলো Blade-এর কাছে
    সাধারণ লেখা, আর সেখানেও ট্যাগটা ধরা পড়ে। ৩ সেপ্টেম্বর ২০২৬-এ ঠিক
    এভাবেই পর্দাটা ৫০০ দিয়েছিল।

    সরাসরি বিক্রয় — নমুনার হুবহু বিন্যাস।

        উপরে সরু স্ট্রিপ   তারিখ · বিল নম্বর · মেয়াদ · DO · গুদাম
        উপরের সারি        স্ট্রিপ · "এই লাইন" · পণ্যের ছবি — এক সারিতে
        এন্ট্রি এলাকা       পণ্য খোঁজা ও লাইনের ঘরগুলো, পুরো প্রস্থে
        কার্ট              SL# থেকে টাকা পর্যন্ত ন'টা কলাম
        ডান পাশের প্যানেল   ক্রেতা, এই চালান, দিতে হবে, পার্টির বকেয়া, গোনা

    ── কেন ডান পাশটা আলাদা কলাম, নিচে নয় ────────────────────────────────
    টাকার অঙ্কগুলো সবসময় চোখের সামনে থাকতে হয়, কার্ট যত লম্বাই হোক।
    নিচে রাখলে দশ লাইনের বিলে Confirm বোতামটা ভাঁজের নিচে চলে যেত, আর
    কাউন্টারের লোককে স্ক্রল করে খুঁজতে হত — যে কাজটা করতে তিনি এসেছেন।

    ── "এই লাইন" প্যানেলটা কেন ────────────────────────────────────────
    কার্টে যোগ করার আগেই লাইনটার টাকা কত হচ্ছে সেটা দেখা যায়। না দেখালে
    ভুল দর বা ভুল ছাড় ধরা পড়ত কার্টে যোগ করার পরে, আর তখন সারিটা মুছে
    আবার লিখতে হত।
--}}
@php
    $vatEnabled = $show['vat'] ?? true;
@endphp

<x-layouts.app :menu="$menu">
    <x-slot:title>{{ __('sales::menu.direct') }}</x-slot:title>

    @if (session('saved'))
        <div role="status"
             class="mb-3 rounded-(--radius-field) bg-(--color-badge-success-bg) px-3 py-2 text-sm
                    text-(--color-badge-success-ink)">
            {{ session('saved') }}
        </div>
    @endif

    @if ($errors->any())
        <div role="alert"
             class="mb-3 rounded-(--radius-field) bg-(--color-badge-danger-bg) px-3 py-2 text-sm
                    text-(--color-badge-danger-ink)">
            <ul class="list-inside list-disc">
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif

    <form method="POST" action="{{ route('sales.direct.store') }}"
          x-data="directSale({{ Illuminate\Support\Js::from($products) }}, {{ Illuminate\Support\Js::from($customerTerms) }}, {{ $walkinId }}, {{ $vatEnabled ? 'true' : 'false' }}, {{ Illuminate\Support\Js::from($packs) }})"
          @bulk-applied.window="absorbBulk($event.detail.rows)"

          {{--
              ── অসমাপ্ত চালান ধরে রাখা ─────────────────────────────────

              `x-init` পাতা খোলার সময় দেখে খসড়া আছে কিনা — **ফেরায় না**,
              কেবল উপরে প্রস্তাব দেখায়।

              `x-effect` প্রতিবার কিছু বদলালেই লিখে রাখে। Alpine নিজেই
              বুঝে নেয় কোন কোন মান পড়া হয়েছে, তাই আলাদা করে watcher
              লিখতে হয় না — আর নতুন একটা ঘর যোগ করলে সেটাও **নিজে থেকেই**
              খসড়ায় ঢোকে, কেউ ভুলে গেলেও।

              ⚠️ `@submit`-এ খসড়া মুছে যায়। **কারণটা ওজন করে নেওয়া:**
              সফল বিক্রির পরে খসড়া থেকে গেলে **প্রতিটা বিক্রির পরেই**
              "ফিরিয়ে আনব?" প্রশ্নটা আসত — আর দিনে পঞ্চাশবার "বাদ দিন"
              চাপতে চাপতে একদিন কেউ **সত্যিকারের একটা খসড়া** বাদ দিয়ে
              ফেলতেন। ⓘ দাম: সার্ভার যদি চালানটা ফিরিয়ে দেয়, কার্টটা
              যায় — কিন্তু সেটা আজও যেত, এই বদলে নতুন কিছু হারায়নি।
          --}}
          x-init="lookForDraft()"
          x-effect="saveDraft()"
          @submit="parkDraft()"

          {{--
              ── কি-বোর্ডের শর্টকাট — POS-এর কী-গুলোই ─────────────────────

              মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬): *"sortcut baton kaj
              koraw"*।

              ── কেন নতুন কী বানানো হয়নি ─────────────────────────────────
              POS-এ এই ব্যবস্থাটা **আগে থেকেই আছে** (F1–F10), আর কাউন্টারের
              লোক ওগুলো হাতে শিখে ফেলেছেন। ⚠️ এখানে আলাদা কী দিলে **একই
              মানুষকে দুই পর্দায় দুই অভ্যাস** রাখতে হত — আর কাউন্টারে
              অভ্যাসই গতি।

                  F1   এই তালিকা          POS-এ একই
                  F2   জমা যোগ            POS-এ "টাকার ঘর" — একই অর্থ
                  F6   চার্ট এন্ট্রি        POS-এ split, এখানে নেই
                  F7   ক্রেতা খোঁজা         POS-এ একই
                  F8   পণ্য খোঁজা          POS-এ একই
                  F9   কার্টে যোগ করুন
                  F10  নিশ্চিত করুন         POS-এ "বিক্রয় সম্পূর্ণ"
                  Esc  খোলা জিনিস বন্ধ

              ⚠️ **F4 ইচ্ছে করে খালি** — POS-এ ওটা "বিল ধরে রাখুন", আর সেই
              কাজটা এই পর্দাতেও আসছে। এখন অন্য কিছুতে দিলে পরে কেড়ে নিতে হত,
              আর কেড়ে নেওয়া অভ্যাস সবচেয়ে বিরক্তিকর।

              ⓘ `.prevent` লাগে কারণ ব্রাউজার F-কী গুলো নিজের কাজে নেয়
              (F7 caret browsing, F10 মেনু) — না দিলে শর্টকাট চলত, সাথে
              ব্রাউজারের কাজটাও।

              ⚠️ Esc দুই ধাপে, POS-এর মতোই: আগে খোলা প্যানেল, তারপর
              বাছাইয়ের ঘর। একবারে সব বন্ধ করলে ভুল চাপে টাইপ করা সব যেত।
          --}}
          @keydown.window.f1.prevent="helping = ! helping"
          @keydown.window.f2.prevent="openPanel('deposit')"
          @keydown.window.f6.prevent="$refs.chartEntry?.querySelector('button')?.click()"
          @keydown.window.f7.prevent="customerPickerOpen = true"
          @keydown.window.f8.prevent="openPicker()"
          @keydown.window.f9.prevent="picked && addToCart()"

          {{--
              ── Enter দিয়েও কার্টে যোগ — মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬)

              *"শর্টকাট Enter key রাখো add করার জন্য"*।

              ⚠️ **শর্তগুলো ছাড়া এটা বিপজ্জনক হত:**

              `picked` না থাকলে কিছু করে না — নাহলে খালি পর্দায় Enter চেপে
              কিছু না ঘটা দেখে মানুষ ভাবতেন পর্দা আটকে গেছে।

              `panel` খোলা থাকলে করে না — জমা বা নোটের ঘরে Enter চাপা মানে
              ওই ঘরের কাজ, কার্টে ফেলা নয়।

              ⓘ পণ্য ও ক্রেতা খোঁজার ঘরে Enter আগে থেকেই **প্রথমটা বাছে**
              (`pickFirst`), আর ওগুলো নিজের ঘরে `.prevent` করে — তাই এই
              window-স্তরের হ্যান্ডলারে পৌঁছায় না। **দুইটা Enter দুই কাজ
              করে, আর কোনটা কখন তা ঘরটাই ঠিক করে।**
          --}}
          @keydown.window.enter="! panel && picked && addToCart()"
          @keydown.window.f10.prevent="canConfirm && $refs.confirm?.click()"
          @keydown.window.escape="panel ? (panel = '') : (pickerOpen || customerPickerOpen
              ? (pickerOpen = false, customerPickerOpen = false)
              : (helping = false))"

          class="grid gap-3 xl:grid-cols-[1fr_17rem]">
        @csrf

        {{--
            ── মজুদ নেই — পর্দার মাঝখানে, লাল, শব্দসহ ──────────────────────

            মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬): *"stock na thakle …
            warning sound dibe, color lal hobe, screen-er majkhane boro
            kore notice dibe"*।

            ── কেন মাঝখানে, কোণায় নয় ──────────────────────────────────
            ⚠️ কাউন্টারে চোখ থাকে পণ্যের ঘরে, আর কোণার ছোট বার্তা **কেউ
            পড়েন না** — বিশেষ করে যখন ক্রেতা সামনে দাঁড়িয়ে কথা বলছেন।
            এটা সরিয়ে না দেওয়া পর্যন্ত পর্দা এগোয় না, তাই না পড়ে
            এগোনোর উপায় নেই।

            ⓘ পণ্যের নাম ও কোড দুইটাই লেখা — একই নামের দুইটা পণ্য থাকলে
            কোনটা তা কোডই বলে (আজকের ডুপ্লিকেশন ইঞ্জিনের কারণ)।

            ⚠️ Esc বা বাইরে চাপলেই বন্ধ — একটা বাড়তি ক্লিক চাপিয়ে দেওয়ার
            মানে নেই, বার্তাটা পড়া হয়ে গেলে কাজ শেষ।
        --}}
        <div x-show="outOfStock" x-cloak @click.self="outOfStock = null"
             @keydown.window.escape="outOfStock = null"
             class="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4">
            <div class="w-full max-w-md rounded-(--radius-card) border-2 border-(--color-danger)
                        bg-(--color-surface-card) p-6 text-center shadow-lg">
                <p class="text-3xl font-bold text-(--color-danger)">
                    {{ __('sales::message.no_stock_title') }}
                </p>

                <p class="mt-3 text-lg font-semibold text-(--color-ink)"
                   x-text="outOfStock?.name"></p>

                <p class="num mt-1 text-2xs text-(--color-ink-muted)"
                   x-text="outOfStock?.code"></p>

                <p class="mt-4 text-sm text-(--color-ink-muted)">
                    {{ __('sales::message.no_stock_hint') }}
                </p>

                <button type="button" @click="outOfStock = null"
                        class="mt-5 w-full rounded-(--radius-field) bg-(--color-danger) px-4 py-2
                               font-semibold text-white hover:bg-(--color-danger-hover)">
                    {{ __('core.action.close') }}
                </button>
            </div>
        </div>

        {{--
            ── F1-এর সাহায্য — শর্টকাটগুলো কোথাও লেখা থাকতেই হবে ───────────

            ⚠️ **শর্টকাট আছে অথচ কোথাও লেখা নেই — এটাই সবচেয়ে অকেজো ধরনের
            সুবিধা।** যিনি জানেন কেবল তিনিই পান, আর নতুন কর্মী কোনোদিন
            জানেনই না যে জিনিসটা আছে।

            ⓘ POS-এও F1 ঠিক এই তালিকাটাই খোলে — একই কী, একই আচরণ।
        --}}
        <div x-show="helping" x-cloak @click.self="helping = false"
             class="fixed inset-0 z-40 flex items-center justify-center bg-black/40 p-4">
            <div class="w-full max-w-sm rounded-(--radius-card) bg-(--color-surface-card) p-4 shadow-lg">
                <h2 class="mb-3 font-medium">{{ __('sales::message.direct_keys') }}</h2>

                <dl class="space-y-1 text-sm">
                    @foreach ([
                        'F1' => 'sales::message.key_help',
                        'F2' => 'sales::message.key_paid',
                        'F6' => 'sales::message.key_chart',
                        'F7' => 'sales::message.key_customer',
                        'F8' => 'sales::message.key_search',
                        'F9' => 'sales::message.key_add_line',
                        'F10' => 'sales::message.key_checkout',
                        'Esc' => 'sales::message.key_close',
                    ] as $key => $label)
                        <div class="flex justify-between gap-4">
                            <dt class="num font-medium">{{ $key }}</dt>
                            <dd class="text-(--color-ink-muted)">{{ __($label) }}</dd>
                        </div>
                    @endforeach
                </dl>
            </div>
        </div>

        {{--
            ── "একটা অসমাপ্ত চালান পড়ে আছে" ───────────────────────────────

            ⚠️ প্রস্তাব, নিজে থেকে ফেরানো নয় — আর পার্থক্যটা টাকার।

            নিজে থেকে ফিরিয়ে আনলে কেউ নতুন বিক্রি শুরু করতে এসে **আগের
            অসমাপ্ত চালানটা না বুঝেই পেয়ে যেতেন**, তার উপরে নতুন লাইন যোগ
            করে নিশ্চিত করে ফেলতেন — **ভুল ক্রেতার নামে ভুল মাল**।

            তাই দুইটা স্পষ্ট বোতাম, আর তারিখ-সময়সহ — কোন সময়ের খসড়া তা
            না জানলে ফেরানো কি বাদ দেওয়া, কোনোটাই নিরাপদ সিদ্ধান্ত নয়।
        --}}
        <div x-show="draftFound" x-cloak
             class="flex flex-wrap items-center gap-3 rounded-(--radius-card) border
                    border-(--color-warning) bg-(--color-badge-pending-bg) p-3 xl:col-span-2">
            <span class="text-sm font-semibold text-(--color-badge-pending-ink)">
                {{ __('sales::message.draft_found') }}
            </span>

            <span class="num text-2xs text-(--color-ink-muted)" x-text="draftAt"></span>

            <button type="button" @click="restoreDraft()"
                    class="ms-auto rounded-(--radius-field) bg-(--color-brand-600) px-4 py-1.5
                           text-xs font-semibold text-white">
                {{ __('sales::action.draft_restore') }}
            </button>

            <button type="button" @click="dropDraft()"
                    class="rounded-(--radius-field) border border-(--color-border) px-3 py-1.5 text-xs">
                {{ __('sales::action.draft_discard') }}
            </button>
        </div>

        {{-- ══ বাঁ দিক: স্ট্রিপ · এন্ট্রি · কার্ট ══════════════════════ --}}
        <div class="min-w-0 space-y-3">
            {{--
                ── কাগজের পরিচয় — দুইটা বাক্স, পাশাপাশি, এক সারিতে ─────────

                ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ───────────────────────
                *"Customar ei linei alada box koro — tahole customer details
                er sathe baki line gulo mota hobe na"*।

                ⚠️ **এই কারণটা আমি নিজে ধরতে পারিনি, আর ওটাই আসল কথা।**
                আগের ধাপে দুইটা এক বাক্সে ঢোকানো হয়েছিল। এক বাক্স মানে
                এক উচ্চতা: ক্রেতার তিন সারি (নাম · এলাকা+ফোন · ঠিকানা)
                বাক্সটাকে উঁচু করত, আর **তারিখ-নম্বর-মেয়াদ-DO-র পাশে বিশাল
                ফাঁকা জায়গা তৈরি হত** — ঘরগুলো "মোটা" দেখাত।

                এখন দুইটা আলাদা `<section>`, একই সারিতে `flex` দিয়ে।
                `items-start` বলে দেয় **যার যতটুকু উচ্চতা দরকার সে ততটুকুই
                নেবে** — একজনের লম্বা পরিচয় অন্যজনের ঘরগুলোকে টানবে না।

                ── আজকের চার ধাপ, যাতে কেউ পিছিয়ে না যায় ───────────────────
                সকাল: ক্রেতা ডান প্যানেল → বাঁ স্ট্রিপে।
                দুপুর: স্ট্রিপ থেকেও উপরে, নিজের বাক্সে, পুরো প্রস্থে।
                বিকাল: দুইটা বাক্স এক করা হলো (এক সারি, এক বর্ডার)।
                সন্ধ্যা: **আবার দুইটা — কিন্তু পাশাপাশি, উপর-নিচ নয়।**

                ⓘ পার্থক্যটা সূক্ষ্ম কিন্তু আসল: সমস্যাটা "দুইটা বাক্স" ছিল না,
                ছিল **দুইটা বাক্স একটার নিচে আরেকটা**। পাশাপাশি বসলে দুইটা
                বাক্স উচ্চতাও বাঁচায়, আর প্রত্যেকে নিজের মাপে থাকে।

                ⚠️ দুইটাই `<form>`-এর ভেতরে থাকতেই হবে — লুকানো
                `customer_id` আর `warehouse_id` এদের ভেতরে, আর বাইরে গেলে
                চালান ক্রেতা বা গুদাম ছাড়াই সেভ হবে, **কোনো ভুলবার্তা ছাড়াই**।
            --}}
            {{--
                ── ⚠️ এই ছকটা নিচের ছকের হুবহু নকল, আর সেটাই এর একমাত্র কাজ ──

                ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ───────────────────────
                লাল কলম দিয়ে প্রতিটা বাক্সের সীমা এঁকে: *"box gulo mark kora
                ache — dekho konta kon porjonto zabe"*।

                মেপে দেখা গেল উপরের সারির ভাগটা নিচের সারির ভাগের সাথে
                **মেলে না**: কাগজের বাক্স ৬২০px-এ শেষ হত, অথচ ঠিক তার নিচের
                এন্ট্রি বাক্স ৭৬৫px-এ। **দুইটা খাড়া রেখা, ১৪৫px দূরে** —
                চোখে ওটা এলোমেলো দেখায়, আর কারণটা ধরা কঠিন।

                ── কেন মিলত না ─────────────────────────────────────────────
                এটা ছিল `flex` + `basis`, আর নিচেরটা `grid` — অর্থাৎ উপরের
                ভাগটা **জিনিসের মাপ দেখে** ঠিক হত, নিচেরটা **কলামের মাপ
                দেখে**। দুইটা আলাদা নিয়ম, তাই মেলার কোনো কারণই ছিল না।

                ⚠️ **তাই এখানে ছকের সংজ্ঞাটা নিচেরটার সাথে অক্ষরে অক্ষরে
                এক** — `lg:grid-cols-[1fr_11rem]` আর
                `2xl:grid-cols-[1fr_13rem_9rem]`। **একটা বদলালে অন্যটাও
                বদলাতে হবে**, নইলে রেখা দুইটা আবার আলাদা হয়ে যাবে।

                ক্রেতার বাক্স `lg:col-span-2` — অর্থাৎ সে নিচের "এই লাইন"
                আর ছবির ঘর, **দুইটা কলাম মিলিয়ে ততটাই** চওড়া। আর সরু
                পর্দায় (মাত্র দুই কলাম) ওটা নিজে থেকেই নিচের সারিতে পুরো
                প্রস্থ নিয়ে নামে — চেপে যাওয়ার বদলে।
            --}}
            <div class="grid gap-3 lg:grid-cols-[1fr_14rem] 2xl:grid-cols-[1fr_27rem] lg:items-start">

                {{-- বাক্স ১ · কাগজটা কী — তারিখ · নম্বর · মেয়াদ · DO --}}
            {{-- বাঁ কলাম: কাগজের পরিচয় → পণ্য খোঁজা → লাইনের ঘর → কার্ট

                 ⚠️ কাগজের বাক্সটা এই কলামের **ভেতরে**, আলাদা ছক-ঘরে নয়।

                 ── কেন (৩ সেপ্টেম্বর ২০২৬) ──────────────────────────────
                 আলাদা ঘরে থাকলে প্রথম সারির উচ্চতা ঠিক হত ডান পাশের "এই
                 লাইন" দেখে — আর ও দুইটার চেয়ে উঁচু। ফলে কাগজের বাক্সের
                 নিচে **~১৩০px ফাঁকা** পড়ে থাকত, আর পণ্য খোঁজার ঘরটা
                 অকারণে নিচে নেমে যেত।

                 এক কলামে সব থাকলে ওরা একটার পিঠে আরেকটা বসে, আর ডান
                 পাশের উচ্চতা কিছুই টানে না। --}}
            <div class="min-w-0 space-y-3 lg:col-start-1">
                {{--
                    ⚠️ সবুজ উপরের রেখাটা তুলে দেওয়া হলো (৩ সেপ্টেম্বর ২০২৬)।

                    মালিক বললেন সব বাক্স একসাথে ডিজাইন করতে। মেপে দেখা গেল
                    পাঁচটা বাক্সের **পাঁচ রকম চেহারা**: একটার সবুজ উপরের
                    রেখা, একটার সাধারণ বর্ডার, একটার বর্ডারই নেই, কারও
                    ছায়া নেই।

                    ⭐ **এখন পাঁচটাই এক নিয়মে** — একই বর্ডার, একই কোণ, একই
                    হালকা ছায়া। D365 বা Odoo-র মতো পর্দায় **কার্ডগুলো
                    নিজেরা চুপ থাকে, কথা বলে ভেতরের জিনিস** — একটা কার্ডের
                    গায়ে রঙিন রেখা মানে সে অন্যদের চেয়ে জরুরি, আর এখানে
                    সেটা সত্যি ছিল না।

                    ⓘ একমাত্র রঙিন কার্ড "এই লাইন", কারণ ওটাই **চলমান
                    অঙ্ক** — বাকিরা ঘর, ওটা ফল।
                --}}
                {{-- ⚠️ এই মোড়কটা আর **দেখা যায় না** — সীমানা, ছায়া আর
                     প্যাডিং তিনটাই ভিতরের দুইটা বাক্সে চলে গেছে।

                     ⛔ না সরালে বাক্সের ভিতরে বাক্স হত — তিনটা
                     সীমানা পাশাপাশি, আর মালিক ঠিক ঐ এলোমেলো
                     রেখাগুলোর কথাই বলছেন। --}}
                <section data-boxed class="min-w-0">
                    <div class="@container">
                        {{-- ধাপগুলো কনটেইনারের নিজের প্রস্থে, আর ধাপ তিনটা।

                             উপরে ওঠার পর স্ট্রিপটা আর পুরো প্রস্থ পায় না —
                             সবুজ প্যানেল আর ছবির ঘর পাশে বসেছে, তাই এই
                             কলামটা ~৩০০px। পুরনো `@xl` (৩৬rem) ওখানে কোনোদিন
                             পৌঁছাত না, ফলে পাঁচটা ঘর দুই কলামে **তিন সারি**
                             হয়ে স্ট্রিপটা আগের চেয়ে উঁচু দেখাত — মালিক ঠিক
                             উল্টোটা চেয়েছিলেন।

                             ১৭rem-এ তিন কলাম (পাঁচটা ঘর দুই সারিতে), আর
                             জায়গা থাকলে ৩৪rem-এ পাঁচটাই এক সারিতে। --}}
                        {{-- একই কারণ, একই সারাই — উপরের ঘরগুলোও স্থির মাপে --}}
                        {{-- ── ঘরগুলো সারি জুড়ে সমানভাবে, ফাঁকা জায়গা ছাড়া ──────

                             ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ────────────────
                             *"ekhan kar faka jayga komaw"* — চারটা জায়গায় লাল
                             দাগ: তারিখের ভেতরে, মেয়াদের ভেতরে, DO-র ভেতরে, আর
                             DO-র পরে সারির শেষ পর্যন্ত।

                             ⚠️ সবচেয়ে বড়টা ছিল **শেষেরটা** — ঘরগুলোর মাপ বাঁধা
                             ছিল (`w-36`, `w-32`, `w-28`), তাই ওরা মিলে ~৫৩০px
                             নিত আর বাক্সের বাকি ~২৩০px খালি পড়ে থাকত।

                             ── আর মাপতে গিয়ে উল্টো একটা সমস্যা বেরোল ────────────
                             **বিল নম্বরটা কেটে যাচ্ছিল**: `w-32` (১২৮px) ঘরে
                             `INV-2026-2027-0001` ধরত না, পর্দায় দেখাত
                             `INV-2026-2027-0`। ⚠️ কাগজের **পরিচয়** অর্ধেক দেখা
                             যাচ্ছিল, আর ওটা স্ক্রিনশটে চোখ এড়িয়ে যায়।

                             এখন ছক: চারটা কলাম সারিটা ভাগ করে নেয়, প্রতিটা ঘর
                             নিজের কলামের পুরোটা। শেষে কিছু বাঁচে না, আর নম্বরও
                             পুরোটা দেখা যায়।

                             ⓘ সরু পর্দায় দুই কলাম — চারটা এক সারিতে চাপালে
                             প্রতিটা এত সরু হত যে তারিখটাই পড়া যেত না। --}}
                        {{-- ── ক্রেতার চিহ্নটা এই সারিরই একটা কলাম ─────────────

                             ── মালিকের প্রশ্ন (৫ সেপ্টেম্বর ২০২৬) ────────────────
                             *"Invoice Date, INV Number, Credit Period, DO No.
                             eigilor faka jayga bad dile bame ze jayga faka hobe
                             tate customer detail dile kemon hoy?"*

                             ⛔ **ফাঁকা জায়গাটা কাটা যায় না** — ঘরগুলোর মাপ
                             ৩ সেপ্টেম্বরে ইচ্ছে করে ছেড়ে দেওয়া হয়েছিল, কারণ
                             বাঁধা মাপে `INV-2026-2027-0001` কেটে গিয়ে
                             `INV-2026-2027-0` দেখাত। ⚠️ এই কোম্পানির সিরিজ ছোট
                             (`INV-0001`) বলে আজ জায়গাটা খালি লাগে; যে ক্রেতার
                             সিরিজে বছর থাকে তার কাছে ওটা ভরাট। ⓘ আজকের লেখার
                             মাপে কাটলে **একটা installation ঠিক হত, পণ্যটা
                             ভাঙত**।

                             ⭐ তাই কাটা নয়, **তোলা**: ক্রেতার চিহ্নটা নিচের
                             সারি ছেড়ে এই সারির পঞ্চম কলামে উঠে এসেছে, আর
                             পরিচয়ের সারিটা কেবল ক্রেতা বাছার পরে নামে।

                             ⓘ ফল — খালি অবস্থায় গোটা একটা সারি কমে গেল (মালিক
                             ঠিক ওটাই দেখেছেন), আর বাছার পরে পরিচয় আগের মতোই
                             পুরো প্রস্থ পায়। **কোনো ঘর সরু হয়নি।**

                             ── ⚠️ ছকটা দুই রকম, আর কেন `@class` ──────────────
                             DO-র ঘরটা সুইচের পেছনে, তাই ঘরটা বন্ধ থাকলে কলাম
                             চারটা। ⛔ আগে ছকে সবসময় চারটা ট্র্যাক লেখা থাকত,
                             তাই DO বন্ধ কোম্পানিতে সারির শেষে একটা **খালি
                             কলাম** পড়ে থাকত — মালিক যে ফাঁকা জায়গার কথা
                             বলছেন, তারই আরেকটা চেহারা।

                             ⚠️ ক্লাসটা PHP দিয়ে জোড়া **যাবে না** — Tailwind
                             ফাইলের লেখা পড়ে ক্লাস বানায়, তাই
                             `grid-cols-[{{ $cols }}]` লিখলে সে কিছুই খুঁজে পেত
                             না আর ছকটা নীরবে ভেঙে যেত। ⓘ `@class` দুইটা
                             **আস্ত লেখা** রাখে, তাই দুইটাই তৈরি হয় আর
                             একটাই বসে। --}}
                        {{-- ── ক্রেতা বাঁয়ে, কাগজের ঘরগুলো ডানে ──────────────

                             ── মালিকের নির্দেশ (৫ সেপ্টেম্বর ২০২৬) ────────────
                             *"customer details inv date er bame nite
                             bolechilam"* — আর তার আগে লাল দাগ দেওয়া একটা ছবি:
                             *"Invoice Date, INV Number, Credit Period, DO No.
                             egulo dui line lal dager mode rako"*।

                             ⛔ **আগে পাঁচটা ঘর এক সারিতে ঠাসার চেষ্টা ছিল**, আর
                             সেটা প্রস্থের উপর ছেড়ে দেওয়া: কনটেইনার ৪৪৮px-এর
                             বেশি হলে পাঁচ কলাম, নাহলে দুই।

                             ⚠️ মেপে দেখা গেছে ঠিক **১৪৪০-এ ঘরটা ৪৩৮px** — দশ
                             পিক্সেলের জন্য সারিটা ভেঙে দুই কলাম হয়ে যেত, অথচ
                             ১২৮০ · ১৬০০ · ১৯২০ — সবগুলোতেই পাঁচ কলাম বসত।
                             ⓘ **ব্রেকপয়েন্টের মাঝের ফাঁক**, আর ১৪৪০ সবচেয়ে
                             চলতি ল্যাপটপের মাপ।

                             ⭐ মালিক ঐ ভাঙা অবস্থাটাই দেখেছেন, আর সমাধানটাও
                             তিনিই দিয়েছেন: এক সারিতে না ঠেসে **দুই লাইনে দুইটা
                             করে**। ⓘ এখন ছকটা প্রস্থের উপর নির্ভর করে না, তাই
                             ডেড জোনও নেই। --}}
                        {{-- ⚠️ **কনটেইনার-কোয়েরি নয়, ভিউপোর্ট-ব্রেকপয়েন্ট** —
                             আর কারণটা মেপে পাওয়া।

                             ⛔ `@sm:` লিখলে CSS হয় `@container (width>=24rem)`,
                             আর এই থিমের রুট ফন্ট **২০px** বলে ওই `24rem`
                             কত পিক্সেল সেটা নির্ভর করে কনটেইনারটা কোথায় বসে
                             আছে তার উপর। ⓘ মেপে দেখা গেছে ১০২৪-এ দুই কলাম হয়,
                             ১৪৪০-এ হয় না — অথচ দুই জায়গাতেই কার্ডের প্রস্থ
                             প্রায় সমান (৪৪৮ আর ৪৩৮)।

                             ⭐ মিডিয়া-কোয়েরির `rem` **সবসময় ১৬px** (CSS-এর
                             নিয়ম, রুট ফন্ট যাই হোক), তাই `sm:` মানে ঠিক
                             ৬৪০px — অনুমেয়, আর প্রতিটা ডেস্কটপ প্রস্থে সত্যি।

                             ⚠️ এই ফাঁদে এই রিপো আগেও পড়েছে: ব্রেকপয়েন্ট
                             দিয়ে একটা এক-সারির সমস্যা সারাতে গিয়ে কাজ হয়নি,
                             কারণ মাপটা যেখানে ধরা হচ্ছিল সেখানে `rem` অন্য। --}}
                        {{-- ⓘ `items-start` তুলে দেওয়া — দুইটা বাক্স এখন সমান উচ্চতায়
                     দাঁড়ায়, নাহলে ছোটটার নিচে একটা ধাপ পড়ে থাকত। --}}
                <div {{-- ⚠️ ভাগটা সমান নয় — মেপে বসানো।

                     ⓘ মালিক আগে সমান দুই ভাগ চেয়েছিলেন, আর তখন ক্রেতার
                     বাক্সে ছিল **চার সারি**। ৬ সেপ্টেম্বর তিনি পাঁচ সারির
                     ছক দিয়েছেন (নাম · পয়েন্ট · ঠিকানা · মোবাইল · বকেয়া),
                     আর হরফও বড় করতে বলেছেন।

                     ⛔ সমান ভাগে তখন তিনটা লেখা কাটছিল — `বাকির সীমা 50,0…`,
                     `15 দিনের ব`, `06-09-20`। ⚠️ কাগজের বাক্সে চারটা **ছোট
                     ঘর**, ক্রেতার বাক্সে পাঁচ সারি **লেখা** — জায়গার দরকার
                     দুই জায়গায় সমান নয়। --}}
                <div class="grid gap-3 sm:grid-cols-[1fr_1.1fr]">
                            {{-- ⓘ ক্রেতা নিজের কলামে — পরিচয়ের সারিটা নিচে
                                 নামলেও এই কলামটাই তার শুরুর রেখা। --}}
                            {{-- বাক্স ১ · ক্রেতা --}}
                            {{-- ── বাক্স ১ · ক্রেতা ────────────────────────────

                                 মালিক, ৬ সেপ্টেম্বর ২০২৬: *"duti parter jonno
                                 duti color diba"* · *"box carkona hoye giyeche…
                                 egulo round koro"* · *"side menu bare module
                                 select korle zerokom ekta cikon color ase bame,
                                 serokom ekta color … diye daw"*।

                                 ⭐ দাগটা হুবহু রেলের নিয়মেই আঁকা —
                                 `inset` ছায়া, প্রস্থ আর রং দুইটাই টোকেন থেকে
                                 (দেখুন `shell/sidebar.blade.php`-এর
                                 `--rail-tile-on-edge`)। ⓘ একই ভাষা দুই
                                 জায়গায়, তাই থিম বদলালে দুইটাই একসাথে বদলায়।

                                 ⚠️ রঙ দুইটাই **ব্যাজের টোকেন**, নতুন কোনো রং
                                 নয় — ⛔ হাতে লেখা রং এই রিপোতে সুইট লাল করে,
                                 আর তার চেয়ে বড় কথা, নয়টা থিমের আটটাতে ওটা
                                 বেমানান হত। --}}
                            {{-- ⚠️ জমিনটা **`surface-*` টোকেন**, `badge-*-bg` নয়।

                                 ⛔ প্রথমে ব্যাজের রং বসিয়েছিলাম, আর ডার্ক
                                 মোডে পর্দাটা পড়াই যেত না: রূপের `dark` ব্লক
                                 কেবল `surface-*` বদলায়, `badge-*-bg` নয় —
                                 দুই থিমেই ওটা `#dbeafe`। ⓘ ফলে হালকা জমিনের
                                 উপর হালকা লেখা।

                                 ⚠️ ব্যাজের রং **ছোট ব্যাজের জন্য**, যেখানে
                                 নিজের গাঢ় কালি সাথে যায় (`badge-*-ink`)।
                                 গোটা কার্ডের জমিনে ওটা ভুল টোকেন। --}}
                            {{-- ⚠️ `relative` — ক্রেতা খোঁজার প্যানেলটা এর সাপেক্ষে
                                 ভাসে (নিচে দেখুন)। --}}
                            <div class="relative min-w-0 rounded-(--radius-card) border border-(--color-border) bg-(--color-surface-selected) p-3 shadow-sm"
                                 style="box-shadow: inset var(--rail-tile-on-edge-w, 2px) 0 0 var(--color-brand-500)">

                        {{--
                            ── ক্রেতা — কাগজের পরিচয়ের একই বাক্সে, নিচের সারিতে ──

                            ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ────────────────
                            *"Invoice Date, INV Number, Credit Period, DO No.
                            eigulor niche sundor kore ekoi box e bosaw"*।

                            ── কেন এটা ঠিক ────────────────────────────────────────
                            কাগজটা **কী** (তারিখ · নম্বর · মেয়াদ · DO) আর কাগজটা
                            **কার** — দুইটাই একই প্রশ্নের অংশ: *এই চালানটা কীসের?*
                            দুইটা আলাদা বাক্সে থাকলে চোখকে দুইবার থামতে হত।

                            ⚠️ আর ক্রেতার বাক্সটা সরে যাওয়ায় উপরের ডান কোণটা খালি
                            হলো — সেখানে **"এই লাইন" উঠে এসেছে**, মালিকের নির্দেশে।
                        --}}
                        {{--
                            ক্রেতা — চিহ্ন বাঁয়ে, পরিচয় ডানে।

                            ── কী বদলাল (২ সেপ্টেম্বর ২০২৬) ──────────────────────
                            এখানে একটা `<select>` ছিল — কোম্পানির প্রতিটা গ্রাহক
                            একটা তালিকায়, আর টাইপ করে খোঁজার উপায় নেই। তিন
                            হাজার দোকানের ডিপোতে ওটা কার্যত অব্যবহার্য: প্রথম
                            অক্ষরের পরে ব্রাউজার আর কিছু মেলায় না।

                            মালিক NEXUS-এর নমুনা পাঠিয়েছেন, আর সেখানে জিনিসটা
                            একটা **চিহ্ন** — চাপলে একটা প্যানেল খোলে যেটা টাইপের
                            সাথে ছাঁকে, আর বাছা হয়ে গেলেই বন্ধ হয়ে যায়।

                            ── কেন নামটা চিহ্নের পাশে, নিচে নয় ───────────────────
                            বাক্সটা চিহ্ন হয়েছিল প্রস্থ ফেরত পেতে; সেই প্রস্থ
                            খালি ফেলে রাখলে লাভটাই থাকত না। তাই এক সারিতেই:
                            চিহ্নটাই মার্ক, নামটা তার গায়ে লেগে শুরু।

                            ── এলাকা ও ফোন পাশাপাশি কেন ─────────────────────────
                            ফোন তোলার সময় মানুষ দুইটা একসাথে পড়েন — "তারাকান্দার
                            দোকানটা, এই নম্বর"। আলাদা সারিতে থাকলে চোখকে দুইবার
                            নামতে হত।
                        --}}
                        {{-- ── পরিচয়ের সারি — কেবল ক্রেতা বাছার পরে ───────────

                             ⛔ এই সারিটা আগে **সবসময়** থাকত, আর ক্রেতা বাছার আগে
                             তাতে কেবল *"Search customer…"* লেখা একটা চিহ্ন বসে
                             থাকত — এক হাজার পিক্সেল প্রস্থ, একটা শব্দ।

                             ⚠️ মালিক ৫ সেপ্টেম্বর ঠিক ঐ সারিটাই দেখেছেন আর জানতে
                             চেয়েছেন ওখানে ক্রেতার তথ্য বসানো যায় কি না। ⓘ উত্তরটা
                             হলো তথ্যগুলো **ওখানেই বসে** — শুধু ক্রেতা বাছার পরে।
                             তিনি খালি অবস্থাটা দেখছিলেন।

                             ⭐ তাই সারিটা এখন শর্তসাপেক্ষ: বাছার আগে থাকেই না,
                             বাছার পরে নামে আর পুরো প্রস্থ নিয়ে Point · Mobile ·
                             ঠিকানা · নোট · ক্রেডিট লিমিট দেখায়।

                             ── ⚠️ পুরনো বোতামটা এখান থেকে তুলে নেওয়া হয়েছে ─────
                             চিহ্নটা উপরের সারিতে চলে গেছে, তাই এখানে ওটা রেখে
                             দিলে **একই বোতাম দুইবার** থাকত — একটা দেখা যেত,
                             আরেকটা লুকানো। ⛔ লুকানো নকলটা একদিন কেউ "কাজ করছে না"
                             বলে সারাতে বসতেন। ⓘ ছবিটা মুছিনি, সরিয়েছি — একই
                             `customerPickerOpen`, একই প্যানেল, একই F7। --}}
                        {{-- ── চিহ্ন বাঁয়ে, নাম ডানে — এক সারিতেই ────────────

                             মালিক (৫ সেপ্টেম্বর ২০২৬): *"customer nam duibar
                             lagbe na, sarch icon bame click korle surch bar
                             asbe, select korle dane bosbe"*।

                             ⛔ একটু আগে নামটা **দুই জায়গায়** ছিল — বোতামের
                             গায়ে আর এই সারিতে। ⚠️ এক কথা দুই জায়গায় লেখা
                             থাকলে একদিন একটা বদলায় আর অন্যটা বদলায় না।

                             ⓘ সারিটা এখন **সবসময়** থাকে: ক্রেতা বাছার আগে
                             ডান পাশে লেখা থাকে *"ক্রেতা খুঁজুন…"*, আর বাছার
                             পরে সেখানেই নাম · এলাকা · ফোন · বাকির সীমা।

                             ⭐ চিহ্নটা কখনো লুকায় না, তাই ক্রেতা বদলানোর
                             পথটাও কখনো হারায় না। --}}
                        {{-- ⭐ `grid`, `flex` নয় — মালিকের নির্দেশ, ৬ সেপ্টেম্বর ২০২৬:
                             *"Customer box e ei likha gulo ekdom bame theke dao"*,
                             সাথে বাঁ পাশের ফাঁকা খাঁজটা লাল বাক্সে ঘেরা।

                             ⛔ `flex` ছিল বলে খোঁজ-চিহ্নটা **পুরো ব্লকটাকে** ডানে
                             ঠেলে রাখত: নামের নিচে পয়েন্ট, ফোন, ঠিকানা, বকেয়া —
                             সবাই ৪৪px ভিতরে শুরু হত, অথচ ঐ খাঁজে কিছুই নেই।

                             ⭐ গ্রিডে চিহ্নটা কেবল **প্রথম সারির প্রথম ঘর** নেয়;
                             বাকি সব দুই কলাম জুড়ে, কার্ডের বাঁ কিনারা থেকে।

                             ⚠️ আর এতে একটা পুরনো সমস্যাও গেল: কলামটা ৪৪px চওড়া
                             হওয়ায় পয়েন্ট ও মোবাইলের ঘর দুইটা এক সারিতে ধরত না,
                             একটার নিচে আরেকটা নামত (মাপা: ২৩৪px কলামে ১১৪ + ১৫৭)।
                             ⓘ **এক বদলে দুইটা** — জায়গা তৈরি হলো, আর লেখা বাঁয়ে
                             এল। --}}
                        <div data-customer-details
                             class="grid grid-cols-[auto_minmax(0,1fr)] items-start gap-x-2">

                            <button type="button" @click="customerPickerOpen = ! customerPickerOpen"
                                    :aria-expanded="customerPickerOpen ? 'true' : 'false'"
                                    data-customer-picker
                                    :class="customerPickerOpen
                                        ? 'border-(--color-brand-600) bg-(--color-brand-600) text-(--color-brand-ink)'
                                        : 'border-(--color-brand-500) bg-(--color-brand-50) text-(--color-brand-700) hover:bg-(--color-brand-100)'"
                                    {{-- ⛔ `relative z-10` — নাহলে ক্রেতা বাছাই-ই খোলে না।

                                         ⚠️ পরের `div`-টা একই ঘরে (row 1, col 1) বসে
                                         আর দুই কলাম জুড়ে থাকে, তাই DOM-এ পরে আসায়
                                         সে **এই চিহ্নের উপরে আঁকা হয়**। ⓘ চোখে
                                         কিছুই ঢাকা পড়ে না (নামের সারিতে `ps-11`
                                         আছে, লেখাটা পাশে সরানো), কিন্তু **ক্লিক
                                         ঐ `div`-এ গিয়ে থামে**।

                                         ⛔ মালিক সাথে সাথেই ধরেছেন — *"customer
                                         dekhay na"*। ⓘ পর্দায় কোনো ভুল দেখাত না,
                                         JS ত্রুটিও ছিল না; কেবল চিহ্নে চাপলে
                                         কিছুই হত না।

                                         ⚠️ `elementFromPoint` দিয়ে মাপা:
                                         চিহ্নের ঠিক মাঝখানে ছিল
                                         `DIV.ps-11 truncate text-xl…`। --}}
                                    {{-- ⭐ ৩৬px, আগে ৪৫px (`size-9`) — মালিকের নির্দেশ,
                                         ৬ সেপ্টেম্বর ২০২৬: *"ei search icon 20% komaw"*,
                                         ছবিতে এই চিহ্নটাই লাল বাক্সে ঘেরা।

                                         ⓘ মাপটা `px`-এ লেখা, Tailwind-এর ধাপে নয়:
                                         এই থিমে `1rem = 20px`, তাই `size-7` হত ৩৫px
                                         আর `size-8` ৪০px — **২০% ঠিক কোনো ধাপে পড়ে না**।
                                         ⚠️ কাছাকাছি ধাপ নিলে "২০%" কথাটাই আন্দাজ হয়ে যেত। --}}
                                    class="relative z-10 col-start-1 row-start-1 grid size-[36px] shrink-0
                                           place-items-center rounded-(--radius-field) border-2 transition-colors">
                                <span class="sr-only">{{ __('sales::message.search_customer') }}</span>
                                <svg viewBox="0 0 24 24" aria-hidden="true" class="size-4 fill-current"
                                     x-show="! customerPickerOpen">
                                    <path d="M10 2a8 8 0 1 0 4.9 14.3l5.4 5.4 1.4-1.4-5.4-5.4A8 8 0 0 0 10 2Zm0 2a6 6 0 1 1 0 12 6 6 0 0 1 0-12Z"/>
                                </svg>
                                <svg viewBox="0 0 24 24" aria-hidden="true" class="size-4 fill-current"
                                     x-show="customerPickerOpen" x-cloak>
                                    <path d="m12 10.6 5.3-5.3 1.4 1.4-5.3 5.3 5.3 5.3-1.4 1.4-5.3-5.3-5.3 5.3-1.4-1.4 5.3-5.3-5.3-5.3 1.4-1.4z"/>
                                </svg>
                            </button>

                            {{--
                                ── ক্রেতার পুরো পরিচয় — এক লম্বা সারিতে ──────────

                                ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ────────────
                                *"Customer Name, search icon tar por lomba kete
                                Point, Mobile, address, note thakle ta, cr limit
                                — egulo"*।

                                আগে জিনিসগুলো তিন সারিতে ভাগ ছিল (নাম · এলাকা+ফোন ·
                                ঠিকানা), আর ক্রেডিট লিমিট একদম আলাদা সারিতে। **তিন
                                সারি মানে বাক্সটা তিন গুণ উঁচু**, অথচ কাগজের বাক্সে
                                জায়গা এখন পাশে, উপরে-নিচে নয়।

                                ⚠️ `flex-wrap` ইচ্ছাকৃত — সরু পর্দায় জিনিসগুলো নিজে
                                থেকে নিচে নামে, **চেপে গিয়ে অপাঠ্য হওয়ার বদলে**।

                                ⓘ ক্রেডিট লিমিট `ms-auto` দিয়ে ডান প্রান্তে, কারণ
                                ওটাই একমাত্র **টাকার** সংখ্যা — বাকিগুলো পরিচয়।
                            --}}
                            {{-- ⓘ পুরো ব্লকটা দুই কলাম জুড়ে, প্রথম সারি থেকে —
                                 অর্থাৎ **কার্ডের বাঁ কিনারা থেকে**। কেবল নামের
                                 সারিটা চিহ্নটাকে জায়গা ছেড়ে দেয় (`ps-11` =
                                 চিহ্নের ৪৫px + ফাঁকের ১০px)। --}}
                            <div class="col-span-2 col-start-1 row-start-1 min-w-0 leading-tight">

                                {{-- ── সারি ১ · ক্রেতার নাম ──────────────────────

                                     ⚠️ নামটা `text-xl`, ঠিক পণ্যের বোতামের মাপে —
                                     মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬)। ⓘ দুইটা
                                     এক ধাপে, নাহলে চোখ শেখে "এইটা বেশি জরুরি",
                                     অথচ ভুল হলে দুইটারই দাম এক গোটা চালান।

                                     ⓘ ক্রেতা বাছার আগে এখানেই লেখা থাকে
                                     *"ক্রেতা খুঁজুন…"* — তাই খালি অবস্থাতেও
                                     বাক্সটা নিজের কাজ বলে দেয়। --}}
                                {{-- ⓘ `pointer-events-none` — এই সারিটা চিহ্নের
                                     উপরে বসে, তাই সে ক্লিক নেয় না। ⚠️ নামটা
                                     কোনো ক্লিকের লক্ষ্য নয়, তাই কিছুই হারায় না;
                                     `z-10`-এর সাথে এটা **দ্বিতীয় তালা**। --}}
                                <div class="pointer-events-none ps-11 truncate text-xl font-semibold text-(--color-ink)"
                                     :title="customer.name"
                                     x-text="customer.name || @js(__('sales::message.search_customer'))"></div>

                                {{-- ── সারি ২ · পয়েন্ট, মোবাইল ───────────────────

                                     ⚠️ পয়েন্ট শুধু সাজসজ্জা নয়, **রুট ঠিক করে**:
                                     কোন গাড়ি কোথায় যাবে, কে ডেলিভারি নেবে। ⛔ একই
                                     নামের দুই দোকান আলাদা পয়েন্টে থাকে, আর নাম
                                     মিলে গেলে ভুল পার্টির খাতায় মাল উঠত।

                                     ⓘ ফোন মোটা হরফে — মালিকের নির্দেশ; কাউন্টার
                                     থেকে সবচেয়ে বেশি ওটাই পড়া হয়। --}}
                                {{-- ⓘ পয়েন্ট এখন **একা একটা সারি**, আর এক ধাপ বড় —
                                     মালিকের নির্দেশ (৬ সেপ্টেম্বর ২০২৬):
                                     *"কেন্দুয়া বাজার > RO BORO Koro"*।

                                     ⚠️ ফোনটা এখান থেকে নেমে গেছে নিজের সারিতে,
                                     ঠিকানার নিচে — তাঁর দেওয়া ছকে ঠিক ওখানেই। --}}
                                {{-- ⭐ পয়েন্ট ও মোবাইল **পাশাপাশি, দুইটাই গোল ঘরে** —
                                     মালিকের নির্দেশ, ৬ সেপ্টেম্বর ২০২৬:
                                     *"কাস্টমারের নিচে পয়েন্ট যেভাবে রাউন্ড করে
                                     দেওয়া, সেইভাবে মোবাইল নম্বরটি তার পাশে
                                     রাউন্ড বক্সে দাও।"*

                                     ⓘ তিনি ছবিতে পয়েন্টের **ডান পাশের ফাঁকা
                                     জায়গাটা** লাল বাক্সে ঘিরে দেখিয়েছেন — ঐ
                                     জায়গাটা খালি পড়ে ছিল, আর মোবাইল নিচে
                                     একা একটা সারি নিত।

                                     ⚠️ ইতিহাসটা রাখা হলো, কারণ এটা **আগের
                                     সিদ্ধান্তের উল্টো**: একই দিনে সকালে ফোনটা
                                     পয়েন্টের পাশ থেকে **নামিয়ে** নিজের সারিতে
                                     বসানো হয়েছিল (*"Mobile: +880… eikhe eivabe
                                     liko"*)। ⓘ তখন ফোনটা ছিল লেবেলহীন কাঁচা
                                     লেখা; এখন সে গোল ঘরে বসে, আর ঘরটাই বলে দেয়
                                     ওটা একটা আলাদা তথ্য। ⛔ ছয় মাস পরে কেউ যেন
                                     "আগে তো নিচে ছিল" ভেবে ফিরিয়ে না নেয়।

                                     ⚠️ `flex-wrap` — সরু পর্দায় দুইটা পিল
                                     পাশাপাশি ধরে না, তখন ফোনটা নিচে নামে।
                                     ⓘ `truncate` ছাড়া লম্বা পয়েন্টের নাম
                                     ফোনটাকে ঠেলে বের করে দিত। --}}
                                <div x-show="customerId && (customer.location || customer.phone)"
                                     x-cloak class="mt-1 flex flex-wrap items-center gap-1.5">
                                    <span x-show="customer.location"
                                          class="inline-flex min-w-0 items-center truncate rounded-full
                                                 border border-(--color-brand-500) bg-(--color-surface-selected)
                                                 px-2.5 py-0.5 text-sm font-medium text-(--color-ink)"
                                          :title="customer.location"
                                          x-text="customer.location"></span>

                                    {{-- ⓘ পয়েন্টের হুবহু একই ঘর — মালিক
                                         *"সেইভাবে"* বলেছেন।

                                         ⛔ **লেবেলের বদলে চিহ্ন**, আর কারণটা মাপা:
                                         "মোবাইল:" লিখলে পিলটা ~২১০px হত, আর
                                         দুইটা ঘর এক সারিতে ধরত না — ব্রাউজারে
                                         দেখা গেল ফোনটা পয়েন্টের **নিচে** নেমে
                                         গেছে, অথচ মালিক *"তার পাশে"* বলেছেন।

                                         ⚠️ চিহ্নটা লেবেলের কাজই করে: একা একটা
                                         নম্বর কীসের নম্বর তা বলে না। ⓘ পর্দা-পাঠক
                                         `sr-only` লেখাটা পায়, তাই তার কাছে
                                         কিছুই হারায় না। --}}
                                    <span x-show="customer.phone"
                                          class="inline-flex shrink-0 items-center gap-1 rounded-full
                                                 border border-(--color-brand-500) bg-(--color-surface-selected)
                                                 px-2.5 py-0.5 text-sm font-medium text-(--color-ink)">
                                        <span class="sr-only">{{ __('customer::field.phone') }}:</span>
                                        <x-ui.icon name="phone" :size="13" drawn
                                                   class="shrink-0 text-(--color-ink-muted)" />
                                        <span class="num font-semibold" x-text="customer.phone"></span>
                                    </span>
                                </div>

                                {{-- ── সারি ৩ · ঠিকানা ───────────────────────────

                                     ⓘ ঠিকানা একাই একটা সারি পায়, কারণ ওটাই সবচেয়ে
                                     লম্বা। ⚠️ আগে এটা চার নম্বর সারির সাথে এক
                                     `flex` সারিতে ছিল, আর সরু পর্দায় `কে.` হয়ে
                                     কেটে গিয়ে বকেয়ার পাশে বসত — মালিক ঠিক ওটাই
                                     দেখেছেন।

                                     ⓘ `truncate` + `title` — লম্বা ঠিকানা এক
                                     লাইনেই থাকে, পুরোটা মাউস রাখলে। --}}
                                {{-- ⓘ ঠিকানা **দুই ধাপ বড়** — মালিক: *"Ro font
                                     2size boro koro"*। `text-2xs` → `text-sm`।

                                     ⚠️ কালিটাও এক ধাপ গাঢ় (`ink-muted` → `ink`),
                                     নাহলে বড় হরফেও ওটা ফ্যাকাশে থাকত — মাপ আর
                                     কনট্রাস্ট দুইটা আলাদা জিনিস। --}}
                                <div x-show="customerId && customer.address" x-cloak
                                     class="mt-1 truncate text-sm text-(--color-ink)"
                                     :title="customer.address"
                                     x-text="customer.address"></div>

                                {{-- ── সারি ৪ · মোবাইল, লেবেলসহ ──────────────────

                                     মালিক: *"Mobile: +8801811000001 >> eikhe
                                     eivabe liko"*।

                                     ⓘ লেবেলটা লাগে কারণ সারিটা এখন একা দাঁড়ায় —
                                     পয়েন্টের পাশে থাকলে প্রসঙ্গেই বোঝা যেত,
                                     একা থাকলে একটা কাঁচা নম্বর কীসের নম্বর তা
                                     বলে না। --}}


                                {{-- ── সারি ৪ · বকেয়া/অগ্রিম · বাকির সীমা ─────────

                                     মালিকের ছক: *"Line4 > Due/Advance, Cr. Limit"*।

                                     ⛔ আগে এখানে **একটাই সংখ্যা** ছিল — *"বাকিতে
                                     দেওয়া যাবে"* — অর্থাৎ সীমা বিয়োগ পাওনা।
                                     ⚠️ ওটা একটা **হিসাবের ফল**, আর ফল দেখালে
                                     বিক্রেতা কাঁচা দুইটা সংখ্যা আর দেখতে পান না।

                                     ⭐ দুইটাই দরকার, দুইটা আলাদা প্রশ্নের উত্তর:
                                     **বকেয়া** বলে সম্পর্কটা আজ কোথায়, **সীমা**
                                     বলে সেটা কতদূর যেতে পারে।

                                     ⓘ বকেয়া ঋণাত্মক মানে **অগ্রিম** — টাকা আগেই
                                     দেওয়া। ⚠️ ওটাকে "বকেয়া −৫,০০০" লিখলে উল্টো
                                     পড়া যেত, তাই নামটাই বদলায়।

                                     ⚠️ সীমা **শূন্য মানে বাকি বন্ধ**, মাল বন্ধ নয়
                                     — তাই সংখ্যার বদলে "নগদ/অগ্রিম"। ⛔ "০" লিখলে
                                     পড়া হত *"কিছুই দেওয়া যাবে না"*, যেটা ভুল। --}}
                                <div x-show="customerId" x-cloak
                                     class="mt-0.5 flex items-center gap-x-3 text-2xs text-(--color-ink-muted)">
                                    <span class="whitespace-nowrap">
                                        <span x-text="customer.due < 0
                                                        ? @js(__('sales::field.advance'))
                                                        : @js(__('sales::field.due'))"></span>
                                        <span class="num ms-1 font-semibold"
                                              :class="customer.due > 0
                                                        ? 'text-(--color-danger)'
                                                        : 'text-(--color-ink)'"
                                              x-text="money(Math.abs(customer.due || 0))"></span>
                                    </span>

                                    @if ($show['credit_limit'])
                                        <span class="whitespace-nowrap">
                                            {{ __('sales::field.credit_limit') }}
                                            <span class="num ms-1 font-semibold"
                                                  :class="customer.limit <= 0
                                                            ? 'text-(--color-ink-muted)'
                                                            : 'text-(--color-ink)'"
                                                  x-text="customer.limit <= 0
                                                            ? @js(__('sales::field.cash_only'))
                                                            : money(customer.limit)"></span>
                                        </span>
                                    @endif
                                </div>
                            </div>
                        </div>

                        {{-- ছাঁকনি — টাইপের সাথে সাথে, আর বাছা হলেই বন্ধ --}}
                        <div x-show="customerPickerOpen" x-cloak
                             @keydown.escape="customerPickerOpen = false"
                             {{-- ⚠️ জমিন `surface-card`, `brand-50` নয় — ডার্ক মোডের পাঠ।

                                  ⛔ মালিক ৬ সেপ্টেম্বর ২০২৬ ডার্ক মোডে ধরেছেন:
                                  তালিকার নামগুলো পড়াই যেত না। মেপে দেখা গেল
                                  কালিটা `#06616f` — **গাঢ় জমিনে গাঢ় টিয়া**।

                                  ⓘ `brand-*` টোকেনগুলো ব্র্যান্ডের রং, আর
                                  ব্র্যান্ড আলো-আঁধারে একই থাকে — সেটাই ঠিক।
                                  ⚠️ কিন্তু **জমিন হিসেবে** ওটা ভুল টোকেন:
                                  জমিন থিমের সাথে বদলাতে হয়, ব্র্যান্ডের সাথে
                                  নয়। ⭐ তাই জমিন `surface-*`, আর ব্র্যান্ডের
                                  রং কেবল **সীমানায়** — যেখানে সে চেনা দেয়,
                                  পড়া আটকায় না। --}}
                             {{-- ⭐ **ভাসমান**, প্রবাহের ভিতরে নয় — মালিকের নির্দেশ,
                                  ৬ সেপ্টেম্বর ২০২৬: *"same obosta customer search eo"*।

                                  ⓘ পণ্যের তালিকায় দোষটা যা ছিল, এখানেও তাই:
                                  প্যানেলটা জায়গা **দখল করত**, তাই ক্রেতা খুঁজতে
                                  গেলেই কার্ডটা লম্বা হয়ে যেত আর নিচের সবকিছু ঠেলে
                                  নামত। ⚠️ তিনি ছবিতে ঐ **তৈরি হওয়া ফাঁকা জায়গাটাই**
                                  লাল বাক্সে ঘিরে দেখিয়েছেন।

                                  ⭐ ক্ষতিটা চোখের নয়, হাতের: ক্রেতা বাছার পর
                                  পণ্যের ঘরটা যেখানে ছিল সেখানে থাকত না।

                                  ⚠️ `z-30` — পাশের কার্ড ও নিচের টেবিলের উপরে
                                  থাকতে হবে; `top-full` — ক্রেতার কার্ডের ঠিক নিচে। --}}
                             class="absolute inset-x-0 top-full z-30 mt-1 rounded-(--radius-card)
                                    border-2 border-(--color-brand-500) bg-(--color-surface-card)
                                    p-1.5 text-(--color-ink) shadow-lg">
                            <input type="search" x-model="customerTerm"
                                   x-effect="customerPickerOpen && $nextTick(() => $el.focus())"
                                   @keydown.enter.prevent="pickFirstCustomer()"
                                   placeholder="{{ __('sales::message.search_customer') }}"
                                   class="h-(--spacing-field-dense) w-full rounded-(--radius-field)
                                          border border-(--color-border) bg-(--color-surface-card) px-2 text-sm">

                            <ul class="mt-1.5 max-h-56 overflow-y-auto">
                                <template x-for="row in customerMatches" :key="row.id">
                                    <li>
                                        <button type="button" @click="chooseCustomer(row.id)"
                                                {{-- ⓘ বাছা সারি ও হোভার — `surface-selected`
                                                     আর `surface-hover`, দুইটাই থিমের সাথে
                                                     বদলায়। ⛔ আগে `brand-100` ছিল, আর ডার্কে
                                                     ওটা হালকা টিয়া হয়ে লেখা গিলে ফেলত। --}}
                                                :class="String(row.id) === customerId
                                                    ? 'bg-(--color-surface-selected) font-semibold' : ''"
                                                class="w-full rounded-(--radius-field) px-2 py-1.5 text-start
                                                       hover:bg-(--color-surface-hover)">
                                            <span class="block truncate text-xs" x-text="row.name"></span>
                                            <span class="num block text-2xs text-(--color-ink-muted)"
                                                  x-text="row.phone"></span>
                                        </button>
                                    </li>
                                </template>

                                <li x-show="customerMatches.length === 0" x-cloak
                                    class="px-2 py-2 text-2xs text-(--color-ink-muted)">
                                    {{ __('sales::message.no_customer_match') }}
                                </li>
                            </ul>
                        </div>

                        {{-- ফর্মের সাথে যা সত্যিই যায় — উপরেরটা কেবল বাছার পর্দা।

                             `<select>`-টা নিজেই `name="customer_id"` বহন করত; চিহ্ন
                             আর তালিকা কোনো ফর্ম-ঘর নয়, তাই মানটা এখানে বসাতে হয়।
                             না বসালে চালান সেভ হত ক্রেতা ছাড়াই। --}}
                        <input type="hidden" name="customer_id" x-model="customerId">
                            </div>

                            {{-- ── কাগজের চারটা ঘর — ২×২ ─────────────────────

                                 ⓘ তারিখ · নম্বর — উপরের লাইনে; শর্ত · DO —
                                 নিচের লাইনে। ⚠️ DO-র ঘরটা সুইচের পেছনে, আর
                                 বন্ধ থাকলে নিচের লাইনে একটাই ঘর — কোনো খালি
                                 কলাম পড়ে থাকে না, কারণ ছকটা সারি ভরে যায়। --}}
                            {{-- বাক্স ২ · কাগজটা কী — তারিখ · নম্বর · শর্ত · DO --}}
                            {{-- ⚠️ ভিতরের দুই কলাম **সমান নয়**, আর কারণটা মাপা।

                                 ⓘ বাইরের দুইটা বাক্স সমান (মালিকের নির্দেশ),
                                 কিন্তু তারিখের ঘরটা ভিতরে **৬০px প্যাডিং** নেয়
                                 — ক্যালেন্ডারের বোতামের জায়গা (`pe-9 ps-3`)।
                                 ⛔ সমান কলামে তাই `05-09-2026` কেটে `05-09-2`
                                 হয়ে যেত, আর একটা কাটা তারিখ **ভুল তারিখের
                                 চেয়েও খারাপ**: মানুষ অর্ধেক পড়ে বাকিটা ধরে নেন।

                                 ⭐ তাই বাঁ কলামটা একটু চওড়া। ⓘ ডান কলামে বিল
                                 নম্বর আর DO — দুইটাই লেখার ঘর, লম্বা হলে
                                 ক্লিক করে পড়া যায়; তারিখে সেটা যায় না। --}}
                            {{-- ⚠️ `content-start` — সারিগুলো উপরে থাকবে, ছড়াবে না।

                                 ⛔ পাশের ক্রেতার বাক্স পাঁচ সারি হয়ে লম্বা
                                 হওয়ায় এই বাক্সটাও সমান লম্বা হয়েছে, আর
                                 ভিতরের দুইটা সারি **উচ্চতা ভাগ করে নিয়ে**
                                 মাঝখানে একটা বড় ফাঁক তৈরি করেছিল — মালিক
                                 ঠিক ওটাই দেখেছেন (*"invoice date box vange
                                 keno?"*)।

                                 ⓘ ছকের সারি ডিফল্টে `stretch` হয়; ঘরগুলোর
                                 নিজের উচ্চতা আছে, তাই ওদের ছড়ানোর দরকার নেই। --}}
                            {{-- ⓘ `self-start` — বাক্সটা **নিজের উচ্চতায়** দাঁড়ায়।

                                 ⛔ আগে সে পাশের ক্রেতার বাক্সের সমান লম্বা
                                 হতে গিয়ে নিচে ২৯px খালি রাখত, আর মালিক
                                 লাল দাগ দিয়ে ঠিক ওটাই দেখিয়েছেন।

                                 ⚠️ দুইটা বাক্সের উচ্চতা এক হওয়া কোনো নিয়ম
                                 নয় — ভিতরে যা আছে তার সমান হওয়াই নিয়ম। --}}
                            <div class="grid min-w-0 self-start grid-cols-1 content-start gap-2 sm:grid-cols-2 rounded-(--radius-card) border border-(--color-border) bg-(--color-surface-sunken) p-3 shadow-sm"
                                 style="box-shadow: inset var(--rail-tile-on-edge-w, 2px) 0 0 var(--color-badge-inventory-ink)">
                            {{-- পুরো তারিখটা দেখা যেতে হবে — মালিকের কথা,
                                 ৩ সেপ্টেম্বর ২০২৬: "০৩-০৯-২০:" পর্যন্ত দেখিয়ে
                                 বছরটা কেটে যাচ্ছিল, আর একটা কাটা তারিখ পড়ে
                                 কেউ নিশ্চিত হতে পারেন না কোন বছরের কাগজ। --}}
                            <label class="min-w-0">
                                <span class="mb-0.5 block text-2xs font-semibold uppercase tracking-wide
                                             text-(--color-ink-muted)">{{ __('sales::field.challan_date') }}</span>
                                {{-- ⓘ `dense` — মালিক, ৫ সেপ্টেম্বর ২০২৬:
                                     *"বিলের তারিখ box slim koro"*।

                                     ⚠️ ঘরটা স্বাভাবিকভাবে ৬০px প্যাডিং নেয়
                                     (ডানে ক্যালেন্ডারের বোতামের জায়গা), অথচ
                                     লেখাটা `05-09-2026` — মাত্র ~৮০px।
                                     ⛔ অর্ধেকের বেশি ঘরই ফাঁকা ছিল। --}}
                                <x-ui.date name="trx_date" dense
                                           :value="old('trx_date', now()->toDateString())"
                                           class="w-full text-sm" />
                                           </label>

                            {{--
                                বিলের নম্বর — এখনই দেখা যায়, আর বদলানোও যায়।

                                ── কেন বদলাল (৩ সেপ্টেম্বর ২০২৬) ─────────────────
                                এখানে "নিশ্চিত করলে" লেখা একটা নিষ্ক্রিয় ঘর ছিল।
                                যুক্তিটা ছিল: আগে থেকে নম্বর দেখালে খসড়া বাতিল
                                হলে ওই নম্বরটা খরচ হয়ে সিরিজে ফাঁক থেকে যেত।

                                মালিক বললেন নম্বরটা এখানেই তৈরি হবে, আর দরকারে
                                বদলানো যাবে। **যুক্তিটা টিকে আছে, শুধু সমাধানটা
                                বদলেছে**: এটা `preview()`, `next()` নয় — অর্থাৎ
                                সিরিজের পরের নম্বরটা কেবল **দেখানো** হয়, খরচ হয়
                                না। খসড়া বাতিল হলে কিছুই হারায় না, আর দুইজন
                                একসাথে কাউন্টার খুললেও দুইজনেই একই নম্বর দেখেন —
                                আসল নম্বরটা বসে সংরক্ষণের মুহূর্তে, তালার ভেতরে।

                                ⚠️ তাই পর্দায় দেখা নম্বরটা **প্রতিশ্রুতি নয়,
                                পূর্বাভাস**। কেউ হাতে বদলালে সেটাই যায়, আর
                                না বদলালে সংরক্ষণের সময়কার আসল পরেরটা।
                            --}}
                            <label class="min-w-0">
                                <span class="mb-0.5 block text-2xs font-semibold uppercase tracking-wide
                                             text-(--color-ink-muted)">{{ __('sales::field.inv_number') }}</span>
                                <input type="text" name="invoice_no" value="{{ old('invoice_no', $invoicePreview) }}"
                                       :title="@js(__('sales::field.invoice_no_editable'))"
                                       placeholder="{{ __('sales::field.on_confirm') }}"
                                       class="h-(--spacing-field-dense) w-full rounded-(--radius-field) border border-(--color-border)
                                              bg-(--color-surface-app) px-2 text-sm">
                            </label>

                            {{--
                                বাকির মেয়াদ — একটাই ঘর, ৩ সেপ্টেম্বর ২০২৬।

                                ── কেন দুইটা ঘর এক হয়ে গেল ───────────────────────
                                কিছুক্ষণ আগে এখানে দুইটা ছিল — "কত দিন" আর "কোন
                                তারিখে"। মালিক দেখেই বললেন একটাই হোক: *"Drop dawn
                                diye ektai box kora huk duto na"*।

                                তিনি ঠিক বলেছেন। দুইটা ঘর মানে প্রতিবার একটা
                                নীরব প্রশ্ন — কোনটায় লিখব — অথচ উত্তরটা প্রায়
                                সবসময় একই। আর দুইটা একসাথে ভরা থাকলে কোনটা
                                জেতে সেটা পর্দা দেখে বোঝার কোনো উপায় ছিল না।

                                ── তালিকাটা কোথা থেকে ─────────────────────────────
                                [[mdm_payment_terms]] — মাস্টার ডাটার সারি, কোডে
                                লেখা তালিকা নয়। কেউ "৪৫ দিন" যোগ করলে পরের দিনই
                                এখানে দেখা যাবে। এটাই মালিকের পুরনো নিয়ম: "কোন
                                কোন ধরনের জিনিস" এমন প্রতিটা তালিকা কোম্পানির
                                নিজের বাড়ানোর কথা।

                                ── ডিফল্ট কেন "আজ" ────────────────────────────────
                                *"day closing date defolt thakbe"* — কাউন্টারে
                                বেশিরভাগ বিক্রিই নগদ, তাই ওটাই স্বাভাবিক অবস্থা,
                                আর বাকি দেওয়াটা ব্যতিক্রম।

                                তবে ব্যতিক্রমটা সিস্টেম নিজেই জানে: ক্রেতা বাছার
                                সাথে সাথে তাঁর নিজের মেয়াদ থাকলে সেটাই বসে যায়
                                ([[chooseCustomer()]])। যাঁর সাথে ৩০ দিনের কথা
                                আছে, তাঁর বেলায় প্রতিবার হাতে বাছতে হয় না।

                                ── "নির্দিষ্ট তারিখ" ───────────────────────────────
                                শেষ বিকল্পটা বাছলেই তারিখের ঘরটা বেরোয় — ঈদের
                                আগের হিসাব বা মিলের সাথে ঠিক করা একটা দিনের জন্য।
                                বাকি সময় ওটা পর্দাতেই থাকে না।
                            --}}
                            <label class="min-w-0">
                                <span class="mb-0.5 block text-2xs font-semibold uppercase tracking-wide
                                             text-(--color-ink-muted)">{{ __('sales::field.terms') }}</span>
                                {{-- ── পাঁচটা ধরন, ক্রয়ের কাউন্টারের সমান ────────

                                     মালিক (৫ সেপ্টেম্বর ২০২৬):
                                     *"r zaza nai sob daw direct sales e"*।

                                     ⛔ আগে বিকল্পের মান ছিল **দিনের সংখ্যা**
                                     (`0`, `7`, `30`) আর একটা `custom`। ⚠️ তাতে
                                     দুইটা ধরন বলাই যেত না — `COD` আর
                                     `মাস শেষ পর্যন্ত` — অথচ ডিপোর কাজে ঐ
                                     দুইটাই সবচেয়ে চলতি।

                                     ⭐ এখন মানটা `kind` বা `kind:days`, হুবহু
                                     ক্রয়ের ছাঁচে। ⓘ দুই পর্দা এক ছাঁচে থাকলে
                                     একটা শেখা মানে দুইটা জানা।

                                     ⚠️ `@selected` সার্ভারেই বসে, `x-model`-এর
                                     ভরসায় নয় — `<template x-for>` দিয়ে আঁকা
                                     বিকল্প DOM-এ থাকে না যখন Alpine শুরু হয়,
                                     আর ব্রাউজার তখন মানটা ফেলে দেয়। ⛔ ক্রয়ের
                                     কাউন্টারে ঠিক ঐ কারণেই "৩ দিন" ডিফল্টটা
                                     বসত না। --}}
                                <select x-model="creditTerm" @change="termChanged()"
                                        class="h-(--spacing-field-dense) w-full rounded-(--radius-field) border border-(--color-border)
                                               bg-(--color-surface-app) px-2 text-sm">
                                    @foreach ($paymentTerms as $term)
                                        <option value="{{ $term['value'] }}"
                                                @selected($term['value'] === $paymentTermDefault)>
                                            {{ $term['label'] }}
                                        </option>
                                    @endforeach
                                </select>
                            </label>

                            {{-- কেবল "নির্দিষ্ট তারিখ" বাছলে।

                                 ⓘ শর্তটা আগে `custom` নামে ছিল; এখন `fixed` —
                                 ক্রয়ের কাউন্টারে যে নামে ওটা আগে থেকেই আছে। --}}
                            <label class="min-w-0" x-show="termKind === 'fixed'" x-cloak>
                                <span class="mb-0.5 block text-2xs font-semibold uppercase tracking-wide
                                             text-(--color-ink-muted)">{{ __('sales::field.due_on') }}</span>
                                {{-- ⚠️ ব্রাউজারের নিজের তারিখের ঘর নয় — `x-ui.date` কম্পোনেন্ট।

                                     ওটা লেখাটা **নিজের লোকেল ধরে** আঁকে, আর
                                     CSS দিয়ে বদলানো যায় না। `05/06` তখন দুইভাবে
                                     পড়া যায় — ৫ জুন না ৬ মে — আর দুইটাই বৈধ
                                     বলে ভুলটা খাতা থেকে ধরাই যায় না।

                                     ⓘ কম্পোনেন্টটা সার্ভারে ISO পাঠায় (লুকানো
                                     ঘরে), তাই `name` ছাড়া আর কিছু লাগেনি।
                                     `dueOn` Alpine-এর ঘরটা আর দরকার নেই। --}}
                                <x-ui.date name="due_on" />
                            </label>

                            {{-- ── সার্ভারে যায় দুইটা জিনিস, আর দুইটাই লাগে ────

                                 ⭐ `payment_term` বলে **কোন ধরনের শর্ত**, আর
                                 `credit_period_days` বলে **কত দিন**।

                                 ⚠️ দিনসংখ্যাটা ধরনটা বলে না: নগদ আর COD —
                                 দুইটাই শূন্য দিন, অথচ একটায় টাকা ড্রয়ারে আর
                                 আরেকটায় ভ্যানে। ⓘ একইভাবে ৫ তারিখে "মাস শেষ"
                                 আর "২৫ দিনের বাকি" — খাতায় হুবহু এক।

                                 ⛔ তাই ধরনটা না পাঠালে *"এই মাসে COD-তে কত
                                 বিক্রি"* প্রশ্নের উত্তর কোথাও থাকত না। --}}
                            <input type="hidden" name="payment_term" :value="termKind">
                            <input type="hidden" name="credit_period_days"
                                   :value="termKind === 'credit' ? termDays : ''">

                            {{-- DO নম্বর — ঐচ্ছিক, আর নিজের সুইচের পেছনে।

                                 ⚠️ এই ঘরটা একবার দুর্ঘটনাক্রমে মুছে গিয়েছিল
                                 (৩ সেপ্টেম্বর ২০২৬): মেয়াদের দুইটা ঘর একটা
                                 ড্রপডাউনে বদলাতে গিয়ে প্রতিস্থাপনের সীমা
                                 বেশি টানা হয়েছিল, আর মাঝের এই ব্লকটাও তার
                                 ভেতরে পড়ে গিয়েছিল।

                                 [[DirectSaleTest::test_every_field_switch_really_hides_its_field]]
                                 পরের রানেই ধরে ফেলেছে — "খোলা থাকলেও ঘরটা
                                 নেই: sales.field_do_no"। স্ক্রিনশটে ধরা
                                 পড়েনি, কারণ ঘরটা এমনিতেই ঐচ্ছিক আর ফাঁকা
                                 দেখায়; একটা কম ঘর চোখে পড়ে না। --}}
                            @if ($show['do_no'])
                                <label class="min-w-0">
                                    <span class="mb-0.5 block text-2xs font-semibold uppercase tracking-wide
                                                 text-(--color-ink-muted)">{{ __('sales::field.do_no') }}</span>
                                    <input type="text" name="do_no" placeholder="{{ __('sales::field.optional') }}"
                                           class="h-(--spacing-field-dense) w-full rounded-(--radius-field) border border-(--color-border)
                                                  bg-(--color-surface-app) px-2 text-sm">
                                </label>
                            @endif

                            </div>
                        </div>

                            {{--
                                গুদাম — স্ট্রিপ থেকে বাদ, ৩ সেপ্টেম্বর ২০২৬।

                                ── মালিকের নির্দেশ, আর কেন এটা নিরাপদ ────────────
                                *"Warehouse bad daw"*। কাউন্টারে দাঁড়িয়ে মাল
                                কোন গুদাম থেকে বেরোচ্ছে সেটা বাছার প্রশ্ন ওঠে না
                                — কাউন্টারের নিজের গুদামটাই বেরোয়, আর ঘরটা
                                প্রতিবার একই মান দেখাত।

                                ⚠️ কিন্তু ঘরটা গেছে, **মানটা যায়নি**। মজুদ কোন
                                গুদাম থেকে কমল সেটা না লিখলে স্টক লেজার আর
                                গুদামের যোগফল আলাদা হয়ে যেত, আর সেটা এমন ভুল
                                যা মাস শেষে ধরা পড়ে। তাই মানটা এখনো যায় —
                                কেবল চোখের আড়ালে।

                                বদলানোর দরকার হলে জায়গাটা [[Control Panel]],
                                কাউন্টারের পর্দা নয়: চালানের মাঝপথে গুদাম
                                বদলানো মানে অর্ধেক লাইন এক গুদামের, বাকি অর্ধেক
                                অন্যটার।
                            --}}
                            {{-- ⓘ গুদামটা লুকানো ঘরে — ছকের কোনো কলাম নেয় না,
                                 কারণ `input[type=hidden]`-এর `display` নেই। --}}
                            <input type="hidden" name="warehouse_id" value="{{ $warehouse?->id }}">
                    </div>

                </section>
                {{--
                    ── পণ্য খোঁজা ও লাইনের ঘর — স্ট্রিপের ঠিক নিচে ──────────

                    ── কেন এখানে (৩ সেপ্টেম্বর ২০২৬) ────────────────────────
                    মালিক লাল বাক্স এঁকে দেখালেন: স্ট্রিপের নিচে একটা বড়
                    ফাঁকা জায়গা পড়ে ছিল, আর নিচের ব্লকটা তার বাইরে।
                    *"upore faka lal box e nicher mark kora box guchiye
                    uporer mark kora box er vitore niye aso"*।

                    ফাঁকাটা ছিল কারণ পাশের সবুজ প্যানেল স্ট্রিপের চেয়ে
                    উঁচু। এখন ব্লকটা ওই জায়গাতেই বসে, আর সারিটার তিনটা
                    কলামই কাছাকাছি উচ্চতার হয়ে যায়।

                    ⚠️ ঘরগুলো এখনো `flex-wrap` + নিজের নিজের প্রস্থে, তাই
                    কলামটা সরু হলে ওরা নিচে নামে — চেপে যায় না।
                --}}

                {{--
                    ── এন্ট্রি এলাকা — পুরো প্রস্থে, কিন্তু ঘরগুলো স্থির ────────

                    ── কেন কলামটা চওড়া, অথচ ঘরগুলো নয় (৩ সেপ্টেম্বর ২০২৬) ──
                    একবার এটাকে উপরের সরু কলামে ঢোকানো হয়েছিল, যাতে ঘরগুলো
                    টেনে লম্বা না হয়। ফল উল্টো: কলামটা ~৪৩০px হওয়ায় দশটা ঘর
                    দুইয়ে দুইয়ে পাঁচ সারি হয়ে গেল, আর পণ্য খোঁজার বারটা এত
                    সরু হল যে লেখাটাই কেটে গেল।

                    আসল সমস্যাটা কলামের প্রস্থ ছিল না, **ঘরের**: ঘরগুলো
                    `w-full` ছিল, তাই যত জায়গা পেত তত টানত। এখন প্রতিটার
                    নিজের মাপ বাঁধা, আর অতিরিক্ত জায়গাটা ডান পাশে ফাঁকা
                    থাকে — যেটাই ঠিক, কারণ একটা সংখ্যার ঘর দুই ইঞ্চি চওড়া
                    হয়ে কিছুই বেশি বলে না।
                --}}
                {{-- ⓘ পণ্যের বাক্স — নিজের রং আর বাঁয়ে দাগ, মালিকের
                     নির্দেশে (*"Producter Box eo dio"*)। --}}
                <section data-boxed class="rounded-(--radius-card) border border-(--color-border)
                                bg-(--color-surface-muted) p-3 shadow-sm"
                         style="box-shadow: inset var(--rail-tile-on-edge-w, 2px) 0 0 var(--color-badge-draft-ink)">

                    {{-- বাঁ: খোঁজা ও ঘরগুলো।

                         @container — মাপটা এই কলামের নিজের প্রস্থে, পর্দার
                         নয়। ভিউপোর্ট ধরে মাপলে ডান পাশের প্যানেল জায়গা
                         নেওয়ায় পাঁচটা ঘর তিন-দুইয়ে ভেঙে চার সারি হয়ে যেত। --}}
                    {{-- ⚠️ `relative` — খোঁজার ফলের তালিকাটা এর সাপেক্ষে ভাসে।
                         নিচের *"খোঁজার ফল"* ব্লকের মন্তব্য দেখুন। --}}
                    <div class="@container relative min-w-0">
                        {{--
                            পণ্য — চিহ্ন বাঁয়ে, বাছা নামটা ডানে।

                            ── কী বদলাল (২ সেপ্টেম্বর ২০২৬) ──────────────
                            এখানে একটা চওড়া লেখার ঘর ছিল, আর তার নিচে
                            ফলের তালিকা। মালিক NEXUS-এর নমুনা পাঠিয়েছেন:
                            **চিহ্নটাই বোতাম**, আর তার পাশে বড় হরফে সেই
                            নামটা যেটা এইমাত্র বাছা হলো।

                            ── কেন নামটা এত বড় ───────────────────────────
                            পর্দার এই একটামাত্র জিনিস বলে **কোন পণ্যটা
                            বিক্রি হতে যাচ্ছে**। আগের মাপে ওটা পাশের
                            সংখ্যাগুলোর চেয়েও ছোট ছিল — ফলে সংখ্যাটা
                            যাচাই হত, নামটা হত না। NEXUS-এ এই ভুলটা
                            ধরা পড়েছিল, আর সারাইটাও ওখান থেকেই।

                            ── ঘরটা খোলা থাকে কেন ────────────────────────
                            লেখা শুরু করলেই তালিকা, আর বাছা হলেই বন্ধ —
                            কাউন্টারে একটার পর একটা পণ্য ওঠে, তাই প্রতিবার
                            চিহ্নে চাপ দেওয়াটা একটা বাড়তি ক্লিক হত।
                            তাই কি-বোর্ড সরাসরি ঘরেই যায়, আর চিহ্নটা
                            থাকে যিনি মাউস ধরে আছেন তাঁর জন্য।
                        --}}
                        <div class="flex items-start gap-3">
                            <button type="button" @click="pickerOpen ? pickerOpen = false : openPicker()"
                                    :aria-expanded="pickerOpen ? 'true' : 'false'"
                                    :class="pickerOpen
                                        ? 'border-(--color-brand-600) bg-(--color-brand-600) text-(--color-brand-ink)'
                                        : 'border-(--color-brand-500) bg-(--color-brand-50) text-(--color-brand-700) hover:bg-(--color-brand-100)'"
                                    {{-- ⭐ ৪৪px, আগে ৫৫px (`size-11`) — মালিকের চূড়ান্ত
                                         নির্দেশ, ৬ সেপ্টেম্বর ২০২৬: *"শুধু search
                                         বোতাম আগের চাইতে ২০% কমাবে"*।

                                         ── ⚠️ পথটা রাখা হলো, কারণ সংখ্যাটা তিনবার বদলেছে ──
                                             ৫৫px  →  ৩০px   *"search icon 50% cuto koro"*
                                                   →  ২৪px   ("২০% কমাও" — ধাপে ধাপে ধরে নিয়েছিলাম)
                                                   →  ৪৪px   *"আগের চাইতে ২০%"*

                                         ⛔ **মাঝের ধাপটা আমার পাঠের ভুল।** ⓘ "২০% কমাও"
                                         মানে ছিল **মূল মাপ থেকে ২০%**, চলতি মাপ থেকে নয়।
                                         ⚠️ ক্রেতার চিহ্নটাও একই নিয়মে ৪৫ → ৩৬px, আর দুইটা
                                         মিলিয়ে দেখলেই নিয়মটা পরিষ্কার — **দুইটাই নিজের
                                         আসল মাপের ৮০%**।

                                         ⭐ আর ৪৪px ঠিক ছোঁয়ার নিরাপদ মাপ, তাই ট্যাবলেটে
                                         POS এলেও এই চিহ্নটা নিয়ে ভাবতে হবে না। --}}
                                    class="grid size-[44px] shrink-0 place-items-center rounded-(--radius-card)
                                           border-2 transition-colors">
                                <span class="sr-only">{{ __('sales::message.type_or_pick') }}</span>
                                <svg viewBox="0 0 24 24" aria-hidden="true" class="size-[20px] fill-current"
                                     x-show="! pickerOpen">
                                    <path d="M10 2a8 8 0 1 0 4.9 14.3l5.4 5.4 1.4-1.4-5.4-5.4A8 8 0 0 0 10 2Zm0 2a6 6 0 1 1 0 12 6 6 0 0 1 0-12Z"/>
                                </svg>
                                <svg viewBox="0 0 24 24" aria-hidden="true" class="size-[20px] fill-current"
                                     x-show="pickerOpen" x-cloak>
                                    <path d="m12 10.6 5.3-5.3 1.4 1.4-5.3 5.3 5.3 5.3-1.4 1.4-5.3-5.3-5.3 5.3-1.4-1.4 5.3-5.3-5.3-5.3 1.4-1.4z"/>
                                </svg>
                            </button>

                            <div class="min-w-0 flex-1">
                                {{--
                                    ঘরটা লুকানো থাকে — চিহ্নে চাপলে খোলে।

                                    ── মালিকের কথা (৩ সেপ্টেম্বর ২০২৬) ────────
                                    *"Search icon e click korle search bar
                                    open hobe, select er por dane bosbe"*।

                                    আগে ঘরটা সবসময় খোলা থাকত আর বাছা পণ্যের
                                    নামটা তার **placeholder**-এ বসত। ওটা দুই
                                    দিক থেকেই ভুল ছিল: খালি একটা ইনপুট বাক্স
                                    দেখে বোঝা যেত না কিছু বাছা হয়েছে কিনা,
                                    আর নামটা placeholder-এ থাকায় সেটা টাইপ
                                    শুরু করলেই উধাও হয়ে যেত।

                                    এখন দুইটা অবস্থা, আর কোনোটাই দ্ব্যর্থ নয়:

                                      বন্ধ  → চিহ্নের পাশে বাছা পণ্যের নাম
                                              (বা "পণ্য বাছুন" লেখা বোতাম)
                                      খোলা → লেখার ঘর, ফোকাস সহ

                                    ── কেন `x-init`-এর অটো-ফোকাস গেল ─────────
                                    ঘরটা এখন লুকানো, আর লুকানো ঘরে ফোকাস
                                    দেওয়া যায় না। ফোকাসটা এখন খোলার সাথে
                                    যায় (`openPicker()`), যেখানে ওটার মানে
                                    আছে।
                                --}}
                                {{--
                                    ── নাম আর কোড এক সারিতে ────────────────

                                    মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬):
                                    *"PRD-0004 — ei id product er dane bosbe"*।

                                    ⚠️ কোডটা আগে **নামের নিচে** ছিল, আর নিচে
                                    ছিল মজুদের ছয়টা সংখ্যাও। ফলে কোডটা
                                    সংখ্যাগুলোর ভিড়ে মিশে যেত — অথচ **ওটাই
                                    একমাত্র পরিচয় যেটা দুইটা একই নামের পণ্যকে
                                    আলাদা করে**, আর ঠিক সেই কারণেই আজ
                                    ডুপ্লিকেশন ইঞ্জিনটা বানাতে হয়েছে।

                                    এখন নামের ঠিক ডানে, তাই চোখ একবারেই
                                    দুইটা পড়ে: *"কসমস বিস্কুট ৪০গ্রাম —
                                    PRD-0004, হ্যাঁ এটাই।"*
                                --}}
                                <div x-show="! pickerOpen" x-cloak class="flex items-baseline gap-2">
                                    <button type="button" @click="openPicker()"
                                            {{-- ⚠️ `text-xl` — ছোট করে আবার **ফিরিয়ে আনা
                                                 হয়েছে**, ৬ সেপ্টেম্বর ২০২৬।

                                                 ⓘ ঐ দিনই মালিক ২০% ছোট করতে বলেছিলেন
                                                 (`text-base`), তারপর দেখে বললেন *"Product
                                                 namer size ager tai ano"*। ⭐ চিহ্নটা
                                                 ৫৫px → ২৪px নামার পর নামটা এমনিতেই বড়
                                                 দেখাচ্ছিল — **আসল সমস্যা ছিল চিহ্নের মাপ,
                                                 নামের নয়**।

                                                 ⚠️ ছয় মাস পরে কেউ যেন "একবার তো ছোট করতে
                                                 বলা হয়েছিল" ভেবে আবার নামিয়ে না দেয়। --}}
                                            {{-- ⚠️ `h-(--spacing-field)` — খোলা অবস্থার লেখার
                                                 ঘরটার সাথে **হুবহু এক উচ্চতা** (৪৮px)।

                                                 ⛔ আগে উচ্চতা আসত `py-2` + `text-xl` থেকে,
                                                 আর সেটা ৪৮px-এর সমান হত না। ⓘ ফলে পিকার
                                                 খুললেই নিচের সব ঘর **১২px উপরে উঠত** —
                                                 মেপে দেখা, ৩৮৫ → ৩৭৩।

                                                 ⚠️ ক্ষতিটা তালিকা ভাসানোর পরেও থেকে গিয়েছিল:
                                                 তালিকা আর জায়গা নেয় না, কিন্তু **মাথার ঘরটাই**
                                                 মাপ বদলাত। ⓘ যিনি না তাকিয়ে টাইপ করেন, তাঁর
                                                 কাছে ১২px আর ২০০px একই জিনিস — ঘরটা সরে গেছে। --}}
                                            class="flex h-(--spacing-field) min-w-0 flex-1 items-center truncate
                                                   rounded-(--radius-field) border border-transparent px-3
                                                   text-start text-xl font-semibold
                                                   transition-colors hover:border-(--color-border)"
                                            :class="picked ? 'text-(--color-ink)' : 'text-(--color-ink-muted)'"
                                            x-text="picked?.name || @js(__('sales::message.type_or_pick'))">
                                    </button>

                                    <span x-show="picked" x-cloak
                                          class="num shrink-0 rounded-(--radius-field) border border-(--color-border)
                                                 px-2 py-0.5 text-2xs font-medium text-(--color-ink-muted)"
                                          x-text="picked?.code"></span>
                                </div>

                                <label class="block" x-show="pickerOpen" x-cloak>
                                    <span class="sr-only">{{ __('sales::message.type_or_pick') }}</span>
                                    <input type="search" x-model="term" x-ref="search"
                                           @keydown.enter.prevent="pickFirst()"
                                           @keydown.escape="pickerOpen = false"
                                           :placeholder="customerId ? @js(__('sales::message.type_or_pick')) : @js(__('sales::message.pick_customer_first'))"
                                           class="h-(--spacing-field) w-full truncate rounded-(--radius-field) border
                                                  border-(--color-border) bg-(--color-surface-app)
                                                  px-3 text-xl font-semibold
                                                  placeholder:font-semibold placeholder:text-(--color-ink-muted)">
                                </label>

                            </div>
                        </div>

                        {{-- বাছাই করা পণ্যের মজুদ — নমুনা দাবি করে এটা পণ্য
                             বাছার সাথে সাথেই দেখা যাবে --}}
                        <p class="mt-1 text-2xs text-(--color-ink-muted)" x-show="! picked" x-cloak>
                            {{ __('sales::message.pick_item_to_see_stock') }}
                        </p>

                        {{--
                            ── মজুদের পাঁচটা সংখ্যা — ওজন অনুযায়ী ─────────────

                            মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬):
                            *"available stock green kore ektu boro kore
                            dekhabe, baki gulo ek ekta ek ek color, normal
                            size"* — আর পরে: *"বিক্রয়যোগ্য মজুদ-এর সাথে
                            **ফ্রি একই মাপ ও রঙ** হবে"*।

                            ── কেন এই দুইটাই বড় ──────────────────────────
                            ⭐ কাউন্টারে **এই দুইটা সংখ্যাই সিদ্ধান্ত নেয়** —
                            "এখন বেচতে পারব কত" আর "ফ্রি দিতে পারব কত"।
                            বাকি তিনটা ব্যাখ্যা: কত আটকানো, কত ধরে রাখা, আর
                            গুদামে মোট কত।

                            ⚠️ পাঁচটাই সমান দেখালে চোখকে প্রতিবার **পাঁচটা
                            পড়ে দুইটা বেছে নিতে** হত — প্রতিটা পণ্যে।

                            ⓘ প্রতিটা রঙ থিমের টোকেন থেকে, হার্ডকোড নয়।
                        --}}
                        <div class="mt-1 flex flex-wrap items-baseline gap-x-4 gap-y-1"
                             x-show="picked" x-cloak>
                            @foreach ([
                                ['available', 'sales::field.available_short', 'text-sm text-(--color-success)', true],
                                ['free_available', 'sales::field.free_available', 'text-sm text-(--color-success)', true],
                                ['reserved', 'sales::field.reserved_short', 'text-2xs text-(--color-warning-hover)', false],
                                ['hold', 'sales::field.hold_short', 'text-2xs text-(--color-accent-pink)', false],
                                ['main', 'sales::field.main_stock', 'text-2xs text-(--color-brand-700)', false],
                            ] as [$key, $label, $tone, $big])
                                {{-- ⚠️ সংখ্যার পিছনে একক — মালিকের প্রশ্ন
                                     (৩ সেপ্টেম্বর ২০২৬): *"Available 1775
                                     uom kothay?"*।

                                     ── কেন প্রশ্নটা ন্যায্য ──────────────────
                                     "১৭৭৫" একা কোনো তথ্য নয়। ১৭৭৫ **পিস**
                                     আর ১৭৭৫ **কার্টন** এক জিনিস নয়, আর
                                     প্যাক-এন্ট্রি চালু থাকলে বিক্রেতা ঠিক
                                     উপরের ঘরেই "বাক্স" বেছে নিচ্ছেন —
                                     ⚠️ তখন **একই পর্দায় দুইটা একক**, একটা
                                     লেখা, আরেকটা অনুমান।

                                     ⓘ এককটা সবসময় পণ্যের **মূল একক**, কারণ
                                     মজুদের পাঁচটা সংখ্যাই মূল এককে রাখা হয়
                                     — বাছা প্যাক যা-ই হোক।

                                     ⓘ হালকা ও ছোট, ইচ্ছে করেই: সিদ্ধান্ত
                                     নেয় সংখ্যাটা, একক কেবল তার মাপকাঠি। --}}
                                <span class="{{ $big ? 'text-sm' : 'text-2xs' }}">
                                    <span class="text-(--color-ink-muted)">{{ __($label) }}</span>
                                    <span class="num font-bold {{ $tone }}"
                                          x-text="qty(picked?.{{ $key }})"></span>
                                    <span class="text-2xs text-(--color-ink-muted)"
                                          x-show="picked?.unit" x-cloak
                                          x-text="picked?.unit"></span>
                                </span>
                            @endforeach
                        </div>

                        {{-- খোঁজার ফল — **ভাসমান**, প্রবাহের ভিতরে নয়।

                             ── ⛔ কী ভাঙা ছিল, ৬ সেপ্টেম্বর ২০২৬ ──────────────
                             মালিক: *"products search korle full box expand hocche
                             keno"*। ⓘ তালিকাটা প্রবাহের ভিতরে আঁকা হত, তাই সে
                             জায়গা **দখল করত**: পণ্য খুঁজতে গেলেই কার্ডটা লম্বা
                             হয়ে যেত আর নিচের এন্ট্রির সারিটা ঠেলে নামত।

                             ⚠️ ক্ষতিটা চোখের নয়, হাতের: পরিমাণের ঘরটা প্রতিবার
                             **অন্য জায়গায়** চলে যেত, তাই যিনি না তাকিয়ে টাইপ
                             করেন তাঁর ছন্দ ভাঙত। ⓘ পর্দার নিচের সব কিছুও
                             একসাথে লাফাত।

                             ⭐ এখন সে ভাসে — কার্ডের উপরে, নিজের জমিন ও ছায়া
                             নিয়ে। ⓘ কার্ডের উচ্চতা আর বদলায় না, তাই **কোনো ঘর
                             নড়ে না**।

                             ⚠️ `z-30` — কার্টের টেবিলের উপরে থাকতে হবে, নাহলে
                             তালিকাটা তার নিচে ঢুকে যেত। --}}
                        <div class="absolute inset-x-0 top-full z-30 mt-1 max-h-40 space-y-0.5
                                    overflow-y-auto rounded-(--radius-card) border border-(--color-border)
                                    bg-(--color-surface-card) p-1 shadow-lg"
                             x-show="pickerOpen" x-cloak>
                            <template x-for="p in visible" :key="p.id">
                                <button type="button" @click="pick(p)"
                                        class="flex w-full items-baseline justify-between gap-2 rounded-(--radius-field)
                                               px-2 py-1 text-start text-sm transition-colors
                                               hover:bg-(--color-surface-hover)">
                                    <span class="min-w-0 flex-1 truncate" x-text="p.name"></span>

                                    {{-- ⭐ মজুদ ও ফ্রি — মালিকের নির্দেশ, ৬ সেপ্টেম্বর ২০২৬:
                                         *"Stock 8979 ctn, free 675 ctn এইভাবে লেখা থাকবে,
                                         সংখ্যাগুলো বোল্ড করে সবুজ কালার, লো স্টক হলে লাল"*।

                                         ⛔ আগে সারিতে ছিল কেবল **একটা কাঁচা সংখ্যা** —
                                         কীসের সংখ্যা তা লেখা ছিল না, আর এককও ছিল না।
                                         ⚠️ ১৭৯২ দেখে বোঝার উপায় ছিল না ওটা পিস না কার্টন,
                                         আর ফ্রি মজুদ আছে কি না তা **তালিকায় দেখাই যেত না** —
                                         পণ্যটা বেছে তবে জানা যেত।

                                         ⭐ রংটা সিদ্ধান্তের: সবুজ মানে নেওয়া যায়, লাল মানে
                                         **বাছার আগেই থামো**। ⓘ সীমাটা পণ্যের নিজের
                                         `reorder_level` — কোডে বসানো কোনো সংখ্যা নয়।

                                         ⚠️ ফ্রি-র ঘরটা শূন্য হলে দেখানো হয় না: প্রতিটা
                                         সারিতে "free 0" লিখলে চোখ ওটা পড়া বন্ধ করে দেয়,
                                         আর তখন যেখানে সত্যিই ফ্রি আছে সেটাও চোখ এড়ায়। --}}
                                    <span class="shrink-0 text-2xs text-(--color-ink-muted)">
                                        {{ __('sales::field.stock_short') }}
                                        <span class="num font-bold"
                                              :class="Number(p.available) > 0
                                                  && Number(p.available) <= Number(p.reorder || 0)
                                                      ? 'text-(--color-danger)'
                                                      : (Number(p.available) <= 0
                                                          ? 'text-(--color-danger)'
                                                          : 'text-(--color-success)')"
                                              x-text="qty(p.available)"></span>
                                        <span x-text="p.unit"></span>
                                    </span>

                                    <span class="shrink-0 text-2xs text-(--color-ink-muted)"
                                          x-show="Number(p.free) > 0" x-cloak>
                                        {{ __('sales::field.free_short') }}
                                        <span class="num font-bold text-(--color-success)"
                                              x-text="qty(p.free)"></span>
                                        <span x-text="p.unit"></span>
                                    </span>
                                </button>
                            </template>
                        </div>

                        {{--
                            ঘরগুলো স্থির মাপে, টেনে লম্বা হয় না।

                            ── মালিকের কথা (৩ সেপ্টেম্বর ২০২৬) ─────────────────
                            *"uporer box gulo zate fixt thake zate na bare …
                            ei box gulo ekho bare eta fix koro"* — ঘরগুলো
                            জায়গা পেলেই বেড়ে যাচ্ছিল।

                            ── কেন `grid` থেকে `flex` ─────────────────────────
                            `grid-cols-5` প্রতিটা কলামকে **সমান ভাগ** দেয়, আর
                            কলামটা চওড়া হলে ঘরও চওড়া হয়। ১৪০০px পর্দায় একটা
                            "পরিমাণ" ঘর দুই ইঞ্চি চওড়া হয়ে দাঁড়াত — অথচ ওতে
                            বসে বড়জোর পাঁচটা অঙ্ক।

                            `flex-wrap` + প্রতিটা ঘরের নিজের `w-*` মানে জায়গা
                            বাড়লে ঘর বাড়ে না, **সারিতে বেশি ঘর ধরে**; আর জায়গা
                            কমলে নিচে নেমে যায়। ফোনেও তাই কিছু ভাঙে না।
                        --}}
                        {{-- প্রথম সারি: পরিমাণ · একক · ফ্রি · একক · মোট --}}
                        {{-- পাঁচ কলাম, আর উপরে একটা সীমা।

                         ── কেন `flex` থেকে `grid` (৩ সেপ্টেম্বর ২০২৬) ──────
                         মালিক দুইটা কথা বলেছেন যেগুলো একসাথে মেলানো দরকার:
                         **"সব এক লাইনে"** আর **"ঘরগুলো যেন না বাড়ে"**।

                         `flex-wrap` + স্থির প্রস্থ প্রথমটা দিতে পারে না:
                         পর্দা সরু হলেই সারি ভেঙে যায়, আর কত ঘর ধরবে তা
                         পর্দার প্রস্থের উপর নির্ভর করে। মাপা গেছে ১৫০০px-এ
                         পাঁচটার মধ্যে চারটা ধরত।

                         `grid-cols-5` **সবসময় পাঁচটাই** রাখে — জায়গা কম
                         হলে ঘরগুলো ছোট হয়, সারি ভাঙে না। আর `max-w-3xl`
                         নিশ্চিত করে জায়গা বেশি হলেও ঘরগুলো একটা মাপের পরে
                         আর বাড়ে না — দ্বিতীয় কথাটা এখানেই রক্ষা পায়।

                         ⚠️ ফোনে পাঁচ কলাম মানে ঘরপ্রতি ৬০px — সংখ্যাও ধরে
                         না। তাই সেখানে দুই, ট্যাবলেটে তিন। --}}
                    {{-- ⭐ কলামগুলো আর সমান নয় — মালিকের নির্দেশ, ৬ সেপ্টেম্বর ২০২৬:
                         *"এই বক্সগুলো মার্ক করা মাপে কেটে ছোট করো, ডানে একই
                         লাইনে Add to Cart বোতাম আনো।"*

                         ⛔ আগে ছয়টা কলামই `1fr` — সমান। ⚠️ ফলে যে ঘরে `1`
                         বসে আর যে ঘরে `pcs` বসে, দুইটা **একই চওড়া** পেত,
                         আর সেই চওড়াটা ঠিক হত সবচেয়ে লম্বা লেবেল দেখে।
                         ⓘ তিনি ছবিতে ঠিক ঐ চারটা ঘর লাল বাক্সে ঘিরে
                         দেখিয়েছেন — পরিমাণ, একক, ফ্রি পরিমাণ, একক।

                         ⭐ এখন প্রতিটা ঘর তার **নিজের লেখার মাপে**:
                             পরিমাণ · ফ্রি পরিমাণ   ৩.৫rem  = ৭০px, চার অঙ্ক ধরে
                             একক (দুইটা)           ৪rem    = ৮০px, "pcs"/"ctn" ধরে
                             মোট পরিমাণ            ≥৪.৫rem  বাকি জায়গা ভাগ করে
                             বিক্রয় দর              ≥৫.৫rem  একটা দাম ধরে
                             কার্টে যোগ করুন         auto     লেখার সমান

                         ⚠️ সব মিলিয়ে সারিটার দরকার **~৬৫১px**। কার্ডটা তার
                         চেয়ে সরু হলে (যেমন ছোট ল্যাপটপে ~৫৯৮px) সারিটা
                         **নিজে স্ক্রল করে** — চেপে গিয়ে অপাঠ্য হয় না।

                         ⚠️ এই থিমে `1rem = 20px`, তাই ৪rem = ৮০px।

                         ⓘ `lg:` -এর নিচে ছক আগের মতোই — সরু পর্দায় সাতটা
                         ঘর এক সারিতে রাখলে প্রতিটা এত সরু হত যে সংখ্যাই
                         পড়া যেত না। --}}
                    {{-- ⛔ নমনীয় দুইটা ঘরের **সর্বনিম্ন মাপ** আছে — নাহলে
                         বোতামটা সারিতে ঢোকার পর ওরা চেপে যেত। ⓘ ব্রাউজারে
                         মেপে দেখা গেছে দরের ঘরটা **৫১px**-এ নেমেছিল, যেখানে
                         একটা দামও লেখা যায় না।

                         ⚠️ আর `overflow-x-auto` মোড়কে: সর্বনিম্ন মাপ দিলে
                         সরু পর্দায় সারিটা ধরবে না, আর তখন **চেপে অপাঠ্য
                         হওয়ার বদলে সে স্ক্রল করবে**। ⓘ পাতার শরীর কখনো
                         আড়াআড়ি স্ক্রল করে না, কেবল এই সারিটাই। --}}
                    <div class="mt-3 max-w-5xl overflow-x-auto">
                    {{-- ⚠️ `min-w-max` নয়: ওটা দিলে নমনীয় ঘর দুইটা সবসময়
                         তাদের **সবচেয়ে বড়** মাপ নিত (মাপা: ২১৬px), আর সারিটা
                         চওড়া পর্দাতেও অকারণে স্ক্রল করত। ⓘ সর্বনিম্ন মাপই
                         যথেষ্ট — জায়গা থাকলে তারা বাড়ে, না থাকলে মোড়কটা
                         স্ক্রল করে। --}}
                    <div class="grid items-end gap-2 grid-cols-2 sm:grid-cols-3
                                lg:grid-cols-[3.5rem_4rem_3.5rem_4rem_minmax(4.5rem,1fr)_minmax(5.5rem,1fr)_auto]
                                gap-x-1.5">
                            <x-sales::entry-field label="sales::field.qty" width="w-full">
                                <input type="number" step="0.01" min="0" x-model="entry.qty"
                                       class="num h-(--spacing-field-dense) w-full rounded-(--radius-field) border border-(--color-border)
                                              bg-(--color-surface-app) px-2 text-end text-sm">
                            </x-sales::entry-field>

                            {{--
                                ── একক — বাছাই, যখন পণ্যটার একাধিক প্যাক আছে ──

                                ── কী বাদ পড়েছিল (মাপা ৩ সেপ্টেম্বর ২০২৬) ─────
                                ঘরটা ছিল কেবল পড়ার — পণ্যের নিজের একক দেখাত।
                                অথচ প্যাক-এন্ট্রির **পুরো ইঞ্জিন আগে থেকেই
                                আছে** ([[PackConversion]]), কন্ট্রোল প্যানেলে
                                সুইচও আছে, আর **ছয়টা ফর্মে ড্রপডাউনটা চলছেও**।

                                ⚠️ **কেবল কাউন্টারের পর্দাটাই বাদ পড়েছিল** —
                                অর্থাৎ ঠিক যেখানে তাড়াহুড়ো সবচেয়ে বেশি, সেখানে
                                বিক্রেতাকে মাথায় গুণে "২ বাক্স"-কে "২০০ পিস"
                                করতে হত, **আর দরটাও নিজে ভাগ করে বসাতে হত**।

                                ── কেন দুইটা ঘর, একটা বাছাই ──────────────────
                                সার্ভার লাইনপ্রতি **একটাই** `unit_id` নেয়, তাই
                                ফ্রি পরিমাণও একই এককে যায়। দ্বিতীয় ঘরটা তাই
                                প্রথমটার প্রতিধ্বনি — দুইটা আলাদা বাছাই দিলে
                                "২ বাক্স বিক্রি, ৫ পিস ফ্রি" সার্ভারে প্রকাশই
                                করা যেত না।

                                ⓘ সুইচ বন্ধ থাকলে `packs` খালি, আর ঘরটা আগের
                                মতোই পড়ার-জন্য থাকে — এক এককে বেচা ব্যবসার
                                প্রতিটা সারিতে বাড়তি ড্রপডাউন কেবল বোঝা।
                            --}}
                            <x-sales::entry-field label="sales::field.uom" width="w-full">
                                <template x-if="entryUnits.length > 0">
                                    <select x-model="entry.unitId"
                                            class="h-(--spacing-field-dense) w-full rounded-(--radius-field) border
                                                   border-(--color-border) bg-(--color-surface-app) px-2 text-sm">
                                        <template x-for="u in entryUnits" :key="u.id">
                                            <option :value="u.id" x-text="u.name"></option>
                                        </template>
                                    </select>
                                </template>

                                <template x-if="entryUnits.length === 0">
                                    <input type="text" readonly :value="picked?.unit || ''"
                                           class="h-(--spacing-field-dense) w-full rounded-(--radius-field) border border-(--color-border)
                                                  bg-(--color-surface-app) px-2 text-sm text-(--color-ink-muted)">
                                </template>
                            </x-sales::entry-field>

                            @if ($show['free_qty'])
                                <x-sales::entry-field label="sales::field.free_qty" width="w-full">
                                    <input type="number" step="0.01" min="0" x-model="entry.freeQty"
                                           class="num h-(--spacing-field-dense) w-full rounded-(--radius-field) border border-(--color-border)
                                                  bg-(--color-surface-app) px-2 text-end text-sm">
                                </x-sales::entry-field>

                                <x-sales::entry-field label="sales::field.uom" width="w-full">
                                    <input type="text" readonly :value="picked?.unit || ''"
                                           class="h-(--spacing-field-dense) w-full rounded-(--radius-field) border border-(--color-border)
                                                  bg-(--color-surface-app) px-2 text-sm text-(--color-ink-muted)">
                                </x-sales::entry-field>
                            @endif

                            {{-- মোট পরিমাণ নিজে থেকেই — বিক্রয় + ফ্রি।

                                 হাতে লিখতে দিলে কেউ ভুল যোগ করত, আর গুদাম
                                 থেকে ভুল সংখ্যক মাল বেরোত। --}}
                            <x-sales::entry-field label="sales::field.total_qty" width="w-full">
                                <input type="text" readonly :value="qty(entryTotalQty)"
                                       class="num h-(--spacing-field-dense) w-full rounded-(--radius-field) border border-(--color-border)
                                              bg-(--color-surface-app) px-2 text-end text-sm font-semibold">
                            </x-sales::entry-field>
                            <x-sales::entry-field label="sales::field.sales_rate" width="w-full">
                                <input type="number" step="0.0001" min="0" x-model="entry.rate"
                                       class="num h-(--spacing-field-dense) w-full rounded-(--radius-field) border border-(--color-border)
                                              bg-(--color-surface-app) px-2 text-end text-sm">
                            </x-sales::entry-field>
                            {{-- ⭐ "কার্টে যোগ করুন" এখন এন্ট্রির সারিতেই, একদম
                                 ডানে — মালিকের নির্দেশ, ৬ সেপ্টেম্বর ২০২৬।

                                 ⓘ ঘরগুলো ভরে বোতামটা চাপা — কাজটা বাঁ থেকে ডানে
                                 একটানা, আর হাতকে ডান প্যানেলে যেতে হয় না।
                                 ⚠️ আগে বোতামটা ছিল সবুজ প্যানেলের ভিতরে, পর্দার
                                 অন্য প্রান্তে; তিনি ছবিতে একটা তীর এঁকে ঠিক এই
                                 জায়গাটাই দেখিয়েছেন।

                                 ⓘ `self-end` — লেবেলবিহীন বোতামটা যেন পাশের
                                 ঘরগুলোর **নিচের কিনারায়** বসে, লেবেলের সারিতে
                                 উঠে না যায়। --}}
                            <button type="button" @click="addToCart()" :disabled="! picked"
                                    class="h-(--spacing-field-dense) self-end whitespace-nowrap rounded-(--radius-field)
                                           bg-(--color-success) px-2 text-2xs font-semibold leading-tight
                                           text-white disabled:opacity-50">
                                {{ __('sales::action.add_to_cart') }}
                            </button>

                        </div>
                    </div>

                        {{--
                            ── উপহারের ঘর — যে পণ্যটা এখন হাতে, তার সাথেই ────

                            খোলে "উপহার" বোতামে, আর বোতামটা পণ্য না বাছা
                            পর্যন্ত নিষ্ক্রিয়। **কোন পণ্যের জন্য — সেটা
                            জিজ্ঞেস করা হয় না**, যেটা হাতে আছে সেটাই।

                            ⚠️ "কার্টে যোগ করুন" চাপার আগেই উপহারটা বসাতে
                            হয়, কারণ যোগ করার সাথে সাথে পণ্যটা হাত থেকে
                            ছুটে যায়। বোতামের ক্রমটাও তাই — উপহার আগে,
                            কার্টে যোগ পরে (মালিকের দেওয়া ক্রম)।
                        --}}
                        @if ($show['gift'])
                            {{-- ⚠️ `x-if`, `x-show` নয় — কারণটা ব্রাউজারে ধরা পড়েছে।

                                 `x-show` কেবল **লুকায়**, ঘরগুলো DOM-এ থেকে যায়।
                                 তাই ভেতরের `x-model="giftDraft.productId"` তখনো
                                 চলত, আর `giftDraft` খালি থাকায় প্রতি রেন্ডারে
                                 কনসোলে ঢালত:

                                     Cannot read properties of null (reading 'productId')

                                 ⚠️ **পর্দায় কিছুই ভাঙা দেখাত না** — কিন্তু Alpine
                                 একটা বাঁধনে হোঁচট খেলে সেই চক্রের পরের বাঁধনগুলো
                                 থেমে যায়। অর্থাৎ **অন্য একটা ঘর কাজ করা বন্ধ করে
                                 দিত, আর কারণটা কোথাও লেখা থাকত না।**

                                 `x-if` ব্লকটাকে তৈরিই করে না যতক্ষণ না `giftDraft`
                                 আছে — বাঁধনও থাকে না। --}}
                            <template x-if="giftDraft">
                            <div class="mt-3 rounded-(--radius-card) border border-(--color-badge-pending-ink)/30
                                        bg-(--color-badge-pending-bg)/50 p-2">
                                {{-- ⓘ লেবেলটা নিজের ঘরে, তারপর ড্যাশ — নাহলে
                                     `EveryFieldSwitch…` পাহারাটা ওটাকে খুঁজে পায়
                                     না। সে দেখে লেখাটার **পরেই একটা ট্যাগ** আছে
                                     কিনা, আর কারণটা যুক্তিসঙ্গত: এক ঘরের লেখা
                                     আরেক ঘরের লেখার ভিতরে মিলে গেলে সুইচ বন্ধ
                                     করেও "আছে" দেখাত। --}}
                                <p class="mb-2 text-2xs font-semibold text-(--color-badge-pending-ink)">
                                    <span>🎁 {{ __('sales::field.gift_item') }}</span> —
                                    <span x-text="picked ? picked.name : ''"></span>
                                </p>

                                <div class="flex flex-wrap items-end gap-2">
                                    <label class="min-w-48 flex-1">
                                        <span class="mb-0.5 block text-2xs text-(--color-ink-muted)">
                                            {{ __('sales::field.item_name') }}
                                        </span>
                                        <select x-model="giftDraft.productId"
                                                class="h-(--spacing-field-dense) w-full rounded-(--radius-field) border
                                                       border-(--color-border) bg-(--color-surface-app) px-2 text-sm">
                                            <option value="">-</option>
                                            <template x-for="p in catalogue" :key="p.id">
                                                <option :value="p.id" x-text="p.name"></option>
                                            </template>
                                        </select>
                                    </label>

                                    <label class="w-24">
                                        <span class="mb-0.5 block text-2xs text-(--color-ink-muted)">
                                            {{ __('sales::field.quantity') }}
                                        </span>
                                        <input type="number" step="0.01" min="0.01" x-model="giftDraft.qty"
                                               @keydown.enter.prevent="commitGift()"
                                               class="num h-(--spacing-field-dense) w-full rounded-(--radius-field) border
                                                      border-(--color-border) bg-(--color-surface-app) px-2 text-end text-sm">
                                    </label>

                                    <label class="min-w-40 flex-1">
                                        <span class="mb-0.5 block text-2xs text-(--color-ink-muted)">
                                            {{ __('sales::field.remarks') }}
                                        </span>
                                        <input type="text" x-model="giftDraft.remarks"
                                               class="h-(--spacing-field-dense) w-full rounded-(--radius-field) border
                                                      border-(--color-border) bg-(--color-surface-app) px-2 text-sm">
                                    </label>

                                    <button type="button" @click="commitGift()"
                                            :disabled="! giftDraft.productId || ! (Number(giftDraft.qty) > 0)"
                                            class="h-(--spacing-field-dense) whitespace-nowrap rounded-(--radius-field)
                                                   bg-(--color-badge-pending-ink) px-4 text-xs font-semibold text-white
                                                   disabled:opacity-40">
                                        {{ __('sales::action.add_to_cart') }}
                                    </button>

                                    <button type="button" @click="giftDraft = null"
                                            class="h-(--spacing-field-dense) whitespace-nowrap rounded-(--radius-field)
                                                   border border-(--color-border) px-3 text-xs">
                                        &times;
                                    </button>
                                </div>
                            </div>
                            </template>

                            {{-- এন্ট্রিতে বসানো উপহারগুলো — কার্টে যাওয়ার আগে --}}
                            <div x-show="entry.gifts.length > 0" x-cloak class="mt-2 flex flex-wrap gap-1">
                                <template x-for="(g, n) in entry.gifts" :key="g.key">
                                    <span class="inline-flex items-center gap-1 rounded-full
                                                 bg-(--color-badge-pending-bg) px-2 py-0.5 text-2xs
                                                 text-(--color-badge-pending-ink)">
                                        🎁 <span x-text="(catalogue.find(p => String(p.id) === String(g.productId)) || {}).name || ''"></span>
                                        <span class="num" x-text="qty(Number(g.qty || 0))"></span>
                                        <button type="button" @click="entry.gifts.splice(n, 1)"
                                                class="text-(--color-danger)">&times;</button>
                                    </span>
                                </template>
                            </div>
                        @endif

                    </div>
                </section>
            </div>

                    {{-- মাঝ: এই লাইন --}}
                    {{--
                        ⚠️ `lg:row-start-1` — ইচ্ছাকৃত, আর ছাড়া চলে না।

                        ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ────────────────
                        *"This line upore dike tulo"* — ক্রেতার বাক্স কাগজের
                        বাক্সে নেমে যাওয়ায় উপরের ডান কোণটা খালি হয়েছিল, আর
                        "এই লাইন" তবু নিচেই পড়ে থাকত।

                        ── কেন নিজে থেকে উপরে ওঠেনি ───────────────────────────
                        CSS-এর অটো-প্লেসমেন্ট **পিছনে ফেরে না**। বাঁ কলামটা
                        `col-start-1` দিয়ে দ্বিতীয় সারিতে বসার পর কার্সরটা
                        ওখানেই থেকে যায় — তাই পরের জিনিসটা **প্রথম সারির খালি
                        ঘরে না গিয়ে** দ্বিতীয় সারিতে বসে।

                        সারিটা হাতে বলে দেওয়াই একমাত্র নিশ্চিত পথ।
                    --}}
                    {{-- ⛔ জমিন `surface-card`, **ব্যাজের সবুজ নয়** — ৬ সেপ্টেম্বর ২০২৬।

                         ── কী ভাঙা ছিল ─────────────────────────────────────────
                         প্যানেলটা `--color-badge-success-bg` পরত। ⚠️ কিন্তু
                         **ব্যাজের টোকেন দিয়ে প্যানেল আঁকা যায় না**: ব্যাজ একটা
                         ছোট পিল — সে নিজের জমিন **আর** নিজের কালি একসাথে আনে,
                         আর ভিতরে আর কিছু বসে না। ⓘ এখানে ভিতরে বোতাম, লেবেল,
                         সংখ্যা, ড্রপডাউন — সবাই বসে, আর তাদের কেউ জানে না তারা
                         সবুজের উপরে আছে; তারা **পাতার কালি** পায়।

                         ⛔ মাপা ফল (গাঢ় থিম, সবুজ জমিনের উপরে):
                             উপহার বোতামের জমিন   ১.০৩:১   কার্যত অদৃশ্য
                             "ঘর খালি করুন" লেখা   ২.৫৫:১   পড়তে চাপ লাগে
                             লেবেলগুলোর কালি      ৪.৯০:১   অথচ পাশের সংখ্যা ১০.৩৫

                         ⭐ **ছাঁচটা মনে রাখার মতো:** টোকেনটা যাচাই হয়েছিল
                         **কার্ডের জমিনে** (১.৩৭:১, পাস), কিন্তু জিনিসটা বসে
                         **অন্য জমিনে**। ⚠️ "টোকেন মাপা আছে" যথেষ্ট নয় — সে
                         কোথায় বসছে সেটাও মাপার অংশ।

                         ⭐ তাই জমিনটা এখন `--color-surface-success-tint` —
                         **আলো থিমে সবুজ ছোপ, গাঢ় থিমে কার্ড**। ⓘ আপনার পছন্দের
                         সবুজ ঘরটা দিনের আলোয় অক্ষত; রাতে সবুজটা **পরিচয় হয়ে
                         বাঁচে** — বাঁয়ের দাগ, কিনারা আর "এই লাইন" শিরোনামে।

                         ⚠️ কেন গাঢ়তে ছোপ রাখা গেল না, তার মাপা তালিকাটা
                         `tokens.css`-এ ঐ টোকেনের পাশে। ⛔ সংক্ষেপে: জমিন যত
                         সবুজ, ভিতরের সবকিছু তত খারাপ — কারণ ভিতরের রংগুলো
                         নিরপেক্ষ গাঢ় কার্ডের সাথে মেপে বাছা। --}}
                    <div class="rounded-(--radius-card) border border-(--color-success)/40
                                bg-(--color-surface-success-tint) p-3 shadow-sm
                                lg:col-start-2 lg:col-end-[-1] lg:row-start-1"
                         style="box-shadow: inset var(--rail-tile-on-edge-w, 2px) 0 0 var(--color-state-on)">
                        {{--
                            ── লেবেল বাঁয়ে, অঙ্ক ডানে — এক সারিতে ─────────────

                            ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ────────────
                            *"Amount daner mark kora box e boro kore rako"* —
                            অঙ্কটা লেবেলের নিচে ছিল, আর ডান পাশটা খালি পড়ে
                            ছিল। এখন দুইটা এক সারিতে, আর **বাক্সটা এক সারি
                            কম উঁচু**।

                            ⚠️ অঙ্কটা **বড়ই থাকল** (`text-2xl`), ছোট করা হয়নি।
                            কাউন্টারে ওটাই একমাত্র সংখ্যা যেটা দূর থেকে পড়া
                            হয় — ক্রেতা পাশে দাঁড়িয়ে জিজ্ঞেস করেন *"কত হলো?"*,
                            আর বিক্রেতা তাকিয়েই বলেন।

                            ⓘ নিচের সারিগুলোও ডান-ঘেঁষা, তাই সব সংখ্যা এখন
                            **একই খাড়া রেখায়** — চোখকে বাঁয়ে-ডানে লাফাতে হয় না।
                        --}}
                        <div class="flex items-baseline justify-between gap-2">
                            {{-- ⓘ শিরোনাম ও অঙ্ক সবুজই — জমিন বদলেছে, পরিচয় নয়।
                                 ⚠️ কালিটা `--color-success` নয়, ব্যাজেরটাই: গাঢ়
                                 থিমে ব্যাজের সবুজ কালি হালকা (`#6ee7b7`), আর
                                 কার্ডের জমিনে সেটাই পড়া যায় — `--color-success`
                                 (#047857) গাঢ় কার্ডে ২:১-এরও নিচে নামত। --}}
                            <p class="text-2xs font-semibold uppercase tracking-wide text-(--color-badge-success-ink)">
                                {{ __('sales::field.this_line') }}
                            </p>
                            <p class="num text-2xl font-bold text-(--color-badge-success-ink)"
                               x-text="'৳' + money(entryNet)"></p>
                        </div>

                        {{--
                            ── এই লাইনের অঙ্ক — টাকার পথের ক্রমেই ─────────────

                            ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ────────────
                            *"Discount e Amount or % box daw · VAT dropdown
                            daw · Net Value — VAT-er niche daw"*।

                            ── ক্রমটা কেন এই ─────────────────────────────────
                            অঙ্কটা যেভাবে সত্যিই গড়ায়, ঠিক সেভাবেই উপর থেকে
                            নিচে:

                                ছাড়        কত কমল
                                ভ্যাট       কত কর বসল
                                নিট মূল্য   শেষ পর্যন্ত কত  ← তাই সবার নিচে
                                মোট পরিমাণ

                            ⭐ **আর নিট মূল্যটা এখন `entryNet`** — বাঁয়ের ঘরের
                            "নিট মূল্য"-র হুবহু একই সংখ্যা।

                            ⚠️ আগে এখানে `entryAfterDiscount` ছিল, অর্থাৎ
                            **এক পর্দায় "নিট মূল্য" নামে দুইটা আলাদা সংখ্যা** —
                            বাঁয়ে ভ্যাট-সহ, এখানে ভ্যাট ছাড়া। ভ্যাট বন্ধ থাকা
                            কোম্পানিতে দুইটা মিলত, তাই কেউ কোনোদিন ধরত না;
                            ভ্যাট চালু হলেই কাউন্টারের লোক দুইটা সংখ্যা দেখে
                            বুঝতেন না কোনটা সত্যি।
                        --}}
                        <dl class="mt-2 space-y-0.5 text-2xs">
                            {{--
                                ── মোট টাকা — সবার উপরে ────────────────────

                                মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬):
                                *"This line Discount er upore Total Amount
                                bosbe"*।

                                ⭐ এতে সারিগুলো **অঙ্কের গল্পটাই** বলে, উপর
                                থেকে নিচে:

                                    মোট টাকা    দর × পরিমাণ
                                    ছাড়         কত কমল
                                    ভ্যাট        কত কর বসল
                                    নিট মূল্য     শেষে কত

                                ⓘ আগে শুরুটাই ছিল না — ছাড় দেখা যেত, কিন্তু
                                **কীসের উপর ছাড়** তা নয়। "৭%" বলতে কত, সেটা
                                যাচাই করার কোনো উপায় পর্দায় ছিল না।
                            --}}
                            <div class="flex justify-between gap-2">
                                <dt class="text-(--color-ink-muted)">{{ __('sales::field.total_amount') }}</dt>
                                <dd class="num" x-text="money(entryBase)"></dd>
                            </div>

                            {{-- ছাড় — এখানেই লেখা যায়, টাকায় বা শতাংশে --}}
                            @if ($show['line_discount'])
                                <div class="flex items-center gap-2">
                                    <dt class="text-(--color-ink-muted)">{{ __('sales::field.line_discount') }}</dt>
                                    <dd class="flex flex-1 items-center gap-1">
                                        <input type="text" inputmode="decimal" x-model="entry.discountInput"
                                               placeholder="{{ __('sales::field.amount_or_pct') }}"
                                               class="num h-(--spacing-inline) w-20 rounded-(--radius-field) border
                                                      border-(--color-border) bg-(--color-surface-card) px-1 text-end">
                                        <span class="num ms-auto w-16 text-end" x-text="money(entryDiscount)"></span>
                                    </dd>
                                </div>
                            @endif

                            {{-- ভ্যাট — পুরো কাগজের ধরন এখানেই বদলানো যায় --}}
                            @if ($vatEnabled)
                                <div class="flex items-center gap-2">
                                    <dt class="text-(--color-ink-muted)">{{ __('sales::field.vat') }}</dt>
                                    <dd class="flex flex-1 items-center gap-1">
                                        <select x-model="vatMode"
                                                title="{{ __('sales::field.vat_per_product_hint') }}"
                                                class="h-(--spacing-inline) w-20 rounded-(--radius-field) border
                                                       border-(--color-border) bg-(--color-surface-card) px-1">
                                            <option value="">{{ __('sales::field.vat_per_product') }}</option>
                                            <option value="exclusive">{{ __('sales::field.vat_exclusive') }}</option>
                                            <option value="inclusive">{{ __('sales::field.vat_inclusive') }}</option>
                                            <option value="exempt">{{ __('sales::field.vat_exempt') }}</option>
                                        </select>
                                        <span class="num ms-auto w-16 text-end" x-text="money(entryVat)"></span>
                                    </dd>
                                </div>
                            @endif

                            @foreach ([
                                'sales::field.net_value' => 'entryNet',
                                'sales::field.total_qty' => 'entryTotalQty',
                            ] as $label => $expr)
                                <div class="flex justify-between gap-2">
                                    <dt class="text-(--color-ink-muted)">{{ __($label) }}</dt>
                                    <dd class="num" x-text="money({{ $expr }})"></dd>
                                </div>
                            @endforeach
                        </dl>
                        <div class="mt-2 border-t border-(--color-badge-success-ink)/20 pt-1 text-2xs">
                            <div class="flex justify-between">
                                <span class="text-(--color-ink-muted)">{{ __('sales::field.in_cart') }}</span>
                                <span class="num" x-text="lines.length + ' ' + @js(__('sales::field.items'))"></span>
                            </div>
                            <div class="flex justify-between font-semibold">
                                <span>{{ __('sales::field.running_total') }}</span>
                                <span class="num" x-text="'৳' + money(subTotal)"></span>
                            </div>
                        </div>

                        {{-- ⚠️ বোতামগুলো "এই লাইন" বাক্সের **ভেতরে**, একদম নিচে।

                             ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ────────────
                             *"Gift, Add to Cart, Costing, Clear Data — eigulo
                             baton this line box er ekdom niche bosbe"*।

                             ── কেন এটা ঠিক ────────────────────────────────────
                             বোতামগুলো বাক্সের বাইরে ভাসত, আর ওদের সাথে অঙ্কের
                             কোনো দৃশ্যমান সম্পর্ক ছিল না। **অথচ ওরা ঠিক ওই
                             অঙ্কটার উপরেই কাজ করে** — "কার্টে যোগ করুন" মানে
                             ওই ৳-টাকেই কার্টে ফেলা।

                             এখন এক বাক্সে: **উপরে কত, নিচে কী করব।**

                             ⚠️ ক্রমটা মালিকের লেখা, অনুমান করে বদলাবেন না।
                             আর "সব মুছুন" নিচের বারে আলাদা — দুইটা মুছে ফেলার
                             বোতাম পাশাপাশি থাকলে ভুল চাপ পড়া নিশ্চিত ছিল। --}}
                        {{-- ⭐ তিনটা বোতাম **এক সারিতে** — মালিকের নির্দেশ,
                             ৬ সেপ্টেম্বর ২০২৬: *"Gift, Costing, Clear Data
                             এক লাইন রাখো।"*

                             ⓘ চতুর্থটা ("কার্টে যোগ করুন") এখান থেকে উঠে
                             এন্ট্রির সারিতে গেছে, তাই বাকি তিনটা ঠিক তিন
                             কলামেই বসে — আগের `grid-cols-2` চারটার জন্য ছিল,
                             আর তিনটা নিয়ে ওটা **দুই সারি + একটা ফাঁকা ঘর**
                             হত। --}}
                        <div class="mt-3 grid grid-cols-3 gap-1 border-t border-(--color-badge-success-ink)/20 pt-3">
                        @if ($show['gift'])
                            {{-- ⚠️ এখানে `:disabled`, একটা কোলন — আর নিচে
                                 "নিশ্চিত করুন" বোতামে `::disabled`, দুইটা।
                                 **দুইটাই ঠিক**: Blade কেবল কম্পোনেন্ট ট্যাগে
                                 `::`-কে `:`-এ নামায়। সাধারণ ট্যাগে দুইটা দিলে
                                 অ্যাট্রিবিউটটা হুবহু `::disabled` হয়ে ব্রাউজারে
                                 যায়, আর **Alpine নীরবে উপেক্ষা করে** — বোতামটা
                                 সক্রিয় দেখাত, চাপলে কিছু হত না।

                                 ⭐ পর্দায় কিছুই ভাঙা দেখাত না, JS ত্রুটিও ছিল না।
                                 ধরেছে `AlpineBindingsReachTheBrowserTest`। --}}
                            <button type="button" @click="openGift()" :disabled="! picked"
                                    class="w-full rounded-(--radius-field) leading-tight border border-(--color-badge-pending-ink)/30 disabled:opacity-40
                                           bg-(--color-badge-pending-bg) px-1 py-1.5 text-2xs font-medium
                                           text-(--color-badge-pending-ink)">
                                {{ __('sales::field.gift') }}
                            </button>
                        @endif

                        {{-- ক্রয়মূল্য — ভেতরের কথা, গ্রাহককে পড়ে শোনানোর
                             জন্য নয়। তাই বোতামের পেছনে: চোখে পড়ে না,
                             কিন্তু দরকার হলে এক চাপ দূরে। --}}
                        <button type="button" @click="showCosting = ! showCosting"
                                class="w-full rounded-(--radius-field) leading-tight border border-(--color-border)
                                       px-1 py-1.5 text-2xs font-medium">
                            {{ __('sales::field.costing') }}
                        </button>

                        <span x-show="showCosting" x-cloak
                              class="num col-span-full text-end text-xs text-(--color-ink-muted)"
                              x-text="picked ? money(picked.cost) : ''"></span>

                        <button type="button" @click="clearEntry()"
                                class="w-full rounded-(--radius-field) leading-tight bg-(--color-danger)/10 px-2 py-1.5
                                       text-2xs font-medium text-(--color-danger) hover:bg-(--color-danger)/20">
                            {{ __('sales::action.clear_data') }}
                        </button>

                        </div>
                    </div>
                    {{--
                        ── ছবির ঘরটা তুলে দেওয়া হলো (৩ সেপ্টেম্বর ২০২৬) ──────

                        মালিকের নির্দেশ: *"একটি পণ্য বেছে নিন — ei box ta
                        utiye daw"*, লাল দাগ দিয়ে ঘেরা।

                        ── কী ছিল, আর কেন গেল ───────────────────────────────
                        বাছা পণ্যের ছবি বসার জায়গা, আর ছবি না থাকায় সেখানে
                        কেবল একটা বাক্সের আইকন আর "একটি পণ্য বেছে নিন" লেখা
                        দেখাত। **অর্থাৎ ৯rem জায়গা নিত, আর বদলে কিছুই বলত
                        না** — পণ্যের নামটা তো বাঁয়ের খোঁজার ঘরেই বড় করে
                        লেখা থাকে।

                        ⚠️ কলামটাও ছকের সংজ্ঞা থেকে গেছে, শুধু ঘরটা নয় —
                        নাহলে ৯rem খালি পড়ে থাকত আর কেউ বুঝত না কেন।
                        এখন `2xl`-এ ডান কলাম একটাই, ২২.৭৫rem — উপরের
                        ক্রেতার বাক্সের ঠিক সমান।

                        ⓘ পণ্যের ছবি ব্যবস্থাটায় আসছে (A3 বসাচ্ছে)। এলে
                        জায়গাটা ফিরিয়ে আনার কথা ভাবা যাবে — কিন্তু তখন
                        ওখানে **সত্যিকারের ছবি** থাকবে, খালি আইকন নয়।
                    --}}

            </div>

            {{-- ── কার্ট ────────────────────────────────────────────── --}}
            <section data-boxed class="overflow-hidden rounded-(--radius-card) border border-(--color-border)
                            bg-(--color-surface-card) shadow-sm">
                <div class="table-responsive">
                    <table class="ui-lines table-cards w-full text-sm">
                        <thead>
                            <tr>
                                <th class="text-start">{{ __('sales::field.sl') }}</th>
                                <th class="text-start">{{ __('sales::field.item_name') }}</th>
                                <th class="text-end">{{ __('sales::field.unit_price') }}</th>
                                <th class="text-end">{{ __('sales::field.quantity') }}</th>
                                @if ($show['free_qty'])
                                    <th class="text-end">{{ __('sales::field.free_unit') }}</th>
                                @endif
                                <th class="text-end">{{ __('sales::field.total_qty') }}</th>
                                @if ($show['line_discount'])
                                    <th class="text-end">{{ __('sales::field.dis') }}</th>
                                @endif
                                @if ($vatEnabled)
                                    <th class="text-end">{{ __('sales::field.vat') }}</th>
                                @endif
                                <th class="text-end">{{ __('sales::field.amount') }}</th>
                                <th><span class="sr-only">{{ __('sales::action.remove_line') }}</span></th>
                            </tr>
                        </thead>

                        <template x-for="(line, i) in lines" :key="line.key">
                        <tbody>
                                <tr class="border-b border-(--color-border)">
                                    <td class="cell" x-text="i + 1"></td>

                                    <td class="cell" data-label="{{ __('sales::field.item_name') }}">
                                        <span x-text="line.name"></span>
                                        <input type="hidden" :name="`lines[${i}][product_id]`" :value="line.id">
                                        {{-- বাছা প্যাকের একক — সার্ভার এটা দেখেই
                                             "২ বাক্স"-কে পিসে নামায়, দর সহ --}}
                                        <input type="hidden" :name="`lines[${i}][unit_id]`" :value="line.unitId || ''">
                                    </td>

                                    <td class="cell-input text-end" data-label="{{ __('sales::field.unit_price') }}">
                                        <input type="number" step="0.0001" min="0" x-model="line.rate"
                                               :name="`lines[${i}][rate]`"
                                               class="num h-(--spacing-field-dense) w-full sm:w-24 rounded-(--radius-field) border
                                                      border-(--color-border) bg-(--color-surface-app) px-2 text-end">
                                    </td>

                                    <td class="cell-input text-end" data-label="{{ __('sales::field.quantity') }}">
                                        <input type="number" step="0.01" min="0.01" x-model="line.qty"
                                               :name="`lines[${i}][qty]`"
                                               class="num h-(--spacing-field-dense) w-full sm:w-20 rounded-(--radius-field) border
                                                      border-(--color-border) bg-(--color-surface-app) px-2 text-end">
                                    </td>

                                    @if ($show['free_qty'])
                                        <td class="cell-input text-end" data-label="{{ __('sales::field.free_unit') }}">
                                            <input type="number" step="0.01" min="0" x-model="line.freeQty"
                                                   :name="`lines[${i}][free_qty]`"
                                                   class="num h-(--spacing-field-dense) w-full sm:w-20 rounded-(--radius-field) border
                                                          border-(--color-border) bg-(--color-surface-app) px-2 text-end">
                                        </td>
                                    @endif

                                    <td class="num cell" data-label="{{ __('sales::field.total_qty') }}"
                                        x-text="qty(Number(line.qty || 0) + Number(line.freeQty || 0))"></td>

                                    @if ($show['line_discount'])
                                        <td class="cell-input text-end" data-label="{{ __('sales::field.dis') }}">
                                            <input type="number" step="0.01" min="0" max="100"
                                                   x-model="line.discountPercent"
                                                   :name="`lines[${i}][discount_percent]`"
                                                   class="num h-(--spacing-field-dense) w-full sm:w-20 rounded-(--radius-field) border
                                                          border-(--color-border) bg-(--color-surface-app) px-2 text-end">
                                        </td>
                                    @endif

                                    @if ($vatEnabled)
                                        <td class="num cell" data-label="{{ __('sales::field.vat') }}"
                                            x-text="money(lineVat(line))"></td>
                                    @endif

                                    <td class="num cell font-medium" data-label="{{ __('sales::field.amount') }}"
                                        x-text="money(lineNet(line))"></td>

                                    <td class="cell-input text-end">
                                        <button type="button" @click="lines.splice(i, 1)"
                                                aria-label="{{ __('sales::action.remove_line') }}"
                                                class="rounded-(--radius-field) px-2 py-1 text-(--color-ink-muted)
                                                       hover:bg-(--color-surface-hover)">&times;</button>
                                    </td>
                                </tr>

                                {{--
                                    ── উপহার — লাইনের নিচে, ভেতরে ────────────

                                    ── কেন সন্তান-সারি, আলাদা টেবিল নয় ───────
                                    মালিকের নমুনা (৩ সেপ্টেম্বর ২০২৬): উপহারটা
                                    পণ্যের ঠিক নিচে, `↳` চিহ্ন দিয়ে।

                                    ⚠️ **এতে "কোন পণ্যের জন্য" প্রশ্নটাই মুছে
                                    যায়** — উত্তরটা জায়গাতেই লেখা। আগে ওটা
                                    একটা ড্রপডাউন ছিল, আর সেটা খালি থাকলে
                                    উপহার কোনো পণ্যের সাথে বাঁধা পড়ত না।

                                    ⓘ NEXUS-এ ঠিক এই ভুলটাই একবার হয়েছিল, আর
                                    সেখানেও সমাধানটা এটাই — ড্রপডাউন তুলে
                                    দিয়ে উপহারকে লাইনের ভেতরে নেওয়া।

                                    দাম-ছাড়-ভ্যাটের ঘরে ড্যাশ, কারণ **উপহারের
                                    দাম নেই** — থাকলে ওটা বিক্রি হয়ে যেত।
                                --}}
                                <template x-for="(gift, g) in line.gifts" :key="gift.key">
                                    <tr class="border-b border-(--color-border) bg-(--color-badge-pending-bg)/40 text-2xs">
                                        <td class="cell text-end text-(--color-badge-pending-ink)">↳</td>

                                        {{-- ⓘ ইন্ডেন্টটা টোকেন থেকে — হাতে লেখা একটা ধ্রুবক
                                             ঘন থিমে একা আগের মাপে বসে থাকত, আর
                                             উপহারের সারিটা মূল সারির সাথে আর
                                             সারিবদ্ধ থাকত না। --}}
                                        <td class="cell text-(--color-badge-pending-ink)"
                                            style="padding-inline-start: calc(var(--grid-pad-x) * 2)"
                                            data-label="{{ __('sales::field.item_name') }}">
                                            🎁 <span x-text="(catalogue.find(p => String(p.id) === String(gift.productId)) || {}).name || ''"></span>
                                        </td>

                                        <td class="cell text-end text-(--color-ink-muted)">—</td>
                                        <td class="cell text-end text-(--color-ink-muted)">—</td>

                                        @if ($show['free_qty'])
                                            <td class="num cell text-(--color-badge-pending-ink)"
                                                data-label="{{ __('sales::field.free_unit') }}"
                                                x-text="qty(Number(gift.qty || 0)) + ' ' + (line.unit || '')"></td>
                                        @endif

                                        <td class="num cell text-(--color-badge-pending-ink)"
                                            data-label="{{ __('sales::field.total_qty') }}"
                                            x-text="qty(Number(gift.qty || 0))"></td>

                                        @if ($show['line_discount'])
                                            <td class="cell text-end text-(--color-ink-muted)">—</td>
                                        @endif

                                        @if ($vatEnabled)
                                            <td class="cell text-end text-(--color-ink-muted)">—</td>
                                        @endif

                                        <td class="cell text-end italic text-(--color-badge-pending-ink)"
                                            data-label="{{ __('sales::field.amount') }}"
                                            x-text="gift.remarks"></td>

                                        <td class="cell-input text-end">
                                            <button type="button" @click="removeGift(i, g)"
                                                    aria-label="{{ __('sales::action.remove_line') }}"
                                                    class="rounded-(--radius-field) px-2 py-1 text-(--color-ink-muted)
                                                           hover:bg-(--color-surface-hover)">&times;</button>
                                        </td>
                                    </tr>
                                </template>
                            </tbody>
                        </template>
                    </table>
                </div>

                <p x-show="lines.length === 0" x-cloak
                   class="p-8 text-center text-sm text-(--color-ink-muted)">
                    {{ __('sales::message.nothing_added') }}
                </p>
            </section>

            {{--
                ── কাজের প্যানেল — কার্টের ঠিক নিচে, পুরো প্রস্থে ──────────

                ── মালিকের সিদ্ধান্ত (৩ সেপ্টেম্বর ২০২৬) ────────────────────
                তিনি জিজ্ঞেস করেছিলেন পপআপ ভালো নাকি বাঁয়ে নিচে খোলা, আর
                যুক্তি শুনে **নিচেরটাই** বেছেছেন।

                ── কেন পপআপ নয় ───────────────────────────────────────────
                ⚠️ এই চারটার তিনটাই **টাকার অঙ্ক বদলায়** — খরচ লিখলে "নিট
                পরিশোধযোগ্য", জমা লিখলে "বিলের বকেয়া"। ওই দুইটা সংখ্যা ডান
                প্যানেলে। **পপআপ ঠিক সেগুলোকেই ঢেকে দিত** — টাইপ এক জায়গায়,
                ফল ঢাকা পড়া জায়গায়।

                ── আর জায়গার হিসাব ────────────────────────────────────────
                ডান প্যানেল ৩৪০px, এখানে ১২৪৫px। পরিবহনের তিনটা ঘর (গাড়ি ·
                চালক · ভাড়া) সরু কলামে একটার নিচে আরেকটা নামত।

                ⓘ উপহারের ঘরটাও আজ এভাবেই ইনলাইনে বসেছে — এক পর্দায় দুই
                রকম নিয়ম (কিছু ইনলাইন, কিছু পপআপ) শেখার বোঝা বাড়ায়।
            --}}
            <div x-ref="actionPanel">
                @include('sales::direct.partials.panels')
            </div>

            {{--
                ── নেওয়া জমাগুলোর তালিকা — চার্টের নিচে, বাম পাশে ──────────

                মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬): *"এই list Item Chart-এর
                নিচে বাম পাশে থাকবে, একাধিক payment add করতে পারবে"*।

                ── কেন এখানে, ডান প্যানেলে নয় ─────────────────────────────
                ডান প্যানেলে জমার **যোগফল** থাকে ("জমা নেওয়া হলো ৳১৫,০০০")।
                ⚠️ সারিগুলোও ওখানে বসালে সরু কলামে পাঁচটা ঘর একটার নিচে
                আরেকটা নামত, আর যোগফলটা নিচে ঠেলে যেত — যে সংখ্যাটা সবচেয়ে
                বেশি দেখা হয়।

                ⭐ কার্টের নিচে বসার আসল কারণটা আলাদা: **এটা কার্টেরই মতো
                একটা জিনিস** — যা যা নেওয়া হলো তার তালিকা। মালের তালিকার
                ঠিক নিচে টাকার তালিকা, একই চোখের পথে।

                ⓘ খালি থাকলে দেখাই যায় না — বেশিরভাগ বাকির চালানে কোনো জমা
                নেই, আর একটা স্থায়ী খালি বাক্স রোজ চোখের সামনে থাকত।
            --}}
            @if ($show['deposit'])
                <div x-show="deposits.length > 0" x-cloak
                     class="rounded-(--radius-card) border border-(--color-border)
                            bg-(--color-surface-card) p-3">
                    <div class="mb-2 flex items-baseline justify-between gap-2">
                        <span class="text-2xs font-semibold text-(--color-ink)">
                            {{ __('sales::field.received_deposit') }}
                        </span>

                        <span class="num text-sm font-bold text-(--color-success)"
                              x-text="'৳' + money(deposit)"></span>
                    </div>

                    <div class="overflow-x-auto">
                        {{-- ⓘ `ui-list` — তিনটা নামের একটা।

                             নাম না থাকলে ছকের মাপ ও ধার **কোনো থিম বদলাতে
                             পারবে না**, আর থিম বদলালে এই ছকটা একা আগের
                             চেহারায় বসে থাকত। ⚠️ ঘরের প্যাডিংও তাই হাতে
                             লেখা হয়নি — ওটা টোকেন থেকে আসে। --}}
                        <table class="ui-list w-full text-2xs">
                            <thead class="text-(--color-ink-muted)">
                                <tr class="border-b border-(--color-border)">
                                    <th class="text-start font-medium">{{ __('sales::field.ref_date') }}</th>
                                    <th class="text-start font-medium">{{ __('sales::field.deposit_method') }}</th>
                                    <th class="text-start font-medium">{{ __('sales::field.account') }}</th>
                                    <th class="text-start font-medium">{{ __('sales::field.deposit_ref') }}</th>
                                    {{-- ⚠️ "বিবরণ", "মন্তব্য" নয় — মালিকের সিদ্ধান্ত
                                         (৪ সেপ্টেম্বর ২০২৬)। লেখাটা
                                         `collections.narration`-এ বসে, আদায়ের
                                         কাগজে ছাপা হয়, আর খতিয়ানের সারিতে যায় —
                                         অর্থাৎ ওটা হিসাবের বক্তব্য, পাশের নোট নয়।
                                         ⓘ এক কলামের দুইটা নাম হলে রিপোর্টে ওরা
                                         মিলত না। --}}
                                    <th class="text-start font-medium">{{ __('sales::field.narration') }}</th>
                                    <th class="text-end font-medium">{{ __('sales::field.amount') }}</th>
                                    <th></th>
                                </tr>
                            </thead>

                            <tbody>
                                <template x-for="(row, i) in deposits" :key="i">
                                    <tr class="border-b border-(--color-border)/50">
                                        <td class="num" x-text="row.refDate || '—'"></td>
                                        <td x-text="depositMethodName(row.methodId)"></td>
                                        <td x-text="depositAccountName(row.accountId)"></td>
                                        <td class="num" x-text="row.reference || '—'"></td>
                                        <td x-text="row.narration || '—'"></td>
                                        <td class="num text-end font-semibold"
                                            x-text="money(row.amount)"></td>

                                        <td class="text-end">
                                            <button type="button" @click="dropDeposit(i)"
                                                    class="rounded px-1.5 text-(--color-danger)"
                                                    aria-label="{{ __('sales::action.remove_line') }}">&times;</button>
                                        </td>

                                        {{-- ⚠️ সার্ভারে যা যায় — সারির ভেতরেই।

                                             উপহারের মতো আলাদা করে সমতল করার
                                             দরকার নেই: তালিকাটা এমনিতেই এক
                                             স্তরের, তাই সূচকটা এখানেই বসে।

                                             ⓘ উপায়ের **কোড আর খাত সার্ভার
                                             নিজে বের করে** — পর্দার পাঠানো
                                             নাম বিশ্বাস করা হয় না। --}}
                                        <td class="hidden">
                                            <input type="hidden" :name="`deposits[${i}][amount]`" :value="row.amount">
                                            <input type="hidden" :name="`deposits[${i}][payment_method_id]`" :value="row.methodId">
                                            <input type="hidden" :name="`deposits[${i}][account_id]`" :value="row.accountId">
                                            <input type="hidden" :name="`deposits[${i}][ref_date]`" :value="row.refDate">
                                            <input type="hidden" :name="`deposits[${i}][reference]`" :value="row.reference">
                                            <input type="hidden" :name="`deposits[${i}][narration]`" :value="row.narration">
                                        </td>
                                    </tr>
                                </template>
                            </tbody>
                        </table>
                    </div>
                </div>
            @endif

            {{--
                ── উপহারগুলো সার্ভারে যায় এখান থেকে ─────────────────────

                কার্টের সারিগুলো নিজেরাই লুকানো ঘর বহন করে, কিন্তু উপহার
                পারে না: সার্ভার একটা **সমতল** তালিকা চায়
                (`gifts[0][…]`, `gifts[1][…]`), অথচ পর্দায় ওগুলো লাইনের
                ভেতরে বাসা বেঁধে আছে।

                `payloadGifts` ওই দুইটাকে মেলায় — আর ঠিক ওখানেই
                `against_product_id` বসে **লাইনের পণ্য থেকে**, কোনো
                ড্রপডাউন থেকে নয়।

                ⚠️ খালি productId বা শূন্য পরিমাণের উপহার এখানে ছেঁকে
                ফেলা হয়, নাহলে সার্ভারে অর্ধেক লেখা সারি যেত।
            --}}
            <template x-for="(g, n) in payloadGifts" :key="n">
                <span>
                    <input type="hidden" :name="`gifts[${n}][product_id]`" :value="g.productId">
                    <input type="hidden" :name="`gifts[${n}][against_product_id]`" :value="g.againstProductId">
                    <input type="hidden" :name="`gifts[${n}][qty]`" :value="g.qty">
                    <input type="hidden" :name="`gifts[${n}][remarks]`" :value="g.remarks">
                </span>
            </template>

        </div>

        {{-- ══ ডান পাশের প্যানেল ═══════════════════════════════════════ --}}
        {{-- ⓘ বিলের মোট — কাগজের **ফল**, তাই নিজের রং আর দাগ।
             ⚠️ `overflow-hidden` লাগে, নাহলে ভিতরের সারিগুলো গোল কোণের
             বাইরে বেরিয়ে যেত। --}}
        <aside class="flex flex-col self-start overflow-hidden rounded-(--radius-card) border
                      border-(--color-border) bg-(--color-surface-selected) shadow-sm
                      xl:sticky xl:top-3 xl:max-h-[calc(100dvh-5.5rem)]"
               style="box-shadow: inset var(--rail-tile-on-edge-w, 2px) 0 0 var(--color-brand-500)">

            <div class="min-h-0 flex-1 overflow-y-auto">

            {{-- এই চালান --}}

            {{-- ⚠️ নিচের ফাঁকটা কাটা — মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬):
                 *"mark kora faka komale scrol bar r thakbe na"*।

                 টাকার সারিগুলোর নিচে ১৫px ফাঁক ছিল, আর তার ঠিক নিচেই
                 বকেয়ার লাল বড়িটা — দুইটার মাঝে ওই ফাঁকের কোনো কাজ ছিল না,
                 কারণ **বড়িটা নিজেই একটা আলাদা আকার**, তাকে আলাদা করতে
                 ফাঁকের দরকার হয় না।

                 ⚠️ উপরের `pt-3` রাখা হয়েছে: ওখানে প্যানেলের কিনারা, আর
                 কিনারা ঘেঁষা লেখা সস্তা দেখায়। --}}
            <div class="space-y-1 px-3 pt-3 pb-1 text-2xs">
                {{--
                    ── বিলের মোট টাকা — প্যানেলের মাথায়, বড় করে ────────────

                    ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ────────────────────
                    *"Invoice Total Amount ei upore mark kora box e boro kore
                    dekhabe"*।

                    ── কেন এটাই ঠিক জায়গা ───────────────────────────────────
                    "এই লাইন" বাক্সে বড় সংখ্যাটা **একটা পণ্যের**; এখানে বড়
                    সংখ্যাটা **পুরো কাগজের**। দুইটা এক মাপে থাকলে চোখ বুঝত না
                    কোনটা কীসের — আর কাউন্টারে ভুলটা দামি: ক্রেতা জিজ্ঞেস
                    করেন *"সব মিলিয়ে কত?"*, আর উত্তরটা এই সংখ্যাটাই।

                    ⓘ নিচের সারিগুলো ছোট থাকল ইচ্ছে করে — ছাড় · ভ্যাট · খরচ
                    সবই **এই সংখ্যাটার ব্যাখ্যা**, প্রতিদ্বন্দ্বী নয়।
                --}}
                <div class="flex items-baseline justify-between gap-2 border-b
                            border-(--color-border) pb-2">
                    <span class="text-2xs font-semibold uppercase tracking-wide text-(--color-ink-muted)">
                        {{ __('sales::field.invoice_total') }}
                    </span>
                    {{-- ⚠️ রঙ ও হাইলাইট — মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬):
                         *"INV Total ৳10.00 — Navy blue or Royal blue kore
                         highlight kore daw"*।

                         ── কেন এই একটা সংখ্যাই ─────────────────────────────
                         প্যানেলে পনেরোটা সংখ্যা আছে, আর ক্রেতা জিজ্ঞেস করেন
                         একটাই: *"সব মিলিয়ে কত?"* **রঙটা ওই একটাকেই বাকি
                         চোদ্দটা থেকে আলাদা করে।**

                         ⚠️ সব সংখ্যা রঙিন করলে রঙের কোনো মানে থাকত না —
                         **আলাদা করা মানে বাকিদের সাদাসিধে রাখা।**

                         ⓘ `--color-brand-700` — থিমের নিজের রয়্যাল ব্লু।
                         হার্ডকোড করলে নয়টা থিমের বাকিগুলোয় বেমানান হত। --}}
                    <span class="num rounded-(--radius-field) bg-(--color-brand-50)
                                 px-2 py-0.5 text-2xl font-bold text-(--color-brand-700)"
                          x-text="'৳' + money(grossTotal)"></span>
                </div>

                @if ($show['sub_total'])
                    <x-sales::panel-row :label="__('sales::field.sub_total_no_vat')">
                        <span class="num" x-text="'৳' + money(subTotal)"></span>
                    </x-sales::panel-row>
                @endif

                {{--
                    ── ছাড় — শতাংশ এখন সত্যিই কাজ করে ───────────────────────

                    ── কী ভাঙা ছিল (মাপা ৩ সেপ্টেম্বর ২০২৬) ──────────────────
                    ঘরটার placeholder বলত **"টাকা বা %"**, অথচ শতাংশ
                    **তিন জায়গার একটাতেও** কাজ করত না:

                        `type="number"`     ব্রাউজার `%` টাইপই করতে দিত না
                        `Number(...)`       শতাংশ হলে NaN → ০
                        সার্ভারে `money()`   সরাসরি টাকা ধরে নিত

                    ⚠️ **অর্থাৎ লেখাটা একটা প্রতিশ্রুতি দিত যেটা ব্যবস্থাটা
                    রাখত না।** কেউ ৬% লিখতে গিয়ে না পেরে ভাবতেন কীবোর্ড
                    নষ্ট, আর কেউ হয়তো "৬" লিখে ৬ টাকা ছাড় দিয়ে ফেলতেন —
                    ৬% ভেবে। **দ্বিতীয়টা টাকার ভুল, আর নীরব।**

                    ── এখন যা হয় ────────────────────────────────────────────
                    ঘরটা লেখার ঘর, তাই `6%` লেখা যায়। পাশে **টাকার অঙ্কটা
                    দেখা যায়** — মালিকের দেখানো NEXUS-এর নমুনায় ঠিক ওটাই।

                    ⭐ **সার্ভার এক অক্ষরও বদলায়নি**: লুকানো ঘরটা সবসময়
                    **টাকা** পাঠায়, শতাংশ নয়। পর্দা হিসাব করে, সার্ভার
                    আগের মতোই টাকা পায় — উপহারের সাথে একই কৌশল।
                --}}
                <x-sales::panel-row :label="__('sales::field.discount_amount')">
                    <span class="flex flex-1 items-center gap-2">
                        <input type="text" inputmode="decimal" x-model="discountInput"
                               placeholder="{{ __('sales::field.amount_or_pct') }}"
                               class="num h-(--spacing-inline) w-20 rounded-(--radius-field) border border-(--color-border)
                                      bg-(--color-surface-app) px-1 text-end">

                        {{-- হিসাব হওয়া টাকাটা — "৬%" লিখে কত হলো তা না
                             দেখালে শতাংশ দেওয়াটা অন্ধ বাজি হয়ে যেত --}}
                        <span class="num ms-auto w-16 text-end"
                              x-text="discountValue > 0 ? '৳' + money(discountValue) : '—'"></span>
                    </span>

                    <input type="hidden" name="discount_amount" :value="discountValue">
                </x-sales::panel-row>

                @if ($vatEnabled)
                    {{--
                        ── ভ্যাট — পুরো কাগজের জন্য একবারে বদলানো যায় ─────────

                        ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ────────────────
                        *"vat er dropdawn daw ni"* — NEXUS-এর নমুনায় ভ্যাটের
                        পাশে একটা বাছাই আছে, ABOS-এ ছিল না।

                        ── কেন ডিফল্টটা "পণ্য অনুযায়ী", আর সেটাই সাধারণ ──────
                        একটা ডিপো এক হারে বিস্কুট বেচে আর অন্য হারে সাবান —
                        **প্রতিটা লাইন নিজের পণ্যের ঘোষিত হারই নেবে**, এটাই
                        একমাত্র সঠিক উত্তর। তাই বাছাইটা ফাঁকা থাকলে কিছুই
                        বদলায় না।

                        বদলটা ওই চালানের জন্য যেটা **সত্যিই এক হারের**:
                        রপ্তানি, অব্যাহতিপ্রাপ্ত ক্রেতা, লিখিতভাবে ঠিক করা
                        হার। ⚠️ **আর কথাটা সারিতেই লেখা থাকে** — কোথাও লুকানো
                        একটা সেটিংসে নয়, যেটা কেউ বসিয়ে ভুলে যেত।

                        ── তিনটা বদল ─────────────────────────────────────────
                            ভ্যাট বাদে   দরের **উপরে** বসে
                            ভ্যাট সহ     দরের **ভেতরেই** আছে
                            ভ্যাট নেই     শূন্য

                        ⚠️ হারের ঘরটা কেবল প্রথম দুইটায় দেখা যায় — "পণ্য
                        অনুযায়ী"-র পাশে একটা হার মানে **এমন একটা সংখ্যা যেটা
                        কোথাও বসে না**, আর "ভ্যাট নেই"-এর পাশে হার অর্থহীন।
                    --}}
                    <x-sales::panel-row :label="__('sales::field.vat')">
                        <span class="flex flex-1 flex-wrap items-center gap-1">
                            <select x-model="vatMode" name="vat_mode"
                                    title="{{ __('sales::field.vat_per_product_hint') }}"
                                    class="h-(--spacing-inline) w-24 rounded-(--radius-field) border
                                           border-(--color-border) bg-(--color-surface-app) px-1">
                                <option value="">{{ __('sales::field.vat_per_product') }}</option>
                                <option value="exclusive">{{ __('sales::field.vat_exclusive') }}</option>
                                <option value="inclusive">{{ __('sales::field.vat_inclusive') }}</option>
                                <option value="exempt">{{ __('sales::field.vat_exempt') }}</option>
                            </select>

                            <template x-if="vatMode === 'exclusive' || vatMode === 'inclusive'">
                                <input type="number" min="0" max="100" step="0.01" x-model="vatRate"
                                       name="vat_rate" placeholder="%"
                                       title="{{ __('sales::field.vat_rate_for_every_line') }}"
                                       class="num h-(--spacing-inline) w-12 rounded-(--radius-field) border
                                              border-(--color-border) bg-(--color-surface-app) px-1 text-end">
                            </template>

                            <span class="num ms-auto" x-text="'৳' + money(vatTotal)"></span>
                        </span>
                    </x-sales::panel-row>
                @endif

                {{--
                    ── খরচ — ঘর বাঁয়ে, অঙ্ক ডানে, কারণ নিচে ──────────────────

                    মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬): *"Expense-এর বাক্সও
                    পাশে নিয়ে আসো, বক্সে বসালে ডানে show করবে, আর নিচে
                    narration on হবে — কী লিখতে হবে সেটা narration বক্সের
                    ভিতরে লেখা থাকবে"*।

                    ⭐ **তিনটা কথাই আলাদা আলাদা কারণে ঠিক:**

                    **ঘর বাঁয়ে** — ছাড়, ভ্যাট, রাউন্ডিংয়ের মতোই। যে সারিতে
                    কিছু লেখা যায়, সেটা এক নজরে চেনা যায়।

                    **অঙ্ক ডানে** — প্যানেলের বাকি পনেরোটা সংখ্যার সাথে এক
                    খাড়া রেখায়। ⚠️ কেবল ঘরটা থাকলে চোখকে **টাকার কলামের
                    মাঝখানে একটা ফাঁক** পেরোতে হত।

                    **কারণ নিচে, ভেতরে ইঙ্গিত** — আগে ইঙ্গিতটা ছিল আলাদা
                    লেবেলে, আর ওটা একটা বাড়তি সারি খেত। ঘরের ভিতরে থাকলে
                    জায়গা লাগে না, আর **টাইপ শুরু করলেই সরে যায়** — যখন
                    আর দরকার নেই।
                --}}
                @if ($show['expense'])
                    <x-sales::panel-row :label="__('sales::field.expense')">
                        {{-- ⚠️ টাকা **বা** শতাংশ — মালিকের নির্দেশ
                             (৩ সেপ্টেম্বর ২০২৬): *"Expense e discount er moto
                             Amount o % thakbe"*।

                             ── কেন শতাংশ দরকার ───────────────────────────
                             পরিবহন ভাড়া প্রায়ই **চালানের মূল্যের অনুপাতে**
                             ধরা হয় — "বিলের ১%"। আগে ঘরটা `type="number"`
                             ছিল, তাই `1%` লেখাই যেত না; কেউ ১ লিখে **এক
                             টাকা** খরচ বসিয়ে দিতেন, আর সেটা নীরব ভুল।

                             ⭐ সার্ভার এক অক্ষরও বদলায়নি: লুকানো ঘরটা
                             সবসময় **টাকা** পাঠায় — ছাড়ের সাথে একই কৌশল। --}}
                        <span class="flex flex-1 items-center gap-2">
                            <input type="text" inputmode="decimal" x-model="expenseInput"
                                   placeholder="{{ __('sales::field.amount_or_pct') }}"
                                   class="num h-(--spacing-inline) w-20 rounded-(--radius-field) border
                                          border-(--color-border) bg-(--color-surface-app) px-1 text-end">

                            <span class="num ms-auto w-16 text-end"
                                  x-text="expenseValue > 0 ? '৳' + money(expenseValue) : '—'"></span>
                        </span>

                        <input type="hidden" name="expense_amount" :value="expenseValue">
                    </x-sales::panel-row>

                    {{--
                        ⚠️ টাকা বসলে তবেই কারণের ঘর — আর তখন বাধ্যতামূলক।

                        বেশিরভাগ চালানে কোনো খরচ নেই। ঘরটা সবসময় দেখালে
                        **প্রতিদিন একটা খালি ঘর** চোখের সামনে থাকত, আর যেদিন
                        সত্যিই দরকার সেদিনও চোখে পড়ত না।

                        ⚠️ আর টাকা বসার পরে ঐচ্ছিক রাখা যায় না: "খরচ ২০০"
                        এক মাস পরে কারও কাজে আসে না — ভাড়া ছিল না হাম্মালি,
                        জানার একমাত্র সময় এখনই।
                    --}}
                    <template x-if="expenseValue > 0">
                        <input type="text" name="expense_narration" maxlength="191" required
                               placeholder="{{ __('sales::field.expense_for_placeholder') }}"
                               class="mt-1 h-(--spacing-inline) w-full rounded-(--radius-field) border
                                      border-(--color-warning) bg-(--color-surface-app) px-2">
                    </template>
                @endif
                {{--
                    ── রাউন্ডিং — চিহ্ন আলাদা, অঙ্ক আলাদা ────────────────────

                    মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬): *"Rounding-এর পাশে
                    বাক্স নিয়ে আসো, এখানে select করার option থাকবে +/−/amount"*।

                    ── কেন চিহ্নটা আলাদা ঘরে ────────────────────────────────
                    রাউন্ডিং দুই দিকেই যায়: ৪,৩০০.৪০-কে ৪,৩০০ করতে **−০.৪০**,
                    আর ৪,২৯৯.৬০-কে ৪,৩০০ করতে **+০.৪০**। আগে ঘরটা কেবল
                    ধনাত্মক নিত, তাই **অর্ধেক কাজটা করাই যেত না** — বিক্রেতা
                    বাধ্য হয়ে ছাড়ের ঘরে বসাতেন, আর তখন ওটা রিপোর্টে ছাড়
                    হিসেবে গোনা হত।

                    ⚠️ ঋণচিহ্ন সরাসরি টাইপ করতে দিলে ভুলে `-৪৩০০` বসে যেতে
                    পারত। দুইটা ঘরে ভাগ করায় **অঙ্কটা সবসময় ধনাত্মক**, আর
                    দিকটা একটা বাছাই — ভুল করা কঠিন।
                --}}
                @if ($show['rounding'])
                    <x-sales::panel-row :label="__('sales::field.rounding')">
                        <span class="flex flex-1 items-center gap-1">
                            <select x-model="roundingSign"
                                    class="h-(--spacing-inline) w-12 rounded-(--radius-field) border
                                           border-(--color-border) bg-(--color-surface-app) px-1 text-center">
                                <option value="+">+</option>
                                <option value="-">−</option>
                            </select>

                            <input type="number" step="0.01" min="0" x-model="roundingInput"
                                   class="num h-(--spacing-inline) w-16 rounded-(--radius-field) border
                                          border-(--color-border) bg-(--color-surface-app) px-1 text-end">

                            <span class="num ms-auto w-16 text-end"
                                  x-text="roundingValue ? money(roundingValue) : '—'"></span>
                        </span>

                        {{-- সার্ভারে চিহ্নসহ একটাই সংখ্যা যায় --}}
                        <input type="hidden" name="rounding_amount" :value="roundingValue">
                    </x-sales::panel-row>
                @endif

                <x-sales::panel-row :label="__('sales::field.net_payable')" strong>
                    <span class="num text-sm" x-text="'৳' + money(netPayable)"></span>
                </x-sales::panel-row>

                {{--
                    ⚠️ "জমা নেওয়া হলো"-র ঘরটা এখান থেকে **তুলে দেওয়া হয়েছে**।

                    মালিকের কথা (৩ সেপ্টেম্বর ২০২৬): *"Received Deposit আলাদাভাবে
                    হয়, এখানে বক্স রাখলে সমস্যা হবে"*।

                    ── কেন তিনি ঠিক ─────────────────────────────────────────
                    জমা একটা **ঘটনা**, একটা সংখ্যা নয়: কবে, কোন পদ্ধতিতে
                    (নগদ/বিকাশ/ব্যাংক), কোন রেফারেন্সে, কোন খাতে। আর একটা
                    বিলে **একাধিক জমা** থাকতে পারে — ৫,০০০ নগদ আর ১০,০০০
                    বিকাশে।

                    ⚠️ এখানে একটা ঘর থাকা মানে **দুই জায়গায় একই জিনিস**, আর
                    দুইটার একটাতে লিখলে অন্যটা জানত না। এখন লেখার একমাত্র
                    জায়গা "জমা যোগ" বোতামের প্যানেল, আর এখানে কেবল **যোগফল**।
                --}}
                @if ($show['deposit'])
                    <x-sales::panel-row :label="__('sales::field.received_deposit')">
                        <span class="num" x-text="Number(deposit) > 0 ? '৳' + money(deposit) : '—'"></span>
                    </x-sales::panel-row>
                @endif

                <x-sales::panel-row :label="__('sales::field.invoice_due')" strong>
                    <span class="num" x-text="'৳' + money(invoiceDue)"></span>
                </x-sales::panel-row>

                {{-- ⚠️ বিলের চেয়ে বেশি নিলে সেটা পর্দায় বলতেই হবে।

                     সারিটা কেবল তখনই দেখা যায় যখন সত্যিই উদ্বৃত্ত আছে —
                     স্বাভাবিক চালানে একটা স্থায়ী শূন্যের সারি চোখের সামনে
                     রাখার কোনো কারণ নেই।

                     ⓘ সবুজ, লাল নয়: উদ্বৃত্ত জমা কোনো সমস্যা নয়, ওটা
                     গ্রাহকের পাওনা — পরের চালানে কাটা যাবে। --}}
                <template x-if="depositExcess > 0">
                    <x-sales::panel-row :label="__('sales::field.kept_as_advance')">
                        <span class="num font-semibold text-(--color-success)"
                              x-text="'৳' + money(depositExcess)"></span>
                    </x-sales::panel-row>
                </template>

                {{--
                    ── আগের হিসাব — নামটা চিহ্ন দেখে বদলায় ──────────────────

                    মালিকের নির্দেশ: *"আগে যদি বাকি থাকে তাহলে **আগের বকেয়া**,
                    আর যদি টাকা জমা থাকে তাহলে **আগের অগ্রিম**"*।

                    ⚠️ আগে একটাই নাম ছিল — "আগের ব্যালেন্স" — আর ঋণাত্মক হলেও
                    ওই নামেই দেখাত। **কাউন্টারে দাঁড়িয়ে "ব্যালেন্স ৫০০" পড়ে
                    বোঝার উপায় ছিল না তিনি ৫০০ পাবেন না দেবেন** — আর ওই
                    ভুলের দাম টাকা।
                --}}
                {{-- ⚠️ কম্পোনেন্ট নয়, হাতে লেখা সারি — আর কারণটা সূক্ষ্ম।

                     `<x-sales::panel-row>`-এর লেবেলটা **PHP-তে রেন্ডার হয়**,
                     তাই ওখানে Alpine-এর বাঁধন (`::label`) বসালে সেটা কেবল
                     একটা অ্যাট্রিবিউট হয়ে পড়ে থাকত — লেখাটা কোনোদিন
                     বদলাত না, আর কোনো ত্রুটিও হত না।

                     এখানে লেবেলটাই বদলায়, তাই সারিটা নিজে হাতে লেখা। --}}
                <div class="flex items-center justify-between gap-2">
                    <span class="text-(--color-ink-muted)"
                          x-text="customer.due < 0
                            ? @js(__('sales::field.previous_advance'))
                            : @js(__('sales::field.previous_due'))"></span>

                    <span class="num"
                          x-text="customer.due ? money(Math.abs(customer.due)) : '—'"></span>
                </div>
            </div>

            {{--
                ── মোট বকেয়া / অগ্রিম — আলাদা, বড়, রঙিন ───────────────────

                মালিকের নির্দেশ: *"আলাদাভাবে round box-এ একটু বড় করে highlight
                হবে — Due হলে লাল solid, Advance হলে সবুজ হালকা"*।

                ── কেন দুইটা রঙ, আর দুইটা আলাদা রকমের ─────────────────────
                ⭐ **বকেয়া একটা সমস্যা, অগ্রিম নয়।** তাই বকেয়া ভরাট লাল —
                চোখ এড়ানো যায় না; আর অগ্রিম হালকা সবুজ — জানা থাকা ভালো,
                কিন্তু কিছু করার নেই।

                ⚠️ দুইটাকে একই রঙে দেখালে **ভালো খবরও লাল দেখাত**, আর তখন
                লাল রঙটার আর কোনো মানে থাকত না।

                ⓘ শূন্য হলে দুইটার কোনোটাই নয় — নিরপেক্ষ ধূসর।
            --}}
            {{-- ⓘ নিচের ফাঁকও কাটা, একই কারণে — নিচে সাথে সাথেই পরিমাণের
                 দল, আর ওটার নিজের উপরের কিনারা-রেখা আছে। দুইটা আলাদা করার
                 কাজটা রেখাটাই করে, ফাঁকটা কেবল স্ক্রলবার আনত। --}}
            <div class="px-3 pb-1">
                <div class="flex items-center justify-between gap-2 rounded-full px-3 py-1"
                     :class="outstanding > 0
                        ? 'bg-(--color-danger) text-white'
                        : (outstanding < 0
                            ? 'bg-(--color-badge-success-bg) text-(--color-badge-success-ink)'
                            : 'bg-(--color-surface-sunken) text-(--color-ink-muted)')">
                    <span class="text-2xs font-semibold uppercase tracking-wide"
                          x-text="outstanding < 0
                            ? @js(__('sales::field.advance'))
                            : @js(__('sales::field.due'))"></span>

                    <span class="num text-sm font-bold"
                          x-text="'৳' + money(Math.abs(outstanding))"></span>
                </div>
            </div>

            {{--
                ── পরিমাণের দল — আলাদা রঙে ────────────────────────────────

                মালিকের নির্দেশ: *"এই group-টাকে আলাদা color করে রাখো"*।

                ⭐ কারণটা যুক্তিসঙ্গত: উপরের সবটাই **টাকা**, আর এই চারটা
                **পরিমাণ** — সম্পূর্ণ আলাদা জিনিস। এক রঙে থাকলে চোখ
                ৳-চিহ্ন খুঁজে খুঁজে আলাদা করত।
            --}}
            <div class="space-y-1 border-t border-(--color-border) bg-(--color-surface-sunken)
                        px-3 py-2 text-2xs">
                @foreach ([
                    ['label' => 'sales::field.total_item', 'expr' => 'counts.totalItem', 'on' => 'total_item'],
                    ['label' => 'sales::field.total_sales_qty', 'expr' => 'counts.totalSalesQty', 'on' => 'sales_qty'],
                    ['label' => 'sales::field.total_free_qty', 'expr' => 'counts.totalFreeQty', 'on' => 'free_qty_total'],
                    ['label' => 'sales::field.total_free_plus_sales', 'expr' => 'counts.totalQty', 'on' => 'total_qty'],
                ] as $row)
                    @if ($show[$row['on']])
                        <x-sales::panel-row :label="__($row['label'])">
                            <span class="num font-semibold text-(--color-brand-700)"
                                  x-text="{{ $row['expr'] }} || '—'"></span>
                        </x-sales::panel-row>
                    @endif
                @endforeach
            </div>

            </div>

            {{--
                ── চালানের কাজগুলো — ডান প্যানেলের নিচে ────────────────────

                ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ────────────────────────
                *"Chart / Bulk DO, Add Note, Clear Full Data / Add Deposit,
                Transportation, / Confirm — ei krome lal box e bosaw"* —
                সংখ্যাগুলোর নিচের ফাঁকা জায়গাটা লাল দিয়ে ঘেরা।

                ── কেন এখানে ভালো ─────────────────────────────────────────
                বোতামগুলো আগে পাতার নিচে একটা আলাদা বারে ছিল। **কিন্তু ওরা
                যে অঙ্কগুলোর উপর কাজ করে, সেগুলো এই প্যানেলেই** — জমা যোগ
                করলে "বিলের বকেয়া" বদলায়, খরচ যোগ করলে "নিট পরিশোধযোগ্য"।
                এখন কারণ আর ফল এক বাক্সে।

                ⚠️ **প্যানেলগুলোও সাথে এসেছে** — নাহলে বোতাম এক জায়গায় আর
                তার ঘরগুলো আরেক জায়গায় খুলত, আর কেউ খুঁজে পেত না।

                ⚠️ স্ক্রল-ঘরের **বাইরে**, তাই সংখ্যাগুলো গড়ালেও বোতামগুলো
                জায়গাতেই থাকে — কাউন্টারে "নিশ্চিত করুন" খুঁজতে স্ক্রল করা
                সবচেয়ে বিরক্তিকর মুহূর্ত।

                ⓘ শিপমেন্ট ও খরচ মালিকের তালিকায় ছিল না, কিন্তু তাঁর
                দ্বিতীয় সারিটা কমা দিয়ে শেষ হয়েছিল — তাই ওরা ওই সারিতেই।
                **নিজে থেকে কোনো বোতাম তোলা হয়নি**, প্রশ্ন করা হয়েছে।
            --}}
            <div class="border-t border-(--color-border) p-2">

                {{--
                    ── ছয়টা বোতাম — এক মাপ, ছয় রঙ ──────────────────────────

                    মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬): *"ek ekta ek ek
                    color daw, solid color diba"*, আর *"sob kotir botam ek
                    soman koro"*।

                    ── কেন আগে সমান ছিল না ─────────────────────────────────
                    "চার্ট" বোতামটা `<x-sales::bulk-sheet>` কম্পোনেন্টের ভেতর
                    থেকে আঁকা হয়, আর সে নিজের ক্লাস নিজেই ঠিক করত। ⚠️ **তাই
                    বাকি পাঁচটা যা-ই হোক, ওটা একা আলাদা দেখাত।** এখন
                    কম্পোনেন্টে একটা `buttonClass` prop, আর সেটা এখান থেকেই
                    দেওয়া — ছয়টাই হুবহু এক নিয়মে।

                    ── রঙের ভাগটা অর্থ ধরে, খেয়ালখুশিতে নয় ─────────────────
                        জমা যোগ    সবুজ    একমাত্র বোতাম যেটা টাকা আনে
                        নোট যোগ    নীলচে   কাগজে ছাপা হবে, তথ্য
                        শিপমেন্ট    ব্র্যান্ড  মাল কোথায় যাচ্ছে
                        চার্ট       গোলাপি   একবারে বহু পণ্য — মালিকের বাছাই
                        পরিবহন     হলুদ    টাকা বেরোয়, তাই সতর্কতার রঙ
                        সব মুছুন    লাল     ফেরানো যায় না

                    ⚠️ **প্রতিটা রঙ থিমের টোকেন থেকে**, হার্ডকোড নয় — নাহলে
                    নয়টা থিমের বাকিগুলোয় বেমানান হত। এটা মালিকের স্থায়ী
                    নিয়ম: কোনো পর্দা রঙ হার্ডকোড করবে না।

                    ⓘ খোলা প্যানেলের বোতামে একটা রিং বসে (`ring-2`), রঙ
                    বদলায় না — রঙটা পরিচয়, খোলা-বন্ধ অবস্থা নয়।
                --}}
                @php
                    $btnBase = 'w-full rounded-(--radius-field) px-1 py-2 text-2xs font-semibold
                                leading-tight text-white transition-colors';
                @endphp

                <div class="grid grid-cols-3 gap-1">
                    {{-- ১ · জমা যোগ --}}
                    @if ($show['deposit'])
                        <button type="button" @click="openPanel('deposit')"
                                :class="panel === 'deposit' ? 'ring-2 ring-(--color-ink)' : ''"
                                class="{{ $btnBase }} bg-(--color-success) hover:bg-(--color-success-hover)">
                            {{ __('sales::action.add_deposit') }}
                        </button>
                    @endif

                    {{-- ২ · নোট যোগ --}}
                    <button type="button" @click="openPanel('note')"
                            :class="panel === 'note' ? 'ring-2 ring-(--color-ink)' : ''"
                            class="{{ $btnBase }} bg-(--color-info) hover:opacity-90">
                        {{ __('sales::action.add_note') }}
                    </button>

                    {{-- ৩ · শিপমেন্ট --}}
                    @if ($show['shipment'])
                        <button type="button" @click="openPanel('shipment')"
                                :class="panel === 'shipment' ? 'ring-2 ring-(--color-ink)' : ''"
                                class="{{ $btnBase }} bg-(--color-brand-600) hover:bg-(--color-brand-700)">
                            {{ __('sales::action.shipment') }}
                        </button>
                    @endif

                    {{-- ৪ · চার্ট — নিজের বোতাম আঁকে, তাই ক্লাসটা পাঠানো হয় --}}
                    {{-- ⓘ `class="contents"` — মোড়কটা ছকে নিজের ঘর নেয় না,
                         তাই ভেতরের বোতামটাই ঘরটা ভরে। F6 এই ref ধরেই
                         বোতামটা চাপে। --}}
                    <div x-ref="chartEntry" class="contents">
                        <x-sales::bulk-sheet :products="$sheetProducts" :stock="$sheetStock"
                                             :free-qty="$show['free_qty']"
                                             button-class="{{ $btnBase }} bg-(--color-accent-pink) hover:bg-(--color-accent-pink-hover)" />
                    </div>

                    {{-- ৫ · পরিবহন --}}
                    @if ($show['transport'])
                        <button type="button" @click="openPanel('transport')"
                                :class="panel === 'transport' ? 'ring-2 ring-(--color-ink)' : ''"
                                class="{{ $btnBase }} bg-(--color-warning) text-(--color-warning-ink)
                                       hover:bg-(--color-warning-hover)">
                            {{ __('sales::action.transportation') }}
                        </button>
                    @endif

                    {{--
                        ৬ · সব মুছুন — লাল, আর ফেরানো যায় না।

                        ⚠️ পাশের "এই লাইন" বাক্সে আরেকটা বোতামে ইংরেজিতে
                        "Clear Data" লেখা, আর ওটা কেবল চলতি লাইনটা মোছে।
                        এটা পুরো চালান মোছে — তাই পার্থক্যটা লেখা নয়,
                        রঙ বলে।
                    --}}
                    <button type="button" @click="clearAll()"
                            class="{{ $btnBase }} bg-(--color-danger) hover:bg-(--color-danger-hover)">
                        {{ __('sales::action.clear_full') }}
                    </button>
                </div>

                {{-- সারি ৩ — শেষ করা।

                     পুরো প্রস্থে আর সবার নিচে: কাউন্টারে এটাই শেষ চাপ, আর
                     অঙ্কটা গায়ে লেখা বলে **কত টাকার কাগজ পাকা হচ্ছে সেটা
                     চাপ দেওয়ার আগেই চোখে পড়ে** (মালিকের সিদ্ধান্ত)। --}}
                <x-ui.button type="submit" tone="primary" class="mt-2 w-full py-2" x-ref="confirm"
                             ::disabled="! canConfirm">
                    {{ __('sales::action.confirm') }}
                    <span class="num ms-2 font-semibold" x-text="'৳' + money(netPayable)"></span>
                </x-ui.button>
            </div>

        </aside>

    </form>

    @push('scripts')
        <script>
            function directSale(catalogue, customers, walkinId, vatEnabled, packs) {
                return {
                    catalogue,
                    customers,
                    vatEnabled,
                    term: '',
                    pickerOpen: false,
                    picked: null,
                    showCosting: false,
                    /*
                     * ⚠️ `gifts` এন্ট্রির **ভেতরে**, আলাদা তালিকা নয়।
                     *
                     * ── কেন (৩ সেপ্টেম্বর ২০২৬) ──────────────────────────
                     * আগে উপহারগুলো একটা আলাদা `gifts: []` তালিকায় থাকত,
                     * আর প্রতিটা উপহারে একটা "কোন পণ্যের জন্য" ড্রপডাউন
                     * ছিল — অর্থাৎ **উপহার আর পণ্যের সম্পর্কটা ছিল একটা
                     * বাছাই, যেটা মানুষ ভুল করতে পারত বা খালি রেখে দিত।**
                     *
                     * আর কোডে ওটা আগে থেকে বসানোর চেষ্টাও ছিল
                     * (`againstProductId: this.picked?.id`), কিন্তু সেটা
                     * **কোনোদিন কাজ করত না**: কার্টে যোগ করার আগে চাপলে
                     * পণ্যটা তালিকায় নেই, আর পরে চাপলে `picked` তখন null।
                     *
                     * এখন উপহারটা লাইনের ভেতরেই থাকে। **সম্পর্কটা আর
                     * বাছাই নয়, অবস্থান** — লাইন মুছলে উপহারও যায়, আর
                     * ভুল লাইনে বসার কোনো পথই নেই।
                     */
                    entry: { qty: '', freeQty: '', rate: '', discountInput: '', unitId: '', gifts: [] },

                    // পণ্যপ্রতি প্যাকের তালিকা — সার্ভার থেকে একবারেই
                    packs,
                    lines: [],

                    // একবারে একটাই উপহারের ঘর খোলা থাকে
                    giftDraft: null,

                    /*
                     * F1-এর সাহায্যের পর্দা।
                     *
                     * ⚠️ শর্টকাট আছে অথচ কোথাও লেখা নেই — এটাই সবচেয়ে
                     * অকেজো ধরনের সুবিধা: যিনি জানেন কেবল তিনিই পান।
                     * POS-এ F1 এই তালিকাটা খোলে, তাই এখানেও।
                     */
                    helping: false,
                    /*
                     * শুরুতে কে বাছা থাকে।
                     *
                     * ── কেন এই লাইনটা লাগল (২ সেপ্টেম্বর ২০২৬) ──────────
                     * আগে এটা ছিল একটা `<select>`, আর ব্রাউজার নিজে থেকেই
                     * প্রথম option-টা বেছে রাখত — অর্থাৎ ক্রেতা কখনো
                     * "কেউ না" হত না, যদিও কোডে কোথাও তা লেখা ছিল না।
                     *
                     * `<select>`-টা চিহ্ন হয়ে যাওয়ায় ওই নীরব ডিফল্টটাও
                     * চলে গেছে। যে কোম্পানিতে `sales.walkin_customer_id`
                     * বসানো নেই, সেখানে `customerId` খালি থেকে যেত আর
                     * চালান যেত ক্রেতা ছাড়াই। ব্রাউজারে ক্লিক করে দেখতে
                     * গিয়ে ধরা পড়েছে, কোড পড়ে নয়।
                     *
                     * তাই ক্রমটা স্পষ্ট করে লেখা: **নগদ ক্রেতা, নাহলে
                     * তালিকার প্রথমজন, নাহলে সত্যিই কেউ না** — শেষেরটা
                     * তখনই, যখন কোম্পানিতে একটাও ক্রেতা নেই।
                     */
                    /*
                     * ⚠️ কেউ আগে থেকে বাছা থাকে না — ইচ্ছাকৃতভাবে।
                     *
                     * ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ──────────────
                     * *"Walk-in Customer default bose thakte parbe na …
                     * eta POS na, eta depot/wholesale counter. Obosoi
                     * dekhe nishchit hoye party select korte hobe."*
                     *
                     * আগে এখানে `walkinId` বসত, নাহলে **তালিকার প্রথম
                     * গ্রাহকটাই** — অর্থাৎ পর্দা খুললেই একজনের নাম বসানো
                     * থাকত, আর সেটা কার নাম তা কেউ বেছে দেয়নি।
                     *
                     * ⚠️ ফাঁকা রাখা মানে "নিশ্চিত করুন" বোতামও বন্ধ থাকে
                     * ([[canConfirm]]), আর সার্ভারেও ঘরটা এখন
                     * `required` — **তিন জায়গাতেই**, কারণ একটাতে ফাঁক
                     * থাকলে বাকি দুইটা অর্থহীন।
                     */
                    customerId: '',
                    customerPickerOpen: false,
                    customerTerm: '',
                    /* ⓘ ডিফল্ট নগদ — কাউন্টারের স্বাভাবিক অবস্থা, আর
                       বাকি দেওয়াটা ব্যতিক্রম। ⚠️ ব্যতিক্রমটা সিস্টেম নিজেই
                       জানে: ক্রেতা বাছলে তাঁর নিজের মেয়াদ বসে যায়। */
                    creditTerm: @js($paymentTermDefault),
                    dueOn: '',
                    discountInput: '',

                    // পুরো কাগজের ভ্যাট-বদল — খালি মানে পণ্য অনুযায়ী
                    vatMode: '',
                    vatRate: '',
                    expenseInput: '',
                    roundingInput: '',
                    roundingSign: '+',
                    /*
                     * ── জমা — এখন একটা নয়, একটা তালিকা ─────────────────
                     *
                     * মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬): *"Add deposit-এ
                     * Ref Date, Payment Method, into, Amount, Narration,
                     * Add to Cart … একাধিক payment add করতে পারবে"*।
                     *
                     * ⚠️ `deposit` আর একটা ঘর নয়, **যোগফল** — নিচে getter।
                     * ঘর রাখলে দুই জায়গায় একই সংখ্যা থাকত (তালিকা আর ঘর),
                     * আর একটায় লিখলে অন্যটা জানত না।
                     */
                    deposits: [],

                    depositDraft: {
                        methodId: '', accountId: '', amount: '',
                        reference: '', refDate: '', narration: '',
                    },
                    panel: '',
                    carriers: @js($carriers),
                    carrierId: '',
                    depositMethods: @js($depositMethods),
                    moneyAccounts: @js($moneyAccounts),

                    nextKey: 1,

                    /*
                     * ══ অসমাপ্ত চালান — রিফ্রেশ বা বিদ্যুৎ গেলেও থাকে ══════
                     *
                     * ── কোন প্রশ্ন থেকে এটা এলো (৩ সেপ্টেম্বর ২০২৬) ─────────
                     * মালিক: *"ধরো আমি একটা সেল করছি, এই মুহূর্তে হঠাৎ
                     * ভুলেই রিফ্রেশ চাপ পড়ে গেছে — অর্ধেক অর্ডার নেওয়ার পরে।
                     * তাহলে তো পুরা কাজই শেষ।"*
                     *
                     * ⚠️ **তিনি ঠিক**: এতদিন সবকিছু কেবল ব্রাউজারের স্মৃতিতে
                     * ছিল। ভুল করে F5, বিদ্যুৎ চলে যাওয়া, ট্যাব বন্ধ — বিশটা
                     * লাইন এক মুহূর্তে শেষ, আর ক্রেতা সামনে দাঁড়িয়ে।
                     *
                     * ── কেন সার্ভারে নয়, ব্রাউজারে ────────────────────────
                     * অসমাপ্ত চালান কোনো **নথি নয়** — ওটার নম্বর নেই, খাতায়
                     * কিছু বসে না, আর কেউ ওটা রিপোর্টে খুঁজবে না। সার্ভারে
                     * রাখলে একটা টেবিল, একটা নম্বর-সিরিজ আর "এই খসড়াগুলো
                     * কে মুছবে" — একটা আস্ত নতুন প্রশ্ন তৈরি হত।
                     *
                     * ⓘ আর ব্রাউজারে রাখাটা **তাৎক্ষণিক**: প্রতিটা কীস্ট্রোকে
                     * সার্ভারে গেলে কাউন্টারে দেরি হত, আর ইন্টারনেট গেলে
                     * সুরক্ষাটাই কাজ করত না — অথচ ঠিক তখনই ওটা সবচেয়ে দরকার।
                     *
                     * ⚠️ **চাবিতে কোম্পানি আর ব্যবহারকারী দুইটাই** — এক
                     * ব্রাউজারে দুইজন কর্মী পালা করে বসেন, আর একজনের অসমাপ্ত
                     * চালান অন্যজনের পর্দায় ফিরে এলে **ভুল পার্টির নামে বিল**
                     * হয়ে যেত।
                     */
                    draftKey: 'abos.direct-sale.{{ App\Core\Support\CompanyContext::id() }}.{{ auth()->id() }}',

                    /** ফিরিয়ে আনার প্রস্তাব — খসড়া পাওয়া গেলে উপরে বার দেখায়। */
                    draftFound: false,
                    draftAt: '',

                    /*
                     * খসড়া লেখা — যা যা টাইপ করা হয়েছে, সব।
                     *
                     * ⚠️ **কার্ট খালি হলে খসড়া মুছে যায়**, রাখা হয় না। খালি
                     * খসড়া ফিরিয়ে আনার প্রস্তাব দেওয়া মানে প্রতিদিন সকালে
                     * একটা অর্থহীন প্রশ্ন করা।
                     */
                    saveDraft() {
                        /*
                         * ⚠️ প্রস্তাব পর্দায় থাকা অবস্থায় কিছু লেখা বা মোছা নয়।
                         *
                         * এই লাইনটা ছাড়া বাগটা নীরব ও মারাত্মক হত: পাতা খোলার
                         * সাথে সাথেই `x-effect` একবার চলে, আর তখন কার্ট খালি —
                         * অর্থাৎ **খসড়াটা মুছে যেত ঠিক সেই মুহূর্তে যখন
                         * ব্যবহারকারীকে ফেরানোর প্রস্তাব দেখানো হচ্ছে**।
                         * বোতামটা থাকত, চাপলে কিছুই ফিরত না।
                         */
                        if (this.draftFound) return;

                        try {
                            if (this.lines.length === 0) {
                                localStorage.removeItem(this.draftKey);

                                return;
                            }

                            localStorage.setItem(this.draftKey, JSON.stringify({
                                at: new Date().toISOString(),
                                customerId: this.customerId,
                                creditTerm: this.creditTerm,
                                dueOn: this.dueOn,
                                lines: this.lines,
                                discountInput: this.discountInput,
                                vatMode: this.vatMode,
                                vatRate: this.vatRate,
                                expenseInput: this.expenseInput,
                                roundingInput: this.roundingInput,
                                roundingSign: this.roundingSign,
                                deposits: this.deposits,
                                nextKey: this.nextKey,
                            }));
                        } catch (e) {
                            /*
                             * ⚠️ চুপ করে থাকা ইচ্ছাকৃত।
                             *
                             * localStorage বন্ধ থাকতে পারে (ব্যক্তিগত উইন্ডো,
                             * সাইট-ডেটা বন্ধ করা ব্রাউজার), আর তখন লেখা
                             * ব্যতিক্রম ছোঁড়ে। **কিন্তু ওটা বিক্রি থামানোর
                             * কারণ নয়** — সুরক্ষাটা না পেলেও কাউন্টার চলবে।
                             */
                        }
                    },

                    /** পাতা খোলার সময় — আছে কিনা দেখা, নিজে থেকে ফেরানো নয়। */
                    lookForDraft() {
                        try {
                            const parked = localStorage.getItem(this.draftKey + '.pending');

                            if (parked) {
                                localStorage.removeItem(this.draftKey + '.pending');

                                /*
                                 * ⚠️ সার্ভার ফিরিয়ে দিয়েছে — তাই প্রশ্ন নয়,
                                 * সরাসরি ফিরিয়ে আনা। ব্যবহারকারী "নতুন
                                 * চালান" চাননি, তিনি **এইটাই** পাঠিয়েছিলেন।
                                 */
                                if (@js($errors->any())) {
                                    this.applyDraft(parked);

                                    return;
                                }
                            }

                            const raw = localStorage.getItem(this.draftKey);

                            if (! raw) return;

                            const d = JSON.parse(raw);

                            if (! d || ! Array.isArray(d.lines) || d.lines.length === 0) return;

                            this.draftFound = true;
                            this.draftAt = d.at ? new Date(d.at).toLocaleString() : '';
                        } catch (e) {
                            localStorage.removeItem(this.draftKey);
                        }
                    },

                    /*
                     * ⚠️ ফিরিয়ে আনা **কেবল চাপ দিলে** — নিজে থেকে নয়।
                     *
                     * নিজে থেকে ফিরিয়ে আনলে সবচেয়ে বিপজ্জনক জিনিসটা ঘটত:
                     * কেউ নতুন বিক্রি শুরু করতে এসে **আগের অসমাপ্ত চালানটা
                     * পেয়ে যেতেন, না বুঝে** — আর তার উপরেই নতুন লাইন যোগ
                     * করে নিশ্চিত করে ফেলতেন। **ভুল ক্রেতার নামে ভুল মাল।**
                     */
                    restoreDraft() {
                        this.applyDraft(localStorage.getItem(this.draftKey));
                        this.draftFound = false;
                    },

                    /**
                     * খসড়াটা পর্দায় বসানো — কোথা থেকে এল তা জানার দরকার নেই।
                     */
                    applyDraft(raw) {
                        try {
                            const d = JSON.parse(raw || '{}');

                            this.customerId = d.customerId ?? '';
                            this.creditTerm = d.creditTerm ?? @js($paymentTermDefault);
                            this.dueOn = d.dueOn ?? '';
                            this.lines = d.lines ?? [];
                            this.discountInput = d.discountInput ?? '';
                            this.vatMode = d.vatMode ?? '';
                            this.vatRate = d.vatRate ?? '';
                            this.expenseInput = d.expenseInput ?? d.expenseAmount ?? '';
                            this.roundingInput = d.roundingInput ?? '';
                            this.roundingSign = d.roundingSign ?? '+';
                            this.deposits = Array.isArray(d.deposits) ? d.deposits : [];
                            this.nextKey = d.nextKey ?? (this.lines.length + 1);
                        } catch (e) {
                            // ভাঙা খসড়া — ফেরানোর চেয়ে বাদ দেওয়াই নিরাপদ
                        }
                    },

                    /*
                     * ── সাবমিটের সময় খসড়া মোছা হয় না, সরিয়ে রাখা হয় ────
                     *
                     * ⚠️ মালিকের কথা (৪ সেপ্টেম্বর ২০২৬): *"এই warning-এ
                     * আমার সব entry হারিয়ে গেল, এটা তো বক্সে বসার কথা"*।
                     *
                     * ── যা ঘটত ──────────────────────────────────────────
                     * `@submit` খসড়াটা **মুছে দিত**, আর তার পরেই সার্ভার
                     * চালানটা ফিরিয়ে দিলে (মজুদ কম, নম্বর নেওয়া, যা-ই হোক)
                     * পাতাটা খালি হয়ে ফিরত — **বিশ লাইনের কার্ট, ক্রেতা,
                     * জমা, সব শেষ**। ভুলটা এক লাইনের, দামটা এক ঘণ্টার কাজ।
                     *
                     * ── এখন ────────────────────────────────────────────
                     * খসড়াটা `.pending`-এ সরে যায়। পাতাটা আবার **ভুলের
                     * বার্তাসহ** খুললে ওটা নিজে থেকেই ফিরে আসে — প্রশ্ন
                     * ছাড়াই, কারণ ব্যবহারকারী কিছু হারাতে চাননি।
                     *
                     * ⓘ চালানটা পাকা হলে পাতাটা আর ফেরে না (ছাপায় চলে
                     * যায়), তাই পরেরবার এখানে এলে `.pending` চুপচাপ মুছে
                     * যায় — পুরনো একটা বিক্রি ফিরিয়ে আনার প্রশ্নই ওঠে না।
                     */
                    parkDraft() {
                        try {
                            const raw = localStorage.getItem(this.draftKey);

                            if (raw) localStorage.setItem(this.draftKey + '.pending', raw);

                            localStorage.removeItem(this.draftKey);
                        } catch (e) {
                            // localStorage বন্ধ — তখন আগের মতোই আচরণ
                        }

                        this.draftFound = false;
                    },

                    /** প্রস্তাব ফিরিয়ে দেওয়া — খসড়াটাও সাথে যায়। */
                    dropDraft() {
                        try {
                            localStorage.removeItem(this.draftKey);
                        } catch (e) {
                            // উপরের একই কারণ
                        }

                        this.draftFound = false;
                    },

                    /*
                     * খোলা মাত্রই তালিকা — ক্রেতার চিহ্নের মতোই।
                     *
                     * ── মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬) ──────────────
                     * *"customer icon e click korle zemon search button
                     * open hoy, products search icon-eও zate emon hoy"*।
                     *
                     * ── পার্থক্যটা কোথায় ছিল ──────────────────────────────
                     * দুইটাই চিহ্নে চাপলে খুলত, কিন্তু:
                     *
                     *     ক্রেতা  খোলা মাত্রই তালিকা — না লিখেও বাছা যায়
                     *     পণ্য    টাইপ না করা পর্যন্ত **কিছুই নেই**
                     *
                     * ⚠️ ফলে পণ্যের চিহ্নটা চেপে মনে হত কিছুই হয়নি — ঘরটা
                     * এসেছে ঠিকই, কিন্তু **নিচে শূন্য**। যিনি পণ্যের নাম
                     * জানেন না, তাঁর পক্ষে শুরু করারই উপায় ছিল না।
                     *
                     * ⓘ ৩০-এর সীমাটা দুই জায়গাতেই এক। পুরো তালিকা আঁকলে দুই
                     * হাজার পণ্যের গুদামে প্রতিটা কীস্ট্রোকে পাতা কাঁপত।
                     */
                    get visible() {
                        if (! this.pickerOpen) return [];

                        /*
                         * ── ⛔ পক্ষ আগে, পণ্য পরে — মালিকের নিয়ম, ৬ সেপ্টেম্বর ২০২৬ ──
                         *
                         * *"ক্রেতা আগে সিলেক্ট করলে পরে প্রোডাক্ট সার্চ হবে,
                         * ক্রেতা না দিলে প্রোডাক্ট শো করবে না।"*
                         *
                         * ── কেন এটা কেবল ক্রম নয় ─────────────────────────
                         * ⚠️ **দর ক্রেতাভেদে আলাদা** — একজনের দর তালিকা আরেকজনের চেয়ে আলাদা, আর বকেয়ার সীমাও। পণ্য আগে বাছলে
                         * পর্দা এমন একটা দর বসাত যেটা এখনো জানা যায়নি কার
                         * জন্য, আর ক্রেতা বাছার পর সেটা **নীরবে ভুল** হয়ে থাকত।
                         *
                         * ⓘ কার্টে লাইন বসানোর পর ক্রেতা বদলালে ঐ দরগুলো আর
                         * নিজে থেকে ঠিক হয় না — অর্থাৎ ভুলটা কাগজ পর্যন্ত যেত।
                         *
                         * ⭐ তাই তালিকাটা খালি থাকে, আর নিচের বার্তাটা বলে দেয়
                         * **কী করতে হবে** — শুধু "পারবেন না" নয় (নিয়ম ১)।
                         */
                        if (! this.customerId) return [];


                        const t = this.term.trim().toLowerCase();

                        if (t === '') return this.catalogue.slice(0, 30);

                        return this.catalogue.filter(p =>
                            p.name.toLowerCase().includes(t)
                            || p.code.toLowerCase().includes(t)
                            || (p.barcode || '').toLowerCase().includes(t)
                        ).slice(0, 30);
                    },

                    get customer() {
                        return this.customers[this.customerId]
                            || { limit: 0, due: 0, days: 0, name: '', phone: '', address: '', location: '' };
                    },

                    /*
                     * চালান নিশ্চিত করা যাবে কখন।
                     *
                     * ── কেন দুইটা শর্ত, একটা নয় ─────────────────────────
                     * আগে কেবল **কার্ট খালি কিনা** দেখা হত, কারণ ক্রেতা
                     * সবসময় আগে থেকেই বসানো থাকত। ডিফল্ট তুলে দেওয়ায়
                     * শর্তটা অসম্পূর্ণ হয়ে গেছে: **মাল আছে অথচ কার নামে
                     * তা কেউ বলেনি** — এমন চালান আর সম্ভব নয়।
                     *
                     * ⚠️ সার্ভারও `required` দেখে, আর সেটাই আসল পাহারা।
                     * এই লাইনটা কেবল **বোতামটা মিথ্যা না বলার জন্য** —
                     * চাপা যায় অথচ কিছু হয় না, ওটাই সবচেয়ে বিরক্তিকর।
                     */
                    /*
                     * বাছা পণ্যটার প্যাকগুলো।
                     *
                     * ⚠️ খালি অ্যারে মানে দুইটা আলাদা কথা, আর দুইটাতেই ঘরটা
                     * পড়ার-জন্য থাকে — তাই আলাদা করার দরকার নেই:
                     *     • কন্ট্রোল প্যানেলের সুইচ বন্ধ
                     *     • পণ্যটার একটাই একক
                     */
                    get entryUnits() {
                        if (! this.picked) return [];

                        return this.packs[this.picked.id] ?? [];
                    },

                    /*
                     * কাজের প্যানেল খোলা ও বন্ধ — একবারে একটাই।
                     *
                     * ── কেন খোলার সাথে পর্দা নামে (৩ সেপ্টেম্বর ২০২৬) ─────
                     * বোতামগুলো **ডান পাশে**, আর প্যানেলটা খোলে **কার্টের
                     * নিচে** — অর্থাৎ চাপ এক জায়গায়, ফল আরেক জায়গায়।
                     *
                     * ⚠️ বিশ লাইনের কার্টে ওটা পর্দার বাইরে পড়ত, আর চেপে
                     * মনে হত **কিছুই হয়নি** — তারপর আবার চাপলে প্যানেলটা
                     * বন্ধ হয়ে যেত। ঠিক এই ফাঁদেই আজ Action মেনুটা পড়েছিল।
                     *
                     * ⓘ `block: 'nearest'` — ইতিমধ্যেই দেখা যাচ্ছে এমন হলে
                     * পর্দা নড়ে না। অকারণ লাফানো নিজেই একটা বিরক্তি।
                     */
                    openPanel(name) {
                        this.panel = this.panel === name ? '' : name;

                        if (! this.panel) return;

                        this.$nextTick(() => this.$refs.actionPanel?.scrollIntoView({
                            behavior: 'smooth',
                            block: 'nearest',
                        }));
                    },

                    get canConfirm() {
                        return this.lines.length > 0 && this.customerId !== '';
                    },

                    /*
                     * ক্রেতা খোঁজা — নাম, কোড আর ফোন, তিনটাতেই।
                     *
                     * ফোনটা ইচ্ছাকৃত: কাউন্টারে অনেক সময় দোকানের নাম
                     * মনে থাকে না, নম্বরটা থাকে — ফোনেই তো অর্ডারটা
                     * এসেছিল।
                     *
                     * ⚠️ খালি লেখায় **পুরো তালিকা** দেখানো হয়, প্রথম
                     * ত্রিশটা। চিহ্নে চাপ দিয়ে কেউ যদি কিছু না লেখেন,
                     * একটা ফাঁকা প্যানেল দেখে তিনি ভাববেন কোনো গ্রাহকই
                     * নেই — অথচ তিনি কেবল এখনো কিছু টাইপ করেননি।
                     */
                    get customerMatches() {
                        const t = this.customerTerm.trim().toLowerCase();

                        const rows = Object.entries(this.customers)
                            .map(([id, c]) => ({ id, ...c }));

                        return (t === '' ? rows : rows.filter(c =>
                            (c.name || '').toLowerCase().includes(t)
                            || (c.code || '').toLowerCase().includes(t)
                            || (c.phone || '').toLowerCase().includes(t)
                        )).slice(0, 30);
                    },

                    /*
                     * শর্ত বদলালে তারিখের ঘরটা মেলানো।
                     *
                     * "নির্দিষ্ট তারিখ" ছাড়া বাকি সব বিকল্পে তারিখটা দিন
                     * থেকে গোনা হয়, আর সার্ভারেও তাই — তাই ঘরটা খালি করে
                     * দেওয়া হয়, নাহলে আগের বাছাইয়ের একটা বাসি তারিখ
                     * ফর্মের সাথে চলে যেত।
                     *
                     * ⚠️ গোনা শুরু হয় **বিলের তারিখ থেকে**, আজ থেকে নয় —
                     * ব্যাক-ডেটেড বিলে দুইটা আলাদা, আর আজ থেকে গুনলে
                     * মেয়াদটা ভুল দিনে পড়ত।
                     */
                    /* ⓘ ঘরটার একটাই মান: `cash` · `cod` · `credit:30` ·
                       `month_end` · `fixed`। ⚠️ কোলনের পরের সংখ্যাটা কেবল
                       `credit`-এ, আর সার্ভারে যায় কোলনের **আগের** অংশটা
                       ধরন হিসেবে, পরেরটা দিনসংখ্যা হিসেবে। */
                    get termKind() {
                        return String(this.creditTerm || '').split(':')[0];
                    },

                    get termDays() {
                        return String(this.creditTerm || '').split(':')[1] || '';
                    },

                    termChanged() {
                        if (this.termKind === 'fixed') {
                            /* ⚠️ আগের হিসাব করা তারিখটা মুছে দেওয়া হয়,
                               নাহলে মানুষটা তারিখের ঘরে একটা **আগের
                               হিসাবের** তারিখ বসা দেখতেন আর ভাবতেন
                               তিনিই বসিয়েছেন। */
                            this.dueOn = '';
                            this.pushDueDate();

                            return;
                        }

                        const el = this.$root.querySelector('input[name=trx_date]');
                        const from = el && el.value
                            ? new Date(el.value + 'T00:00:00')
                            : new Date();

                        if (Number.isNaN(from.getTime())) {
                            this.dueOn = '';
                            this.pushDueDate();

                            return;
                        }

                        if (this.termKind === 'month_end') {
                            /*
                             * ⭐ চালানের **মাসের** শেষ দিন।
                             *
                             * ⚠️ `new Date(y, m + 1, 0)` মানে "পরের মাসের
                             * শূন্যতম দিন", অর্থাৎ চলতি মাসের শেষ দিন।
                             * ⛔ `+30 দিন` গুনলে ফেব্রুয়ারিতে মার্চে গিয়ে
                             * পড়ত, আর লিপ ইয়ারে আরও একদিন।
                             */
                            this.dueOn = this.isoDay(
                                new Date(from.getFullYear(), from.getMonth() + 1, 0)
                            );
                            this.pushDueDate();

                            return;
                        }

                        /*
                         * ⭐ নগদ আর COD — দুইটার তারিখই চালানের দিন।
                         *
                         * ⚠️ তবু দুইটা এক নয়, আর পার্থক্যটা টাকার ঘরে:
                         * নগদে টাকা ড্রয়ারে ঢুকেছে, COD-তে মাল ভ্যানে
                         * গেছে আর টাকা ফিরবে ডেলিভারিম্যানের সাথে।
                         * ⓘ তাই ধরনটা আলাদা করে সার্ভারে যায়।
                         */
                        const days = this.termKind === 'credit'
                            ? Number(this.termDays || 0)
                            : 0;

                        from.setDate(from.getDate() + days);
                        this.dueOn = this.isoDay(from);
                        this.pushDueDate();
                    },

                    /**
                     * তারিখটা `YYYY-MM-DD` হয়ে।
                     *
                     * ⚠️ `toISOString()` নয় — ওটা UTC-তে নামায়, আর
                     * বাংলাদেশে সন্ধ্যার পর তারিখটা একদিন পিছিয়ে যেত।
                     * ⓘ পুরনো কোডে ঠিক ওটাই ছিল।
                     */
                    isoDay(d) {
                        return [
                            d.getFullYear(),
                            String(d.getMonth() + 1).padStart(2, '0'),
                            String(d.getDate()).padStart(2, '0'),
                        ].join('-');
                    },

                    /*
                     * তারিখটা কম্পোনেন্টের ভেতরে বসানো।
                     *
                     * ── কেন সরাসরি `x-model` নয় ─────────────────────────
                     * ঘরটা আর ব্রাউজারের নিজের তারিখের ঘর নয় — সে
                     * ওটার লেখা নিজের লোকেলে আঁকত, আর `05/06` তখন ৫ জুন
                     * না ৬ মে বলার উপায় থাকত না (`x-ui.date` ঠিক এই
                     * কারণেই আছে)। কম্পোনেন্টটার নিজের Alpine স্কোপ আছে,
                     * তাই বাইরের `x-model` ওখানে পৌঁছায় না।
                     *
                     * ⓘ `$refs.dueDate` লেখার ঘরটাকে ধরে, আর সেটা
                     * কম্পোনেন্টের স্কোপের **ভিতরে** — তাই `Alpine.$data()`
                     * ওই স্কোপটাই ফেরত দেয়। দুইটা ঘর বসাতে হয়: `iso`
                     * সার্ভারের জন্য, `text` চোখের জন্য।
                     */
                    pushDueDate() {
                        const el = this.dateBox('due_on');

                        if (! el || ! window.Alpine) return;

                        const box = window.Alpine.$data(el);

                        box.iso = this.dueOn;
                        box.text = this.dueOn
                            ? this.dueOn.split('-').reverse().join('-')
                            : '';
                    },

                    chooseCustomer(id) {
                        this.customerId = String(id);

                        /*
                         * ক্রেতার নিজের মেয়াদ থাকলে সেটাই বসে।
                         *
                         * ডিফল্ট "আজ" কাউন্টারের স্বাভাবিক অবস্থা, কিন্তু
                         * যে দোকানের সাথে ৩০ দিনের কথা আছে তাঁর বেলায় ওটা
                         * ব্যতিক্রম — আর ব্যতিক্রমটা সিস্টেম নিজেই জানে।
                         * প্রতিবার হাতে বাছতে দেওয়া মানে একদিন কেউ ভুলে
                         * যাবেন, আর নগদ বলে বসে থাকা একটা বাকি বিল
                         * কাউকে তাগাদা দেওয়া হবে না।
                         *
                         * তালিকায় ওই দিনসংখ্যা না থাকলে বদলানো হয় না —
                         * নাহলে ড্রপডাউনটা এমন একটা মান দেখাত যা তার
                         * নিজের বিকল্পে নেই, আর ঘরটা ফাঁকা দেখাত।
                         */
                        /*
                         * ⚠️ মানটা এখন `credit:30`, শুধু `30` নয় — ৫
                         * সেপ্টেম্বর ২০২৬ থেকে ঘরটা ধরন বোঝে।
                         *
                         * ⛔ পুরনো লেখাটা রয়ে গেলে ড্রপডাউনে এমন একটা মান
                         * বসত যা তার নিজের কোনো বিকল্পে নেই, আর ঘরটা
                         * **ফাঁকা** দেখাত — ক্রেতা বাছার ঠিক পরমুহূর্তে।
                         */
                        const days = Number(this.customers[this.customerId]?.days || 0);
                        const want = 'credit:' + days;
                        const has = [...this.$root.querySelectorAll('select option')]
                            .some(o => o.value === want);

                        if (days > 0 && has) {
                            this.creditTerm = want;
                            this.dueOn = '';
                            this.termChanged();
                        }
                        this.customerTerm = '';
                        this.customerPickerOpen = false;
                    },

                    pickFirstCustomer() {
                        const first = this.customerMatches[0];
                        if (first) this.chooseCustomer(first.id);
                    },

                    /*
                     * চিহ্নে চাপলে ঘরটা খোলে, আর সাথে সাথেই ফোকাস।
                     *
                     * `$nextTick` ছাড়া চলত না: `x-show` ঘরটাকে ওই মুহূর্তে
                     * এখনো `display:none`-এ রাখে, আর লুকানো ঘরে ফোকাস দিলে
                     * ব্রাউজার নীরবে কিছুই করে না — বোতামে চাপ দিয়ে মানুষ
                     * টাইপ শুরু করতেন আর কোথাও কিছু বসত না।
                     */
                    openPicker() {
                        this.pickerOpen = true;
                        this.term = '';
                        this.$nextTick(() => this.$refs.search?.focus());
                    },

                    /*
                     * ── মজুদ নেই এমন পণ্য বাছা যায় না ────────────────────
                     *
                     * মালিকের নির্দেশ (৩ সেপ্টেম্বর ২০২৬): *"kono product
                     * stock na thakle poriborton hobe na, ekta warning sound
                     * dibe, color lal hobe, screen-er majkhane boro kore
                     * notice dibe"*।
                     *
                     * ── কেন থামানোই ঠিক, সতর্ক করে এগোনো নয় ─────────────
                     * পণ্যটা বসতে দিলে কাউন্টারের লোক পরিমাণ-দর টাইপ করে
                     * কার্টে ফেলতেন, আর ভুলটা ধরা পড়ত **সবার শেষে, নিশ্চিত
                     * করার সময়** — তখন লাইনটা মুছে আবার শুরু।
                     *
                     * ⚠️ আর ওই মুহূর্তে ক্রেতা সামনে দাঁড়িয়ে। **যত আগে "না"
                     * বলা যায়, তত কম কাজ নষ্ট হয়।**
                     */
                    outOfStock: null,

                    pick(product) {
                        if (! (Number(product.available) > 0)) {
                            this.refuse(product);

                            return;
                        }

                        this.picked = product;
                        this.entry.rate = product.rate;
                        this.entry.qty = this.entry.qty || '1';
                        this.term = '';
                        // বাছা হয়ে গেছে — তালিকাটা আর কিছু বলার নেই
                        this.pickerOpen = false;
                    },

                    refuse(product) {
                        this.outOfStock = product;
                        this.beep();
                    },

                    /*
                     * সতর্কতার শব্দ — কোনো ফাইল নয়, ব্রাউজারই বাজায়।
                     *
                     * ── কেন Web Audio, কেন mp3 নয় ───────────────────────
                     * একটা শব্দের ফাইল মানে একটা সম্পদ: লাইসেন্স, আকার, আর
                     * ডিপ্লয়ে ওটা সত্যিই গেল কিনা তার একটা নতুন প্রশ্ন।
                     * ⭐ **মালিকের নিয়ম — কোনো paid tool নয়** — আর এখানে
                     * ফাইলই লাগে না: ব্রাউজার নিজেই সুর বানাতে পারে।
                     *
                     * ⚠️ ব্রাউজার ব্যবহারকারীর প্রথম ক্লিকের আগে শব্দ বাজাতে
                     * দেয় না। কাউন্টারে পণ্য বাছতে গেলে ক্লিক হয়েই যায়, তাই
                     * বাস্তবে সমস্যা নেই — তবু `try` দিয়ে ঘেরা, কারণ **শব্দ
                     * না বাজলেও বিক্রি থামা চলবে না।**
                     */
                    beep() {
                        try {
                            const ctx = new (window.AudioContext || window.webkitAudioContext)();
                            const osc = ctx.createOscillator();
                            const gain = ctx.createGain();

                            osc.type = 'square';
                            osc.frequency.value = 440;
                            gain.gain.value = 0.08;

                            osc.connect(gain).connect(ctx.destination);
                            osc.start();
                            osc.stop(ctx.currentTime + 0.18);
                            osc.onended = () => ctx.close();
                        } catch (e) {
                            // শব্দ বাজেনি — লাল বাক্স আর নোটিশ তবু আছে
                        }
                    },

                    pickFirst() {
                        const first = this.visible[0];
                        if (first) this.pick(first);
                    },

                    // ── চলতি লাইনের অঙ্ক ────────────────────────────────
                    get entryBase() {
                        return (Number(this.entry.qty) || 0) * (Number(this.entry.rate) || 0);
                    },

                    get entryAfterDiscount() {
                        return this.entryBase
                            - this.entryDiscount;
                    },

                    /*
                     * লাইনের ছাড় — টাকায় বা শতাংশে, একটাই ঘর।
                     *
                     * ── কেন একটাই (৩ সেপ্টেম্বর ২০২৬) ────────────────────
                     * আগে ছাড়ের ঘরটা বাঁয়ের ফর্মে ছিল ("ছাড় %"), আর অঙ্কটা
                     * দেখা যেত না। মালিক ঘরগুলো এক সারিতে নামিয়ে বাকিগুলো
                     * তুলে দিতে বললেন — *"this line e egulo ache, dubar
                     * dorkar nai"* — তাই ছাড়টা এখন কেবল "এই লাইন" প্যানেলে,
                     * আর **লেখা ও ফল পাশাপাশি**।
                     *
                     * শেষে `%` থাকলে শতাংশ, নাহলে সোজা টাকা।
                     *
                     * ⚠️ ছাড় লাইনের মোটের চেয়ে বড় হতে দেওয়া হয় না — দিলে
                     * লাইনটা ঋণাত্মক হত, আর তখন **বিক্রির কাগজে একটা সারি
                     * গ্রাহককে টাকা ফেরত দিত**।
                     */
                    get entryDiscount() {
                        const raw = String(this.entry.discountInput || '').trim();

                        if (raw === '') return 0;

                        const isPercent = raw.endsWith('%');
                        const n = Number(raw.replace('%', '').trim());

                        if (! isFinite(n) || n <= 0) return 0;

                        const value = isPercent ? this.entryBase * n / 100 : n;

                        return Math.min(value, this.entryBase);
                    },

                    /*
                     * সার্ভার লাইনের ছাড় **শতাংশে** নেয়, তাই টাকায় লেখা হলে
                     * এখানে ফিরিয়ে দেওয়া হয়।
                     *
                     * ⚠️ ভিত্তি শূন্য হলে শতাংশ বের করা যায় না (কোনো দর বা
                     * পরিমাণ বসানো হয়নি) — তখন ০, কারণ ছাড় বলে কিছু নেই।
                     */
                    get entryDiscountPercent() {
                        if (this.entryBase <= 0) return '';

                        return String(this.entryDiscount * 100 / this.entryBase);
                    },

                    get entryVat() {
                        if (! this.vatEnabled || ! this.picked) return 0;
                        return this.vatOn(this.entryAfterDiscount, this.picked);
                    },

                    get entryNet() {
                        return this.isInclusive(this.picked)
                            ? this.entryAfterDiscount
                            : this.entryAfterDiscount + this.entryVat;
                    },

                    /* বিক্রয় + ফ্রি — গুদাম থেকে মোট যতটা বেরোবে।
                       নমুনায় ঘরটা নিজে থেকেই ভরে, হাতে লেখা যায় না। */
                    get entryTotalQty() {
                        return (Number(this.entry.qty) || 0) + (Number(this.entry.freeQty) || 0);
                    },

                    /*
                     * শিট থেকে আসা সারিগুলো কার্টে।
                     *
                     * ── কেন ইভেন্ট, সরাসরি ডাকা নয় ────────────────────
                     * শিটটা জানে না কে শুনছে — তাই একই শিট চালানে,
                     * সরাসরি বিক্রয়ে আর ক্রয়েও বসে। তিনটা কপি থাকলে
                     * একদিন একটার অঙ্ক বদলাত, বাকি দুইটা থাকত।
                     *
                     * ── একই পণ্য দুইবার এলে সারি বাড়ে না ──────────────
                     * পরিমাণ বাড়ে। নাহলে এক পণ্যে দুই সারি, আর কাগজে
                     * একই নাম দুইবার।
                     */
                    absorbBulk(rows) {
                        (rows || []).forEach(row => {
                            const p = this.catalogue.find(c => String(c.id) === String(row.product_id));
                            if (! p) { return; }

                            const already = this.lines.find(l => l.id === p.id);

                            if (already) {
                                already.qty = String((parseFloat(already.qty) || 0)
                                    + (parseFloat(row.qty) || 0));
                                already.freeQty = String((parseFloat(already.freeQty) || 0)
                                    + (parseFloat(row.free_qty) || 0)) || '';

                                return;
                            }

                            this.lines.push({
                                key: this.nextKey++,
                                id: p.id,
                                name: p.name,
                                unit: p.unit,
                                vatRate: p.vatRate || 0,
                                vatInclusive: !! p.vatInclusive,
                                qty: String(row.qty || '0'),
                                freeQty: row.free_qty || '',
                                rate: String(row.rate || p.rate || 0),
                                discountPercent: '',
                                gifts: [],
                            });
                        });

                        this.panel = '';
                    },

                    addToCart() {
                        if (! this.picked) return;

                        this.lines.push({
                            key: this.nextKey++,
                            id: this.picked.id,
                            name: this.picked.name,
                            unit: this.picked.unit,
                            vatRate: this.picked.vatRate || 0,
                            vatInclusive: !! this.picked.vatInclusive,
                            qty: this.entry.qty || '1',
                            freeQty: this.entry.freeQty || '',
                            rate: this.entry.rate || '0',
                            discountPercent: this.entryDiscountPercent,
                            unitId: this.entry.unitId || '',
                            gifts: this.entry.gifts,
                        });

                        this.clearEntry();
                        this.$nextTick(() => this.$refs.search.focus());
                    },

                    clearEntry() {
                        this.picked = null;
                        this.entry = { qty: '', freeQty: '', rate: '', discountInput: '', unitId: '', gifts: [] };
                        this.giftDraft = null;
                        this.term = '';
                        this.showCosting = false;
                    },

                    clearAll() {
                        this.lines = [];
                        this.discountInput = '';
                        this.expenseInput = '';
                        this.roundingInput = '';
                        this.deposits = [];
                        this.depositDraft = {
                            methodId: '', accountId: '', amount: '',
                            reference: '', refDate: '', narration: '',
                        };
                        this.clearEntry();

                        /*
                         * ⚠️ সংরক্ষিত খসড়াও যায় — মালিকের নির্দেশ:
                         * *"এরপর না লাগলে ক্লিয়ার ডাটা দিলে যেন মুছে যায়"*।
                         *
                         * না মুছলে "সব মুছুন" একটা মিথ্যা হয়ে যেত: পর্দা
                         * খালি দেখাত, অথচ পরের বার পাতা খুললেই **মুছে ফেলা
                         * চালানটা ফিরে আসার প্রস্তাব** আসত।
                         */
                        this.dropDraft();

                        /*
                         * ── ⛔ ক্রেতা আর কাগজের ঘরগুলোও যায় ─────────────────
                         *
                         * মালিক, ৬ সেপ্টেম্বর ২০২৬: *"ei buton gulo duti ache,
                         * ekto kaj kore na"*।
                         *
                         * ⚠️ চালিয়ে দেখা গেল দুইটাই চলে — কিন্তু "সব মুছুন"
                         * চাপার পর **ক্রেতা রয়ে যেত**, আর সাথে শর্ত ও DO।
                         * ⓘ পর্দাটা তখনো ভরা দেখাত, তাই বোতামটা "কাজ করেনি"
                         * মনে হত — আর মালিকের পাঠটাই সঠিক পাঠ: বোতামের নাম
                         * "সব মুছুন", তাই সব মুছতে হবে।
                         *
                         * ⛔ আর ক্ষতিটা কেবল চোখের নয়: পরের ক্রেতার মাল
                         * আগের ক্রেতার নামে বসে যেত, কারণ কেউ খেয়াল করতেন
                         * না যে নামটা বদলায়নি।
                         *
                         * ⓘ তারিখ আর বিল নম্বর **থাকে** — ওদুটো কাগজের
                         * পরিচয়, চালানের বিষয়বস্তু নয়। ⚠️ নম্বরটা মুছলে
                         * সিরিজ থেকে আরেকটা তুলতে হত, আর সেটা ফাঁক তৈরি করত।
                         */
                        this.customerId = '';
                        this.customerTerm = '';
                        this.customerPickerOpen = false;
                        this.creditTerm = @js($paymentTermDefault);
                        this.dueOn = '';
                        this.termChanged();

                        const doNo = this.$root.querySelector('[name=do_no]');
                        if (doNo) doNo.value = '';
                    },

                    /*
                     * ── উপহার — যে পণ্যটা এখন হাতে, তার সাথেই ─────────────
                     *
                     * বোতামটা পণ্য না বাছা পর্যন্ত নিষ্ক্রিয়। কারণ উপহার
                     * সবসময় **কোনো একটা পণ্যের সাথে** যায়, আর কোনটার সাথে
                     * সেটা এখানে জিজ্ঞেস করা হয় না — যেটা হাতে আছে, সেটাই।
                     */
                    openGift() {
                        if (! this.picked) return;

                        this.giftDraft = this.giftDraft
                            ? null
                            : { productId: '', qty: '1', remarks: @js(__('sales::message.not_for_sales')) };
                    },

                    commitGift() {
                        const g = this.giftDraft;

                        if (! g || ! g.productId || ! (Number(g.qty) > 0)) return;

                        this.entry.gifts.push({ key: this.nextKey++, ...g });
                        this.giftDraft = null;
                    },

                    removeGift(lineIndex, giftIndex) {
                        this.lines[lineIndex].gifts.splice(giftIndex, 1);
                    },

                    /*
                     * সব লাইনের উপহার একসাথে — গোনার জন্য।
                     *
                     * ⚠️ এন্ট্রির উপহারগুলো এখানে নেই, ইচ্ছে করে: ওগুলো
                     * এখনো কার্টে যায়নি, তাই যোগফলেও যাওয়ার কথা নয়।
                     */
                    get allGifts() {
                        return this.lines.flatMap((line) => line.gifts || []);
                    },

                    /*
                     * সার্ভারে যা যায়।
                     *
                     * ⭐ `against_product_id` কোনো ড্রপডাউন থেকে আসে না —
                     * **উপহারটা যে লাইনের ভেতরে বসে আছে, সেই লাইনের পণ্য**।
                     * তাই ওটা খালি থাকতে পারে না, আর ভুলও হতে পারে না।
                     */
                    get payloadGifts() {
                        return this.lines.flatMap((line) =>
                            (line.gifts || [])
                                .filter((g) => g.productId && Number(g.qty) > 0)
                                .map((g) => ({
                                    productId: g.productId,
                                    againstProductId: line.id,
                                    qty: g.qty,
                                    remarks: g.remarks,
                                })),
                        );
                    },

                    // ── কার্টের অঙ্ক ────────────────────────────────────
                    lineBase(line) {
                        return (Number(line.qty) || 0) * (Number(line.rate) || 0);
                    },

                    lineAfterDiscount(line) {
                        return this.lineBase(line)
                            - this.lineBase(line) * (Number(line.discountPercent) || 0) / 100;
                    },

                    /*
                     * ভ্যাট — সার্ভারের নিয়মেই, দুই রকম।
                     *
                     * বাইরের ভ্যাট দরের উপরে বসে; ভেতরের ভ্যাট দরের ভেতরেই
                     * আছে, তাই ওটা মোট বাড়ায় না — কেবল কতটুকু কর তা আলাদা
                     * করে দেখায়। আগে দুইটাই যোগ করা হত, আর ভেতরের বেলায়
                     * পর্দার সংখ্যা বিলের চেয়ে বেশি দেখাত।
                     *
                     * অঙ্কটা এখানে কেবল **দেখানোর** জন্য; খাতায় যেটা বসে
                     * সেটা সার্ভার নিজে গোনে (CalculatesSalesLines)।
                     */
                    vatOn(net, item) {
                        if (! this.vatEnabled || ! item) return 0;

                        /*
                         * পুরো কাগজের জন্য বদল — থাকলে পণ্যের নিজের হার ও
                         * ধরন দুইটাই এটাই ঢেকে দেয় (৩ সেপ্টেম্বর ২০২৬)।
                         *
                         * ⚠️ **ধরনটাও বদলায়, শুধু হার নয়।** কেউ "ভ্যাট সহ"
                         * বাছলে দরটা এখন ভ্যাট-সহ ধরা হবে — পণ্যটা নিজে
                         * "ভ্যাট বাদে" বলা থাকলেও। ওটাই তো বদলের মানে।
                         */
                        if (this.vatMode === 'exempt') return 0;

                        const override = this.vatMode === 'exclusive' || this.vatMode === 'inclusive';
                        const rate = (override ? Number(this.vatRate || 0) : Number(item.vatRate || 0)) / 100;

                        if (! rate) return 0;

                        const inclusive = override ? this.vatMode === 'inclusive' : !! item.vatInclusive;

                        return inclusive
                            ? net - (net / (1 + rate))
                            : net * rate;
                    },

                    /*
                     * দরটা ভ্যাট-সহ কিনা — লাইনের মোট গুনতে লাগে।
                     *
                     * ⚠️ আগে সরাসরি `line.vatInclusive` পড়া হত। পুরো কাগজের
                     * বদল এলে সেটা মিথ্যা বলত: ভ্যাট গোনা হত নতুন নিয়মে,
                     * আর মোট গোনা হত পুরনো নিয়মে — **দুইটা সংখ্যা মিলত না**।
                     */
                    isInclusive(item) {
                        if (this.vatMode === 'exempt') return false;

                        if (this.vatMode === 'exclusive') return false;
                        if (this.vatMode === 'inclusive') return true;

                        return !! (item && item.vatInclusive);
                    },

                    lineVat(line) {
                        return this.vatOn(this.lineAfterDiscount(line), line);
                    },

                    lineNet(line) {
                        return this.isInclusive(line)
                            ? this.lineAfterDiscount(line)
                            : this.lineAfterDiscount(line) + this.lineVat(line);
                    },

                    get subTotal() {
                        return this.lines.reduce((s, l) => s + this.lineAfterDiscount(l), 0);
                    },

                    get vatTotal() {
                        return this.lines.reduce((s, l) => s + this.lineVat(l), 0);
                    },

                    /*
                     * সারির নিট যোগ করা, subTotal + vatTotal নয়।
                     *
                     * ভেতরের ভ্যাটে দ্বিতীয় হিসাবটা দুইবার কর যোগ করত।
                     * সারি ধরে গুনলে দুই ধরনের ভ্যাট একসাথে থাকা বিলেও
                     * সংখ্যাটা ঠিক থাকে।
                     */
                    get grossTotal() {
                        return this.lines.reduce((s, l) => s + this.lineNet(l), 0);
                    },

                    /*
                     * ছাড় — টাকা, নাকি শতাংশ?
                     *
                     * ── নিয়ম ────────────────────────────────────────────
                     * শেষে `%` থাকলে শতাংশ, নাহলে সোজা টাকা। **এই একটাই
                     * getter সবখানে** — পর্দার সংখ্যা, দিতে হবে, আর
                     * সার্ভারে যাওয়া লুকানো ঘর, তিনটাই এখান থেকে।
                     *
                     * ⚠️ শতাংশটা **মোট বিলের উপরে** বসে, কোনো একটা লাইনের
                     * উপরে নয় — লাইনের নিজের ছাড় আলাদা ঘরে, কার্টে।
                     *
                     * ⚠️ ছাড় বিলের চেয়ে বড় হতে দেওয়া হয় না। দিলে "দিতে
                     * হবে" ঋণাত্মক হত, আর তখন চালানটা **গ্রাহককে টাকা
                     * ফেরত দেওয়ার কাগজ** হয়ে যেত — যেটা সম্পূর্ণ আলাদা
                     * জিনিস (বিক্রয় ফেরত), আর তার নিজের পর্দা আছে।
                     */
                    get discountValue() {
                        const raw = String(this.discountInput || '').trim();

                        if (raw === '') return 0;

                        const isPercent = raw.endsWith('%');
                        const n = Number(raw.replace('%', '').trim());

                        if (! isFinite(n) || n <= 0) return 0;

                        const value = isPercent ? this.grossTotal * n / 100 : n;

                        return Math.min(value, this.grossTotal);
                    },

                    /*
                     * খরচ — টাকা, নাকি চালানের শতাংশ।
                     *
                     * ছাড়ের সাথে হুবহু এক নিয়ম, ইচ্ছে করেই: পাশাপাশি বসা
                     * দুইটা ঘর আলাদা নিয়মে চললে **সেটাই পরের ভুলের কারণ**।
                     *
                     * ⚠️ একটা জায়গায় ছাড়ের সাথে মেলে না — খরচে উপরের
                     * সীমা নেই। ছাড় বিলের চেয়ে বড় হলে কাগজটা উল্টে যায়
                     * (ফেরতের কাগজ), কিন্তু খরচ বড় হওয়া অস্বাভাবিক হলেও
                     * **অসম্ভব নয়** — সামান্য মালের দূরের ডেলিভারিতে ভাড়াই
                     * বেশি পড়তে পারে, আর ওটা সত্যি ঘটনা, ভুল নয়।
                     */
                    get expenseValue() {
                        const raw = String(this.expenseInput || '').trim();

                        if (raw === '') return 0;

                        const isPercent = raw.endsWith('%');
                        const n = Number(raw.replace('%', '').trim());

                        if (! isFinite(n) || n <= 0) return 0;

                        return isPercent ? this.grossTotal * n / 100 : n;
                    },

                    /*
                     * রাউন্ডিং — চিহ্নসহ একটাই সংখ্যা।
                     *
                     * ⚠️ অঙ্কটা সবসময় ধনাত্মক টাইপ হয়, দিকটা আলাদা বাছাই —
                     * তাই ভুল করে `-৪৩০০` বসে যাওয়ার পথ নেই।
                     *
                     * ⓘ সার্ভারে এই সংখ্যাটাই যায়, আর সেখানে রাউন্ডিং
                     * ঋণাত্মক হতে দেওয়া হয়েছে (কেবল রাউন্ডিং, ছাড় বা
                     * খরচ নয় — ওদের ঋণাত্মক হওয়ার কোনো মানে নেই)।
                     */
                    get roundingValue() {
                        const n = Number(this.roundingInput);

                        if (! isFinite(n) || n <= 0) return 0;

                        return this.roundingSign === '-' ? -n : n;
                    },

                    get netPayable() {
                        return this.grossTotal
                            - this.discountValue
                            + this.expenseValue
                            + this.roundingValue;
                    },

                    /*
                     * সব জমার যোগফল।
                     *
                     * ⓘ সার্ভারও ঠিক এই যোগটাই আবার করে (`depositTotal()`)
                     * — পর্দার সংখ্যা বিশ্বাস করে খাতায় বসানো হয় না।
                     */
                    get deposit() {
                        return this.deposits.reduce(
                            (sum, row) => sum + (Number(row.amount) || 0), 0
                        );
                    },

                    /*
                     * ── কোন খাতগুলো বাছা যাবে ────────────────────────────
                     *
                     * মালিকের নির্দেশ (৪ সেপ্টেম্বর ২০২৬): *"Method Cash হলে
                     * Received into-তে cash account গুলো, Bank হলে bank
                     * account, MFS হলে Mobile bank — আর method না select
                     * করলে Received into-তে কিছু দেখাবে না"*।
                     *
                     * ⚠️ **তিনটা আলাদা অবস্থা, তিনটাই আলাদা উত্তর:**
                     *
                     *   উপায় বাছা হয়নি      →  একটাও খাত নয়
                     *   উপায় বাছা, ধরন জানা  →  কেবল ওই মায়ের সন্তানরা
                     *   উপায় বাছা, ধরন নেই   →  সব টাকার খাত
                     *
                     * ⭐ প্রথমটা কেন খালি: খাত বাছার আগে **টাকাটা কীভাবে
                     * এল** সেটা জানা দরকার, নইলে বিকাশের টাকা নগদ ড্রয়ারে
                     * বসে যেত — আজ সকালে ঠিক ওই ভুলটাই সারানো হয়েছে।
                     *
                     * ⓘ তৃতীয়টা নিরাপদ ছাড়: কোনো কোম্পানি যদি নিজের একটা
                     * উপায় বানিয়ে ধরন না দেন, তাঁর কাজ আটকে যাবে না।
                     */
                    /*
                     * ⚠️ চেক ব্যাংক নয় — ১১০৪ "হাতে চেক"।
                     *
                     * ── কেন, আর ভুলটা কত সহজ ─────────────────────────────
                     * আমি প্রথমে চেককে ব্যাংকের ঘরে ফেলেছিলাম, কারণ চেকের
                     * টাকা তো ব্যাংকেই যায়। ⚠️ **কিন্তু আজ যায় না** — হাতে
                     * পাওয়া চেক এখনো টাকা নয়, ওটা পাশ হতে পারে, ফেরতও
                     * আসতে পারে।
                     *
                     * ⭐ রিপোতে এই ভুলটা একবার হয়েছিল আর সারানোও হয়েছে —
                     * চেকের মাইগ্রেশনের নামটাই সেটা: *"হাতে চেক ইতিমধ্যেই
                     * ব্যাংকের টাকা ছিল"*। `ChequeService` চেক নিলে
                     * **Dr ১১০৪** করে, ব্যাংক নয়; পাশ হলে তবে ব্যাংকে যায়।
                     *
                     * ⓘ তাই কাউন্টারে চেক বাছলে খাতের তালিকায় ব্যাংক হিসাব
                     * দেখানো **ভুল হত** — আর ওটা নীরব ভুল, কারণ সংখ্যাটা
                     * ঠিকই বসত, কেবল ভুল খাতে, আর ব্যাংকের ব্যালেন্স
                     * বাস্তবের চেয়ে বেশি দেখাত।
                     */
                    depositKindParents: { cash: '1101', bank: '1102', mfs: '1105', cheque: '1104' },

                    get depositAccounts() {
                        const method = this.depositMethodRow;

                        if (! method) return [];

                        const parent = this.depositKindParents[method.kind];

                        if (! parent) return this.moneyAccounts;

                        return this.moneyAccounts.filter(a => a.parent === parent);
                    },

                    /** বাছা উপায়ের সারিটা — কোড, খাত, নম্বর লাগবে কিনা। */
                    get depositMethodRow() {
                        return this.depositMethods.find(
                            m => String(m.id) === String(this.depositDraft.methodId)
                        ) || null;
                    },

                    /*
                     * ⚠️ নম্বরের ঘরটা কেবল যে উপায়ে দরকার।
                     *
                     * নগদে TrxID নেই, তাই ঘরটা সবসময় দেখালে প্রতিটা নগদ
                     * জমায় একটা খালি ঘর পার হতে হত — আর যেদিন সত্যিই
                     * দরকার সেদিনও চোখে পড়ত না।
                     */
                    get depositNeedsReference() {
                        return this.depositMethodRow?.needsReference === true;
                    },

                    /*
                     * ⚠️ শর্তটা **খাত**, উপায় নয় — আর কারণটা মাপা।
                     *
                     * খাতা যেটা সত্যিই দেখে সেটা খাত; উপায় কেবল বলে
                     * টাকাটা কীভাবে এসেছিল। তাছাড়া `mdm_payment_methods`
                     * একটা **সেটিংসের তালিকা**, আর নতুন কোম্পানিতে ওটা
                     * আজ **খালি** (মেপে দেখা, ৩ সেপ্টেম্বর ২০২৬)।
                     *
                     * উপায় বাধ্যতামূলক করলে যে ব্যবসা তালিকাটা এখনো
                     * সাজায়নি, **তাদের কাউন্টারে জমাই নেওয়া যেত না** —
                     * একটা সেটআপের ফাঁক দাঁড়িয়ে যেত রোজকার কাজের পথে।
                     */
                    get depositReady() {
                        return Number(this.depositDraft.amount) > 0
                            && this.depositDraft.accountId !== ''
                            && (! this.depositNeedsReference
                                || String(this.depositDraft.reference || '').trim() !== '');
                    },

                    /*
                     * উপায় বাছলে খাতটা নিজে থেকে বসে — কিন্তু বদলানো যায়।
                     *
                     * ⚠️ এক উপায়ের একাধিক খাত থাকতে পারে ("ব্যাংক" উপায়ে
                     * তিনটা ব্যাংক হিসাব), তাই ঘরটা তালাবদ্ধ নয়।
                     */
                    pickDepositMethod() {
                        this.depositDraft.accountId = this.depositMethodRow?.accountId || '';

                        /*
                         * ⚠️ উপায় বদলালে আগের খাতটা আর মানানসই না-ও হতে পারে।
                         *
                         * নগদ বেছে নগদের খাত বসানোর পর কেউ ব্যাংক বাছলে ঘরটা
                         * **নগদের খাত ধরে বসে থাকত**, অথচ তালিকায় ওটা আর নেই।
                         * পর্দায় দেখাত খালি, কিন্তু সার্ভারে যেত পুরনো মানটাই —
                         * নীরবে, আর টাকা ভুল খাতে।
                         */
                        if (! this.depositAccounts.some(a => a.id === this.depositDraft.accountId)) {
                            this.depositDraft.accountId = '';
                        }

                        if (! this.depositNeedsReference) {
                            this.depositDraft.reference = '';
                        }
                    },

                    /*
                     * ⚠️ `$refs` এখানে কাজ করে না, আর কারণটা সহজে মিস হয়।
                     *
                     * `x-ref` যে স্কোপে **থাকে** সেখানেই নিবন্ধিত হয়, আর
                     * ঘরটা `x-ui.date` কম্পোনেন্টের নিজের `x-data="abosDate(...)"`
                     * এর ভিতরে। তাই ফর্মের স্কোপ থেকে `$refs.depositDate`
                     * **সবসময় undefined** — আর নীরবে: তারিখটা খালি যেত,
                     * কোনো ত্রুটি ছাড়াই। মালিক তালিকায় ফাঁকা "Ref. Date"
                     * দেখে ধরেছেন (৪ সেপ্টেম্বর ২০২৬)।
                     *
                     * ⓘ লুকানো ঘরটার `name` আছে, তাই ওটাই ধরার হাতল —
                     * আর সেটাও কম্পোনেন্টের স্কোপের ভিতরে, তাই
                     * `Alpine.$data()` ওই স্কোপটাই ফেরত দেয়।
                     */
                    dateBox(name) {
                        return this.$root.querySelector(`input[name="${name}"]`);
                    },

                    get depositRefDate() {
                        const el = this.dateBox('deposit_ref_date');

                        return el && window.Alpine ? (window.Alpine.$data(el).iso || '') : '';
                    },

                    clearDepositDate() {
                        const el = this.dateBox('deposit_ref_date');

                        if (! el || ! window.Alpine) return;

                        const box = window.Alpine.$data(el);

                        box.iso = '';
                        box.text = '';
                    },

                    addDeposit() {
                        if (! this.depositReady) return;

                        this.deposits.push({
                            ...this.depositDraft,
                            refDate: this.depositRefDate,
                        });

                        this.depositDraft = {
                            methodId: '', accountId: '', amount: '',
                            reference: '', refDate: '', narration: '',
                        };

                        this.clearDepositDate();
                    },

                    dropDeposit(index) {
                        this.deposits.splice(index, 1);
                    },

                    /** তালিকায় দেখানোর জন্য নাম — id নয়। */
                    depositMethodName(id) {
                        return this.depositMethods.find(
                            m => String(m.id) === String(id)
                        )?.label || '—';
                    },

                    depositAccountName(id) {
                        return this.moneyAccounts.find(
                            a => String(a.id) === String(id)
                        )?.label || '—';
                    },

                    get invoiceDue() {
                        const due = this.netPayable - (Number(this.deposit) || 0);
                        return due > 0 ? due : 0;
                    },

                    /*
                     * বিলের চেয়ে বেশি জমা — যেটুকু বেশি, সেটুকু অগ্রিম।
                     *
                     * ⚠️ মালিক ধরেছেন (৩ সেপ্টেম্বর ২০২৬): ৬০,৫৬৫ টাকার
                     * বিলে ৫৬ লাখ জমা লিখলে পর্দা বলত **"বকেয়া ০"**, আর
                     * বাকি টাকাটা নিয়ে **একটা শব্দও বলত না**। ক্যাশিয়ার
                     * হাতে টাকা নিয়েছেন, অথচ পর্দা ভুলে গেছে।
                     */
                    get depositExcess() {
                        const extra = this.deposit - this.netPayable;

                        return extra > 0 ? extra : 0;
                    },

                    /*
                     * ⚠️ `invoiceDue` নয়, না-কাটা সংখ্যাটা।
                     *
                     * `invoiceDue` শূন্যে থেমে যায় (একটা বিলে ঋণাত্মক
                     * বকেয়ার মানে নেই)। কিন্তু **পক্ষের** হিসাবে উদ্বৃত্তটা
                     * সত্যি — ওটা তাঁর অগ্রিম, আর পরের চালানে কাটা হবে।
                     * তাই এখানে বাদ দিলে নিচের লাল/সবুজ বড়িটা মিথ্যা বলত।
                     */
                    get outstanding() {
                        return (Number(this.customer.due) || 0)
                            + this.netPayable
                            - this.deposit;
                    },

                    /*
                     * আর কত বাকিতে দেওয়া যাবে।
                     *
                     * ── কেন সীমাটা একা যথেষ্ট নয় ────────────────────────
                     * "বাকির সীমা ৭৫,০০০" একটা **চুক্তির** সংখ্যা, কাউন্টারের
                     * নয়। ⚠️ যাঁর ৭০,০০০ আগেই বাকি, তাঁর জন্য খোলা আছে
                     * মাত্র ৫,০০০ — অথচ পর্দায় ৭৫,০০০ দেখলে বিক্রেতা
                     * নির্দ্বিধায় পুরো ট্রাক তুলে দেবেন।
                     *
                     * ⓘ `outstanding` ব্যবহার করা হলো, কেবল `customer.due`
                     * নয় — কারণ **এই চালানটাও বাকির অংশ**। কার্টে মাল
                     * ওঠার সাথে সাথে সংখ্যাটা কমে, আর সীমা ছোঁয়ার মুহূর্তটা
                     * চালান নিশ্চিত করার *আগেই* চোখে পড়ে।
                     *
                     * ⚠️ সীমা ০ হলে এই সংখ্যাটা দেখানোই হয় না — শূন্য মানে
                     * "মাল বন্ধ" নয়, "বাকি বন্ধ"; ওটা আলাদা কথা, আর সেটা
                     * লেখাই থাকে।
                     */
                    get availableCredit() {
                        return (Number(this.customer.limit) || 0) - this.outstanding;
                    },

                    get counts() {
                        const sales = this.lines.reduce((s, l) => s + (Number(l.qty) || 0), 0);
                        const free = this.lines.reduce((s, l) => s + (Number(l.freeQty) || 0), 0)
                            + this.allGifts.reduce((s, g) => s + (Number(g.qty) || 0), 0);

                        return {
                            totalItem: this.lines.length,
                            totalSalesQty: this.qty(sales),
                            totalFreeQty: this.qty(free),
                            totalQty: this.qty(sales + free),
                        };
                    },

                    /* যে বোতামের কাজটা এই পাতাতেই আছে, সেটা ওই ঘরে নিয়ে
                       যায় — নতুন কোনো পপ-আপ নয়। খরচ বসাতে গিয়ে একটা জানালা
                       খুলে আবার বন্ধ করা কাউন্টারে দুইটা বাড়তি চাপ। */
                    /* $root, $el নয়: বোতাম থেকে ডাকা হলে $el হয় ওই
                       বোতামটাই, আর বোতামের ভেতরে ঘরটা থাকে না — তখন
                       কিছুই ফোকাস হত না, নীরবে। জাবেদা ও নগদ গণনার
                       পর্দায় এই একই ভুল দুইটা ফিচার মেরে রেখেছিল। */
                    focusField(name) {
                        const el = this.$root.querySelector(`[name="${name}"]`);
                        if (el) { el.focus(); el.select?.(); }
                    },

                    money(v) {
                        return Number(v || 0).toLocaleString('en-US', {
                            minimumFractionDigits: 2, maximumFractionDigits: 2,
                        });
                    },

                    qty(v) {
                        return String(Number(v || 0));
                    },
                };
            }
        </script>
    @endpush
</x-layouts.app>
