<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * মালিক একটা ফ্রিজ নিয়ে গেলেন, আর খাতা সেটাকে নগদ বলল।
 *
 * ── ⭐ মালিকের নির্দেশ, ১৮ সেপ্টেম্বর ২০২৬ ────────────────────────────
 * নমুনার উত্তোলনের পর্দায় "কী দিয়ে" ঘরটা আছে, আর মালিক পর্দা খুলে
 * লাল দাগ দিয়ে ওটাই দেখিয়েছেন।
 *
 * ⓘ আমি ঘরটা আগে বসাইনি, ইচ্ছাকৃতভাবে: `fin_withdrawals`-এ কলামটা ছিল
 * না, আর কলাম ছাড়া ঘর বসালে বাছাইটা **নীরবে হারাত** — ঠিক যেভাবে
 * ঐ দিন `kind` হারাচ্ছিল। ⭐ এখন কলামটা বসছে, তাই ঘরটাও বসবে।
 *
 * ── ⚠️ কেন এটা কেবল একটা লেবেল নয় ───────────────────────────────────
 * ⛔ উত্তোলন টাকায় হলে ক্রেডিট যায় নগদ বা ব্যাংকে। কিন্তু মালিক একটা
 * ফ্রিজ নিয়ে গেলে ক্রেডিট যায় **স্থায়ী সম্পদে**, আর পণ্য নিয়ে গেলে
 * **মজুদে**।
 *
 * ⚠️ ঘরটা না থাকায় সবই টাকা ধরা হত — অর্থাৎ সিন্দুক থেকে টাকা বেরোনো
 * দেখাত, অথচ সিন্দুকে টাকা অক্ষত। ⓘ মাস শেষে ক্যাশ বই মিলত না, আর
 * কেউ মনে করতে পারত না কেন।
 *
 * ── ⓘ নামটা মূলধনেরটার হুবহু ───────────────────────────────────────
 * `acc_capital_entries.in_kind`-এর মতোই `in_kind`, একই তিনটা মান।
 * ⭐ এক খাতার দুই দিক, তাই এক নাম — পরে কেউ দুইটা মিলিয়ে দেখতে গেলে
 * অনুবাদ করতে হবে না।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('fin_withdrawals', function (Blueprint $table): void {
            /*
             * ⚠️ ডিফল্ট `cash`, কারণ আগের সব সারি টাকাতেই বেরিয়েছে।
             * ⛔ অন্য কিছু ধরলে পুরনো হিসাব নীরবে বদলে যেত।
             */
            $table->string('in_kind', 24)->default('cash')->after('kind');
        });
    }

    public function down(): void
    {
        Schema::table('fin_withdrawals', function (Blueprint $table): void {
            $table->dropColumn('in_kind');
        });
    }
};
