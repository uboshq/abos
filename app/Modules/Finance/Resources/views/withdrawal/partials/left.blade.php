{{-- সীমায় আর কতটা বাকি।

     ── কেন শূন্যের নিচে গেলে লাল ─────────────────────────────────────
     সীমা পেরোনো অবস্থাটা সাধারণত ঘটে না — সেবাটা আটকায়। কিন্তু সীমা
     **পরে কমালে** আগের উত্তোলনগুলো তো রয়েই গেছে, আর তখন সংখ্যাটা
     ঋণাত্মক হয়। ওটা ভুল নয়, একটা খবর: এই মাসে ইতিমধ্যেই নতুন সীমার
     বেশি তোলা হয়ে গেছে। --}}
@if ($row['left'] === null)
    <span class="text-(--color-ink-muted)">—</span>
@else
    <span @class(['tabular-nums',
        'text-(--color-badge-danger-ink)' => bccomp((string) $row['left'], '0', 4) < 0])>
        {{ \App\Core\Support\Money::format($row['left']) }}
    </span>
@endif
