{{--
    এই ধরনে কয়টা জমা খোলা হয়েছে — আর সংখ্যাটা সেই তালিকায় নামে।

    ── ⓘ কেন "সব জমা"র পাতায়, ২০ সেপ্টেম্বর ২০২৬ ─────────────────────
    সংখ্যাটা **সব অবস্থার** জমা গোনে (চালু, ভাঙা, বন্ধ)। ⚠️ ইস্যুকারীর
    নিজের পাতা অবস্থার ট্যাবে ছাঁকা থাকে, তাই ওখানে নামালে সংখ্যা আর
    তালিকা দুইটা আলাদা কথা বলত — আর সেটাই সবচেয়ে খারাপ ভুল।

    ⓘ শূন্য হলে লিংক নয়: ফাঁকা তালিকায় নামানোর চেয়ে চুপ থাকা ভালো।
--}}
@if ((int) $kind->deposits_count === 0)
    {{ $kind->deposits_count }}
@else
    <a href="{{ route('finance.deposit.all', ['kind' => $kind->id]) }}"
       class="num text-(--color-brand-500) underline-offset-2 hover:underline">
        {{ $kind->deposits_count }}
    </a>
@endif
