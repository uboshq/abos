{{--
    প্রতিষ্ঠানের নাম → তার নিজের পাতা।

    ── ⓘ নিরীক্ষা চ১, ২১ সেপ্টেম্বর ২০২৬ ─────────────────────────────
    ⓘ প্রতিষ্ঠানের পাতায় ঐ ব্যাংকের সবটা একসাথে থাকে — ঋণসীমা, জমা,
    বীমা, জোড়া খাত। তাই নামটা যেখানেই লেখা হোক, সেখান থেকেই নামা যায়।

    ⚠️ `institution_id` না থাকলে (তালিকা আসার আগের কাগজ) নিছক লেখা —
    পুরনো সারিতে নামটা হাতে লেখা ছিল, আর তার নিজের পাতা নেই।

    @param $id     প্রতিষ্ঠানের আইডি, নয়তো null
    @param $label  যা লেখা থাকবে
--}}
@if ($id && auth()->user()?->can('finance.institution.view'))
    <a href="{{ route('finance.institution.show', $id) }}"
       class="text-(--color-brand-600) underline-offset-2 hover:underline">{{ $label }}</a>
@else
    {{ $label ?: '—' }}
@endif
