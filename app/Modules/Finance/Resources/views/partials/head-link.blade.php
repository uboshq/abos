{{--
    খাতের নাম → হিসাবের তালিকায় সেই খাতটা।

    ── ⓘ নিরীক্ষা চ১, ২১ সেপ্টেম্বর ২০২৬ ─────────────────────────────
    ⛔ খরচ আর আয়ের তালিকায় **অঙ্কটা** ক্লিকযোগ্য ছিল, নামটা নয়। অথচ
    "দোকান ভাড়া" পড়ে প্রশ্ন জাগে খাতটা নিয়েই, আর মানুষ নামেই ক্লিক
    করেন — অঙ্কে নয়। ⓘ গোটা ABOS-এর নিয়ম: যে নামটার নিজের পাতা আছে,
    সেটা ক্লিকযোগ্য ([[bank-charge/partials/bank-link]])।

    ⚠️ অনুমতি না থাকলে নিছক লেখা — একই পাতার "খতিয়ান" ঘরটাও ঠিক এই
    শর্তেই লিংক দেখায়, তাই দুইটা ঘর একই কথা বলে।

    @param $account  খাতের মডেল
--}}
@if (auth()->user()?->can('view', $account))
    <a href="{{ route('accounts.coa.show', $account) }}"
       class="text-(--color-brand-600) underline-offset-2 hover:underline">{{ $account->name() }}</a>
@else
    {{ $account->name() }}
@endif
