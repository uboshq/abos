{{--
    খাতের নাম → হিসাবের তালিকায় সেই খাতটা।

    ── ⓘ নিরীক্ষা চ১, ২১ সেপ্টেম্বর ২০২৬ ─────────────────────────────
    ⛔ বাজেটের তিনটা পাতায় খাতের নাম নিছক লেখা ছিল — "৫১০২ — দোকান
    ভাড়া" পড়ে কেউ জানতে চাইলে খাতটা কী, তাঁকে মেনু ঘুরে হিসাবের
    তালিকায় গিয়ে খুঁজতে হত। ⓘ একই নিয়ম গোটা ABOS-এ: **যে নামটা
    অন্য পাতায় আছে, সেটা ক্লিকযোগ্য** ([[bank-charge/partials/bank-link]])।

    ⚠️ খতিয়ানে নয়, খাতের পাতায় — কারণ বাজেটে বসে প্রশ্নটা "এই খাতে
    কী কী বসেছে" নয়, "এই খাতটা আসলে কী"।

    @param $account  খাতের মডেল
    @param $center   বিভাগ, নয়তো null (বাজেটের তালিকায় নামের সাথেই বসে)
--}}
<a href="{{ route('accounts.coa.show', $account->id) }}"
   class="text-(--color-brand-600) underline-offset-2 hover:underline">{{ $account->code }} — {{ $account->name() }}</a>
@if (! empty($center))
    <span class="text-(--color-ink-muted)"> · {{ $center->name() }}</span>
@endif
