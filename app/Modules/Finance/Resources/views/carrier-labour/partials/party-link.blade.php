{{--
    পক্ষের নাম → এই পর্দার খতিয়ান, আর পাশের তীর → তাঁর নিজের পাতা।

    ── ⚠️ কেন দুইটা লিংক, একটা নয় ──────────────────────────────────────
    নামটা এখানে দুইটা প্রশ্নের উত্তর দেয়, আর প্রশ্ন দুইটা আলাদা:
    ⓘ "এই খাতে তাঁর সাথে কী কী হয়েছে" — সেটা এই পাতারই খতিয়ান;
    ⓘ "তিনি কে, তাঁর সাথে সব মিলিয়ে কী" — সেটা তাঁর নিজের পাতা।
    ⛔ একটাকে আরেকটার জায়গায় বসালে একটা প্রশ্ন উত্তরহীন থাকত, তাই নামটা
    এখানেই রাখা হল আর দ্বিতীয় দরজাটা ছোট একটা তীর হয়ে পাশে বসল
    (মালিক, ২০ সেপ্টেম্বর ২০২৬: *"সব জায়গায় হাইপার লিংক দেওয়ার কথা"*)।
--}}
<a href="{{ route('finance.carrier_labour.index', $keep + ['party' => $p['key']]) }}"
   class="text-(--color-brand-600) underline-offset-2 hover:underline">{{ $p['label'] }}</a>

@if (! empty($p['route']))
    <a href="{{ route($p['route'][0], $p['route'][1]) }}"
       title="{{ __('finance::carrier_labour.open_party') }}"
       class="ms-1 text-2xs text-(--color-ink-muted) underline-offset-2 hover:underline">↗</a>
@endif
