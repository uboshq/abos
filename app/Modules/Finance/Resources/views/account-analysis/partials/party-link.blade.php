{{--
    পক্ষের নাম → তাঁর নিজের পাতা।

    ⭐ মালিকের কথা, ২০ সেপ্টেম্বর ২০২৬: *"সব জায়গায় হাইপার লিংক দেওয়ার কথা"*।
    ⓘ ঠিকানাটা সেবা থেকেই আসে ([[PartyRegistry::routesOf]]), তাই এখানে
    "সরবরাহকারী হলে এই রুট" লেখা নেই — নতুন ধরনের পক্ষ যোগ হলেও এই
    ফাইলটা ছুঁতে হবে না।

    ⚠️ ঠিকানা না থাকলে (মডিউল বন্ধ, বা পক্ষটা মুছে গেছে) নামটা থাকে,
    লিংকটা থাকে না — রিপোর্ট ভেঙে পড়ে না।

    @param $p  ['label' => …, 'route' => [রুট, ঘর] | null]
--}}
@if (! empty($p['route']))
    <a href="{{ route($p['route'][0], $p['route'][1]) }}"
       class="text-(--color-brand-600) underline-offset-2 hover:underline">{{ $p['label'] }}</a>
@else
    {{ $p['label'] }}
@endif
