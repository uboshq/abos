{{--
    "৭ বার" আর "৳ ৪২০" → ঐ সাতটা কাটা।

    ── ⭐ কেন সংখ্যাটা লিংক ─────────────────────────────────────────────
    মালিকের কথা, ২০ সেপ্টেম্বর ২০২৬: *"সব জায়গায় হাইপার লিংক দেওয়ার কথা"*,
    আর সংখ্যার বেলায় নিয়মটা আরও নির্দিষ্ট: **সংখ্যা মানে তার পিছনের সারিগুলো**।
    ⓘ "বিকাশে ৭ বার চার্জ কেটেছে" পড়ার পরের প্রশ্ন সবসময়ই "কোন সাতবার" —
    আগে সেটা জানতে নিচের গোটা তালিকা চোখে খুঁজতে হত।

    ⚠️ সময়ের ঘরগুলো (`$keep`) সাথে যায়, নাহলে লিংকে চেপে হঠাৎ অন্য মাসের
    সারি দেখা যেত — আর সংখ্যাটা তখন মিলত না।

    @param $bank  Account, নয়তো null
    @param $keep  ঠিকানায় যা ধরে রাখতে হবে (period/from/to)
    @param $text  ঘরে যা লেখা থাকবে
--}}
@if ($bank)
    <a href="{{ route('finance.bank_charge.index', $keep + ['bank_id' => $bank->id]) }}"
       class="text-(--color-brand-600) underline-offset-2 hover:underline">{{ $text }}</a>
@else
    {{ $text }}
@endif
