# ফোল্ডারটা গিটে থাকে, ভিতরের APK থাকে না।
#
# ⚠️ একটা APK পাঁচান্ন মেগাবাইট — গিটে বসালে প্রতিটা রিলিজে
# রিপোটা পাঁচান্ন মেগা করে বাড়ত, আর ক্লোন করা অসম্ভব হয়ে যেত।
# ⓘ ফাইলটা সার্ভারে সরাসরি আপলোড হয়, আর ডিপ্লয়ে মোছে না
# (`git pull` অট্যাকড ফাইল ছোঁয়ায় না)।
*
!.gitignore
