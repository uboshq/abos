@props(['menu' => [], 'slot' => null, 'region'])

{{--
    রূপের নিজের খোলস — যে টুকরোগুলো টোকেন দিয়ে হয় না।

    ── কেন এই ফাইলটা লাগল ──────────────────────────────────────────────
    রং, ঘনত্ব, ধার — সবই টোকেনে হয়। কিন্তু Odoo-র ৯-ফোঁটার লঞ্চার,
    D365-এর শেভরন বার, Fiori-র লঞ্চপ্যাড টালি — এগুলো CSS নয়, **markup**।
    প্রতিটা রূপের নিজের অংশ, আর অন্য রূপে ওগুলোর কোনো অস্তিত্বই নেই।

    ── কেন ছড়ানো `@if` নয় ─────────────────────────────────────────────
    সহজ পথটা হত টপবারে `@if ($look === 'apps') … @endif` লিখে দেওয়া।
    আটটা রূপ × ছয়টা অংশ = আটচল্লিশটা শর্ত, শেলের ফাইলগুলোতে ছড়ানো।
    ছয় মাস পরে নবম রূপ যোগ করতে গেলে আটচল্লিশ জায়গা খুঁজে বেড়াতে হত,
    আর একটা ভুলে গেলে সেটা কেবল ওই রূপে ধরা পড়ত।

    তাই প্রতিটা রূপ পায় নিজের ফাইল — `chrome/apps.blade.php` — আর সেখানে
    তার সব অংশ এক জায়গায়। এই ফাইলটার কাজ কেবল ঠিক ফাইলটা ডাকা।

    ── "পর্দা তার রূপ জানে না" নিয়মটা ভাঙে না ──────────────────────────
    এটা পর্দা নয়, **শেল**। শেলের কাজই খোলসটা ঠিক করা। ভেতরের ২৫২টা
    পাতার একটাও জানে না এই ফাইলটা আছে।

    ── অঞ্চল ────────────────────────────────────────────────────────────
    একটা রূপের অংশগুলো শেলের আলাদা আলাদা জায়গায় বসে: লঞ্চারের বোতাম
    টপবারের বাঁয়ে, তার শিট গোটা পাতার উপরে, ফুটার সবার নিচে। তাই
    `region` দিয়ে বলা হয় কোন টুকরোটা চাওয়া হচ্ছে, আর ফাইলটা কেবল
    সেটুকুই আঁকে।
--}}
@php
    $look = \App\Core\Support\Ui::clean(auth()->user()?->ui);
    $partial = 'components.shell.chrome.'.$look;
@endphp

@if (view()->exists($partial))
    @include($partial, ['menu' => $menu, 'region' => $region])
@endif
