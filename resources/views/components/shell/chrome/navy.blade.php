@props(['menu' => [], 'region'])

{{--
    ABOS-এর নিজের খোলস — মালিকের নকশা থেকে, ২ সেপ্টেম্বর ২০২৬।

    ── এই ফাইলটা কেন লাগল ───────────────────────────────────────────────
    নকশাটা মালিকের নিজের হাতে আঁকা (`E:\ABOS\brand\design\`), আর তার
    Design Language পাঁচ লাইনে বলা: SAP-এর মতো ভারী না · Odoo-র মতো
    সাধারণ না · Apple-এর মতো Clean · Linear-এর মতো Fast · VS Code-এর
    মতো Productive।

    ⚠️ **এর বেশিরভাগটা টোকেনে হয় না।** রং আর কোণ `Looks/navy.php`-এ
    বসে, কিন্তু নিচের যোগফলের পট্টি আর কীবোর্ডের সারি **markup** — আর
    ওগুলো কেবল এই রূপের।

    ── ⛔ কেন পর্দায় নয়, খোলসে ──────────────────────────────────────────
    সহজ পথটা হত তালিকার পর্দাগুলোয় `@if ($look === 'navy')` লিখে দেওয়া।
    ⚠️ ১০২টা পর্দা × দশটা রূপ — আর দশম রূপ যোগ করতে ওই একশোটা ফাইল
    খুলতে হত। পর্দা তার রূপ জানে না; খোলস জানে।

    ── অঞ্চল ─────────────────────────────────────────────────────────────
        page-foot   যোগফলের পট্টি + কীবোর্ডের হিন্ট
--}}

@if ($region === 'page-foot')
    @php
        $showTotals = \App\Core\Support\Ui::listFoot(auth()->user()?->ui) === 'totals';
        $showHints = \App\Core\Support\Ui::hints(auth()->user()?->ui) === 'keys';

        /*
         * পর্দার ঘোষণা করা যোগফলগুলো ([[x-ui.list-totals]])।
         *
         * ⚠️ `@stack` কেবল একবার পড়া যায়, তাই মানটা ধরে রাখা হয় —
         * নিচে দুইবার দরকার: আছে কি না দেখতে, আর আঁকতে।
         */
        $totals = trim($__env->yieldPushContent('list-totals'));
    @endphp

    {{-- ⛔ পট্টিটা **কেবল তালিকার পর্দায়** — আর শর্তটা একটাই: পর্দা
         নিজের যোগফল ঘোষণা করেছে কি না।

         ⚠️ প্রথমে শর্তটা ছিল `যোগফল আছে **অথবা** হিন্ট চালু`। যেহেতু
         ABOS-এর রূপে হিন্ট সবসময় চালু, ফলটা হয়েছিল: **ফর্ম, রেকর্ড আর
         ড্যাশবোর্ডেও** পট্টিটা বসত, আর তাতে লেখা থাকত "↑↓ সারি · ↵
         খুলুন" — অথচ ওই পাতাগুলোয় একটাও সারি নেই।

         ⛔ সেটা কেবল বাড়তি জিনিস নয়, **মিথ্যা প্রতিশ্রুতি**: হিন্ট
         দেখে কেউ তীর চাপতেন, কিছুই হত না, আর পরেরবার তালিকার পাতায়
         সত্যিকারের হিন্টটাও তিনি বিশ্বাস করতেন না।

         ⓘ ধরা পড়েছে ছয় ধরনের পাতা খুলে দেখে — তালিকা, ফর্ম, রেকর্ড,
         ড্যাশবোর্ড, কাউন্টার। ⭐ পাঁচটাতেই পট্টি বসছিল; বসার কথা
         একটাতে।

         ⚠️ ফল হিসেবে কীবোর্ডও কেবল ওখানেই চালু — `list-keys.js`
         এই চিহ্নটাই খোঁজে, তাই দুইটা এক সাথেই থাকে বা এক সাথেই যায়। --}}
    @if ($showTotals && $totals !== '')
        {{-- ⚠️ `bottom-0` নয় — স্ট্যাটাস বারের **উপরে**।

             ⛔ প্রথমে `sticky bottom-0 z-[3]` লেখা হয়েছিল, আর পট্টিটা
             পুরোপুরি অদৃশ্য ছিল: স্ট্যাটাস বার `fixed inset-x-0 bottom-0
             z-20` (`statusbar.blade.php:22`), অর্থাৎ একই জায়গায় বসে আর
             উপরে থাকে।

             ⚠️ ধরা পড়েছে **ছবি তুলে**, লেখা পড়ে নয় — probe বলছিল সব
             ঠিক (লেখাটা DOM-এ ছিল, সংখ্যাগুলোও মিলছিল), অথচ পর্দায়
             একটা পিক্সেলও দেখা যেত না।

             ⓘ ফোনে স্ট্যাটাস বার লুকানো থাকে, ওখানে নিচের নেভিগেশন —
             তাই দুই মাপে দুই ফাঁক। --}}
        <div data-look-listfoot
             class="sticky bottom-(--spacing-bottom-nav) z-[3] flex flex-wrap items-center
                    gap-x-5 gap-y-1 border-t border-(--color-border)
                    bg-(--color-surface-card) px-3 py-1.5 text-2xs
                    md:bottom-(--spacing-status-bar) md:px-5 lg:px-6">

            {{-- বাঁয়ে যোগফল — যে তিনটা সংখ্যা সবচেয়ে বেশি দেখা হয়।

                 ⭐ নকশার মূল কথাটাই এটা: **সংখ্যাগুলো কখনো স্ক্রল হয়ে
                 চোখের বাইরে যায় না।** আজ ওগুলো তালিকার শেষে বসে, তাই
                 পঞ্চাশ সারির পাতায় নিচে না নামলে দেখাই যেত না। --}}
            @if ($showTotals && $totals !== '')
                {!! $totals !!}
            @endif

            {{-- ডানে কীবোর্ডের হিন্ট — "Linear-এর মতো Fast"।

                 ⛔ হিন্ট দেখানো আর শর্টকাট কাজ করা এক জিনিস নয়। এখানে
                 কেবল লেখাটা; কাজটা করে `resources/js/list-keys.js`, আর
                 দুইটাই একই সুইচে (`hints=keys`) — একটা ছাড়া অন্যটা নয়।

                 ⚠️ ছোট পর্দায় লুকানো: ফোনে কীবোর্ড নেই, আর ওখানে এই
                 সারিটা কেবল জায়গা নিত। --}}
            @if ($showHints)
                <span data-look-hints
                      class="ms-auto hidden items-center gap-3 text-(--color-ink-muted) md:flex">
                    @foreach ([
                        '/' => 'core.keys.search',
                        'N' => 'core.keys.new',
                        '↑↓' => 'core.keys.row',
                        '↵' => 'core.keys.open',
                    ] as $key => $label)
                        <span class="inline-flex items-center gap-1">
                            <kbd class="rounded-(--radius-badge) border border-(--color-border)
                                        bg-(--color-surface-sunken) px-1 font-medium">{{ $key }}</kbd>
                            {{ __($label) }}
                        </span>
                    @endforeach
                </span>
            @endif
        </div>
    @endif
@endif
