@props(['menu' => [], 'region'])

{{--
    Linear — যার পরিচয় কিছু **না থাকা**।

    ── কেন এই ফাইলটা বাকিগুলোর উল্টো ────────────────────────────────────
    বাকি পাঁচটা chrome ফাইল শেলে কিছু **যোগ** করে: ওডুর লঞ্চার শিট,
    D365-এর ওয়াফল, রেডউডের ইটরঙা পটি। এটা যোগ করে না — এটা সেই জিনিসগুলো
    ফিরিয়ে আনে যেগুলো টপবারের সাথে চলে গেছে।

    Linear-এ উপরে কোনো বার নেই; পাতা পর্দার একদম উপর থেকে শুরু হয়। কিন্তু
    ভাষা বদলানো, চেহারা বদলানো, নিজের প্রোফাইল — এগুলো তো লাগেই। Linear
    ওগুলো রাখে **বাঁ পাশের তালিকার পায়ে**, আর এখানেও তাই।

    ── কেন এটা "মুছে ফেলা" নয় ───────────────────────────────────────────
    ২৭ অগাস্ট ২০২৬-এ ধরা পড়েছিল যে এই রূপটা রেল **আর** টপবার দুইটাই
    রাখছে — অর্থাৎ দশটার মধ্যে একমাত্র এটাই তার আসলটার উল্টো করছিল।

    সহজ পথটা হত টপবারটা `@if` দিয়ে বাদ দেওয়া, আর সেখানেই থেমে যাওয়া।
    তাতে রূপটা দেখতে Linear-এর মতো হত, কিন্তু ওই রূপ বেছে নেওয়া
    ব্যবহারকারী **ভাষা বদলাতে পারতেন না, চেহারা বদলাতে পারতেন না, এমনকি
    সাইন আউটও করতে পারতেন না**।

    একটা নকল সম্পূর্ণ করতে গিয়ে কাজের জিনিস কেড়ে নেওয়া নকল করা নয়,
    ভাঙা। তাই নিয়ন্ত্রণগুলো জায়গা বদলেছে, হারায়নি।

    ── অঞ্চল ─────────────────────────────────────────────────────────────
    সবটাই `rail-foot`, কারণ Linear-এ ওই ব্লকটা তালিকার একদম নিচে বসে,
    আর উপরে কিছুই থাকে না।
--}}
@php
    $user = auth()->user();
    $company = $user?->currentCompany;
    $branch = $user?->currentBranch;
@endphp

@if ($region === 'rail-foot')
    {{-- `data-quiet-foot` — পাহারার চিহ্ন। বারটা সরানোর পর নিয়ন্ত্রণগুলো
         সত্যিই কোথাও গিয়ে বসেছে কি না, সেটা এই চিহ্ন ধরেই মেলানো হয়। --}}
    <div data-quiet-foot
         class="mt-auto flex flex-col gap-1 border-t border-(--color-border) px-2 py-2">

        {{-- কোম্পানি ও শাখা — Linear-এ workspace switcher তালিকার পায়েই
             থাকে, আর এখানেও ওটাই "আমি কোথায় কাজ করছি"-র উত্তর।

             ── কেন এখানে তিনটা মাপ চাপিয়ে দিতে হলো ──────────────────
             কম্পোনেন্টটা লেখা হয়েছিল **টপবারের** জন্য, যেখানে জায়গা
             বেশি: নামের ঘর `max-w-64` (২৫৬px) আর লোগো `max-w-32`
             (১২৮px) — মিলে প্রায় ৩৮৪px।

             রেলটা ২৬০px। মাপ না বেঁধে বসানোয় বোতামটা মেপে পাওয়া গেল
             **৩৭৯px চওড়া, ২০১px-এর ঘরে** — অর্থাৎ সে সাইডবার ছাড়িয়ে
             কনটেন্টের উপর গিয়ে পড়ত। পাতার আড়াআড়ি স্ক্রল হত না বলে
             কোনো পাহারাতেও ধরা পড়ত না; ধরা পড়েছে ব্রাউজারে বোতামটার
             মাপ গুনে।

             ── কেন কম্পোনেন্টটা বদলানো হয়নি ─────────────────────────
             ওটা তার নিজের জায়গায় ঠিক — টপবারে ২৫৬px নামের ঘরই লাগে,
             নাহলে "ট্রেড ডিপো (প্রধান শাখা)" কেটে যেত। সরু জায়গায়
             বসানোর দায় বসানোর জায়গার, কম্পোনেন্টের নয়।

             `w-full` বোতামটাকে ঘরের মাপে আনে, আর নামের span-এ আগে
             থেকেই `min-w-0 truncate` আছে বলে সে নিজেই ছোট হয়ে যায়।
             (`max-w-64` একটা **সর্বোচ্চ**, সর্বনিম্ন নয় — তাই ওটা
             ছোট হতে বাধা দেয় না।)

             নির্বাচকটা `[&>div>button]`, `[&>button]` নয়: কম্পোনেন্টের
             মূল উপাদান একটা `<div x-data>` — ড্রপডাউনটা ধরে রাখার
             জন্য — আর বোতামটা তার ভেতরে। প্রথমবার এক ধাপ কম লিখে
             মাপটা একই ৩৭৯px থেকে গিয়েছিল। --}}
        @if ($company)
            <div class="min-w-0 [&>div]:w-full [&>div>button]:w-full [&_img]:max-w-16">
                <x-shell.company-switcher :company="$company" :branch="$branch" />
            </div>
        @endif

        <div class="flex items-center gap-1">
            <x-shell.notification-bell />
            <x-shell.approval-quick-access />

            {{-- চেহারার সুইচ। কম্পোনেন্টটার মূল উপাদান `<form class="contents">`,
                 তাই মোড়কটা নিজেই flex আইটেম হয় — আলাদা কিছু লাগে না। --}}
            <x-shell.theme-toggle />

            {{-- ভাষা। টপবারে এটা ১২৮০px-এর নিচে লুকানো থাকে, কারণ ওখানে
                 জায়গার লড়াই। সাইডবারের পায়ে ওই লড়াই নেই — তালিকাটা
                 ২৬০px চওড়া আর সারিটা নিজের একটা লাইন পায়, তাই এখানে
                 সবসময় দেখা যায়। --}}
            <form method="POST" action="{{ route('locale.switch') }}" class="contents">
                @csrf
                <button type="submit"
                        class="grid size-8 place-items-center rounded-[4px] text-xs font-semibold
                               text-(--color-ink-muted) transition-colors
                               hover:bg-(--color-surface-hover) hover:text-(--color-ink-body)"
                        title="{{ __('core.action.switch_language') }}">
                    {{ app()->getLocale() === 'bn' ? 'EN' : 'বাং' }}
                </button>
            </form>

            <div class="ms-auto">
                <x-shell.profile-menu :user="$user" />
            </div>
        </div>
    </div>
@endif
