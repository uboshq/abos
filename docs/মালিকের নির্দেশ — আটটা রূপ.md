# মালিকের নির্দেশ — আটটা রূপ

মালিক যা যা বলেছেন, হুবহু, তারিখসহ। **এটাই চুক্তি।** আমার মতামত এখানে
নেই — মতামত অন্য কাগজে (`Plan — আটটা থিম.md`, `Plan — Theme Engine.md`)।

কোনো সিদ্ধান্ত নিয়ে দ্বিমত হলে এই কাগজটাই শেষ কথা।

---

## ১ · রূপ মানে গোটা ERP, একটা পর্দা নয়

> "egulo kono pordar desine na egulo theme bujla, mane app select korle
> odoo hoyezabe, tiles select hole sap hobe"
> — ২০ আগস্ট

> "mone rakhbe zokhon ze theme select korbe tokhon tai hobe, app select
> korle **same** odoo hobe, redwood select korle oracle hobe same"
> — ২০ আগস্ট

একটা বাছাই → দুইশো বাহান্নটা পর্দা বদলাবে। রং বদলানো নয়।

---

## ২ · ১০০% মানে ১০০%

> "nokol mane 100% nokol. ami 100% copy chai, 99% na. 100% copy/nokol
> amake ensure koro"
> — ২১ আগস্ট

> "100% mane 100%, ok 99% holeo holo na. tik evabei kaj korbe. ekta
> korbe r ekta kore cheek korbe"
> — ২১ আগস্ট

**৯৯% মানে হয়নি।** আর কাজ হবে **একটা একটা করে**, প্রতিটা শেষ করে
যাচাই করে, তারপর পরেরটা।

---

## ৩ · কাঠামোও ১০০% — শেলের সব অংশ

> "নিজস্ব সম্পূর্ণ আলাদা কাঠামো পাবে zemon odoo, dynamic, sap, oracle
> etc hubohu hote hobe"
> — ২১ আগস্ট

> "কাঠামো ~১০০% milte hobe. theme bodlabar sathe sathe sidebar,
> topbar, botom bar, footer — all are everything change hobe"
> — ২১ আগস্ট

> "100% original masterpiece hobe — dekhte & kaje o"
> — ২১ আগস্ট

দেখতে **এবং** কাজে। সাইডবার · টপবার · নিচের বার · ফুটার — সব।

**এর ফলে যা করা হয়েছে:** `tools/theme-parts.py` — ১০০% মানে কী, তা
গুনে লেখা: **৪৯টা অংশ**। `tools/verify-themes.py` ওগুলো ব্রাউজারে
খোঁজে আর একটাও না থাকলে অসফল হয়। "হয়ে গেছে" আর কারও মতামত নয়।

---

## ৪ · নামগুলো ও কোনটা কার নকল

| # | নাম | কার নকল |
|---|---|---|
| ১ | **Navy** | ABOS-এর নিজের — **ডিফল্ট** |
| ২ | Rose | ABOS-এর নিজের |
| ৩ | Tiles | SAP Fiori |
| ৪ | Suite | Oracle NetSuite |
| ৫ | Apps | Odoo |
| ৬ | Dynamic | Microsoft Dynamics 365 |
| ৭ | Redwood | Oracle Fusion Cloud |
| ৮ | Classic | খতিয়ান রূপ — কারও নকল নয়, নিজস্ব নকশা |

> "nadiye list e 1st navy sl#1 rako & ডিফল্ট abos original ta koro.
> sobar last e classic rako"
> — ২১ আগস্ট

> "Redwood নাম দাও" — Oracle-এর ডিজাইন-ব্যবস্থার নামও এটাই, আর সেটা
> মালিককে বলা হয়েছে; তিনি জেনেই নামটা রেখেছেন।
> — ২০ আগস্ট

---

## ৫ · মাপকাঠি — দুইটা আর্টিফ্যাক্ট

> "tumar bortoman theme okkoto thakbe &
> <https://claude.ai/code/artifact/4b0e0e40-586f-4d7e-acb4-8c24b91efbec>"
> — ২১ আগস্ট

> "classic er darona ei link theke naw
> <https://claude.ai/code/artifact/fed36f24-c094-4eeb-9ec6-16ef204a2d06>"
> — ২১ আগস্ট

আটটা রূপের মান আসে প্রথমটা থেকে, ক্লাসিকের সম্পূর্ণ নকশা দ্বিতীয়টা
থেকে। **মাপকাঠি কারও স্মৃতি বা স্ক্রিনশট নয় — আর্টিফ্যাক্ট।**

> "tumar bortoman theme okkoto thakbe bolte ami gotokal ze rupe chilo
> setar kotha bolechi"
> — ২১ আগস্ট

গতকালের (২০ আগস্টের) লাইভ রূপটা অক্ষত থাকবে। সেটাই এখন **Navy**।

---

## ৬ · টপবার এক লাইনে

> "টপবারে দুই লাইন → ek line. ek line likhar jonno pasapasi likho koma
> diye othoba bracket diye. zemon = Adi Corporation (Mymensingh
> Branch), Al-Amin Shuvo (Owner). eivabe koro, taw same lagbe"
> — ২১ আগস্ট

ফলে ৫৬px-এর মেঝেটা উঠে গেছে; প্রতিটা রূপ এখন নিজের আসল উচ্চতায় —
Fiori ৪৪, Odoo ৪৬, NetSuite ৩৬, খতিয়ান ৩৪।

---

## ৭ · গোল্ডেন রুল — আগে ব্রাউজার, তারপর ডিপ্লয়

> "local e tumi age edge ba browser open kore live test kore milabe tar
> por deploy hobe, etai ekhon theke golden rule... karon ekhon theke
> live e real business add hobe **25/8/2026** theke, tai live e dile
> somossa howar agei somadhan"
> — ২১ আগস্ট

**২৫ আগস্ট ২০২৬ থেকে লাইভে আসল ব্যবসা।** তার আগে প্রতিটা বদল লোকালে
ব্রাউজারে, **সারি থাকা পর্দায়**, দেখে নিতে হবে — তারপর ডিপ্লয়।

---

## ৮ · থিম ইঞ্জিন — আলাদা ও বড়

> "alada Theme engine banaw. ERP te onek boro boro theme ache, amar
> mone hoy alada lagbe" · "আলাদা, বড় একটা ইঞ্জিন হওয়া উচিত"
> — ২১ আগস্ট

> "ok somossa hole tumar jonnoi koro engine, alada aladai valo"
> — ২১ আগস্ট

Ava-তেও একটা চলছে; দুইটা আলাদাই থাকবে। বিস্তারিত ও চুক্তি —
`docs/Plan — Theme Engine.md`।

---

## ৯ · কাজের ধরন

> "ektane checklist sesh koro, tarpor sob sesh hole switch, test,
> deploy ekotre. somoy bachbe"
> — ২০ আগস্ট

> "checklist koro sei onuzayi kaj koro"
> — ২১ আগস্ট

> "tumi kokhono bose thakbe na" · "nijeke paharay bosaw zate kaj
> sheser age na thamo"
> — ২০ আগস্ট

চেকলিস্ট ধরে কাজ, মাঝপথে থেমে রিপোর্ট নয়, আর একটা আটকে গেলে অন্যটা।

---

## ১০ · যেগুলো এখনো খোলা

- **বাকি ৪৯টা অংশের মধ্যে কতটা হয়েছে** — `tools/verify-themes.py`
  চালালেই সংখ্যাটা পাওয়া যায়। আজ ০/৪৯।
- ক্রম: **Apps → Dynamic → Tiles → Redwood → Suite → Classic → Rose**।
  প্রতিটা শেষ করে, স্ক্রিপ্ট সবুজ করে, ছবি দেখিয়ে, তারপর পরেরটা।
