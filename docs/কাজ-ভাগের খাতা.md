# কাজ-ভাগের খাতা

> ⭐ মালিকের প্রশ্ন, ২৫ সেপ্টেম্বর ২০২৬: *"tumi ekhankar co ordinator r
> tumi bolcho kaj k korteche jano na. keno?"* — এই খাতা তার উত্তর।
>
> ⭐ মালিকের নির্দেশ, ২৬ সেপ্টেম্বর ২০২৬: *"সবার কাজ একদম নিখুঁতভাবে বুঝিয়ে
> দাও … যাতে কেউ কারো কাজের ব্যাঘাত সৃষ্টি না করে।"* — তাই নিচে প্রত্যেকের
> **ফাইলের মালিকানা** লেখা। ⛔ নিজের তালিকার বাইরের কোনো ফাইলে হাত নয়।

## ১ · এখন কে কী করছেন, আর কোন ফাইল কার

| সেশন | কাজ | যে ফাইল ছোঁবেন (কেবল এগুলো) | টেস্ট DB |
|---|---|---|---|
| **abos-13** (সমন্বয়কারী) | বাকির সীমার কড়া দেয়াল — মিউট্যান্ট → ছোট সারাই → কমিট → বিল্ড → ডিপ্লয় | Sales-এর সেবা (`CreditExposure`, `SalesInvoiceService`, `DeliveryChallanService`, `DirectSaleService`, `SalesOrderService`), `DirectSaleController`, `Sales/module.php`, Sales-এর `lang` (validation · message · bn/field), `views/direct/index` ও `partials/totals`, Customer-এর `module.php` · `CustomerPolicy` · `Customer` মডেল · `show`, SystemAdmin-এর ControlPanel ও Settings (নিয়ামক · পর্দা · lang), `SettingsService`, `ModuleServiceProvider`, `Core/Contracts/CreditHolds`, `Core/Services/NoCreditHolds`, `DemoSeeder`, `resources/js/counter/direct-sale.js` ও তার পরীক্ষা, নিজের পরীক্ষা · **`public/build` কেবল সমন্বয়কারী** | `abos_test_8b` · `abos_test_13` |
| **abos-7d** | বিক্রয়ের দরজার পরীক্ষা (তাঁর ভাগ): চালান · আদেশ · বিল · আদায় (চেক ফেরতসহ) — কন্ট্রোলারের মিউট্যান্ট, তারপর কমিট · পরে: ম্যাক মিনি (মালিক SSH খুললে) | `tests/Feature/Modules/Sales/EverySalesDoorAsksForItsOwnKeyTest.php` · মিউট্যান্টে **সাময়িকভাবে** কেবল: `DeliveryChallanController`, `CollectionController`, `SalesOrderController`, `SalesInvoiceController` | `abos_test_7d` |
| **abos-d3** | বিক্রয়ের দরজার পরীক্ষা (তাঁর ভাগ): জমার দাবি · POS ফেরত · স্কিম · কমিশন · ফেরত (২০ দাবি) → কমিট; পরে শিপমেন্ট আলাদা কমিটে | নিজের নতুন পরীক্ষা ফাইলগুলো (`TheClaimDecision…`, `TheCounterReturn…`, `TheSchemeDoors…`, `TheReturnDoors…`, পরে শিপমেন্ট) · মিউট্যান্টে সাময়িকভাবে কেবল তাঁর ভাগের নিয়ামক (Claim · Pos · Scheme · Commission · SalesReturn · Shipment) | `abos_test_d3` |
| **abos-39** | Trade Promotion মডিউল — চেকলিস্ট ধরে কোড; রান আগামীকাল (মালিকের নির্দেশ) | `app/Modules/Promotion/**`, `tests/Feature/Modules/Promotion/**`, প্রমোশনের চেকলিস্ট, `x-ui.icon`-এ কেবল নিজের সারি · ⛔ Sales-এ হাত নয় (ধাপ ৯ সমন্বয়কারীর কমিটের পরে) | `abos_test_3c` |
| **abos-67** (আগে abos-af) | বাজেটের কাজের পুরো Architecture যাচাই (ভাগে ভাগে) → লালগুলো আবার → `AJsonColumn…` পাহারার সারাই · নিরাপত্তার নকশা মালিকের সামনে | `tests/Feature/Architecture/AJsonColumnWithoutACastKillsTheInsertTest.php` · দরকারে নিজের Purchase বাজেটের ফাইল | `abos_test_63` |

## ২ · সংঘাত এড়ানোর নিয়ম

1. **নিজের তালিকার বাইরের ফাইলে হাত নয়।** দরকার হলে আগে সমন্বয়কারীকে বলুন।
2. **মিউট্যান্ট:** এক সময়ে একটা, কপি রেখে, try/finally দিয়ে ফেরানো, শেষে `git diff --stat -- <ফাইল>` খালি। ⛔ অন্যের তালিকার ফাইলে মিউট্যান্ট নয়।
3. **টেস্ট DB নিজের নামে** — অন্যের DB-তে কখনো নয়। রানের মাঝে নিজের পরীক্ষার ফাইল বদলাবেন না।
4. **অন্যের জায়গায় অদ্ভুত লাল এলে** — নিজে সারাবেন না; নাম ধরে মালিক-সেশনকে জানান (বাকির সীমা বা সুইচের লাল → abos-13)।
5. **কমিট:** `git add -- <পথ>` আর `git commit --only -- <একই পথ>`, নতুন ফাইলও নাম ধরে; তারপর `git show --name-only HEAD` গুনে মেলানো। `public/build` কেউ কমিট করবেন না। হ্যাশ সমন্বয়কারীকে।
6. **পুশ, ডিপ্লয় আর লাইভ** — কেবল সমন্বয়কারী।
7. **শেয়ার করা ডেভ DB (`abos`)-এ `migrate`** — কেবল `--path` দিয়ে নিজের ফাইল, আগে `migrate:status` দেখে।
8. নতুন বড় কাজ ধরার আগে সমন্বয়কারীকে এক লাইনে জানানো; বার্তায় সই ট্যাবের নামে।

## ৩ · সমন্বয়কারীর সময়সূচি (abos-13)

| ক্রম | কাজ | কখন |
|---|---|---|
| ১ | সার্ভারের ৮টা মিউট্যান্ট | এখন চলছে |
| ২ | `CreditExposure`-এ কোম্পানির ছাঁকনি (abos-7d-র ধরা) → আবার চালানো · CSP পরীক্ষা | মিউট্যান্টের পরে |
| ৩ | ৩৬ ফাইলের একটাই কমিট → ক্লাসের পাহারা আবার (সবুজ দেখা) → `npm run build` → `public/build` কমিট | তারপর |
| ৪ | abos-67-এর যাচাই সবুজ হলে **ডিপ্লয়** — পুশ · লাইভ ব্যাকআপ · migrate · optimise · সব মেনু পাতা · পপ-আপ ও সুইচ হাতে দেখা · লাইভের `purchase.screen_requisitions` সারি | তারপর |
| ৫ | ভূমিকার বদল: জমার দাবি → কেবল হিসাবরক্ষক; বিক্রয়কর্মী → কেবল অর্ডার ও দেখা — ছাঁচে **ও** লাইভের পুরনো ভূমিকায় | ডিপ্লয়ের পরে |
| ৬ | সরাসরি বিক্রয়ের পর্দার নতুন কাজ (আটকে থাকা টাকা গ্রাহকের ঘরে · চালান নম্বর · অসমাপ্ত বিলের ড্রপডাউন · লট/জায়গার বাক্স · কার্টের পরে সীমা) | মালিকের ক/খ/গ উত্তরের পরে |
| ৭ | চেক খাতায় কেবল ক্লিয়ারের পরে · সীমা টুকরো করে বাড়ানোর ফাঁক · নির্ধারিত রিপোর্টের ফাঁস | তারপর |
| ৮ | লাইভে **Test Company Limited** — প্রতিটা ভূমিকার অ্যাকাউন্ট, নতুন ট্যাবে ব্যবসার পাঁচ ধাপ, এক ডকুমেন্টে রিপোর্ট | ডিপ্লয়ের পরে, নতুন ট্যাব খুললে |

⛔ **৫–৭-এর ফাইল সমন্বয়কারীর জন্য সংরক্ষিত:** `views/direct/partials/*`, `direct-sale.js`, `ChequeService`, আদায়ের সেবা, `CustomerService`, `ScheduledReportRunner`, প্রতিটা মডিউলের `role_templates`। অন্য কেউ ছোঁবেন না।

## ৪ · শেষ হওয়া কাজ (পুশ হয়েছে)

| সেশন | কাজ | কমিট |
|---|---|---|
| abos-c1 | Integration ও Auto Approval নকশা · পুরনো নথি ঠিক · বিক্রয়কর্মী-ডিলারের বাঁধনের নকশা | `790a62e1` `0bb707f0` `05c4443a` `72cb4aa4` `3895b52d` — **সেশন বন্ধ করা যায়** |
| abos-d3 | বিক্রয়ের চেকলিস্ট | `de052cdc` |
| abos-7d | ছাপার নমুনার দাবিতে বিলের নম্বর | `0205f5ff` |
| abos-af/67 | ক্রয়ের বাজেট নিয়ন্ত্রণ (যাচাই চলছে) · নিরাপত্তার নকশা | `982f7e58` `96932dbb` `0edac7ca` |
| abos-13 | ছাপার ৫০০-এর সারাই + পাহারা | `adc276a0` (লাইভে) |
