# প্রস্তাব — Integration Platform (বাইরের দিক) ও Auto Approval

**২৬ সেপ্টেম্বর ২০২৬** · abos-c1 · কেবল নকশা, কোনো কোড ছোঁয়া হয়নি।
চেকলিস্ট: `Checklist — Autonomous Enterprise OS…` ভাগ ২ (Integration) ও ভাগ ৪ (Auto Approval)।

> ⛔ মালিক হ্যাঁ না বলা পর্যন্ত কোড নয়। নিচের **§ছ · মালিকের সিদ্ধান্ত**
> অংশটাই এই কাগজের আসল প্রশ্ন।

চিহ্ন: ✅ আছে · 🟨 আংশিক · ⛔ নেই · ⚠️ বিপদ · ⓘ ব্যাখ্যা

---

# ভাগ ক · আজ কী আছে — মাপা, ফাইল:লাইন ধরে

## ক১ · অনুমোদন ইঞ্জিন

| জিনিস | কোথায় | কী করে |
|---|---|---|
| ছকের টাকার সীমা | `app/Models/ApprovalFlow.php:93` `appliesTo()` | ⓘ `threshold_amount`-এর **নিচে অনুমোদনই লাগে না** — কোনো সারি বসে না, কোনো চিহ্ন থাকে না |
| ছকের শর্ত | `ApprovalFlow.php:121` `catches()` | সীমা + সব শর্ত (AND) |
| অনুরোধ তৈরি | `app/Core/Engines/Approval/ApprovalEngine.php:74` `request()` | ছক না ধরলে `null` = *"এগোও"* (`:118`); নাহলে PENDING সারি (`:150`) |
| নিজের কাগজে নিজে সই | `ApprovalEngine.php:1005`, সীমা `approval.self_limit` (`:1093`) | ডিফল্ট শূন্য = কখনো নয় |
| কর্তৃত্বের সীমা | `AuthorityService.php:84` `allows()` | *"এই রোল এত টাকা পর্যন্ত সই দিতে পারে"* |
| কাগজের পাহারা | `DocumentApproval.php:50` `stopping()`, `:222` `assertClear()` | ১৬টা সেবা ডাকে; সই হয়ে থাকলে ছাপ মিলিয়ে ছাড়ে |
| সিদ্ধান্তের সারি | `2026_08_04_100400_…:93` | ⚠️ `approval_decisions.user_id` **NOT NULL** — প্রতিটা সিদ্ধান্ত একজন মানুষের |

⭐ **সৎ কথা:** *"এই সীমার নিচে নিজে থেকে পাশ"* আজ **অর্ধেক আছে** —
`threshold_amount` ঠিক এটাই করে। ⛔ কিন্তু নীরবে: সীমার নিচের কাগজ
অনুমোদনের কোনো খাতায় ওঠে না, তাই মালিক কোনোদিন দেখতে পান না **কী কী
নিজে থেকে পার হলো**, কত টাকার, কার হাত দিয়ে।

⓵ তাই Auto Approval মানে নতুন সীমা নয়, বরং: **একটা নিয়ম সই দেয়, সইটা খাতায়
থাকে, আর নিয়মটার দৈনিক সীমা থাকে।**

## ক২ · দেয়াল দুইটা — অনুমোদনের আগে, আজ ঠিক জায়গায়

| দেয়াল | কোথায় | অনুমোদনের আগে? |
|---|---|---|
| বাকির সীমা, চালান | `Sales/Services/DeliveryChallanService.php:232` `assertRoom()` → `:251` `assertClear()` | ✅ |
| বাকির সীমা, বিল | `SalesInvoiceService.php` (`confirm`, `:130`-এর আশেপাশে) | ✅ |
| সরাসরি বিক্রয়, আটকে থাকা | `DirectSaleService.php:367` `finishHeld()` → `:410` ও `:414`-এ আবার `confirm()` | ✅ দেয়াল আবার চলে |
| বাজেট | `Purchase/Services/PurchaseRequisitionService.php:114` → `:116` | ✅ |

⓵ অর্থাৎ ইঞ্জিনকে যে ডাকে, সে **দেয়াল পার হয়ে তবেই** ডাকে। Auto
Approval যদি ইঞ্জিনের **ভিতরে** বসে, তবে গঠনগতভাবেই দেয়ালের পরে পড়ে।

⚠️ **কিন্তু একটা দরজা দেয়ালটাকেই চওড়া করে:**
`Customer/Services/CustomerService.php:138` — বাকির সীমা **বাড়ানো** নিজেই
একটা অনুমোদন-কাজ (`customer.credit_limit`)। ⛔ এখানে নিজে-পাশ খাটলে
৪৯,০০০ করে দশবার বাড়িয়ে যেকোনো সীমা বানানো যেত — কোনো মানুষের সই ছাড়া।
এটা **নিষিদ্ধ তালিকায়** যাবে (§খ৪)।

🔎 **আজকের একটা ফাঁক, এই কাজের বাইরে (সমন্বয়কারীকে জানানো হচ্ছে):** ঐ
একই দরজায় আজই `threshold_amount` বসালে একই টুকরো-টুকরো কৌশল খাটে —
প্রতিটা বাড়ানো সীমার নিচে, তাই কোনোটাতেই সই লাগে না। ⓘ Customer
সমন্বয়কারীর এলাকা, তাই আমি হাত দিইনি।

## ক৩ · সরাসরি `request()` ডাকা তিনটা জায়গা — নিজে-পাশের ফাঁদ

| ডাকে | কোথায় | ফেরত মানকে কী ধরে |
|---|---|---|
| বিলের ছাড় | `SalesInvoiceService.php:182`, `:190` | `null` না হলে **আটকাও** |
| ভাউচার | `Accounts/Services/VoucherApproval.php:185` | ফেরত মান সোজা `stopping()` থেকে — `null` না হলে আটকাও |
| উত্তোলন | `Finance/Services/WithdrawalService.php:118` | ফেরত মান দেখে না |

⚠️ নিজে-পাশ করা সারি ফেরত দিলে প্রথম দুইটা ওটাকে *"আটকে আছে"* পড়ত —
কাগজ নিজে পাশ হতো, আর পর্দা বলত সইয়ের অপেক্ষায়। ⭐ **তাই চুক্তি অক্ষত
থাকবে: নিজে-পাশ হলে `request()` `null` ফেরায়** (§খ২)।

## ক৪ · কিউ — চেকলিস্টের কথাটা আর সত্য নয়

| কী বলা আছে | কোথায় | বাস্তবতা |
|---|---|---|
| *"এখনো কিউ নেই, ইচ্ছাকৃতভাবে"* | `app/Core/Events/EventRegistry.php:26-32` | ⚠️ পুরনো |
| *"ABOS কিছুই কিউ করে না"* | `docs/স্থাপনা — cPanel…:226` | ⚠️ পুরনো |
| `queue:work --stop-when-empty --max-time=50 --tries=3`, প্রতি মিনিটে | `routes/console.php:222` (১৭ সেপ্টেম্বর) | ✅ চলে |
| কেউ কি কিউতে কাজ দেয়? | `ShouldQueue` খুঁজে | ⛔ **একটাও না** (`NewsByMail.php:32`-এ কেবল মন্তব্য) |

⓵ অর্থাৎ worker আছে, কাজ নেই। cron (প্রতি মিনিটে `schedule:run`) হলো
**একমাত্র চলমান অংশ**, আর সেটা ব্যাকআপের জন্য এমনিতেই বাঁচিয়ে রাখতে হয়।

## ক৫ · বাইরের দিকের ভিত

| জিনিস | কোথায় | অবস্থা |
|---|---|---|
| API টোকেন (Sanctum, `abilities`) | `routes/api.php:57`, `:95` | ✅ — কিন্তু **মানুষের** টোকেন, অংশীদারের নয় |
| ফোনের সিঙ্ক | `app/Core/Engines/Sync/*`, `routes/api.php:155` | ✅ ভেতরমুখী; `unique(device_id, change_id)` দিয়ে দুইবার-বসা ঠেকায় — ⭐ webhook-এর জন্য অবিকল এই ছাঁচ |
| বাইরে HTTP কল | `Http::` / Guzzle খুঁজে | ⛔ **একটাও নেই** |
| webhook গ্রহণ | — | ⛔ |
| ব্যাংক মিলকরণ | `Accounts/Services/BankReconciliationService.php` | ✅ হাতে টিক; ⛔ ব্যাংকের বিবরণীর সারি ঢোকানোর পথ নেই |
| কুরিয়ার | — | ⛔ |
| শেয়ার্ড হোস্টিং | `docs/স্থাপনা — cPanel…` | ⚠️ দীর্ঘ প্রক্রিয়া চলে না; cron আছে |
| পরীক্ষার বাক্স (ম্যাক মিনি) | মেমরি, ২৬ সেপ্টেম্বর | ⚠️ বাইরে থেকে পৌঁছানো যায় না — **webhook ওখানে কোনোদিন আসবে না** |

---

# ভাগ খ · Auto Approval — নকশা

## খ১ · এক বাক্যে

⭐ ছকের একটা ধাপে মালিক একটা **নিয়ম** বসান — *"৫,০০০ টাকার নিচে, আর এই
কর্মীর আজকের মোট ২০,০০০ ছাড়ায়নি, তাহলে ধাপ ১ নিয়মটাই সই দেবে"*। নিয়মটা
সই দিলে সেটা ঠিক মানুষের সইয়ের মতো খাতায় ওঠে, শুধু নামের জায়গায় নিয়মের
সংকেত।

## খ২ · কোথায় বসবে — একটা জায়গায়

`ApprovalEngine::request()`, **`catches()`-এর পরে, `Approval::create()`-এর আগে**
(`ApprovalEngine.php:118` আর `:150`-এর মাঝখানে)।

- নিয়ম মিললে: একটা APPROVED সারি (`state_hash` সহ, তাই পরে কাগজ বদলালে
  পুরনো সই আর খাটে না — `DocumentApproval.php:119`-এর নিয়মটাই) আর একটা
  সিদ্ধান্তের সারি `auto_rule_id` সহ। তারপর **`null` ফেরত** — তিন ডাকা
  জায়গার চুক্তি অক্ষত (§ক৩)।
- কয়েক ধাপের ছকে নিয়ম কেবল **নিজের ধাপটা** সই দেয়; পরের ধাপ মানুষের,
  আর তখন PENDING সারিই ফেরে (`current_level = 2`)।
- ⛔ মডিউলে একটা লাইনও বদলায় না। নিয়ম ১৬টা ডাকা জায়গায় আলাদা করে বসালে
  একদিন একটা ভুলে যেত।

## খ৩ · দেয়াল কখনো পাশ কাটাবে না — কীভাবে প্রমাণ হবে

⓵ গঠন: নিয়মটা ইঞ্জিনের ভিতরে, আর ইঞ্জিন ডাকা হয় দেয়ালের পরে (§ক২)।
⚠️ কিন্তু *গঠন* একটা দাবি, প্রমাণ নয়। তাই:

1. **দাবি, আসল দরজায়:** ৫০,০০০ সীমার ক্রেতা, ৮৯,৭২০ টাকার চালান, আর একটা
   নিয়ম যেটা *সব অঙ্ক* নিজে পাশ করে → চালান **ফিরে আসে বাকির সীমার
   বার্তা নিয়ে**, আর `approvals`-এ **একটাও সারি বসে না**। বাজেটের জন্য একই।
2. **মিউটেশন:** `assertRoom()` লাইনটা সরিয়ে দিলে দাবি ১ লাল হতে হবে।
3. **স্থাপত্য-পাহারা:** যে সেবাগুলো `assertRoom`/`assertWithinBudget` ডাকে,
   সেই মেথডে ঐ ডাক `assertClear`-এর **আগে** থাকতে হবে। ⚠️ পাহারাটা
   ইচ্ছে করে ভেঙে দেখা হবে যে সে সত্যিই কিছু খুঁজে পায়।
4. ইঞ্জিনের বাইরে কেউ `Approval::APPROVED` লিখতে পারবে না — স্থাপত্য-পাহারা।

## খ৪ · নিষিদ্ধ তালিকা — কোডে, সেটিংসে নয়

একটা `public const` (সেটিংস নয়, কারণ সেটিংস পর্দা থেকে বদলানো যায়):

| কাজ | কেন |
|---|---|
| `customer.credit_limit` | ⛔ দেয়াল চওড়া করার দরজা (§ক২) — মালিকের নিয়ম, আলোচনার বাইরে |
| বাজেটের অঙ্ক বদলানো (যদি অনুমোদনে যায়) | ⛔ একই কারণ |
| বাকিগুলো | কোম্পানি নিজে বেছে নেবে, ডিফল্ট বন্ধ (§গ৬) |

ছকের পর্দায় ঐ কাজগুলোর জন্য নিয়ম বসানোর ঘরটাই দেখাবে না, আর সেবা স্তরেও
আটকাবে (পর্দা একটা দরজা, সেবা আসল দরজা)।

## খ৫ · টুকরো করার কৌশল ঠেকানো

⚠️ *"৫,০০০-এর নিচে নিজে পাশ"* শুনলে প্রথম কৌশল: ২০,০০০-এর একটা কাগজকে
চারটা ৪,৯৯৯-এ ভাঙা। তাই প্রতিটা নিয়মে বাধ্যতামূলক **দৈনিক সীমা**:

- একজন অনুরোধকারীর, এই কাজে, আজ নিজে-পাশ হওয়া মোট
- একই পক্ষের (ক্রেতা/সরবরাহকারী), এই কাজে, আজ নিজে-পাশ হওয়া মোট

⓵ মোট ছাড়ালে নিয়ম চুপ করে সরে যায় আর কাগজ মানুষের কাছে যায় — কাজ থামে
না, কেবল সই লাগে।

## খ৬ · ছক (যোগমূলক)

- `approval_auto_rules`: `approval_flow_id`, `level`, `max_amount`,
  `daily_cap_per_requester`, `daily_cap_per_party`, `is_active`, সংকেত
  (`AR001`), নিরীক্ষা
- শর্ত: আজকের `ApprovalCondition` আবার ব্যবহার — নতুন শর্ত-ভাষা নয়
- `approval_decisions`: `user_id` nullable, নতুন `auto_rule_id` nullable;
  ⚠️ *"ঠিক একটা ভরা"* শর্ত — MariaDB (লাইভ) ও MySQL ৮.৪ (লোকাল) দুইটাতেই
  চালিয়ে দেখা হবে
- কোম্পানি-ব্যাপী সুইচ `approval.auto_enabled`, ডিফল্ট **বন্ধ** — নিয়ম না
  বসালে আজকের আচরণ অবিকল

## খ৭ · মালিক যা দেখবেন

- অনুমোদনের রিপোর্টে নতুন পাতা: **"নিজে পাশ হওয়া কাগজ"** — নিয়ম, অঙ্ক, কে চেয়েছিলেন
- ইনবক্সের যাত্রাপথে (`timeline`) সইয়ের জায়গায় *"নিয়ম AR003"*
- ধাপের সইকারীকে দিনে একবার সারসংক্ষেপ (Notice Center দিয়ে)
- ⓘ সবই `Approval` মডিউলে — SystemAdmin, Sales, Customer ছোঁয়া লাগে না

---

# ভাগ গ · Integration Platform — নকশা

## গ১ · কেন্দ্রের প্রশ্ন: worker ছাড়া নির্ভরযোগ্যতা

⭐ **উত্তর: বাইরের কল কখনো ব্যবহারকারীর অনুরোধের ভিতরে হবে না। অনুরোধ কেবল
একটা সারি লেখে; cron সারিটা পাঠায়।** (নাম: transactional outbox)

```
ব্যবহারকারী "চালান নিশ্চিত" চাপেন
  └─ একই DB লেনদেনে: চালান নিশ্চিত + integration_outbox-এ একটা সারি
       (লেনদেন ফিরলে সারিও ফেরে — ভূত বার্তা যায় না;
        লেনদেন টিকলে সারিও টেকে — বার্তা হারায় না)

প্রতি মিনিটে cron → abos:integrations-due
  └─ যে সারির next_attempt_at পেরিয়েছে, সর্বোচ্চ ৪০ সেকেন্ড ধরে
       ├─ সফল → sent, উত্তরটা জমা
       ├─ সাময়িক ব্যর্থ (৫xx, সময় ফুরানো) → attempts+1, পরের চেষ্টা ১·৫·৩০ মিনিট·২·১২ ঘণ্টা পরে
       └─ স্থায়ী ব্যর্থ (৪xx) বা ৬ বার ব্যর্থ → dead, Notice Center-এ খবর
```

**কেন Laravel-এর `jobs` টেবিল নয়, যদিও worker আছে (§ক৪):**

| | `jobs` | নিজের outbox |
|---|---|---|
| সফল হলে | ⛔ সারি মুছে যায় — *"কী পাঠানো হয়েছিল"* উত্তর নেই | ✅ থাকে, উত্তরসহ |
| ব্যর্থ হলে | ⛔ `failed_jobs` — যে টেবিল কেউ খোলে না (ব্যাকআপের ছয় দিনের শিক্ষা) | ✅ পর্দায়, অংশীদার ধরে, *"আবার চেষ্টা"* বোতাম |
| ডিপ্লয়ের পর | ⚠️ জমা কাজে পুরনো ক্লাসের serialize করা রূপ | ✅ কেবল ডেটা (JSON) |
| একই লেনদেনে লেখা | ⚠️ `afterCommit` মনে রাখতে হয় | ✅ সাধারণ সারি |

⚠️ **cron নিজেই মরে গেলে?** `onFailure` কোনোদিন ডাকা হয় না — ব্যাকআপের
ঠিক সেই শিক্ষা (`routes/console.php:138-158`)। ⭐ তাই `abos:backup-fresh`-এর
যমজ একটা পাহারা: **সবচেয়ে পুরনো অপেক্ষমাণ সারি ৩০ মিনিটের বেশি পুরনো হলে**
`logger()->critical()` আর পর্দার উপরে লাল দাগ। পাহারাটা কমান্ডের দাবি
দেখে না, **সারির বয়স** দেখে।

⚠️ **দুইবার পাঠানো:** cron মাঝপথে মরলে একটা কল হয়তো গেছে কিন্তু *sent*
লেখা হয়নি। ⭐ প্রতিটা সারির একটা স্থায়ী `idempotency_key` (সারির UUID)
প্রতিটা চেষ্টায় একই যায়; অংশীদার সেটা মানলে দ্বিতীয়টা উপেক্ষিত হয়।
যে অংশীদার মানে না, তার জন্য পাঠানোর আগে *"এটা কি আগেই আছে?"* জিজ্ঞেস
(যেমন কুরিয়ারে আমাদের চালান-নম্বর দিয়ে খোঁজা)।

## গ২ · ভেতরমুখী (webhook)

`POST api/v1/hooks/{partner}` — ⓘ `api/*`-এর নিচে, তাই ভুলের উত্তর JSON, ৩০২ নয়।

1. স্বাক্ষর যাচাই (HMAC, অংশীদারের গোপন চাবি) — না মিললে ৪০১, কিছুই লেখা হয় না
2. কাঁচা দেহটা `integration_inbox`-এ, `unique(partner, external_id)` —
   ⭐ `sync_changes`-এর অবিকল ছাঁচ; একই ঘটনা দুইবার এলে দ্বিতীয়টা ২০০ পায় আর কিছু করে না
3. সাথে সাথে ২০০ — প্রক্রিয়া পরে, একই cron-এ

⛔ **webhook কখনো খাতায় সরাসরি লেখে না।** কুরিয়ারের *"পৌঁছেছে"* কেবল
চালানের বিতরণ-অবস্থা বদলায়; কুরিয়ারের টাকা-জমা (COD) একটা **খসড়া** আদায়
বানায়, যেটা আজকের দরজা দিয়েই (অনুমোদনসহ) বসে। ব্যাংকের সারি কেবল
মিলকরণের **প্রস্তাব** দেয় — মিলকরণ খাতা বদলায় না (`Plan — ব্যাংক মিলকরণ.md`-এর অটল নিয়ম)।

⚠️ **webhook হারাতে পারে** (অংশীদারের দিকে ভুল, আমাদের সার্ভার বন্ধ)। তাই
প্রতিটা সংযোগে একটা **টেনে আনা** পথও থাকবে (প্রতি ঘণ্টায় *"গত দুই ঘণ্টায়
কী বদলেছে?"*) — webhook দ্রুততা দেয়, টেনে আনা নিশ্চয়তা দেয়।
ⓘ ম্যাক মিনিতে webhook পৌঁছায় না, তাই ওখানে কেবল টেনে আনাটাই পরীক্ষা হবে।

## গ৩ · বাইরের টোকেন

- `integration_connections`: কোম্পানি ধরে, অংশীদার ধরে; চাবি ও গোপন
  শব্দ **encrypted cast** — ⛔ `.env`-এ নয়, কারণ একাধিক কোম্পানির আলাদা হিসাব
- মেয়াদ থাকলে নবায়ন cron-এ, মেয়াদ ফুরানোর আগে; ব্যর্থ হলে সংযোগটা
  *"বিচ্ছিন্ন"* আর Notice Center-এ খবর
- পর্দায় চাবি কখনো পুরো দেখায় না, শেষ চার অক্ষর মাত্র
- ভেতরমুখী দিকে অংশীদারের জন্য নিজের Sanctum টোকেন, একটাই ability
  (`hooks:<partner>`) — মানুষের টোকেনের সাথে মেশে না

## গ৪ · অংশীদার-সংযোজক (adapter)

একটা চুক্তি (`IntegrationAdapter`): `send(message)`, `verify(request)`,
`parse(inbox)`, `poll(since)`। প্রতিটা অংশীদার একটা ক্লাস, **নিজের মডিউলে**
(কুরিয়ার Sales-এ বা নতুন `logistics` মডিউলে, ব্যাংক Accounts-এ), আর কোর কোনো
অংশীদারের নাম জানে না (§১৯.৭)। নিবন্ধন `module.php`-এর ঘোষণায়।

## গ৫ · শেয়ার্ড হোস্টিং-এর সীমা

- এক রানে সর্বোচ্চ ৪০ সেকেন্ড, প্রতি কলে ১০ সেকেন্ডের সময়সীমা → এক মিনিটে
  বড়জোর ~৩০টা কল। ⓘ একটা ডিপোর দিনে শ-খানেক চালান, যথেষ্ট।
- ⚠️ কিছু হোস্ট বাইরে যাওয়া সংযোগ আটকায়। **কোড লেখার আগে** লাইভে একটা
  এক-লাইনের পরীক্ষা: অংশীদারের সার্ভারে HTTPS কল যায় কি না।

## গ৬ · ⭐ প্রতিটা কোম্পানি নিজের পছন্দে — মালিকের নির্দেশ, ২৬ সেপ্টেম্বর ২০২৬

> *"সফটওয়্যারটা তৈরি হচ্ছে মাল্টি সিস্টেম ব্যবসায়ীদের জন্য, এটা বিক্রয় হবে,
> যার যেমন পছন্দ তেমনভাবে কাজ করবে।"*

⓵ অর্থাৎ *"কোন ব্যাংক, কোন কুরিয়ার, কত টাকা"* মালিক একবার ঠিক করে দেবেন না।
**প্রতিটা ক্রেতা-কোম্পানি নিজের পর্দায় বেছে নেবে।** নকশায় তার মানে:

- **একাধিক সংযোজক পাশাপাশি।** একটা কোম্পানি একসাথে দুইটা কুরিয়ার চালাতে পারে।
  প্রতিটা চালানে বেছে নেওয়া যাবে কোন কুরিয়ারে যাবে (ডিফল্ট কোম্পানির সেটিং থেকে)।
- **কোনো সংযোগ না থাকাও একটা বৈধ পছন্দ।** যে কোম্পানি নিজের গাড়িতে মাল পাঠায়,
  তার পর্দায় কুরিয়ারের কিছুই দেখাবে না, আর কোনো কাজ বদলাবে না।
- **ব্যাংক বাছাই কোডে থাকবে না।** বিবরণীর ফাইল ঢোকানোর পর্দায় একটা **কলাম-মিলানোর
  ছাঁচ** থাকবে (কোন কলামে তারিখ, কোনটায় জমা, কোনটায় খরচ, কোনটায় বিবরণ)। কোম্পানি
  প্রতিটা ব্যাংকের ছাঁচ একবার বানিয়ে রেখে দেবে। ⓘ চেনা ব্যাংকগুলোর ছাঁচ আগে থেকে
  বানানো থাকতে পারে, কিন্তু সেগুলো শুধু সুবিধা, শর্ত নয়।
- **নিজে-পাশের সংখ্যা, কোন কাজে চলবে, দৈনিক সীমা — সব কোম্পানির ছকে।**
  ডিফল্ট **বন্ধ**। নতুন কোম্পানি কিছু না বসালে আচরণ আজকের মতোই থাকবে।
- ⛔ **ব্যতিক্রম একটাই: বাকির সীমা বাড়ানো নিজে-পাশ হবে না, কোনো কোম্পানিতেই।**
  কারণ বাকির সীমার দেয়ালটা আজ কোডে সবার জন্য এক (মালিকের নিয়ম)। নিজে-পাশ ঐ
  দেয়ালকে চুপচাপ চওড়া করার পথ হয়ে যেত।
- ⚠️ নতুন সংযোগ বা নিয়ম পুরনো কোম্পানিতে নিজে থেকে চালু হবে না। এটা রোলের
  ছাঁচের নিয়মের মতোই: নতুন জিনিস ইচ্ছে করে চালু করতে হয়।

---

# ভাগ ঘ · ক্রম

| ধাপ | কী | কেন এই ক্রমে |
|---|---|---|
| ১ | **Auto Approval** (খ২–খ৭) | ছোট, পুরোটা ঘরের ভিতরে, বাইরের কারো অপেক্ষা নেই; Level 3-এর একটা বাক্স টিক |
| ২ | Integration-এর ভিত: outbox · inbox · সংযোগ · cron · বয়সের পাহারা · পর্দা | অংশীদার ছাড়াই পরীক্ষা করা যায় (নকল adapter দিয়ে) |
| ৩ | **ব্যাংক — বিবরণীর ফাইল** (CSV/Excel), কোম্পানির নিজের কলাম-ছাঁচ দিয়ে → মিলকরণের প্রস্তাব | ⓘ যেকোনো ব্যাংকে চলে, কোনো ব্যাংক বাছতে হয় না; মিলকরণ (✅ আছে) সাথে সাথে কাজে লাগে |
| ৪ | **কুরিয়ার সংযোজক**, একটা একটা করে — পার্সেল বানানো, অবস্থা টেনে আনা + webhook, COD খসড়া আদায় | কোনটা আগে, সেটা বাজারের চাহিদা ধরে (§ছ) |
| ৫ | ব্যাংকের API, যে ব্যাংক দেয় | ধাপ ৩-এর উপরেই, কেবল উৎস বদলায় |
| ৬ | সরকারি পোর্টাল | ⚠️ আগে জানতে হবে কোনটার সত্যিই API আছে — অনেকগুলো কেবল ফাইল নেয়; তখন কাজটা *"ঠিক ছাঁচের ফাইল রপ্তানি"* |

⓵ প্রতিটা ধাপ আলাদা কমিট, আর প্রতিটা ধাপ শেষে ৮ ধাপের ছাঁকনি (মেমরি:
enterprise-grade-is-the-floor) পুরোটা।

---

# ভাগ ঙ · ঝুঁকি

| ঝুঁকি | কী হতো | প্রতিকার |
|---|---|---|
| নিজে-পাশ দেয়ালের আগে বসে | ⛔ সীমা পার করা বিল পাশ | ইঞ্জিনের ভিতরে একমাত্র জায়গা + দাবি + মিউটেশন (§খ৩) |
| বাকির সীমা বাড়ানো নিজে-পাশ | ⛔ দেয়াল নিজে থেকে চওড়া | নিষিদ্ধ তালিকা, কোডে (§খ৪) |
| টুকরো করা | বড় কাগজ ছোট ছোট হয়ে পার | দৈনিক সীমা, বাধ্যতামূলক (§খ৫) |
| `request()`-এর চুক্তি ভাঙা | পাশ হওয়া কাগজ *"অপেক্ষায়"* দেখায় | নিজে-পাশে `null` ফেরে (§ক৩) |
| cron মরে যায় | বার্তা চুপচাপ জমে | সারির বয়স দেখা পাহারা (§গ১) |
| একই বার্তা দুইবার | দুইটা পার্সেল, দুইবার টাকা | idempotency key + আগে-আছে-কি জিজ্ঞাসা |
| webhook জাল | ভুয়া *"টাকা পৌঁছেছে"* | HMAC; আর webhook কখনো খাতায় লেখে না |
| চাবি ফাঁস | অংশীদারের হিসাব দখল | encrypted cast; পর্দায় শেষ চার অক্ষর |
| পরীক্ষার বাক্সে webhook আসে না | ⚠️ *"পরীক্ষা সবুজ"*, অথচ পথটা কেউ চালায়নি | টেনে আনা পথ আলাদা করে পরীক্ষা; webhook লাইভে ডেমো ডেটায় |
| পুরনো মন্তব্য | পরের কেউ ভাবে কিউ নেই, বা আছে বলে `ShouldQueue` বসায় | ধাপ ২-এ `EventRegistry.php:26-32` আর cPanel কাগজের `:226` সংশোধন |

---

# ভাগ চ · যা আমি ছুঁইনি, আর কেন

- Sales, Customer, SystemAdmin — সমন্বয়কারীর বাকির সীমার কাজ চলছে। ⓘ ভালো
  খবর: পুরো নকশায় ঐ তিনটার একটা ফাইলও বদলাতে হয় না (Auto Approval থাকে
  `Core/Engines/Approval` আর `Modules/Approval`-এ)। ⚠️ ব্যতিক্রম কুরিয়ার (ধাপ ৪)
  — চালানের বিতরণ-অবস্থা Sales-এর; ততদিনে ঐ কাজ শেষ হওয়ার কথা, তবু ধরার আগে
  জিজ্ঞেস করব।
- AI — বাদ (২৪ সেপ্টেম্বর)। নিজে-পাশ একটা নিয়ম, আন্দাজ নয়।

---

# ভাগ ছ · মালিকের সিদ্ধান্ত লাগবে

ⓘ ২৬ সেপ্টেম্বর মালিক বললেন সফটওয়্যারটা অনেক ব্যবসায়ীর কাছে বিক্রি হবে, আর
প্রত্যেকে নিজের পছন্দে চালাবে (§গ৬)। তাই আগের *"কোন ব্যাংক, কোন কুরিয়ার, কত
টাকা, কোন কাজে"* প্রশ্নগুলো **উঠে গেল**। ওগুলো এখন প্রতিটা কোম্পানির নিজের সেটিং।
বাকি থাকল কেবল পণ্যের প্রশ্ন:

❓ **১ · বাকির সীমা বাড়ানোয় নিজে-পাশ সব কোম্পানিতেই বন্ধ থাকবে তো?**
⭐ আমার প্রস্তাব: হ্যাঁ, কারণ দেয়ালটা আজ সবার জন্য এক। কোনো ক্রেতা-কোম্পানি
চাইলেও খুলবে না।

❓ **২ · আজকের `threshold_amount` কী হবে?** আজ ঐ সীমার নিচের কাগজ কোনো চিহ্ন
ছাড়াই পার হয়।
- ক · যেমন আছে থাকুক, নিজে-পাশ আলাদা জিনিস
- খ · ⭐ (আমার প্রস্তাব) দুইটাই থাকবে, কোম্পানি বেছে নেবে। তবে ছকের পর্দায় লেখা
  থাকবে যে সীমার নিচের কাগজ খাতায় ওঠে না, আর নিজে-পাশের কাগজ ওঠে।

❓ **৩ · কোন কুরিয়ার আগে বানানো হবে?** সবগুলো একসাথে হবে না। প্রতিটার আলাদা API,
আলাদা পরীক্ষা লাগে। আপনার ক্রেতারা বেশি কোনটা ব্যবহার করেন? (যেমন Steadfast,
Pathao, RedX, Paperfly)

❓ **৪ · সরকারি পোর্টাল — কোনটা আগে?** VAT (মূসক রিটার্ন), আয়কর, না অন্য কিছু?
ⓘ উত্তর না জেনে এখানে কিছু বানালে সেটা সাজসজ্জা হবে।

❓ **৫ · ক্রম ঠিক আছে?** আগে নিজে-পাশ (ছোট, সব ঘরের ভিতরে), তারপর বাইরের দিক।
