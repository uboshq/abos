# Checklist — Approval & Workflow Management

> মালিকের নির্দেশ, ২৪ সেপ্টেম্বর ২০২৬: *"ki ki ache dekhe naw tar por plan
> onuzayi layoute soho impliment korar jonno ekta cheeklist banaw tar por kaj
> doro, tate subidha hobe amar mone hoy."*
>
> **এই ফাইলটাই কাজের সারি।** উপর থেকে নিচে, একটার পর একটা। মালিককে কিছু
> জিজ্ঞেস করার দরকার নেই — সিদ্ধান্তের টেবিলে যা লেখা আছে সেটাই উত্তর।
> একটা ধাপ শেষ হলে বাক্সে টিক দিন, আর *"যেখানে থামা হয়েছে"* লাইনটা বদলান।

**যেখানে থামা হয়েছে:** ধাপ ১ শুরু হবে (সিদ্ধান্ত পাঁচটাই নেওয়া হয়ে গেছে)

---

## ⚠️ শুরু করার আগে — চারটা নিয়ম

**১ · অনুমোদন কোনো মডিউলের অংশ নয়।** ইঞ্জিনটা কেন্দ্রীয়
(`app/Core/Engines/Approval/`), আর মডিউলগুলো কেবল *"এই কাগজটা কি আটকাবে?"*
জিজ্ঞেস করে। ⛔ নতুন কোনো নিয়ম কোনো মডিউলের সার্ভিসে লেখা যাবে না — লিখলে
পরের মডিউলে সেটা আবার লিখতে হবে, আর একদিন দুইটা আলাদা হবে।

**২ · সামনের দিকের সুইচ নিরাপত্তা নয়।** প্রতিটা `approve`/`reject` পথে
backend-এ অনুমতি ও কর্তৃত্ব দুইটাই মিলিয়ে দেখতে হবে। ⓘ আজ ওটা আছে
(`assertCanDecide()`), আর নতুন কোনো পথ যেন ওটা এড়িয়ে না যায়।

**৩ · প্রতিটা দাবিকে বিপজ্জনক ইনপুট খাওয়াতে হবে।** *"অনুরোধকারী নিজে
অনুমোদন করতে পারেন না"* দাবিটা লিখলে সত্যিই অনুরোধকারীকে দিয়ে চেষ্টা করাতে
হবে। ⛔ নাহলে দাবিটা খালি জালে মাছ খোঁজে।

**৪ · অনুমোদনের সিদ্ধান্ত মোছা বা বদলানো যাবে না।** `approval_decisions`
আজ কেবল `create()` দিয়ে লেখা হয় — কোনো `update`/`delete` পথ নেই। ⚠️ সেটা
দুর্ঘটনাক্রমে, নকশায় নয়। ধাপ ৮-এ ওটা নিয়মে বদলানো হবে।

---

## ✅ যা আগে থেকেই আছে — আবার বানানো নিষেধ

| স্পেক যা চায় | ABOS-এ কোথায় | অবস্থা |
|---|---|---|
| কেন্দ্রীয় অনুমোদন ইঞ্জিন | `Core/Engines/Approval/ApprovalEngine.php` | ✅ |
| মডিউল-নিরপেক্ষ জিজ্ঞাসা | `DocumentApproval::assertClear()` | ✅ |
| ধাপে ধাপে অনুমোদন (sequential) | `ApprovalEngine::approve()` — `current_level` এগোয় | ✅ |
| এক ধাপে সবার সই (all-must) | `approval_flow_steps.requires_all` | ✅ |
| অনুরোধকারী ≠ অনুমোদনকারী | `ApprovalEngine::canDecide()` + `approval.self_limit` | ✅ |
| Backend কর্তৃত্ব যাচাই | `assertCanDecide()` + `can:approval.decide` | ✅ |
| টাকার সীমায় অনুমোদন লাগবে কি না | `approval_flows.threshold_amount` | ✅ |
| প্রবাহের নিজের কোড ও নাম | `approval_flows.code` · `steps.step_name` | ✅ |
| ইনবক্স · আমার অনুরোধ · বিস্তারিত পর্দা | `Approval/.../inbox/{index,mine,show}.blade.php` | ✅ |
| প্রবাহের তালিকা ও সম্পাদক | `flow/{index,form}.blade.php` | ✅ |
| কোথায় অনুমোদন বসানো নেই | `flow/coverage.blade.php` | ✅ |
| চারটা রিপোর্ট | `Approval/Reports/ApprovalReports.php` | ✅ |
| কাগজ অন্যকে দেওয়া (ad-hoc) | `ApprovalEngine::forward()` → `approvals.assigned_to` | ✅ |
| অঙ্ক বদলালে সই বাতিল | `Approval::covers()` + `DocumentApproval::stopping()` | ✅ |
| মোবাইলে ইনবক্স | `mobile/lib/features/approvals/` | ✅ |
| **২৬টা কাজ ইতিমধ্যে অনুমোদনে যায়** | ৮টা মডিউলের `module.php` → `'approvals'` | ✅ |

⛔ **এই তালিকার কিছু আবার লেখা মানে দুইটা সত্য তৈরি করা।**

---

## ⛔ কে কোথায় হাত দেবেন না

২৪ সেপ্টেম্বর সকালে **এই আটটা ফাইল অন্য একজনের হাতে** (staged+unstaged):

```
app/Models/ApprovalFlow.php · ApprovalFlowStep.php
Approval/Http/Controllers/ApprovalFlowController.php
Approval/Http/Requests/ApprovalFlowRequest.php
Approval/Resources/views/flow/{form,index}.blade.php
Approval/Services/ApprovalFlowService.php
Approval/module.php
```

⚠️ ধাপ ৩ · ৪ · ৯ ঐ ফাইলগুলো ছোঁয়। **শুরুর আগে সমন্বয়কের কাছে জেনে নিন
কাজটা ছাড়া হয়েছে কি না** ([[sessions-share-one-working-tree]])।

---

## ধাপ ০ · মালিকের সিদ্ধান্ত — ✅ পাঁচটাই নেওয়া হয়েছে

> ২৪ সেপ্টেম্বর ২০২৬। ⛔ **এই টেবিলের কোনো প্রশ্ন আর জিজ্ঞেস করা যাবে না।**

| # | প্রশ্ন | মালিকের উত্তর | কোন ধাপে খাটে |
|---|---|---|---|
| ১ | নিজে অনুমোদনের সীমা | **৫,০০০ টাকাই থাকবে** | ৪ |
| ২ | SLA কত সময় | **প্রতিটা ধাপে আলাদা করে বসানো যাবে**, ডিফল্ট ৪ ঘণ্টায় মনে করানো · ২৪ ঘণ্টায় উপরে | ১ |
| ৩ | দেরি হলে কার কাছে | **প্রবাহে ঠিক করা একজন** — পরের ধাপের জন নয় | ১ |
| ৪ | সই কখন বাতিল | **যেকোনো ঘর বদলালেই** | ৬ |
| ৫ | bulk কোথায় নিষিদ্ধ | **টাকা নড়ার কাজে** | ৭ |

⚠️ **৩ আর ৪ নম্বর দুইটাই কড়া দিক বেছেছেন, আর দুইটারই দাম আছে:**

ⓘ ৩ · প্রতিটা ধাপে *"দেরি হলে কার কাছে"* আলাদা করে বসাতে হবে — সেটা না
বসালে কাগজটা কোথাও যাবে না। ⛔ তাই ধাপ ১-এ একটা দাবি থাকবে: **যে ধাপে
SLA বসানো আছে অথচ গন্তব্য নেই, সেটা ধরা পড়বে** — নাহলে সুইচটা বসিয়ে
মালিক ভাবতেন কাজ হচ্ছে, আর কাগজ চুপচাপ পড়ে থাকত।

ⓘ ৪ · *"যেকোনো ঘর"* মানে মন্তব্য বা সংযুক্তি বদলালেও নতুন সই লাগবে।
⚠️ এতে কাজ বাড়বে, কিন্তু সিদ্ধান্তটা মালিকের আর কারণটা পরিষ্কার: আজ অঙ্ক
ঠিক রেখে **পণ্য বদলে দিলে পুরনো সইটাই চলে**। ⓘ ছয় মাস পরে কেউ যদি বলেন
"মন্তব্য বদলালে তো লাগার কথা নয়", তখন ধাপ ৬-এর ঘোষণার তালিকা থেকে ঘরটা
বাদ দেওয়া এক লাইনের কাজ — কিন্তু **ঢিলে থেকে কড়া করা** অনেক কঠিন।

- [x] পাঁচটার উত্তর নেওয়া, আর এই টেবিলে লিখে রাখা

---

## ধাপ ১ · কাগজ কত দিন পড়ে আছে, আর দেরি হলে কী

**কেন এটা আগে:** আজ একটা অনুরোধ **চিরকাল** পড়ে থাকতে পারে। ⛔ কেউ জানে না
কোনটা দেরি হচ্ছে, কেউ মনে করায় না, আর কারও কাছে যায় না। রিপোর্টে
`waiting_days` দেখা যায় — কিন্তু **দেখতে হলে কাউকে গিয়ে দেখতে হয়**।

- [ ] মাইগ্রেশন: `approval_flow_steps`-এ `sla_hours` · `warn_hours` · `escalate_hours` · `escalate_to_type` · `escalate_to_id`
- [ ] ডিফল্ট: মনে করানো ৪ ঘণ্টা, উপরে পাঠানো ২৪ ঘণ্টা — প্রতিটা ধাপে বদলানো যাবে (সিদ্ধান্ত ২)
- [ ] ⛔ গন্তব্য **প্রবাহে বসানো একজন**, পরের ধাপের জন নয় (সিদ্ধান্ত ৩)
- [ ] মাইগ্রেশন: `approvals`-এ `due_at` · `escalated_at` · `escalated_to` · `reminded_at`
- [ ] `ApprovalEngine::request()` ধাপের SLA থেকে `due_at` বসাবে
- [ ] ধাপ এগোলে (`approve()`) পরের ধাপের `due_at` নতুন করে বসবে
- [ ] `ApprovalSla` সার্ভিস — অবস্থা: সময়ের ভিতরে · কাছাকাছি · পার · উপরে পাঠানো
- [ ] `abos:approvals-escalate` কমান্ড, `routes/console.php`-এ প্রতি ঘণ্টায়
- [ ] মনে করানোর বার্তা — `NotificationService` দিয়ে, নতুন কোনো পথ নয়
- [ ] ইনবক্সে SLA-র চিহ্ন, আর *"দেরি"* ধরে ছাঁকনি
- [ ] দাবি: SLA পার হলে কমান্ড সত্যিই উপরের জনের কাছে পাঠায়
- [ ] দাবি: **সময়ের ভিতরে থাকা** একটা অনুরোধ কমান্ড ছোঁয় না (নিয়ন্ত্রণ সারি)
- [ ] দাবি: কমান্ড দুইবার চললে দুইবার পাঠায় না (`escalated_at`)
- [ ] ⭐ দাবি: **SLA বসানো অথচ গন্তব্য নেই** — এমন ধাপ `flow/coverage`-এ ধরা পড়ে
      ⓘ নাহলে সুইচটা বসানো থাকত, কাগজ কোথাও যেত না, আর কেউ জানত না

⚠️ শেষ দুইটা ছাড়া প্রথমটা কিছুই প্রমাণ করে না — একটা **সবাইকে পাঠানো**
কমান্ডও ওটা পাস করত।

---

## ধাপ ২ · সই দেওয়ার ভার অন্যকে, তারিখ ধরে

**আজ কী আছে:** `forward()` — একটা কাগজ একজনকে দেওয়া যায়। ⛔ কিন্তু *"আমি
২৫ থেকে ৩০ তারিখ ছুটিতে, এই সময়ের সব বিক্রয় অনুমোদন সহকারী দেখবেন"*
বলা যায় না, আর ফিরে এসে কিছু বন্ধও করতে হয় না।

- [ ] মাইগ্রেশন: `approval_delegations` — from_user · to_user · starts_on · ends_on · modules(json) · actions(json) · reason · revoked_at
- [ ] `DelegationService` — `grant()` · `revoke()` · `activeFor($user, $module, $action)`
- [ ] `canDecide()` ভার পাওয়া মানুষকেও চিনবে
- [ ] সিদ্ধান্তে **দুইজনের নামই** থাকবে — আসল ও ভারপ্রাপ্ত
- [ ] মেয়াদ শেষে নিজে থেকে বন্ধ (তারিখ ধরে, কোনো কাজ ছাড়া)
- [ ] পর্দা: `approval.delegation.index` + ফর্ম
- [ ] দাবি: মেয়াদের **বাইরে** ভারপ্রাপ্ত জন সিদ্ধান্ত নিতে পারেন না
- [ ] দাবি: ভার দেওয়া কাজের **বাইরের** মডিউলে পারেন না
- [ ] দাবি: খতিয়ানে আসল মানুষের নামও আছে

---

## ধাপ ৩ · টাকার অঙ্ক ছাড়াও শর্ত

**আজ কী আছে:** একটাই শর্ত — `threshold_amount`। ⛔ *"ছাড় ১৫% পার হলে"*,
*"ক্রেতার বাকি আটকানো থাকলে"*, *"পিছনের তারিখে হলে"* — কিছুই বলা যায় না।

⚠️ ঐ ফাইলগুলো এখন অন্যের হাতে — আগে জেনে নিন।

- [ ] মাইগ্রেশন: `approval_conditions` — flow_id · field · operator · value · joiner
- [ ] শর্ত মাপার জায়গা: মডিউল যা পাঠায় সেই `payload` (আজ কলামটা আছে, **ব্যবহার হয় না**)
- [ ] `DocumentApproval::assertClear()` ঘরগুলো পাঠাবে, কেবল একটা অঙ্ক নয়
- [ ] `ApprovalFlow::appliesTo()` শর্তের তালিকা মিলিয়ে দেখবে
- [ ] ফর্মে শর্ত যোগ-বিয়োগ
- [ ] দাবি: শর্ত না মিললে প্রবাহটা **ধরে না**
- [ ] দাবি: দুইটা শর্ত AND হলে একটা মিললেই ধরে না

---

## ধাপ ৪ · কে কত টাকা পর্যন্ত অনুমোদন করতে পারেন

**আজ কী আছে:** কিছুই না — কেবল নিজে-অনুমোদনের একটা সীমা। ⛔ একজন
বিক্রয় ব্যবস্থাপক দশ লাখের অর্ডারও অনুমোদন করতে পারেন, যদি প্রবাহে
তাঁর ধাপ থাকে।

- [ ] মাইগ্রেশন: `approval_limits` — role_id · module · action · branch_id(nullable) · max_amount
- [ ] `AuthorityService::limitFor($user, $module, $action, $branch)`
- [ ] `canDecide()` সীমা পার হলে **না** বলবে, কারণ জানিয়ে
- [ ] পর্দা: `approval.limit.index` + ফর্ম
- [ ] দাবি: সীমার বেশি অঙ্কে ঐ রোল সিদ্ধান্ত নিতে পারে না
- [ ] দাবি: একই রোল **অন্য শাখায়** আলাদা সীমা পায়

---

## ধাপ ৫ · সমান্তরাল অনুমোদন — কয়জনের মধ্যে কয়জন

**আজ কী আছে:** এক ধাপে **সবাই** সই করতে হয় (`requires_all`)। ⛔ *"তিনজনের
মধ্যে যেকোনো দুইজন"* বলা যায় না।

- [ ] মাইগ্রেশন: `approval_flow_steps`-এ `min_approvals` (ডিফল্ট ১)
- [ ] `ApprovalEngine` ধাপ এগোবে যখন `min_approvals` পূর্ণ
- [ ] `requires_all` = পুরনো আচরণ, ভাঙবে না
- [ ] দাবি: তিনজনের ধাপে দুইজন সই দিলে এগোয়, একজনে এগোয় না
- [ ] দাবি: পুরনো `requires_all` প্রবাহগুলো **অবিকল আগের মতো** চলে

---

## ধাপ ৬ · সই দেওয়ার পর কাগজ বদলালে

**⛔ এটা নিরাপত্তার ফাঁক, সুবিধা নয়।** আজ কেবল **টাকার অঙ্ক** বদলালে সই
বাতিল হয়। ⚠️ পণ্য বদলে দিলে, পরিমাণ বদলে দিলে, ক্রেতা বদলে দিলে — সই
আগের মতোই দাঁড়িয়ে থাকে, আর কাগজটা অনুমোদিত দেখায়।

ⓘ পর্দায় *"গত সপ্তাহের সংখ্যা"* সতর্কতা আছে, কিন্তু ওটা **কেবল দেখায়** —
আটকায় না।

- [ ] ⛔ **যেকোনো ঘর বদলালেই সই বাতিল** (সিদ্ধান্ত ৪) — তাই ঘোষণার তালিকা নয়,
      কাগজের গোটা অবস্থার একটা ছাপ
- [ ] যে ঘরগুলো ইচ্ছাকৃতভাবে **বাদ** (`updated_at`, `printed_count` জাতীয়) সেগুলো
      এক জায়গায়, কারণসহ — তালিকাটা ছোট রাখা
- [ ] অনুমোদনের সময় ঐ ঘরগুলোর একটা ছাপ (hash) `approvals`-এ
- [ ] পোস্ট করার আগে ছাপ মিলিয়ে দেখা — না মিললে সই বাতিল, নতুন অনুরোধ
- [ ] দাবি: অঙ্ক **না বদলে** পণ্য বদলালেও সই বাতিল হয়
- [ ] দাবি: **মন্তব্য বদলালেও** সই বাতিল হয় (মালিকের বাছাই — কড়া দিক)
- [ ] দাবি: বাদ-দেওয়া ঘর (`updated_at`) বদলালে সই **টেকে**, নাহলে প্রতিটা
      সংরক্ষণেই নতুন অনুমোদন লাগত

---

## ধাপ ৭ · একসাথে অনেকগুলো

- [ ] ইনবক্সে বাছাইয়ের ঘর, আর *"বাছাই করা কয়টা"*
- [ ] `bulkApprove()` — প্রতিটা আলাদা করে `canDecide()` দিয়ে যায়
- [ ] ⛔ **টাকা নড়ার কাজে bulk নেই** (সিদ্ধান্ত ৫) — পরিশোধ · উত্তোলন ·
      টাকা স্থানান্তর · আদায় · বছর বন্ধ
- [ ] কোনটা "টাকা নড়ার কাজ" তা মডিউলের `module.php`-এ ঘোষিত, কন্ট্রোলারে নয়
- [ ] দাবি: যেগুলোর অনুমতি নেই সেগুলো **বাদ পড়ে**, গোটা কাজ ব্যর্থ হয় না
- [ ] দাবি: নিষিদ্ধ কাজে bulk বোতাম নেই — **আর সরাসরি POST করলেও আটকায়**
      ⓘ বোতাম লুকানো নিরাপত্তা নয়

---

## ধাপ ৮ · ব্যতিক্রমের সারি, আর খতিয়ান পাথরে

- [ ] `ApprovalDecision`-এ `IsAudited` (আজ নেই — `Approval`/`ApprovalFlow`-এ আছে)
- [ ] মডেলে `updating`/`deleting` আটকানো, কারণসহ
- [ ] `approval.exception.index` — কর্তৃত্বের সীমা পার · অচেনা অনুমোদনকারী · প্রবাহ নেই · ভারের মেয়াদ শেষ · SLA পার · সইয়ের পর কাগজ বদলেছে
- [ ] দাবি: একটা সিদ্ধান্ত বদলানোর চেষ্টা **ছুড়ে ফেলে**
- [ ] দাবি: প্রতিটা ব্যতিক্রম সত্যিই সারিতে আসে (ছয়টার ছয়টাই)

---

## ধাপ ৯ · কোথায় আটকে আছে

**আজ কী আছে:** চারটা রিপোর্ট — কে অপেক্ষা করছে, কত দিন। ⛔ *"কোন ধাপে
সবচেয়ে বেশি আটকায়"* প্রশ্নের উত্তর নেই।

- [ ] রিপোর্ট: ধাপ ধরে গড় দেরি ও জমে থাকা সংখ্যা
- [ ] রিপোর্ট: মডিউল · শাখা · রোল ধরে
- [ ] বাতিলের কারণ ধরে গোনা (ধাপ ১০-এ কারণ বসার পর)
- [ ] দাবি: যে ধাপে সত্যিই বেশি জমে, রিপোর্ট তাকেই উপরে রাখে

---

## ধাপ ১০ · বাতিলের কারণ, আর সংশোধনে ফেরত

- [ ] মাইগ্রেশন: `approval_decisions`-এ `reason_code`
- [ ] বাতিলের সময় কারণ **বাধ্যতামূলক**
- [ ] *"সংশোধন করে আনুন"* — বাতিল থেকে আলাদা একটা পথ
- [ ] দাবি: কারণ ছাড়া বাতিল করা যায় না
- [ ] দাবি: ফেরত পাঠানো কাগজ অনুরোধকারী আবার পাঠাতে পারেন

---

## ধাপ ১১ · পর্দার ছাঁচ

**⚠️ এটা শেষে, আর সেটা ইচ্ছাকৃত।** ⓘ ইনবক্স আর বিস্তারিত পর্দা আজ কাজ
করে। ⛔ আগে ছাঁচ বদলালে উপরের দশটা ধাপের প্রতিটা আবার ঐ ছাঁচে বসাতে হত।

- [ ] বিস্তারিত পর্দা তিন কলামে: কাগজ · সিদ্ধান্তের প্যানেল · প্রসঙ্গ
- [ ] অনুমোদনকারী যেন অন্য পর্দায় যেতে বাধ্য না হন — ক্রেতার বাকি · সীমা · মজুদ · আগের কাগজ
- [ ] সময়রেখা: কে · কোন ভূমিকায় · কখন · কত সময় নিল
- [ ] ছকের আটকানো মাথা ও আটকানো কাজের কলাম
- [ ] কিবোর্ড: `Ctrl+K` খোঁজা, `Esc` বন্ধ, তীর দিয়ে চলাচল
- [ ] ⛔ জরুরি কাজে shortcut দিয়েও নিশ্চিতকরণ এড়ানো যাবে না
- [ ] দাবি: তিনটা কলামই সত্যিই আঁকা হয় (কেবল ক্লাস নয়)

---

## যা এই চেকলিস্টে **নেই**, আর কেন

| স্পেকের অংশ | কেন এখন নয় |
|---|---|
| Workflow Designer (টেনে বসানো) | আজকের ফর্ম ধাপগুলো বসাতে পারে। ⚠️ টেনে বসানো একটা **দেখার** সুবিধা, ক্ষমতা নয় — উপরের কোনো ফাঁক এটা ভরে না |
| Workflow versioning + simulation | ⓘ প্রবাহ বদলালে চলতি অনুরোধ যেন পুরনো নিয়মে চলে — ওটা আসল প্রয়োজন, আর সেটা ধাপ ৬-এর ছাপের সাথে একসাথে করা সস্তা |
| Approval Matrix-এর আলাদা পর্দা | `flow/coverage.blade.php` একই প্রশ্নের উত্তর দেয়। দুইটা পর্দা মানে দুইটা সত্য |
| MFA / PIN | অনুমোদনের নয়, লগইনের প্রশ্ন — আলাদা কাজ |
| SMS / Push | `NotificationService`-এর কাজ, অনুমোদনের নয় |

⭐ **এগুলো বাদ দেওয়া হয়নি — পিছিয়ে দেওয়া হয়েছে।** উপরের এগারোটা ধাপ শেষ
হলে এগুলোর মধ্যে কোনটা তখনো দরকার, সেটা তখন মেপে দেখা যাবে।
