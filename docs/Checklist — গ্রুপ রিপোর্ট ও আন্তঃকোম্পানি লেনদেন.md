# Checklist — গ্রুপ রিপোর্ট ও আন্তঃকোম্পানি লেনদেন

> মালিকের প্রশ্ন, ২৫ সেপ্টেম্বর ২০২৬: *"এক মালিকের একাধিক কোম্পানি থাকলে কী
> হবে? সে তো একসাথে হিসাব দেখতে চাইবে। গ্রুপের অ্যাকাউন্ট থেকে টাকা নিলে?"*
>
> **এই ফাইলটাই কাজের সারি।** দুইটা ধাপ, আর দ্বিতীয়টা প্রথমটার উপর দাঁড়ায় না —
> প্রথমটা কেবল পড়ে, দ্বিতীয়টা লেখে, তাই ক্রমটা ঝুঁকি অনুযায়ী।

**যেখানে থামা হয়েছে:** ⭐ দুইটা ধাপই শেষ — ২৫ সেপ্টেম্বর ২০২৬।

⚠️ **একটা লাইনে `[~]`** — সেটা হয়নি নয়, **সীমিতভাবে হয়েছে**, আর কারণ পাশে
লেখা। ⛔ ঐ লাইনে টিক দেওয়া হয়নি ইচ্ছাকৃতভাবে: একটা মিথ্যা টিক কোনো টিক না
থাকার চেয়ে খারাপ — সে চিরকাল বসে থাকে, আর পরে কেউ ঐ কাজটা খুঁজতেই যায় না।

⚠️ **আর এই চেকলিস্টটা কাজের আগে নয়, পরে লেখা** — মালিক জিজ্ঞেস করার পর।
ⓘ সেটা নিয়মের বিরুদ্ধে, আর সেটা এখানে স্বীকার করে রাখা হলো: কাজটা একটা
বার্তার নম্বরওয়ালা তালিকা ধরে হয়েছে, কোনো লেখা সারি ধরে নয়।

---

## ⚠️ শুরু করার আগে — চারটা নিয়ম

**১ · টেন্যান্টের দেয়াল ভাঙা যাবে না।** মালিকের তিনটা কোম্পানি একই মালিকের,
কিন্তু ABOS বহু-ক্রেতার পণ্য — CLAUDE.md টেন্যান্ট বিচ্ছিন্নতাকে সুবিধা নয়,
**আইনি বাধ্যবাধকতা** বলে। ⛔ তাই ছাঁকনি কখনো *"সব কোম্পানি"* নয়, সবসময়
*"এই ব্যবহারকারী যে কোম্পানিগুলোতে আছেন"* — `company_user` পিভট ধরে।

**২ · সীমানা পেরোনো পর্দার নিজের চাবি লাগে।** ⚠️ বাকি প্রতিটা পর্দা এক
কোম্পানির ভেতরে থাকে, তাই `accounts.report.final` বা `accounts.voucher.create`
থাকলেই কেউ যেন গ্রুপের ছবি বা অন্য কোম্পানির খাতা না পান।

**৩ · `DemoSeeder`-এর ঢালাও `accounts.%` নিয়ম প্রতিটা নতুন চাবি বিলি করে।**
⛔ এটা আজকের আগে **তিনবার** ঘটেছে (`sales.discount.override`,
`sales.target.manage`, `accounts.backdate.override`)। ⓘ প্রতিটা সীমা-অতিক্রমকারী
চাবি ছাড়ের তালিকায় বসাতে হবে, আর দাবিটা মিউটেশন দিয়ে প্রমাণ করতে হবে।

**৪ · অন্য কোম্পানিতে লিখতে `CompanyContext::forCompany()`, কখনো `::set()`।**
ⓘ প্রথমটা `finally`-তে আগের প্রসঙ্গ ফিরিয়ে দেয়; দ্বিতীয়টা ফাঁস করে, আর তখন
অনুরোধের বাকি প্রতিটা কোয়েরি ভুল কোম্পানিতে চলে — **কোনো ত্রুটিবার্তা ছাড়াই**।

---

## ধাপ ০ · জরিপ — কী আছে, কী নেই

- [x] `consolidat` · `inter_company` · `intercompany` · `cross_company` — **কিছুই নেই**
      (grep শূন্য ফেরত দিয়েছে, কাটা পড়েনি)
- [x] `company_user` পিভট ধরে কোয়েরির ছাঁচ আছে — **১২+ জায়গায়**, কিন্তু সবগুলোই
      এক কোম্পানিতে (`CompanyContext::id()`)
- [x] দেয়াল পেরোনোর **অনুমোদিত নজির** পাওয়া গেছে —
      `SharedAcrossCompaniesWhenAsked` (মালিকের সুইচ, পড়া চওড়া হয় লেখা নয়,
      ত্রুটি হলে দেয়াল বন্ধ)
- [x] `ReportEngine` একাধিক কোম্পানি নিতে পারে না — `ReportEngine.php:351`
      একটাই `company_id` বসায়
- [x] মাপা: ঐ ছাঁকনিটা **৫৩ জায়গায়, ১০টা রিপোর্ট ফাইলে** ব্যবহৃত

### ⭐ সিদ্ধান্তের টেবিল

| প্রশ্ন | উত্তর | কেন |
|---|---|---|
| ইঞ্জিনটা চওড়া করা হবে? | **না** | ৫৩ জায়গায় `=` → `whereIn`; একটাও বাদ পড়লে ঐ রিপোর্ট নীরবে কেবল এক কোম্পানির সংখ্যা দেখাত |
| দেয়াল খোলার সুইচ বসানো হবে? | **না** | দেয়ালটা Eloquent গ্লোবাল স্কোপ; `DB::table()` ওটা দেখেই না, তাই খোলার দরকার নেই |
| কোম্পানি `PartyRegistry`-তে পক্ষ হবে? | **না** | তাহলে কোম্পানির নাম রসিদ, পরিশোধ ও নোটের পক্ষ-বাছাইয়ে দেখা দিত |
| প্রতি ভাই-কোম্পানির আলাদা খাত? | **না** | ছক কোম্পানির সাথে বাড়ত, আর নতুন কোম্পানির দিন কেউ খাত বানাতে ভুলত |

---

## ধাপ ১ · গ্রুপের হিসাব — `2d392274`

- [x] `GroupLedgerService` — একটাই কোয়েরি, `DB::table()`-এ, দেয়াল ছোঁয়া ছাড়াই
- [x] কোম্পানির তালিকা `company_user` পিভট ধরে, কখনো `Company::all()` নয়
- [x] অঙ্ক সবটা bcmath-এ, scale ৪ — কলাম `decimal(18,4)`
- [x] প্রতিটা খাতের **নিজের `nature`** ধরে চিহ্ন, ধরনের ডিফল্ট নয়
- [x] `ONLY_FULL_GROUP_BY`-নিরাপদ: যে তিনটা কলাম নির্বাচিত, তিনটাই `group by`-তে
- [x] `GroupReportController` + নিজের চাবি `accounts.report.group`
- [x] পর্দা `x-ui.table`-এর `:rows`/`:columns`/`:totals` দিয়ে
- [x] ভাষার চাবি দুই ভাষায়
- [x] মেনুর সারি, চূড়ান্ত হিসাবগুলোর পরে
- [x] `DemoSeeder`-এর ছাড়ের তালিকায় চাবিটা বাদ
- [x] টেস্ট — ৩/৩ সবুজ, ১২টা দাবি
- [x] পর্দা HTTP kernel দিয়ে খুলে দেখা: চাবি ছাড়া ৪০৩, চাবি নিয়ে ২০০

### ⛔ যা নীরবে ভুল হত

| ভুল | কীভাবে ধরা পড়ল |
|---|---|
| `defaultNatureFor()` ধরে চিহ্ন — "সঞ্চিত অবচয়" সম্পদ হয়েও ক্রেডিট প্রকৃতির | ঐ পদ্ধতির নিজের মন্তব্য পড়ে |
| ছকে হাতে `<tr>` — খালি অবস্থায় ঠিক চলত, প্রথম সারিতে ৫০০ | `asset/index.blade.php`-এর মন্তব্যে একই ভুলের ইতিহাস |
| `core.action.filter` — চাবিটাই নেই (আসলটা `core.toolbar.filter`) | ব্যবহার খুঁজে দেখায়, কেবল নিজের ফাইলে |
| `accountant` রোল ঢালাও নিয়মে চাবিটা পেয়ে যাচ্ছিল | টেস্ট ৪০৩ চেয়ে ২০০ পেয়েছিল |

---

## ধাপ ২ · আন্তঃকোম্পানি লেনদেন — `069d0b35`, `3575f4ca`

- [x] মাইগ্রেশন `acc_inter_company` — `public_id`, ছোট ইনডেক্স-নাম (৬৪ অক্ষরের নিচে)
- [x] `StandardChart::INTER_COMPANY` (`1180`), প্যারেন্ট `1100` — তিন স্তরের সীমা
- [x] মডেল `InterCompanyTransfer` — `decimal:4` কাস্ট, `isBalanced()`
- [x] `InterCompanyService` — দুই খাতায় আয়না দাখিলা, একটাই লেনদেনে
- [x] `CompanyContext::forCompany()` দিয়ে অন্য কোম্পানিতে লেখা
- [x] দুইটা কোম্পানিতেই সদস্যপদ যাচাই — **সেবা স্তরে**, ফরমে নয়
- [x] অন্য কোম্পানির ত্রুটিতে সেই কোম্পানির নাম জুড়ে দেওয়া
- [x] `InterCompanyRequest` — `before_or_equal:today` সহ
- [x] তালিকা ও ফরম, দুইটা পর্দা; ফরমটা **JS ছাড়া দুই ধাপে**
- [x] নিজের চাবি `accounts.inter_company` + ছাড়ের তালিকায় বাদ
- [x] ভাষার চাবি দুই ভাষায় (menu, field, message, validation)
- [x] টেস্ট — ৪/৪ সবুজ, ৮টা দাবি
- [ ] ~~পাওয়ার দিকের তালিকা~~ → **নেই, আর কারণ নিচে**

### `[~]` অন্য কোম্পানির **খরচ** সরাসরি দেওয়া

⛔ **হয়নি, আর ইচ্ছাকৃতভাবে হয়নি।** ADI যদি TCL-এর দোকানভাড়া দেয়, TCL-এর দিকে
ডেবিটটা নগদ নয় — **খরচের খাত**। ⓘ তখন ফরমে দুই কোম্পানির দুইটা ভিন্ন খাত
বাছতে হত, আর ব্যবহারকারীকে অন্য কোম্পানির গোটা ছক চিনতে হত।

⚠️ মালিকের কথা ছিল *"অ্যাকাউন্ট থেকে টাকা নিলে"* — অর্থাৎ **টাকা সরানো**, আর
সেটাই করা হয়েছে। ⭐ সীমাটা `InterCompanyService`-এর মাথায়, আর **পর্দাতেও** লেখা
(`inter_company_moves_money_only`), যাতে কেউ ভেবে না বসে খরচের দিকটাও হয়ে গেছে।

### ⛔ যা নীরবে ভুল হত

| ভুল | ফল যদি না ধরা পড়ত |
|---|---|
| `trx_date`-এ ভবিষ্যতের সীমা নেই | কালকের তারিখে টাকা সরানো; মাস শেষের হিসাব তা গুনত |
| ত্রুটি সবসময় `from_account_id` ঘরে | অন্য কোম্পানির খাতের ভুলে ব্যবহারকারী নিজের ঘর বদলে দেখতেন |
| অর্থবছরের বার্তায় কোম্পানির নাম নেই | তিনি নিজের ছকে খুঁজে কিছুই পেতেন না |
| `accounts.inter_company` ঢালাও নিয়মে বিলি | হিসাবরক্ষক অন্য কোম্পানির খাতায় দাখিলা লিখতে পারতেন |

---

## ধাপ ৩ · যাচাই

- [x] নিজের দুইটা টেস্ট — ৩/৩ আর ৪/৪ সবুজ
- [x] **মিউটেশনে প্রমাণিত** — ছাড় তুলে নিলে অনুমতির দাবি **লাল** হয় (৪০৩ চেয়ে ২০০);
      তারপর মিউট্যান্ট তুলে `git diff` মিলিয়ে দেখা
- [x] বাইরের কোম্পানির অঙ্ক যোগফলে আসে না — মিউটেশনে প্রমাণিত (জুড়ে দিলে সংখ্যা বদলায়)
- [x] চারটা ব্লেড কম্পাইল হয়, কাঁচা `x-` ট্যাগ টেকে না
- [x] ব্যবহৃত **৮৮টা** ভাষা-চাবি দুই ভাষাতেই আছে
- [x] পুরো `tests/Feature/Architecture` — **২৪৫-এর ২৪৪ সবুজ**
- [x] একমাত্র লাল (`NoDocumentIsDatedInTheFutureTest`) আমার ছিল, সারানো (`3575f4ca`),
      ঐ পাহারা আলাদা চালিয়ে ২/২ সবুজ
- [x] চারটা কমিটই `origin/main`-এ পৌঁছেছে

### ⭐ যে শিক্ষাটা লিখে রাখার মতো

আমার **সাতটা দাবি সবুজ** ছিল, তবু পাহারা একটা আসল ত্রুটি ধরেছে: কালকের তারিখে
টাকা সরানো যেত। ⚠️ কারণ আমার প্রতিটা দাবি **আজকের** তারিখ ব্যবহার করে — প্রশ্নটাই
কখনো করা হয়নি।

⛔ **নিজের টেস্ট কেবল সেই প্রশ্নের উত্তর দেয় যেটা আপনি করেছেন।** তাই পুরো
`tests/Feature/Architecture` চালানো ঐচ্ছিক নয়।

---

## ⚠️ ডিপ্লয়ে যা লাগবে

| | |
|---|---|
| মাইগ্রেশন | `2026_12_15_100000_one_company_paid_for_another_and_neither_book_said_so` |
| অনুমতি | `accounts.report.group` · `accounts.inter_company` → `abos:sync-permissions` |
| সিঙ্কের পরে | হিসাবরক্ষকের রোল খুলে দেখুন **দুইটার একটাও** ওখানে নেই |
| হাঁটানোর পাতা | `/accounts/reports/group` · `/accounts/inter-company` · `/accounts/inter-company/create` |

⛔ লাইভের রোল `PermissionSyncer` চালায়, `DemoSeeder` নয় — তাই ছাড়ের তালিকার
সারাইটা লাইভে **নিজে থেকে পৌঁছায় না**।

---

## ⛔ যা এখনো খোলা, আর যা আমার হাতে নয়

ⓘ দুইটাই অফিসের ম্যাক মিনির (`abos.univer.com.bd`), আর দুইটাই মালিকের সিদ্ধান্ত।
কোঅর্ডিনেটর সেশনের তালিকায় আছে।

**১ · তিনটা ডেমো অ্যাকাউন্ট জীবন্ত, পাসওয়ার্ড `password` এখনো চলে**
```
owner@abos.test    PASSWORD-STILL-WORKS   ← super-admin
accounts@abos.test PASSWORD-STILL-WORKS
sales@abos.test    PASSWORD-STILL-WORKS
```
ⓘ প্রশ্নটা `DemoSeeder`-এর মাথায় ১২ সেপ্টেম্বর থেকে লেখা ছিল; উত্তরটা আজ নেওয়া
হলো। ⭐ `erp.adi.com.bd` পরিষ্কার — কোঅর্ডিনেটর গোটা `users` টেবিল হেঁটে মেপেছে।

**২ · ঐ সার্ভারের HTTPS ভাঙা** — HTTPS পাতার ভেতরে সব ঠিকানা `http://`, তাই
CSS/JS আটকে যায়, পাসওয়ার্ডের ফরম সাদা HTTP-তে যায়, কুকিতে `secure` নেই।
ⓘ কোডের দিকটা `01b3286a`-তে সারানো, কিন্তু **যাচাই করা হয়নি** — ঐ সার্ভারে
ডিপ্লয় হয়নি। সারাইয়ের বাকি দুই অংশ কোডে নয়: Caddyfile-এ
`php_fastcgi 127.0.0.1:9000 { env HTTPS on }`, আর `.env`-এ
`SESSION_SECURE_COOKIE=true`।
