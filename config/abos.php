<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | ব্যাকআপ
    |--------------------------------------------------------------------------
    |
    | সার্ভার অফিসের একটা মেশিন, ক্লাউড ক্লাস্টার নয়। একটা ডিস্ক ফেল করলে
    | প্রতিষ্ঠানের পুরো হিসাব শেষ — কার কাছে কত পাওনা, কাকে কত দিতে হবে,
    | কেউ বলতে পারবে না। তাই ব্যাকআপ কোনো ঐচ্ছিক ফিচার নয়।
    |
    */

    'backup' => [

        // ডাম্পগুলো কোথায় জমবে (storage/ এর ভেতরে নয় — storage নিজেও
        // ব্যাকআপের অংশ, আর ব্যাকআপ নিজের ভেতরে নিজেকে রাখতে পারে না)
        'path' => env('ABOS_BACKUP_PATH', storage_path('../backups')),

        /*
         * দ্বিতীয় গন্তব্য — অন্য ডিস্ক, নেটওয়ার্ক ড্রাইভ বা পেনড্রাইভ।
         *
         * একই ডিস্কে রাখা ব্যাকআপ ডিস্ক ফেল করলে ব্যাকআপও নিয়ে যায়।
         * খালি রাখলে সতর্কবার্তা আসে, কাজ থামে না — কারণ একটা ব্যাকআপ
         * শূন্যের চেয়ে ভালো।
         */
        'mirror' => env('ABOS_BACKUP_MIRROR'),

        // কত দিনের ডাম্প রাখা হবে। পুরনোগুলো নিজে থেকে মুছবে, নাহলে
        // ডিস্ক ভরে গিয়ে একদিন ব্যাকআপ নেওয়াই বন্ধ হত।
        'keep_days' => (int) env('ABOS_BACKUP_KEEP_DAYS', 30),

        // রোজ রাতে — অফিস বন্ধ থাকার সময়
        'daily_at' => env('ABOS_BACKUP_AT', '01:30'),

        /*
         * mysqldump কোথায়।
         *
         * Laragon-এ এটা PATH-এ থাকে না, আর না পেলে ব্যাকআপ নীরবে ব্যর্থ
         * হওয়ার চেয়ে স্পষ্ট করে বলা ভালো — নীরব ব্যর্থতা মানে ছয় মাস পর
         * জানা যাবে যে একটাও ব্যাকআপ নেই।
         */
        'mysqldump' => env('ABOS_MYSQLDUMP', 'mysqldump'),
        'mysql' => env('ABOS_MYSQL', 'mysql'),

        /*
         * শেল থাকলেও বিশুদ্ধ PHP-র পথে যেতে বাধ্য করা।
         *
         * ⓘ দুইটা কারণে এটা আছে, আর দুইটাই আসল:
         *
         * ১. ⭐ **পরীক্ষা করার জন্য** — লাইভের পথটা (`proc_open` বন্ধ)
         *    এই মেশিনে নকল না করতে পারলে ঐ পথটা অপ্রমাণিত থেকে যেত।
         *    ⚠️ আর আজকের রোগটাই ছিল অপ্রমাণিত পথ: ছয় দিন `DONE`।
         *
         * ২. যেখানে `mysqldump` আছে কিন্তু সংস্করণ মেলে না, সেখানে
         *    হাতে করে নিরাপদ পথটা বেছে নেওয়ার উপায় থাকা দরকার।
         */
        'force_php' => (bool) env('ABOS_BACKUP_FORCE_PHP', false),
    ],

    /*
     * ইমেইল বদলানোর লিংকটা কত মিনিট বাঁচে।
     *
     * ⓘ পাসওয়ার্ড রিসেটের মতোই ৬০ — ⚠️ দুই জায়গায় দুই মেয়াদ থাকলে
     * মানুষ একটা মনে রাখতেন আর অন্যটায় ঠকতেন। ⛔ সংখ্যাটা চিঠিতেও
     * হাতে লেখা নেই; ওখানে এই ঘরটাই পড়া হয়, নাহলে একদিন এটা বদলে
     * চিঠিটা মিথ্যা বলত।
     */
    'email_change_expire' => (int) env('ABOS_EMAIL_CHANGE_EXPIRE', 60),

    /*
     * লাইসেন্স — মালিকের সিদ্ধান্ত, ২২ সেপ্টেম্বর ২০২৬।
     *
     * ── ⭐ কেবল **পাবলিক** চাবি, আর সেটা নিরাপদ ─────────────────────
     * এই চাবি দিয়ে সই **যাচাই** করা যায়, **বানানো** যায় না। ⓘ তাই
     * এটা কোডে থাকা কোনো ঝুঁকি নয় — প্রতিটা ক্রেতার সার্ভারে এটা
     * থাকবেই, আর থাকতেই হবে।
     *
     * ⛔ গোপন চাবিটা এখানে **কোনোদিন আসবে না** — রিপোতে নয়, `.env`-এ
     * নয়, কোনো সার্ভারে নয়। ⚠️ ওটা কেবল মালিকের কাছে; হারালে নতুন
     * লাইসেন্স ইস্যু করা যায় না (পুরনোগুলো চলতে থাকে), আর ফাঁস হলে
     * তালাটার আর কোনো মানে থাকে না।
     *
     * ── ⚠️ কেন `env()` দিয়ে, কোডে হাতে লেখা নয় ──────────────────────
     * চাবি বদলাতে হলে (ফাঁস হলে) কোড আপডেট না দিয়েও বদলানো যায়।
     * ⓘ আর ডিফল্ট খালি — খালি মানে **কোনো কাগজই যাচাই হয় না**, তাই
     * ব্যবস্থাটা তালাবদ্ধ থাকে, খোলা নয়।
     */
    'licence' => [
        'public_key' => env('ABOS_LICENCE_PUBLIC_KEY', ''),

        /*
         * ⛔ তালাটা কি সত্যিই আটকাবে?
         *
         * ⚠️ ডিফল্ট `false`, আর সেটা ইচ্ছাকৃত: আজ পর্যন্ত কোনো
         * ইনস্টলে লাইসেন্স ফাইল নেই, তাই `true` দিলে **এই মুহূর্তে
         * প্রতিটা সার্ভার তালাবদ্ধ** হয়ে যেত — মালিকেরটাসহ।
         *
         * ⓘ চাবি বসানো আর কাগজ ইস্যু করার পর এটা `true` করা হয়, আর
         * সেটা একটা সচেতন পদক্ষেপ — একটা ডিফল্ট নয়।
         */
        'enforced' => (bool) env('ABOS_LICENCE_ENFORCED', false),
    ],
];
