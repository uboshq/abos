<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * একটা বিরিয়ানি বিক্রি হলে চাল-মাংস-তেলও কমতে হবে।
 *
 * ── কী নেই, আর কেন সেটা রেস্টুরেন্টের প্রথম ইট ───────────────────────
 * আজ ABOS একটা পণ্য বেচলে সেই পণ্যটাই কমে। মুদি দোকানে ওটাই ঠিক — এক
 * প্যাকেট চাল বেচলে এক প্যাকেট চাল কমে।
 *
 * রেস্টুরেন্টে কথাটা আর খাটে না। "চিকেন বিরিয়ানি" নামে কোনো জিনিস
 * গুদামে ঢোকে না; ওটা তৈরি হয় চাল, মাংস, তেল আর মসলা থেকে। রেসিপি ছাড়া
 * বিক্রি হলে খাতা বলবে বিরিয়ানি বিক্রি হয়েছে, আর **চালের স্টক অক্ষত
 * থেকে যাবে** — মাসের শেষে গুদাম আর খাতা দুই কথা বলবে।
 *
 * ── দুইটা আলাদা বাস্তবতা, দুইটাই দরকার ───────────────────────────────
 * `to_order` — অর্ডারে রান্না। বিক্রির মুহূর্তেই উপকরণ কমে। চা, চিকেন
 * ফ্রাই। একজন চা চাইলেন, তখনই দুধ-চিনি-পাতা কমল।
 *
 * `batch` — হাঁড়িতে রান্না। এক হাঁড়ি বিরিয়ানি সকালে চড়ে, তাতে ৫০ প্লেট
 * হয়। উপকরণ কমে **রান্নার সময়**, বিক্রির সময় নয়; বিক্রিতে কমে তৈরি
 * খাবারটা।
 *
 * একটাকে অন্যটা দিয়ে চালানো যায় না। হাঁড়িকে অর্ডারে-রান্না ধরলে সন্ধ্যায়
 * ৩০ প্লেট নষ্ট হলে খাতা কিছুই জানবে না — কারণ ওই ৩০ প্লেটের উপকরণ
 * কোনোদিন কমেইনি। আর চা-কে হাঁড়ি ধরলে প্রতিটা কাপের জন্য একটা উৎপাদন
 * এন্ট্রি লিখতে হবে।
 *
 * ── খরচের দর কোথা থেকে আসবে, আর কোথা থেকে নয় ────────────────────────
 * FIFO স্তর থেকেই — `CostLayerService::issue()`। এখানে কোনো দর রাখা
 * হয়নি, আর সেটা ইচ্ছাকৃত।
 *
 * ৭ আগস্ট ২০২৬-এ ধরা পড়েছিল মাল ঢুকত এক দামে, বেরোত আরেক দামে —
 * মাস্টারের `purchase_price` আর চালানের আসল দর কখনো মিলত না, আর ৭৯৭টা
 * টেস্ট সবুজ থেকেও অঙ্কটা ভুল ছিল। রেসিপিতে দর বসালে ঠিক ওই ভুলটাই
 * ফিরত, এবার দুইয়ের বদলে **তিন** জায়গায়।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('inv_recipes', function (Blueprint $table) {
            $table->id();
            $table->publicId();
            $table->foreignId('company_id')->constrained()->cascadeOnDelete();

            /*
             * কোন খাবারের রেসিপি।
             *
             * এক পণ্যের একটাই রেসিপি — নিচের ইউনিক সূচক সেটা বাঁধে।
             * একাধিক রাখলে "কোনটা দিয়ে কমবে" প্রশ্নের উত্তর কোডকে
             * আন্দাজে ঠিক করতে হত, আর দুই রাঁধুনির দুই হিসাব খাতায়
             * ঢুকত।
             */
            $table->foreignId('product_id')->constrained('inv_products')->cascadeOnDelete();

            /*
             * `to_order` না `batch` — উপরের ব্যাখ্যা।
             *
             * ডিফল্ট `to_order`, কারণ ওটাই কম বিপজ্জনক: ভুল করে ওটা
             * বসলে উপকরণ বিক্রির সময় কমে, অর্থাৎ কমে অন্তত — উল্টোটা
             * হলে কোনোদিনই কমত না।
             */
            $table->string('kind', 16)->default('to_order');

            /*
             * এক রান্নায় কয়টা হয়।
             *
             * চায়ের রেসিপিতে ১, বিরিয়ানির হাঁড়িতে ৫০। উপকরণের পরিমাণ
             * এই ফলনের **জন্য** লেখা — অর্থাৎ ৫০ প্লেটের হাঁড়িতে
             * "১০ কেজি চাল" মানে ৫০ প্লেটে ১০ কেজি, প্রতি প্লেটে নয়।
             *
             * ভগ্নাংশ লাগে: আধা হাঁড়ি রান্না হতেই পারে।
             */
            $table->decimal('yield_qty', 18, 4)->default('1');

            /*
             * নিষ্ক্রিয় রেসিপি — মুছে ফেলা নয়।
             *
             * পুরনো রেসিপিতে রান্না হওয়া খাবারের ইতিহাস ওই রেসিপির
             * দিকে দেখায়। মুছে ফেললে ইতিহাসটা অনাথ হত (নিয়ম ৫)।
             */
            $table->boolean('is_active')->default(true);

            $table->text('note')->nullable();
            $table->timestamps();
            $table->softDeletes();

            $table->unique(['company_id', 'product_id'], 'recipe_one_per_product');
        });

        Schema::create('inv_recipe_lines', function (Blueprint $table) {
            $table->id();
            $table->publicId();
            $table->foreignId('company_id')->constrained()->cascadeOnDelete();

            $table->foreignId('recipe_id')->constrained('inv_recipes')->cascadeOnDelete();
            $table->foreignId('product_id')->constrained('inv_products')->cascadeOnDelete();

            /*
             * উপরের ফলনের জন্য কতটা লাগে — অপচয় ছাড়া।
             *
             * অর্থাৎ রান্নায় যতটা সত্যিই **যায়**, বাজার থেকে যতটা আনতে
             * হয় তা নয়। পার্থক্যটা নিচের কলামে।
             */
            $table->decimal('qty', 18, 4);

            /*
             * অপচয়ের শতাংশ — খোসা, কাটাকুটি, শুকিয়ে যাওয়া।
             *
             * ── কেন আলাদা কলাম, পরিমাণে যোগ করে নয় ──────────────────
             * এক কেজি আলুর খোসা ছাড়ালে ৮৫০ গ্রাম থাকে। রাঁধুনি জানেন
             * "রান্নায় ৮৫০ গ্রাম যায়", আর ক্রেতা জানেন "১ কেজি কিনতে
             * হয়"। দুইটা আলাদা সংখ্যা, আর দুইটাই সত্যি।
             *
             * পরিমাণেই মিশিয়ে দিলে রেসিপিটা আর রেসিপি থাকত না, আর
             * "অপচয় কত" প্রশ্নের উত্তর কোনোদিন পাওয়া যেত না — অথচ
             * রেস্টুরেন্টে ওটাই সবচেয়ে বড় ফাঁক।
             *
             * স্টক কমে **অপচয় সহ**, কারণ গুদাম থেকে ওটাই বেরোয়।
             */
            $table->decimal('waste_pct', 8, 4)->default('0');

            $table->unsignedSmallInteger('sort')->default(0);
            $table->timestamps();

            /*
             * এক রেসিপিতে একই উপকরণ দুইবার নয়।
             *
             * দুইবার লেখা গেলে "২ কেজি চাল" আর "৩ কেজি চাল" পাশাপাশি
             * বসত, আর কোনটা ঠিক তা কেউ বলতে পারত না। দরকার হলে একটা
             * সারিতেই পরিমাণ বাড়ানো হবে।
             */
            $table->unique(['recipe_id', 'product_id'], 'recipe_line_one_per_product');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('inv_recipe_lines');
        Schema::dropIfExists('inv_recipes');
    }
};
