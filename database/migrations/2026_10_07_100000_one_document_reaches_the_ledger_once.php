<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * এক ডকুমেন্ট খতিয়ানে একবারই পৌঁছায় — ডাটাবেজ ধরে রাখে, কোড নয়।
 *
 * ── কী ভাঙা ছিল, ৩১ আগস্ট ২০২৬ ───────────────────────────────────────
 * [[PostingEngine::post()]] বসানোর আগে দেখত ডকুমেন্টটা আগে বসেছে কি না
 * ([[PostingEngine::assertNotAlreadyPosted()]])। কিন্তু ওই দেখাটা চলত
 * **লেনদেনের বাইরে**, কোনো লক ছাড়া।
 *
 * দুইটা রিকোয়েস্ট একসাথে এলে — Save-এ দুইবার ক্লিক, বা ধীর নেটওয়ার্কে
 * ব্রাউজারের পুনরায় পাঠানো — দুইজনেই "বসেনি" দেখে, দুইজনেই বসিয়ে দেয়।
 * ইঞ্জিনের নিজের মন্তব্যেই ঘটনাটা হুবহু লেখা ছিল; শুধু পাহারাটা ওটা
 * ঠেকাত না।
 *
 * ── কেন এটা সবচেয়ে বিপজ্জনক ধরনের বাগ ────────────────────────────────
 * দুইটা সেটই ডেবিট=ক্রেডিট মেলা। তাই **রেওয়ামিল মেলে, বইয়ের যাচাই সবুজ
 * থাকে, কোনো পাহারা লাল হয় না** — শুধু বিক্রি, খরচ আর বকেয়া দ্বিগুণ
 * দেখায়। ভুলটা ধরা পড়ে অনেক পরে, যখন কেউ একটা সংখ্যা মেলাতে গিয়ে
 * আটকে যান।
 *
 * ── কেন খতিয়ানে সরাসরি unique ইনডেক্স নয় ─────────────────────────────
 * প্রথমে `ledger_entries (source_type, source_id)`-এ unique ইনডেক্সের
 * কথা ভাবা হয়েছিল। **ওটা বসালে কোনো বিলই আর পোস্ট হত না**: এক ডকুমেন্ট
 * অনেক খতিয়ান-সারি বানায় (মেপে দেখা — একটা বিলে চারটা)।
 *
 * `source_line_id` জুড়ে দিলেও চলত না: মাপার দিন ৮৪টা সারির **৫৪টাতেই
 * ওটা NULL**, আর MySQL unique ইনডেক্সে যত খুশি NULL নেয় — অর্থাৎ ঠিক
 * যেখানে পাহারা দরকার, সেখানেই পাহারা থাকত না।
 *
 * তাই আলাদা একটা প্রহরী-টেবিল, যেখানে **এক ডকুমেন্ট = এক সারি**। সারিটা
 * বসে ঠিক ওই লেনদেনের ভেতরে যেটা খতিয়ানের সারিগুলো বসায়, তাই দ্বিতীয়
 * চেষ্টাটা unique key-তে ধাক্কা খেয়ে **পুরো লেনদেনটাই ফিরিয়ে দেয়** —
 * অর্ধেক বসা খতিয়ান বলে কিছু থাকে না।
 *
 * ── কেন ইঞ্জিনের ভেতরে, প্রতিটা সার্ভিসে নয় ──────────────────────────
 * খতিয়ানে লেখার দরজা একটাই। ওখানে বসালে আজকের কুড়িটা কল-সাইট আর
 * আগামীর সবগুলোও একসাথে ঢাকা পড়ে — নতুন কোনো মডিউল এটা বসাতে ভুলতে
 * পারে না, কারণ বসানোর কিছু নেই।
 *
 * ── বাতিল করে আবার বসানো আগের মতোই চলে ───────────────────────────────
 * উল্টো এন্ট্রি নিজের আলাদা source_type নিয়ে আসে (`…:reversal`), তাই
 * সে নিজের সারি পায়। আর মূল ডকুমেন্টের সারিটা থেকে যায় — অর্থাৎ
 * বাতিলের পর একই ডকুমেন্ট আবার বসানো যায় না, ঠিক যেমন আগেও যেত না।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('posted_documents', function (Blueprint $table) {
            $table->id();
            $table->publicId();

            $table->foreignId('company_id')->constrained('companies')->cascadeOnDelete();

            /*
             * ডকুমেন্টটা কে — খতিয়ানের সারিতে যে জোড়াটা বসে, ঠিক সেটাই।
             *
             * দৈর্ঘ্য ৬৪, `ledger_entries.source_type`-এর সমান। ছোট
             * রাখলে লম্বা নামের কোনো source_type নীরবে কাটা পড়ত, আর
             * তখন দুইটা আলাদা ডকুমেন্ট একই চাবিতে মিলে যেত।
             */
            $table->string('source_type', 64);
            $table->unsignedBigInteger('source_id');

            $table->timestamp('posted_at');

            /*
             * কে বসাল — জানা থাকলে ভালো, না থাকলেও পাহারা কাজ করে।
             *
             * nullable, কারণ কিছু পোস্টিং কনসোল কমান্ড থেকে হয় (খোলার
             * জের, বছর-শেষ), আর ওখানে কোনো লগইন করা মানুষ নেই।
             */
            $table->foreignId('posted_by')->nullable()->constrained('users')->nullOnDelete();

            /*
             * এই একটা লাইনই আসল পাহারা। বাকি সব সুবিধা।
             *
             * company_id সামনে, কারণ দুই কোম্পানির দুইটা ডকুমেন্টের
             * id এক হতেই পারে — ওরা আলাদা বই।
             */
            $table->unique(['company_id', 'source_type', 'source_id'], 'uq_posted_document');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('posted_documents');
    }
};
