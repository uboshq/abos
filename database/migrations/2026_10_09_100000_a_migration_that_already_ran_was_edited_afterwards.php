<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\Schema;

/**
 * খরচের কেন্দ্রের টেবিলটা কোথাও `mdm_`, কোথাও `acc_` — আর সেটা কেউ জানত না।
 *
 * ── কী ঘটেছিল, ৩১ আগস্ট ২০২৬-এ ধরা পড়ল ───────────────────────────────
 * `2026_09_09_100000_the_delivery_cost_had_nowhere_to_land` টেবিলটা
 * প্রথমে `mdm_cost_centers` নামে বানিয়েছিল। পরে কমিট `4645035` ওই
 * **আগেই চলে যাওয়া** মাইগ্রেশন ফাইলটাই এডিট করে নামটা `acc_cost_centers`
 * করে দেয়।
 *
 * চলে যাওয়া মাইগ্রেশন আর দ্বিতীয়বার চলে না — `migrations` টেবিলে তার
 * নাম বসে আছে। তাই যে ডাটাবেজ ওই কমিটের **আগে** মাইগ্রেট হয়েছে, তার
 * টেবিলের নাম আজও `mdm_cost_centers`, আর `migrate:status` দিব্যি বলে
 * "সব চলেছে"।
 *
 * ── কী ভাঙে ─────────────────────────────────────────────────────────
 * `CostCenter` মডেল `acc_cost_centers` খোঁজে। পুরনো নামের ডাটাবেজে —
 *
 *   · খরচের কেন্দ্রের মাস্টার পর্দা **৫০০** দেয়, সবসময়;
 *   · `CoreReports`-এর খরচ-কেন্দ্রের রিপোর্ট ওই টেবিলে join করে, তাই
 *     সেটাও ভাঙে।
 *
 * ── কেন কোনো টেস্ট বা CI এটা ধরেনি ───────────────────────────────────
 * CI প্রতিবার `migrate:fresh` চালায়, অর্থাৎ শূন্য থেকে। শূন্য থেকে
 * চালালে নতুন নামটাই তৈরি হয়, আর সব সবুজ। **এই শ্রেণির ভুল কেবল
 * সেখানেই দেখা যায় যেখানে ডাটাবেজটা পুরনো** — অর্থাৎ উন্নয়নের মেশিনে
 * আর চালু সার্ভারে, ঠিক যে দুই জায়গায় ভুলটা দামি।
 *
 * ধরা পড়েছে পর্দাটা ব্রাউজারে খুলে, ১৫৯টা পর্দা ঝাড়তে গিয়ে। কোনো
 * পরীক্ষা এটা ধরতে পারত না।
 *
 * ── কেন এই মাইগ্রেশনটা নিরাপদ ────────────────────────────────────────
 * নতুন ডাটাবেজে পুরনো নামটাই থাকে না, তাই কিছুই ঘটে না। পুরনো
 * ডাটাবেজে নামটা বদলায়, আর MySQL-এর `RENAME TABLE` বাইরের কী-গুলোকে
 * নিজে থেকেই নতুন নামে তাক করায় — `ledger_entries` ও `voucher_lines`
 * দুইটার জোড়াই অক্ষত থাকে।
 *
 * ── আর শিক্ষাটা ─────────────────────────────────────────────────────
 * **চলে যাওয়া মাইগ্রেশন আর সম্পাদনা করা যায় না।** নাম বদলাতে হলে নতুন
 * একটা মাইগ্রেশন, যেটা `rename` করে — তাহলে প্রতিটা ডাটাবেজ একই পথে
 * একই জায়গায় পৌঁছায়।
 */
return new class extends Migration
{
    public function up(): void
    {
        if (Schema::hasTable('mdm_cost_centers') && ! Schema::hasTable('acc_cost_centers')) {
            Schema::rename('mdm_cost_centers', 'acc_cost_centers');
        }
    }

    public function down(): void
    {
        /*
         * ফেরানো হয় না — ইচ্ছাকৃত।
         *
         * ফিরিয়ে দিলে ডাটাবেজটা আবার সেই ভাঙা অবস্থায় ফিরত যেখানে
         * মডেল এক নাম খোঁজে আর টেবিল অন্য নামে বসে থাকে। rollback-এর
         * উদ্দেশ্য আগের **কাজ করা** অবস্থায় ফেরা, আগের ভুলে নয়।
         */
    }
};
