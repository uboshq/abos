<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * একই ছাঁকনি রোজ সকালে আবার টাইপ করা হত।
 *
 * ── কী সমস্যা ────────────────────────────────────────────────────────
 * তালিকার পর্দাগুলোর ছাঁকনি কোয়েরি-স্ট্রিংয়ে থাকে — সেটা ইচ্ছাকৃত, কারণ
 * তখন লিংকটা পাঠালে অন্যজন ঠিক সেটাই দেখেন। কিন্তু **ফিরে আসার** কোনো
 * উপায় ছিল না: যিনি রোজ "এই মাসের, বকেয়া, ময়মনসিংহ এলাকার" তালিকাটা
 * দেখেন, তাঁকে রোজ তিনটা ঘর আবার ভরতে হত।
 *
 * ── কেন এটা D365-এর নকলের সাথে জড়িত ─────────────────────────────────
 * Dynamics 365-এর তালিকার পর্দার সবচেয়ে চেনা জিনিসটাই হলো **শিরোনামটা
 * নিজেই একটা ড্রপডাউন** — "Active Accounts ⌄" — আর ওই তালিকায় থাকে
 * সংরক্ষিত দৃশ্যগুলো।
 *
 * ২৭ অগাস্ট ২০২৬-এ দশটা রূপ মিলিয়ে দেখতে গিয়ে ওই সারিটা "নেই" এসেছিল।
 * কিন্তু ওটা কেবল একটা ড্রপডাউন বসিয়ে দেওয়ার কাজ নয় — ড্রপডাউনে
 * **রাখার মতো কিছু** না থাকলে সেটা একটাই আইটেমের মৃত বোতাম, আর এই
 * প্রকল্পের নিজের নিয়মই ওরকম বোতাম নিষেধ করে।
 *
 * তাই আগে জিনিসটা, তারপর তার চেহারা।
 *
 * ── কেন `query` একটা string, সাজানো কলাম নয় ──────────────────────────
 * প্রতিটা পর্দার ছাঁকনি আলাদা: পণ্যের তালিকায় ব্র্যান্ড ও শ্রেণি, বিলের
 * তালিকায় তারিখ ও পক্ষ ও অবস্থা। কলামে ভাঙলে প্রতিটা নতুন ছাঁকনির জন্য
 * একটা মাইগ্রেশন লাগত।
 *
 * সংরক্ষিত জিনিসটা আসলে **ওই পর্দার URL-এর কোয়েরি অংশ** — যেটা এমনিতেই
 * পর্দাগুলোর নিজস্ব চুক্তি। তাই ওটাই হুবহু রাখা হয়, আর ফিরিয়ে দেওয়ার
 * সময় কেবল জোড়া লাগানো হয়।
 *
 * ── কেন কোম্পানিও কলামে ─────────────────────────────────────────────
 * একই ব্যবহারকারী একাধিক কোম্পানিতে কাজ করতে পারেন, আর এক কোম্পানির
 * এলাকা বা ব্র্যান্ডের ছাঁকনি অন্য কোম্পানিতে অর্থহীন — সেখানে ওই
 * আইডিগুলোরই অস্তিত্ব নেই। দৃশ্যটা তাই কোম্পানির ভেতরেই থাকে।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('ui_saved_views', function (Blueprint $table) {
            $table->id();

            /*
             * বাইরের কী।
             *
             * ── কেন এটা এখানেও, যদিও দৃশ্য বাইরে যায় না ──────────────
             * প্রথম লেখায় এটা বাদ দেওয়া হয়েছিল, যুক্তি ছিল: দৃশ্যগুলো
             * কেবল নিজের, কোনো API-তে বেরোয় না।
             *
             * [[PublicIdTest]] তাতে লাল হলো, আর ঠিকই হলো। ওর ছাড়ের
             * তালিকায় কেবল ফ্রেমওয়ার্কের টেবিল, আর ওই তালিকা বাড়তে
             * দিলে একদিন সেটাই পুরো তালিকা হয়ে যেত — এই প্রকল্পে
             * ওরকম পাহারা আগে তিনবার ধরা পড়েছে।
             *
             * কলামটা সস্তা, আর ব্যতিক্রমটা দামি। তাই কলামটাই।
             */
            $table->publicId();

            $table->foreignId('user_id')->constrained()->cascadeOnDelete();
            $table->foreignId('company_id')->constrained()->cascadeOnDelete();

            /*
             * কোন পর্দার দৃশ্য — রুটের নাম, URL নয়।
             *
             * URL রাখলে একদিন পথ বদলালে (`/products` → `/inventory/products`)
             * সব সংরক্ষিত দৃশ্য অনাথ হয়ে যেত। রুটের নাম কোডের ভেতরের
             * চুক্তি, আর সেটা বদলালে টেস্টই ধরে।
             */
            $table->string('screen', 120);

            $table->string('name', 80);

            /*
             * ছাঁকনির কোয়েরি — `brand=3&status=due&sort=-date`।
             *
             * `text`, `string` নয়: অনেকগুলো ছাঁকনি একসাথে দিলে ২৫৫ ছাড়াতে
             * পারে, আর তখন কাটা পড়া দৃশ্যটা **ভুল তালিকা** দেখাত — খালি
             * নয়, ভুল। নীরব ভুলের চেয়ে বড় ঘর সস্তা।
             */
            $table->text('query');

            /*
             * পর্দাটা খুললে নিজে থেকেই এই দৃশ্যটা।
             *
             * প্রতি ব্যবহারকারী প্রতি পর্দায় একটাই — নিচের আংশিক ইউনিক
             * সূচক সেটা বাঁধত, কিন্তু MySQL আংশিক সূচক বোঝে না। তাই
             * নিয়মটা কোডে (`SavedView::makeDefault()`), আর এখানে কেবল
             * খোঁজার সূচক।
             */
            $table->boolean('is_default')->default(false);

            $table->timestamps();

            /*
             * একই পর্দায় একই নামের দুইটা দৃশ্য বিভ্রান্তিকর — ড্রপডাউনে
             * পাশাপাশি দুইটা "বকেয়া" দেখে কেউ বুঝতেন না কোনটা কোনটা।
             */
            $table->unique(['user_id', 'company_id', 'screen', 'name'], 'saved_view_name_unique');

            // পর্দা খোলার সময়ের প্রশ্নটা: "আমার এই পর্দার দৃশ্যগুলো কী"
            $table->index(['user_id', 'company_id', 'screen'], 'saved_view_lookup');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('ui_saved_views');
    }
};
