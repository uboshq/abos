<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

/**
 * প্রথম যেটা তাঁরা দেখবেন, সেটা আমাদের নিজেরটা হোক।
 *
 * ── কাল যা ঠিক ছিল, আজ আর নয় ────────────────────────────────────────
 * গতকাল `users.ui`-এর ডিফল্ট রাখা হয়েছিল `classic`, আর যুক্তিটা ছিল
 * সতর্কতা: মাইগ্রেশন চললেই কারও পর্দা যেন নিজে থেকে বদলে না যায়।
 *
 * সতর্কতাটা ঠিক ছিল, কিন্তু প্রশ্নটা ভুল ছিল। আসল প্রশ্ন হল — একজন
 * ব্যবহারকারী প্রথম দিন ABOS খুলে কোনটা দেখবেন?
 *
 * বেশিরভাগ মানুষ চেহারার পাতাটা কোনোদিন খুলবেন না। ডিফল্ট ক্লাসিক
 * থাকলে তাঁরা কোনোদিন ABOS-এর নিজের নকশাটা দেখতেনই না — সাতটা চেহারা
 * তৈরি হয়ে পড়ে থাকত, আর সফটওয়্যারটা নিজের কাজ নিজেই লুকিয়ে রাখত।
 *
 * ── যাঁরা এখন `classic`-এ আছেন তাঁদেরও সরানো হচ্ছে, আর কেন সেটা নিরাপদ ─
 * কলামটা তৈরি হয়েছে গতকাল, ২০ সেপ্টেম্বরের মাইগ্রেশনে, আর তার ডিফল্টই
 * ছিল `classic`। অর্থাৎ আজ যত সারিতে `classic` লেখা আছে তার একটাও
 * কারও **বাছাই** নয় — সবগুলোই ওই ডিফল্টের ছাপ।
 *
 * কেউ সত্যিই ক্লাসিক পছন্দ করলে চেহারার পাতা থেকে এক ক্লিকে ফিরে
 * যেতে পারবেন, আর তখন সেটা সত্যিকারের বাছাই হবে।
 *
 * পরে কোনোদিন এই ধরনের বদল করতে গেলে এটা আর করা যাবে না — তখন
 * `classic` মানে হবে "একজন মানুষ এটা বেছেছেন", আর কারও বাছাই
 * মাইগ্রেশনে মুছে ফেলা যায় না।
 */
return new class extends Migration
{
    public function up(): void
    {
        /*
         * সারিগুলো আগে, ডিফল্ট পরে — ক্রমটা ইচ্ছাকৃত।
         *
         * উল্টো করলে দুইটার মাঝের মুহূর্তে নতুন কোনো ব্যবহারকারী তৈরি
         * হলে তিনি `navy` পেতেন, আর তার পরপরই নিচের UPDATE তাঁকে
         * ছুঁত না — কারণ তিনি `classic` নন। ফল একই হত, কিন্তু কারণটা
         * দুর্ঘটনা হত, নকশা নয়।
         */
        DB::table('users')->where('ui', 'classic')->update(['ui' => 'navy']);

        Schema::table('users', function ($table) {
            $table->string('ui', 16)->default('navy')->change();
        });
    }

    public function down(): void
    {
        Schema::table('users', function ($table) {
            $table->string('ui', 16)->default('classic')->change();
        });

        /*
         * ফেরার পথে সারিগুলো ছোঁয়া হয় না।
         *
         * এই মাইগ্রেশনটা চলার পরে কেউ ইচ্ছে করে নেভি বেছে থাকতে
         * পারেন। উল্টোদিকে গিয়ে সব নেভিকে ক্লাসিক করে দিলে তাঁর
         * বাছাইটাই মুছে যেত — আর সেটা এই মাইগ্রেশনের চেয়ে খারাপ,
         * কারণ এটা অন্তত জানত যে কোনো বাছাই ছিল না।
         */
    }
};
