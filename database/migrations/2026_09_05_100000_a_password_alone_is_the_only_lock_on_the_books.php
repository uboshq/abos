<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * খাতার একমাত্র তালা একটা পাসওয়ার্ড।
 *
 * ── আজ যা ঘটতে পারে ─────────────────────────────────────────────────
 * মালিকের পাসওয়ার্ডটা জানলেই গোটা ব্যবসার খাতা খোলা — প্রতিটা বিল,
 * প্রতিটা ক্রয়মূল্য, প্রতিটা ব্যাংক হিসাব। আর পাসওয়ার্ড ফাঁস হওয়ার পথ
 * অনেক: কর্মীর দেখে ফেলা, কাগজে লিখে রাখা, একই পাসওয়ার্ড অন্য কোথাও
 * ব্যবহার করা।
 *
 * ── কেন TOTP, SMS নয় ────────────────────────────────────────────────
 * SMS-এ টাকা লাগে (মালিকের নিয়ম "paid sarvis bad"), আর বাংলাদেশে
 * দেরিতে আসে বা আসেই না — তখন কেউ নিজের ব্যবস্থায় ঢুকতে পারেন না।
 * Google Authenticator ধাঁচের অ্যাপ ইন্টারনেট ছাড়াই কোড বানায়, তাই
 * ডিপোতে নেট গেলেও লগইন চলে।
 *
 * ── কেন পুনরুদ্ধার কোড বাধ্যতামূলক ──────────────────────────────────
 * ফোন হারানো, ভাঙা, বা চুরি — তিনটাই বাস্তব। পুনরুদ্ধারের পথ ছাড়া MFA
 * চালু করা মানে একদিন মালিক নিজেই নিজের খাতা থেকে চিরতরে বাইরে,
 * আর সেই দিন MFA-টাই সবচেয়ে বড় ক্ষতি করত।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('users', function (Blueprint $table): void {
            /*
             * গোপন চাবিটা — খালি মানে MFA চালু নেই।
             *
             * ── কেন আলাদা একটা "চালু" ঘর নেই ────────────────────────
             * দুইটা ঘর থাকলে তারা আলাদা হতে পারত: চাবি আছে অথচ বন্ধ,
             * বা চালু অথচ চাবি নেই। দ্বিতীয়টা মানে কেউ কোনোদিন ঢুকতেই
             * পারতেন না। একটা ঘরে ওই অবস্থাগুলোর অস্তিত্বই নেই।
             */
            $table->text('mfa_secret')->nullable()->after('password');

            /*
             * কবে থেকে চালু।
             *
             * চাবি বসানোর পর প্রথম কোডটা মেলা পর্যন্ত MFA চালু হয় না —
             * নাহলে ভুল করে বসানো একটা চাবি মানুষটাকে বাইরে রেখে দিত।
             * এই তারিখটাই বলে দেয় যাচাই হয়ে গেছে।
             */
            $table->timestamp('mfa_confirmed_at')->nullable()->after('mfa_secret');

            /*
             * পুনরুদ্ধার কোডগুলো — হ্যাশ করা, JSON তালিকায়।
             *
             * পাসওয়ার্ডের মতোই হ্যাশ, কারণ এগুলো পাসওয়ার্ডের সমান
             * ক্ষমতা রাখে: একটা দিয়েই MFA পেরিয়ে ঢোকা যায়। ডাটাবেজ
             * ফাঁস হলে সাদা কোডগুলো MFA-টাকে অর্থহীন করে দিত।
             */
            $table->text('mfa_recovery_codes')->nullable()->after('mfa_confirmed_at');
        });
    }

    public function down(): void
    {
        Schema::table('users', function (Blueprint $table): void {
            $table->dropColumn(['mfa_secret', 'mfa_confirmed_at', 'mfa_recovery_codes']);
        });
    }
};
