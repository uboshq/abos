<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * কোম্পানির নিজের রূপ — থিম ইঞ্জিনের ধাপ ২।
 *
 * ── আজ পর্যন্ত যা ছিল ───────────────────────────────────────────────
 * দশটা রূপ, আর দশটাই আমাদের লেখা। একটা কোম্পানি নিজের রং চাইলে
 * একমাত্র উত্তর ছিল "আমরা কোডে বসিয়ে দেব" — অর্থাৎ প্রতিটা গ্রাহকের
 * জন্য একটা করে ডিপ্লয়।
 *
 * ── কেন উত্তরাধিকার, পুরো একটা সেট নয় ────────────────────────────────
 * একটা রূপে ষাটের বেশি টোকেন। কোম্পানি সাধারণত চায় **ছয়টা** —
 * তার নিজের নীল, তার লোগোর সবুজ। বাকি চুয়ান্নটা কপি করে রাখলে
 * তিনটা সমস্যা:
 *
 *   · আমরা মূল রূপের একটা রং শোধরালে কোম্পানিরটা পুরনো থেকে যেত
 *   · কোনটা সে ইচ্ছে করে বদলেছে আর কোনটা কেবল কপি, বলা যেত না
 *   · একটা সারিতে ষাটটা মান — সম্পাদনার পর্দাটা ভয়ংকর হত
 *
 * তাই সারিতে থাকে **কেবল পার্থক্যটুকু**, আর `parent` বলে কার উপর
 * দাঁড়ানো। "Navy + ছয়টা বদল" — ষাটটা নয়।
 *
 * ── কেন `company_id` নালযোগ্য ────────────────────────────────────────
 * শূন্য মানে সিস্টেমের নিজের রূপ — সব কোম্পানিতে দেখা যায়। ধাপ ৩-এ
 * আমরা নিজেরাই এভাবে নতুন রূপ পাঠাতে পারব, কোড ডিপ্লয় ছাড়াই।
 *
 * ── প্রকাশ না হওয়া পর্যন্ত কেউ দেখে না ────────────────────────────────
 * `published_at` খালি মানে খসড়া। কনট্রাস্ট গেট পাশ না করলে ওটা কখনো
 * ভরে না — অর্থাৎ পড়া যায় না এমন রূপ কারো পর্দায় পৌঁছাতেই পারে না।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('look_skins', function (Blueprint $table) {
            $table->id();
            $table->uuid('public_id')->unique();

            /*
             * নাল মানে সিস্টেমের রূপ — সবাই দেখে।
             *
             * `cascadeOnDelete` নয়, কারণ কোম্পানি সফট-ডিলিট হয়; সারিটা
             * থেকে গেলে ফিরিয়ে আনার দিন রূপটাও ফেরে।
             */
            $table->foreignId('company_id')->nullable()
                ->constrained('companies')->nullOnDelete();

            // পর্দায় দেখানোর নাম — "আদির নিজস্ব", "শীতের রূপ"
            $table->string('name', 120);

            /*
             * কার উপর দাঁড়ানো — `Ui::keys()`-এর একটা, বা আরেকটা স্কিন।
             *
             * টেক্সট, বিদেশি চাবি নয়: পূর্বপুরুষ একটা কোড-রূপও হতে পারে
             * (`navy`), যার কোনো সারি নেই। দুইটা আলাদা ঘর রাখলে প্রতিটা
             * কোয়েরিতে "কোনটা ভরা" জিজ্ঞেস করতে হত।
             */
            $table->string('parent', 64);

            /*
             * কেবল পার্থক্যটুকু — টোকেনের নাম => মান।
             *
             * হালকা ও গাঢ় দুইটা আলাদা, ঠিক `Looks/*.php`-এর মতোই।
             */
            $table->json('tokens');

            /*
             * প্রকাশের সময়। খালি মানে খসড়া, কেউ দেখে না।
             *
             * কনট্রাস্ট গেট পাশ না করলে এটা ভরে না — পড়া যায় না এমন
             * রূপ কারো পর্দায় পৌঁছাতে পারে না, আর সেটাই গেটের মানে।
             */
            $table->timestamp('published_at')->nullable();

            $table->foreignId('created_by')->nullable()
                ->constrained('users')->nullOnDelete();
            $table->timestamps();
            $table->softDeletes();

            /*
             * এক কোম্পানিতে এক নামে দুইটা রূপ নয়।
             *
             * দুইটা "আদির নিজস্ব" থাকলে বাছাইয়ের পর্দায় কোনটা কোনটা
             * বলা যেত না, আর মানুষ ভুলটা বেছে ভাবতেন সেভ হয়নি।
             */
            $table->unique(['company_id', 'name'], 'look_skin_name_unique');
            $table->index(['company_id', 'published_at'], 'look_skin_lookup');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('look_skins');
    }
};
