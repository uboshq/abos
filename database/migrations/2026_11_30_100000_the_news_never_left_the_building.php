<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * খবরটা ভবনের বাইরে যেত না — চিঠির সুইচ।
 *
 * ── ⛔ যা মেপে পাওয়া গেল, ২২ সেপ্টেম্বর ২০২৬ ────────────────────────
 * ABOS আজ **তিনটা** চিঠি পাঠায়, আর তিনটাই অ্যাকাউন্ট নিয়ে: পাসওয়ার্ড
 * রিসেট, ইমেইল বদলের লিংক, ইমেইল বদলের সতর্কতা।
 *
 * ⚠️ ব্যবসার একটাও ঘটনা চিঠি পাঠায় না। অনুমোদন হলো বা ফেরত এল, জমার
 * মেয়াদ ফুরাচ্ছে, হাতধারের তারিখ পেরিয়েছে, সূচিমতো রিপোর্ট তৈরি —
 * সবগুলোই কেবল **ঘণ্টায় একটা সারি**, আর ঘণ্টাটা অ্যাপের ভিতরে।
 *
 * ⛔ অর্থাৎ যিনি লগইন করেন না, তিনি কোনোদিন জানেন না। ⓘ সবচেয়ে ধারালো
 * উদাহরণ সূচিমতো রিপোর্ট: **সূচির গোটা মানেই** হলো কেউ না চাইতেই
 * জিনিসটা এসে পৌঁছাবে — অথচ আজ সেটা private ডিস্কে বসে থাকে কেউ খুঁজতে
 * আসার অপেক্ষায়।
 *
 * ⭐ ABOS-এর চেনা রোগটাই: কাজটা হয়েছে (ঘণ্টা আছে, SMTP বসানো আছে,
 * চিঠির ছাঁচ আছে), জোড়াটা নয়।
 *
 * ── ⓘ কেন `enabled`-এর পাশে আরেকটা ঘর, আলাদা টেবিল নয় ───────────────
 * পছন্দটা একই জিনিসের দুইটা দিক: *"এই খবরটা আমি চাই"* আর *"এটা আমার
 * ইনবক্সেও যাক"*। ⚠️ আলাদা টেবিল হলে একজনের একই ধরনের জন্য দুই জায়গায়
 * দুইটা সারি থাকত, আর একটা মুছে গেলে অন্যটা নীরবে টিকে থাকত।
 *
 * ── ⭐ কেন `null`, `false` নয় ───────────────────────────────────────
 * `null` মানে *"আমি কিছু বলিনি — ধরনটার নিজের নিয়ম চলুক"*
 * ([[NotificationKinds]]-এর `MAIL_BY_DEFAULT`)। ⛔ ডিফল্ট `false` বসালে
 * ফিচারটা জন্মের দিনেই অদৃশ্য হত: কেউ সেটিংসে গিয়ে টিক না দেওয়া
 * পর্যন্ত একটা চিঠিও যেত না, আর কেউ জানতই না যে যাওয়ার কথা ছিল।
 *
 * ⚠️ আর ডিফল্ট `true` (সব খবর চিঠিতে) এক সপ্তাহে মানুষকে ABOS-এর
 * ঠিকানা স্প্যামে ফেলতে বাধ্য করত — তারপর যে চিঠিটা সত্যিই জরুরি,
 * সেটাও স্প্যামে পড়ত। ⓘ এটা ঠিক ঐ ভুল যেটা [[NotificationService]]-এর
 * নিজের টীকা ঘণ্টার বেলায় বারণ করে।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('notification_choices', function (Blueprint $table) {
            /*
             * ⓘ nullable, ডিফল্ট নেই — খালি থাকাটাই একটা অর্থবহ উত্তর:
             * "ধরনটার নিজের নিয়ম চলুক"। ⚠️ `->default(false)` লিখলে
             * পুরনো প্রতিটা সারি (যেগুলো কেবল ঘণ্টা বন্ধ করতে লেখা
             * হয়েছিল) হঠাৎ "চিঠি চাই না" বলতে শুরু করত — অথচ ঐ মানুষরা
             * চিঠির ব্যাপারে **কিছুই বলেননি**, কারণ তখন চিঠি ছিলই না।
             */
            $table->boolean('by_email')->nullable()->after('enabled');
        });
    }

    public function down(): void
    {
        Schema::table('notification_choices', function (Blueprint $table) {
            $table->dropColumn('by_email');
        });
    }
};
