<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * ⛔ দুইটা ক্লিক দুইটা সারি বানিয়েছিল — ১৩ সেপ্টেম্বর ২০২৬।
 *
 * ── কী ঘটেছিল ────────────────────────────────────────────────────────
 * মালিক মূলধনের ফর্মে তথ্য ভরে Save-এ **দুইবার ক্লিক করেছেন**, আর
 * `CAP-0001` ও `CAP-0002` — দুইটা সারি তৈরি হয়েছে। ⚠️ ২৫,০০,০০০ টাকা
 * দুইবার। [[DuplicationEngine]] এটা ধরেনি, আর ধরার কথাও নয়: সে
 * **মাস্টার রেকর্ডের** নকল ধরে, লেনদেনের নয়।
 *
 * ⓘ মেপে দেখা: ১৪৭টা ব্লেড ফাইলে POST ফর্ম, ২৬৯টা POST রুট, আর
 * রিপোর **কোনো স্তরেই** দ্বৈত-জমার পাহারা ছিল না। অর্থাৎ এটা মূলধনের
 * পর্দার রোগ নয়, প্রতিটা পর্দার।
 *
 * ── ⭐ কেন অঙ্ক মিলিয়ে ধরা যায় না ───────────────────────────────────
 * এই রিপো সিদ্ধান্তটা আগেই নিয়েছে, [[SlipIsNotUsedTwice]]-এ মালিকের
 * নিজের কথায়: *"একই গ্রাহক পরপর দুই সপ্তাহে হুবহু একই মাল নিতে পারেন,
 * হুবহু একই টাকা দিতে পারেন — ওটা নকল নয়, ওটাই ব্যবসা।"* ⛔ পেলোড
 * তুলনা করে আটকালে সফটওয়্যার **সৎ লেনদেনই আটকাত**, আর মালিক আজ স্পষ্ট
 * বলেছেন একই মালিক আবার বিনিয়োগ করতে পারেন।
 *
 * ⭐ যা কখনো দুইবার হয় না তা হলো **ফর্মের একটা রেন্ডার**। তাই নিয়মটা
 * অঙ্কের নয়, রেন্ডারের: **একটা রেন্ডার থেকে সর্বোচ্চ একটা সারি**।
 * ⓘ বৈধ পুনরাবৃত্তি আপনাআপনি ছাড় পায় — তার জন্য মানুষ ফর্মটা আবার
 * খোলেন, আর সেটা নতুন রেন্ডার, নতুন টোকেন। দুইবার ক্লিক একটাই রেন্ডার।
 *
 * ── ⚠️ কেন টেবিল, আর কেন ইউনিক ইনডেক্সটাই আসল পাহারা ────────────────
 * দুইবার ক্লিকে দুইটা অনুরোধ **কয়েক মিলিসেকেন্ডের ব্যবধানে** আসে, আর
 * দুইটাই "আগে দেখি সারিটা আছে কিনা" করলে **দুইটাই দেখত নেই** — তারপর
 * দুইটাই লিখত। ⛔ পড়া-তারপর-লেখা এখানে কিছুই আটকায় না।
 *
 * ⭐ তাই সিদ্ধান্তটা ডাটাবেসের হাতে: `token`-এ ইউনিক ইনডেক্স, আর
 * **INSERT নিজেই বিচারক** — একটা ঢোকে, অন্যটা ছিটকে যায়। ⓘ এটা এই
 * রিপোর পরিচিত ছাঁচ: `vouchers_bank_reference_unique` ঠিক একই কাজ করে
 * একই ব্যাংক লেনদেন দুইবার ওঠা আটকাতে।
 *
 * ── কেন `company_id` নেই ────────────────────────────────────────────
 * সারিটা ব্যবসার তথ্য নয়, **যন্ত্রের হিসাব** — `sessions` বা `cache`-এর
 * মতো। ⓘ একটা জমা কোন কোম্পানির, সেটা যে সারিটা তৈরি হলো সে-ই জানে।
 * এখানে কলামটা বসালে একই সত্য দুই জায়গায় থাকত।
 * (`EveryRawQueryNamesItsCompanyTest`-এর `NO_COMPANY_COLUMN`-এ কারণসহ
 * ঘোষিত।)
 *
 * ── ⚠️ ফাইলের তারিখটা ৯ নভেম্বর কেন ─────────────────────────────────
 * এই রিপোর মাইগ্রেশনের তারিখ বাস্তব দিনের চেয়ে এগিয়ে — গুনে দেখা,
 * সবচেয়ে পরেরটা `2026_11_08_110000`। ⛔ আজকের তারিখ (`2026_09_13`)
 * দিলে এই ফাইলটা অর্ধেক টেবিল জন্মানোর **আগে** চলত, `migrate` সবুজ
 * দেখাত, আর টেবিলটা বসত না।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('submitted_forms', function (Blueprint $table) {
            $table->id();

            /*
             * ⭐ পুরো পাহারাটা এই একটা লাইনে।
             *
             * ⓘ uuid7, ৩৬ অক্ষর। `unique()` ছাড়া এই টেবিলটার কোনো মানে
             * নেই — ওটাই দুইটা একসাথে আসা অনুরোধের মধ্যে বিচার করে।
             */
            $table->string('token', 36)->unique();

            /*
             * কে জমা দিলেন — nullable, কারণ অতিথির ফর্মও আছে
             * (লগইন, পাসওয়ার্ড রিসেট)।
             *
             * ⓘ `cascadeOnDelete` নয়, `nullOnDelete`: ব্যবহারকারী
             * কখনো শক্ত করে মোছা হয় না (নিয়ম ৫), কিন্তু কলামটা
             * যন্ত্রের হিসাব — কেউ মুছে গেলে সারিটা থেকে যাক।
             */
            $table->foreignId('user_id')->nullable()->constrained()->nullOnDelete();

            // কোন দরজা দিয়ে — খোঁজার সময় এটাই সবচেয়ে কাজে লাগে
            $table->string('route', 191)->nullable();

            /*
             * ⛔ প্রথম জমার ফলটা — আর এটাই দ্বিতীয় ক্লিককে সৎ উত্তর
             * দেওয়ার একমাত্র উপায়।
             *
             * ⚠️ এটা না রাখলে দ্বিতীয় অনুরোধ কেবল বলতে পারত "আগে হয়ে
             * গেছে", কিন্তু **কোথায় হয়েছে তা দেখাতে পারত না** — আর
             * ব্যবহারকারী তখন নিজে খুঁজতে যেতেন।
             *
             * খালি থাকে যতক্ষণ প্রথম অনুরোধটা চলছে; তাই দ্বিতীয়টা
             * অপেক্ষা করে, তারপর এখান থেকেই ঠিকানা নেয়।
             */
            $table->string('result_url', 2048)->nullable();

            $table->timestamp('created_at')->nullable();
            $table->timestamp('completed_at')->nullable();

            /*
             * ছাঁটাইয়ের জন্য — `routes/console.php`-এ রোজ একবার পুরনো
             * সারি মুছে ফেলা হয়, আর সে এই ইনডেক্সটা ধরেই খোঁজে।
             */
            $table->index('created_at');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('submitted_forms');
    }
};
