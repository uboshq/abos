<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * নিজের প্রোফাইলে যোগাযোগের ঘর — মোবাইল, বিকল্প মোবাইল, ঠিকানা।
 *
 * ── কী ছিল না, ১৪ সেপ্টেম্বর ২০২৬ ────────────────────────────────────
 * মালিক প্রোফাইল পাতা খুলে বললেন: *"Nam ache email ache… mobile no nai
 * secondary mobile nai, address nai"*। ⛔ আর কথাটা পর্দার নয়, ডাটাবেজের:
 * `users` টেবিলে ঐ তিনটা **ঘরই ছিল না**, তাই পাতায় বসানোরও কিছু ছিল না।
 *
 * ── ⚠️ কেন `users`-এ, `hr_employees`-এ নয় ────────────────────────────
 * ⓘ HR-এর কর্মী-রেকর্ডে ইতিমধ্যে একটা `mobile` আছে, আর প্রথম দেখায় মনে
 * হয় ওটাই যথেষ্ট। ⛔ কিন্তু দুইটা আলাদা জিনিস:
 *
 *   `hr_employees.mobile`  → কর্মীর সরকারি নম্বর — বেতন, ছুটি, পরিচয়পত্র
 *   `users.mobile`         → **যিনি এখন বসে আছেন** তাঁকে কোথায় পাওয়া যাবে
 *
 * ⚠️ প্রতিটা ব্যবহারকারী কর্মী নন (মালিক নিজে, বাইরের হিসাবরক্ষক, ভাড়া
 * করা প্রশাসক), আর তাঁদের কারো কোনো `hr_employees` সারি নেই। HR-এর ঘরে
 * রাখলে ঐ মানুষগুলোর নম্বর রাখার জায়গাই থাকত না।
 *
 * ⭐ আর কাঠামোগত কারণটা আরও কঠিন: প্রোফাইল পাতাটা **কোরের**, আর কোর
 * `App\Modules\` চেনে না — এটা এই রিপোর মাপা নিয়ম। পদবির বেলায় সেই
 * দেয়ালটা [[App\Core\Panels\FactRegistry]] দিয়ে ঘুরে যাওয়া হয়েছে,
 * কারণ পদবি কেবল **দেখানো** হয়। কিন্তু নম্বর ও ঠিকানা এখানে
 * **সম্পাদনাযোগ্য**, আর কোর থেকে HR-এর সারিতে লেখা মানে দেয়ালটা ভাঙা।
 *
 * ── ⓘ কেন দুইটা নম্বরই ঐচ্ছিক ────────────────────────────────────────
 * যে ব্যবহারকারীরা আজ আছেন তাঁদের কারো নম্বর জানা নেই, আর `NOT NULL`
 * দিলে এই মাইগ্রেশনটাই চলত না। ⚠️ তার চেয়েও বড় কথা: জোর করে নম্বর
 * চাইলে মানুষ শূন্য বা `000` বসিয়ে দেন, আর তখন ঘরটা ভরা থাকে কিন্তু
 * কাজে লাগে না — খালি ঘরের চেয়ে বাজে, কারণ খালি ঘর সৎ।
 *
 * ── ⚠️ দৈর্ঘ্য ২৫, ১৫ নয় ─────────────────────────────────────────────
 * ⓘ বাংলাদেশি নম্বর ১১ অক্ষর, কিন্তু মানুষ লেখেন `+880 1712-345678`
 * বা `01712-345678 (অফিস)`। ⛔ ১৫-এ কেটে গেলে ডাটাবেজ চুপচাপ ছেঁটে দিত
 * বা ভুল ছুঁড়ত, আর দুইটাই ব্যবহারকারীর দোষ নয়।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('users', function (Blueprint $table): void {
            $table->string('mobile', 25)->nullable()->after('email');
            $table->string('mobile_alt', 25)->nullable()->after('mobile');

            /*
             * ⓘ `text`, `string` নয় — ঠিকানা এক লাইনের জিনিস নয়।
             * "বাড়ি ১২, রোড ৫, ব্লক সি, বনশ্রী, রামপুরা, ঢাকা-১২১৯"
             * এখনই ৫৫ অক্ষর, আর গ্রামের ঠিকানা এর দ্বিগুণ হয়।
             */
            $table->text('address')->nullable()->after('mobile_alt');
        });
    }

    public function down(): void
    {
        Schema::table('users', function (Blueprint $table): void {
            $table->dropColumn(['mobile', 'mobile_alt', 'address']);
        });
    }
};
