<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * কিছু ভাঙলে কেউ যেন জানতে পারে।
 *
 * ── কেন এটা লাগল, ১ সেপ্টেম্বর ২০২৬ ──────────────────────────────────
 * ১,২৭,০০০ লাইনের এই অ্যাপে `Log::` কল ছিল **ছয়টা**, আর কোনো error
 * tracking নেই। অর্থাৎ কিছু ভাঙলে তা কোথাও লেখা হত না — কেবল
 * ব্যবহারকারীর পর্দায় একটা ৫০০, আর তারপর নীরবতা।
 *
 * ৩১ আগস্টের নিরীক্ষায় যা যা **নীরবে** ভাঙা পাওয়া গেছে:
 *
 *   · স্কিম ও কমিশনের পর্দা কারও জন্য খুলত না — কতদিন, জানা নেই;
 *   · খরচের কেন্দ্রের পর্দা প্রতিবার ৫০০ — কেউ বলেনি;
 *   · CI তিন দিন ধরে একটাও টেস্ট চালায়নি;
 *   · আর সবচেয়ে জোরালোটা: ডিপ্লয়ের পর **লাইভে বিল কাটা প্রায় দুই
 *     ঘণ্টা ভাঙা ছিল**, আর সেটা জানা গেছে দৈবক্রমে, CI-র লগ দেখতে গিয়ে।
 *
 * প্রতিটার একই স্বভাব: **ব্যবস্থাটা জানত, কিন্তু বলার কোনো জায়গা ছিল না।**
 *
 * ── কেন ডাটাবেজ, কেবল ফাইল-লগ নয় ─────────────────────────────────────
 * ফাইলে লিখলে দেখতে হলে ssh লাগে, আর যিনি ব্যবসা চালান তিনি ssh করেন না।
 * বিক্রির জন্য বানানো পণ্যে "সার্ভারে ঢুকে লগ দেখুন" কোনো উত্তর নয়।
 * টেবিলে থাকলে একটা পর্দা হয়, আর পর্দাটা যে কেউ খুলতে পারেন।
 *
 * ফাইল-লগও থাকে (`daily` চ্যানেল) — দুইটা একে অপরের বিকল্প নয়। টেবিল
 * ভরে গেলে বা ডাটাবেজই যদি ভাঙে, ফাইলটাই শেষ ভরসা।
 *
 * ── কেন আঙুলের ছাপ (fingerprint) ─────────────────────────────────────
 * একটা ভাঙা তালিকার পাতা একজন ব্যবহারকারী দশবার রিফ্রেশ করলে দশটা সারি
 * বসত, আর পঞ্চাশ জন করলে পাঁচশো। তখন খাতাটা নিজেই অপঠনীয় হয়ে যেত, আর
 * নিচে চাপা পড়ত সেই একটা ভিন্ন ভুল যেটা সত্যিই নতুন।
 *
 * তাই একই শ্রেণি + একই ফাইল + একই লাইন = **একটাই সারি**, আর তার গোনা
 * বাড়ে। "কতবার" আর "প্রথম কবে" — দুইটাই তখন পড়া যায়, আর ওই দুইটাই
 * বলে দেয় জিনিসটা নতুন না পুরনো।
 *
 * ── যা ইচ্ছাকৃতভাবে রাখা হয় না ───────────────────────────────────────
 * **অনুরোধের ইনপুট নয়।** মানুষ প্রায়ই আসল পাসওয়ার্ড ভুল ঘরে টাইপ করে,
 * আর তখন খাতাটাই পাসওয়ার্ডের তালিকা হয়ে যেত — ঠিক যে কারণে
 * `login_history`-তেও ইনপুট বসে না।
 *
 * ঠিকানার প্রশ্নাংশও (query string) নয়, কেবল পথ — ওখানে টোকেন বা চাবি
 * থাকতে পারে।
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('error_events', function (Blueprint $table) {
            $table->id();
            $table->publicId();

            /*
             * কোম্পানি জানা না-ও থাকতে পারে, আর সেটাই স্বাভাবিক।
             *
             * ভুলটা প্রসঙ্গ বসার **আগেও** ঘটতে পারে — লগইনের পর্দায়,
             * বা ResolveCompanyContext নিজে ভাঙলে। বাধ্যতামূলক করলে
             * ঠিক ওই ভুলগুলোই লেখা যেত না, যেগুলো সবচেয়ে গুরুতর।
             */
            $table->foreignId('company_id')->nullable()
                ->constrained('companies')->nullOnDelete();

            $table->foreignId('user_id')->nullable()
                ->constrained('users')->nullOnDelete();

            /*
             * একই শ্রেণি + ফাইল + লাইনের সংক্ষিপ্ত রূপ।
             *
             * ৪০ অক্ষর কারণ sha1। unique নয় — কোম্পানি ধরে আলাদা সারি
             * হয়, নাহলে এক কোম্পানির ভুল আরেক কোম্পানির পর্দায় দেখা যেত।
             */
            $table->char('fingerprint', 40);

            $table->string('class', 191);
            $table->text('message');
            $table->string('file', 500)->nullable();
            $table->unsignedInteger('line')->nullable();

            /* কোন দরজা দিয়ে — রুটের নাম, পথ ও পদ্ধতি */
            $table->string('route', 191)->nullable();
            $table->string('method', 10)->nullable();
            $table->string('path', 500)->nullable();

            /*
             * স্ট্যাক ট্রেস — ছাঁটা।
             *
             * পুরোটা রাখলে একেকটা সারি কয়েক কিলোবাইট, আর নিচের অর্ধেক
             * সবসময় ফ্রেমওয়ার্কের ভিতরের ফ্রেম যা কিছুই বলে না।
             */
            $table->text('trace')->nullable();

            $table->unsignedInteger('times')->default(1);
            $table->timestamp('first_seen_at');
            $table->timestamp('last_seen_at');

            /*
             * দেখা হয়েছে বলে চিহ্নিত — মুছে ফেলা নয়।
             *
             * মুছতে দিলে যে ভুলটা কেউ বুঝতে পারেনি সেটাই সবার আগে মুছে
             * যেত। চিহ্নিত করলে তালিকা পরিষ্কার থাকে আর ইতিহাস থাকে।
             */
            $table->timestamp('acknowledged_at')->nullable();
            $table->foreignId('acknowledged_by')->nullable()
                ->constrained('users')->nullOnDelete();

            /*
             * একই ভুল আবার এলে খোঁজার পথ — এটাই সবচেয়ে বেশি ব্যবহৃত।
             * company_id সামনে, কারণ পর্দা সবসময় এক কোম্পানির।
             */
            $table->index(['company_id', 'fingerprint'], 'error_events_lookup');

            /* তালিকার স্বাভাবিক ক্রম: নতুনটা আগে */
            $table->index(['company_id', 'last_seen_at'], 'error_events_recent');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('error_events');
    }
};
